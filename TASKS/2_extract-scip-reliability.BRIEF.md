# Extract field reports: SCIP reliability

Implement this single batch as Sol in your assigned lane worktree, based on cc46e4435. Parent route: extract-field-reports-parent. User requests worktree isolation, cost-conscious sequential Sol implementation, and using extract to read extract. No additional agents. Do not modify another worktree, merge, push, install globally, or edit issues. No compiler kernel/type/binding/evaluation semantics changes. Current session authorizes these extractor fixes despite historical planning-only text.

Read AGENTS.md and v6/sprefa-extract/AGENTS.md. Current session rules: use apply_patch; format once immediately before committing; keep meaningful deterministic tests; never UPDATE_SNAP wholesale. Work only under v6/sprefa-extract except your final receipt. Give this lane its own CARGO_TARGET_DIR under a mktemp directory. Commit the finished batch and return SHA, focused/full test results, changed coverage, and unresolved cases. Send completion to the parent with boop. Do not proceed to other batches.

Existing base already has Falcon repository-topology staging, bounded indexer diagnostics, Cargo.lock staging, aliases, telemetry and missing-source/re-export fixes. Do not redo them. Relevant current files: src/scip.rs, src/scip_ensure.rs, src/project.rs, src/bin/extract.rs, src/schema.rs, src/wire.rs; tests/scip_freshness.rs, tests/8_scip_families_cli.rs, named-indexer tests.

Warmup: generic boop-start was bypassed for this Rust-only task because it builds into a shared target and installs unrelated TS workspaces. The sibling hafley-rs dependency link is prepared. Build using your own CARGO_TARGET_DIR, use the existing binary below for initial orientation, and satisfy applicable commit hooks using your built binary. Do not bypass meaningful checks. No TS changes are requested.

Orient with extract first. Existing binary /tmp/sprefa-extract-main-integration-target.LCsjvv/debug/extract can read your source with DL_TRAIL=0 and --resolve --family call --witness over the relevant files. Filter output before returning it to context; preserve resolution_origin and unresolved evidence. Read exact source spans afterward. Do not index the entire repository or run a compiler indexer for orientation.

Implement in this order:

1. Persistent stage pruning: prune_unstaged currently removes source files but leaves empty directories. A removed workspace member crates/css leaves crates/css/src, so Cargo members=["crates/*"] fails. Remove empty staged directories bottom-up, constrained to the task's staging tree. Preserve target directories and their contents, current sources, and unrelated nonempty paths. Add a deterministic regression that stages a workspace, removes a member, stages again, and checks the absent member directory plus retained live member and target artifacts. Inspect both historical and repository-snapshot staging paths.

2. Explicit index contract: extract --family scip ROOT --scip-index FILE currently ignores FILE. Honor it as an explicit read with no spawned indexer; precedence is explicit CLI path, then existing env/cache behavior when absent. A missing/invalid explicit file is an error, never a silent rebuild. Reject incompatible --scip-index plus --indexer / --scip-build clearly where needed; retain the existing fast/slow alias conflict contract. Keep named-indexer caches separated. Test with a supplied fixture index and a sentinel indexer that must not execute. Update help/schema as appropriate. Keep external consumers' existing public constructors usable if practical; report necessary API changes.

3. Reuse evidence: expose index modification time and a staleness indicator on scip_index records. Define mtime units explicitly in schema/help. Requested staleness is evidence that a readable indexed document is newer than the index; do not claim mtime proves semantic freshness. Missing indexed files should contribute explicit uncertainty or stale evidence rather than crashing. Preserve explicit-index usage even when stale, emitting evidence. Avoid adding nondeterministic fields to semantic record classes or changing index-selection policy beyond explicit CLI handling. Use controlled file times / existing filesystem timestamp APIs for deterministic fixtures. If the existing record model cannot represent unknown evidence without a significant contract change, send the concrete alternatives to parent before that part; continue the first two fixes.

Validation: run focused regressions during development, then cargo test --features cli --no-fail-fast once at the finished integration checkpoint. Fix in-scope failures; rerun affected tests then repeat the full gate only after changes invalidate it. Do not weaken unrelated tests. Write concise v6/sprefa-extract/reports/3_scip_reliability.md recording source changes, self-extraction use, exact test results, and limitations. Final branch should be clean and committed. No human-facing permission flow is required for this authorized batch; surface actual new contract decisions to parent.
