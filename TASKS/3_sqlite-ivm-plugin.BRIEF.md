# SQLite IVM plugin, semantic coverage, and telemetry

## Authority and scope

User: keep implementing the SQLite IVM plugin, cover DD/IVM semantics before performance, and add logging throughout. Use Sol for code writing. Work only inside this new lane worktree based on feature/postgres-ivm-crossover, preserving all previous implementations and receipts. Parent handles sampled review and integration. No native collaboration tools; communicate through Boop. No merge, push, global installation, user database mutation, or compiler/kernel semantic changes. Kernel changes require prior user explanation and explicit approval. Current user permission covers this lab/plugin implementation despite historical v6 planning notes.

Read AGENTS.md, v6/AGENTS.md, and the current reports and source below. Re-find symbols rather than trusting line numbers. Keep new files numbered in dependency/reading order, starting at 0 in a new component. Commit substantive milestones often, with current test receipts. Run cargo fmt once immediately before a Rust commit; include its output. Do not reformat during implementation. Do not modify HOME, CODEX_HOME or user global settings. Dedicated build target supplied by lane. All runtime databases and servers must be task-owned.

## Starting evidence

Read v6/labs/exec_shootout/postgres_pglite_ivm/{16_sqlite_ivm_capabilities.md,21_sqlite_template_reuse.md,24_three_way_comparison.md}, then 17/19/20/22/23 source/tests and the shared 9/12/13 fixture/runner. Existing benchmark: same nonrecursive inner join and grouped COUNT/SUM, pg_ivm / compiler-generated SQLite affected-group triggers / actual DD. Existing SQLite arm recomputes affected groups per input row; it is not arithmetic delta maintenance. Keep it as a comparison arm.

Reuse audited SQL generation and existing SQLite competitors where applicable. Inspect sprefa-store and existing compiler SQL templates rather than inventing another host-side IVM engine. Do not read or use v6/dd-runner as a design input. Existing native DD example and upstream DD operator semantics are comparison inputs. All new implementations join the existing shootout Bash entry 13_crossover_run.sh and its outputs, never a disconnected benchmark.

## 0. Executable plugin slice

Implement a stock-SQLite loadable extension with a SQL-facing installation API accepting a SELECT, such as sqlite_ivm_create(name, select_sql). The consumer should use ordinary SQL writes/reads; no Python/JS/Rust update loop, explicit host flush, or required Prolog compiler process at runtime. A small extension implementation in C or Rust is acceptable, SQL remains the consumer language and SQL owns maintained relational state. Choose and pin a reusable parser/extension library after checking primary docs/source and licenses. Do not hand-roll a SQL parser or ABI bindings. The prior report lists liteparser and sqlite3-parser as researched candidates, not mandated dependencies. No qualitative build-vs-buy claims.

First close the existing integer equijoin + GROUP BY COUNT/SUM slice end-to-end, supporting real table/column names through AST/catalog binding rather than fixture-name pattern matching. Validate supported types, NULL policy, expressions and numeric bounds explicitly. Reject unsupported queries before mutation. Installation/drop must be atomic under savepoints and preserve caller transactions; include failed installation cleanup, multiple views, collisions, reopen, second connections and required-writer settings. Test .load + SQL through the stock sqlite3 executable. Record extension path, ABI/SQLite versions and consumer setup. Fail closed on unsupported writes/settings rather than corrupting results. No unchecked identifier interpolation or raw user SQL concatenation across statement boundaries.

## 1. Arithmetic maintenance and semantics

Add signed arithmetic COUNT/SUM maintenance for the admitted slice where justified, retaining affected-group recomputation as its own arm. Exercise changes to both join sides, OLD/NEW key moves, simultaneous multirow updates, duplicate supports, retract-to-zero, negative/zero values, empty/global aggregates, NULL keys/values/all-NULL sums if admitted, overflow and affinity/collation boundaries. Enforce restrictions rather than silently accepting unhandled SQL values. No hidden full refresh advertised as delta maintenance. If a capability cannot be supported safely within this pass, commit an executable rejection test and record the missing lowering algorithm.

Construct a source-backed semantic coverage matrix with concrete fixture/test IDs and engine status. Inventory projection/filter/map, multiset and set behavior, signed multiplicity/consolidation, joins including self/multiway/outer, semijoin/antijoin/negation, DISTINCT, UNION/set operations, COUNT/SUM/AVG/MIN/MAX, grouped/global aggregates, subqueries/CTEs, recursion/fixpoints/cyclic deletion, ordering/top-k/windows, DD time/frontiers/iteration, and transaction/durability/concurrency boundaries. Distinguish DD library expressibility from a SQL plugin capability; never equate one timestamp fixture to arbitrary DD time semantics. Reuse existing upstream/local tests and examples. Add executable shared cases wherever each engine admits a common contract. Test unsupported cases explicitly and report them separately, not as successful parity. This is a finite enumerated coverage target, not a claim to cover all possible programs or arbitrary user-defined DD operators. Do not implement new language/kernel semantics to fill matrix cells.

## 2. Logging and diagnostics (explicit user requirement)

Provide structured, bounded, configurable telemetry at load/install/bind/lower/schema/drop boundaries, maintenance/change processing, transaction/rollback/error boundaries, and benchmark stages. Include view/operation identifiers, SQLite extended error code, durations, affected inputs/groups/output deltas, algorithm, configuration and build provenance where actually observable. Do not fabricate transaction events that SQLite APIs do not expose. Explain per-connection telemetry visibility and trace ownership. Reuse hafley-observe/tracing conventions for Rust components; libraries must not seize a process-global subscriber, replace a host SQLite trace callback, or log via SQL execution inside prohibited SQLite hooks. Reuse existing framework facilities for C if selected. Preserve stdout SQL/JSONL protocols; diagnostic streams go to stderr or an explicit sink. Row values and expanded SQL are opt-in, bounded, and redacted by default. No unbounded in-memory log buffer or per-row persistent log growth. Logging failures cannot silently alter successful transaction semantics. Test telemetry on success, rejection, rollback, second-connection behavior and disabled mode.

Keep ordinary writer requirements explicit: instrumentation must not silently introduce a new mandatory host-language service, extension-per-writer dependency, global callback, or unbounded storage. Where full visibility conflicts with the pure-SQL ordinary-writer contract, document the exact limit and instrument observable boundaries.

## 3. Shared validation, then performance

Run full recomputation oracles against every admitted transition, compare actual inputs and output multiplicities/state, and preserve seeded failing traces. Exercise rollback/savepoint, reopen, independent connections, REPLACE/UPSERT/IGNORE/FAIL/ABORT and supported storage variants. Add injected-failure cases that demonstrate detectors catch incorrect results. Require exact comparisons on admitted bounded integers; float order/tolerance and NULL/collation semantics require separate explicit contracts.

Integrate the actual loaded plugin as an arm in 13_crossover_run.sh. Shared identical fixtures and timer boundaries, warmups, rotated arm order, repeated trials, input/output hashes, failure accounting and raw receipts. Generate charts/tables through existing reporting commands. Record compiler/extension hashes, logging settings, durability, RSS scopes, DB/WAL/temp bytes, size, batch, fanout, skew, churn and output cardinality. Compare normal logging-disabled execution and a bounded logging-enabled overhead run separately. No equal-durability claim between volatile DD and durable SQL. No fixed total RAM claim from cache settings. Preserve existing reports/receipts and old SQLite arm.

Start correctness at small sizes; then repeat the existing <=12k grid. Each benchmark process <=120s, each sweep <=20min; use existing memory-pressure guard and do not launch unbounded scale sweeps. Larger sizes only after the small matrix passes and measured host resource headroom permits; preserve blocked rows. Full tests: up to four complete attempts per substantive milestone, focused tests during fixes; finish with one complete post-fix gate, avoid redundant full reruns after an unchanged passing tree. If budget exhausted because of a concrete failure, report it and continue independent authorized work.

## 4. Handoff

Produce TASKS/4_sqlite-ivm-plugin.REPORT.md with actual commits, build/load/SQL example, public signatures, maintenance sequence, filesystem reading order and absolute artifact paths, current test counts/CI coverage, supported/rejected/missing matrix, generated shootout results, logging controls and overhead, and remaining blockers. Explain any departure from the prior proposed SQL interface. Do not declare the general plugin finished merely because the first query passes. Send milestone and completion hails to sprefa-ivm-extract-parent with --no-wait; reports must be current and committed. Keep progressing executable work, not research alone. If new authority or a compiler-kernel decision is required, pause only that portion and state the exact decision.
