# Brief: the compiler and sprefa-extract documented by extract + dl6

Issue: `@compiler-extract-self` (epic `@cheap-fast-analysis`). Base sha: `0d19e3471` (origin/main).
FIRST ACTION in the worktree: `git merge --ff-only 0d19e3471`. Failure = stop and report.
Never spawn subagents. Deliver through a GitHub PR against `main` with the receipts below in its body;
the PR body ends with `Refs-Issue: @compiler-extract-self`.

## Laws in force (CLAUDE.md, user-set 2026-08-21)
- tsv2 is paused: Rust door only, never edit `v6/tsv2/**`, never run sweep.
- Zero shell in the engine: hosts link Rust executors through adapter rows; templates are the readable spelling.
- The words "ground truth" are banned in any spelling or root form. Say oracle, expected, golden.
- No new syntax. `? q(...) order by col desc.` exists. `decode(Json, [... {k: V}])` spread exists. Aggregates
  `count/min/max/group_concat(Text, Sep, Ordinal)` exist.
- Every command you run wraps `timeout <seconds>`. Nothing foreground over 10s; batteries run in the
  background with a per-test cap and are polled. A command that hits its timeout is a finding, never a wait.
- `tracing` only in Rust. No em dashes. Comment budget. Banned words in prose and identifiers:
  provenance, substrate, load-bearing, regime, refusal, honest(ly), ground* as a verb, support.
- Every `.dl6` snippet in the report carries its pure-rxjs lowering as a comment.

## Read first
`CLAUDE.md`; `v6/dl/deadcode/dead-module-rail.dl6` + `.sh` + `.adapters.json` (the working pattern: `files`
through soopy, extract-shaped hosts in-process, `cargo_targets` json host, `? ... order by`, harness flags
`--arrive`, `--live-hosts`, `--final-only`, `--final-tsv`); `v6/dl/reach/` if present (a peer lane's
reachability program); `v6/sprefa-extract/src/schema.rs` (every record per family), `src/lang/prolog/`
(the prolog family: predicates, clauses, calls, module imports; run
`timeout 10 v6/sprefa-extract/target/release/extract --family call v6/prolog/lower.pl | head` to see rows),
`src/lang/rust.rs`; `v6/prolog/compile/registry.pl:330-420` (`host_input_contract` rows; a new host NAME
needs a row; prefer existing names `call_node_at`, `call_ref`, `type_node_at`, `sig_at`, `extract`);
`v6/justfile` recipe `self-map` and `v6/tools/staleness-gate.sh` (the existing self-map gate: `SELF MAP HOLDS
diagrams=4 lines=692`; read what it draws and from what); `v6/prolog/ARCH.pl` (the arc ledger);
`docs/` (where generated docs land today); the `unga` diagram laws in
`~/.claude/CLAUDE.md` (24 shapes per board, 8 code blocks, d2 or mermaid, height under width).

## Deliverables
1. `v6/dl/selfdoc/selfdoc.dl6` + `.adapters.json` + `selfdoc.sh` (compile through `compile_dl6/3` with
   `emitter(emit_rust:emit_program)`, run the harness, print with `--final-tsv`; bash only invokes the
   runner and `printf`, no python, no jq). Inputs: `files` over `v6/prolog/**/*.pl` and
   `v6/sprefa-extract/src/**/*.rs` (mind the git pathspec law in the rail's header: `*` crosses `/`),
   `call_node_at` (defs), `extract` (call sites), `type_node_at`/`sig_at` where the family exists for the
   language. Rels (names are yours, columns must be read by a rule): `module(path, lang, defs, sites)`,
   `predicate(path, name, arity)` for prolog and `function(path, name)` for rust, `calls(from_path,
   from_name, to_name)`, `module_edge(from_path, to_path, weight)` (distinct callee names resolved to
   defining files, count), `phase(path, phase_name)` for the compiler (derive from the file name prefix
   `0_`..`8_` and the driver list in `compile.pl`; state the rule), `host_roster(host, inputs, outputs,
   executor)` read from the adapters files under `v6/dl/**` plus `registry.pl` rows through a `files`
   + `extract` pair, or state why the registry needs a host of its own.
2. Queries, each `order by`: `? selfdoc_module(...)`, `? selfdoc_phase_edge(...)`, `? selfdoc_hot_hub(path,
   fan_in, fan_out) order by fan_in desc`, `? selfdoc_host(...)`, `? selfdoc_orphan_predicate(path, name,
   arity)` (a prolog predicate no clause calls and no module exports; say how export is read).
3. `docs/selfdoc/` generated by `selfdoc.sh`: `compiler.md` and `extract.md`, each opening with a TOC, then
   one mermaid `flowchart LR` per board under the 24-shape law (phases as boards: parse, plan, lower, emit;
   extract: cli, dispatch, families, resolve, scip), a table per board beside the diagram (module / defs /
   fan-in / fan-out), the host roster table, the orphan list. Rendered from `--final-tsv` rows by the
   script with `printf` only. Commit the generated files; a `just selfdoc` recipe regenerates; a `just
   selfdoc-check` recipe regenerates into a temp dir and diffs (exit nonzero on drift), wired into
   nothing else yet.
4. Fold the existing self-map: if `just self-map` draws a subset of what selfdoc now draws, point its
   gate at the new output and delete the old generator, with the diff in the PR; if it draws something
   selfdoc does not, keep it and say what.
5. Oracle check: pick 5 compiler predicates and 5 extract functions, confirm by reading the code that
   their fan-in/fan-out rows are right; paste the table.
6. Wall: whole `selfdoc.sh` under 10s on this machine, 3 runs pasted with `RUST_LOG=sprefa_engine_rs=info
   DL_TRACE_SUMMARY=1` top rows. Row volume law: declare no host column a rule never reads.
7. Gates, each wrapped in `timeout`, outputs pasted in the PR body: `cd v6/prolog/conformance &&
   swipl -g go -t halt go.pl` (439 PASS 0 FAIL), `bash v6/sprefa-engine-rs/grade.sh` (439/335 rc=0,
   unmoved), `cd v6 && just plunit` only if you add registry rows (1041), `just selfdoc-check`,
   `swipl -g go -t halt v6/prolog/ARCH.pl` (7/0).

## File ownership (five peer lanes are live)
YOURS: `v6/dl/selfdoc/**` (new), `docs/selfdoc/**` (new), `v6/justfile` (two appended recipes and the
`self-map` recipe only if deliverable 4 folds it), `v6/tools/staleness-gate.sh` and the self-map
generator only under deliverable 4, `v6/prolog/compile/registry.pl` new rows only in one contiguous block
under `% selfdoc hosts`.
FORBIDDEN: `v6/sprefa-engine-rs/**`, `v6/sprefa-extract/**`, `v6/dl/deadcode/**`, `v6/dl/ghcacher/**`,
`v6/dl/reach/**`, `v6/prolog/**` except the registry block, `v6/tsv2/**`. An extractor gap (a prolog
fact you need that the prolog family does not emit) is a request in the PR body with the file, function,
record shape, and a fixture proving absence.

## Report (PR body), tables and lists only
rel table (rel / columns / rule / rx lowering); board list (board / shapes / file); oracle check table;
wall table; gate outputs; requests. Commit messages imperative, ending
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`, last paragraph `Refs-Issue: @compiler-extract-self`.
