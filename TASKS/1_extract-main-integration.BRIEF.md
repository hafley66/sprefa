# Integrate completed extract fixes onto current main

User requests extract in main. You are Sol, implement only in your new Boop lane worktree based on current main. Parent owns final main merge. Do not merge, push, globally install, alter primary dirty checkout, modify user source databases, or change compiler/kernel semantics. No native collaboration tools. Read current AGENTS.md and v6/sprefa-extract/AGENTS.md and this brief. Current user scope permits the extract integration despite historical planning-only notes.

Source branch fix/extract-final-gate at 001eeeb9e is fully tested (778 pass, 14 ignored) but branches from DL7 development, not current main. Main already has independent extractor improvements including Go checker, Kotlin/Python module facts, SCIP conformance and named-indexer selection. PRESERVE them. Never replace the subtree from the old branch or merge its unrelated DL7/DBSP/kernel history.

Integrate these selected extractor commits, oldest first, with semantic conflict resolution as needed:

5fa36e780 (fast/slow aliases and build identity), aaf14bcba, 7592bb5db, a8008ebb8, a43816d77, 044f71bfe, d83d9cc91, 7d6272bbc, 5eca5163a, c58c33fec, c8865c730, 001eeeb9e.

Cherry-pick or equivalently transplant only their changes. Do not weaken main features to match old tests. Fast/slow alias conflicts must account for current Go checker/indexer flags. Preserve selected-indexer cache separation and named-indexer dispatch while adding diagnostics. Keep the missing-file readable-source behavior and re-export discrimination without dropping main conformance/module logic. Retain exact original commit provenance in the integration report. Source files have formatter differences; review semantics, no minimizing formatter churn. Use apply_patch for conflict/code edits. Run cargo fmt once immediately before Rust commits and include its output. Commit substantive milestones often, no hooks that write unrelated files or user state.

Run cargo test --features cli from v6/sprefa-extract, dedicated CARGO_TARGET_DIR supplied by lane. Up to four full attempts, focused regressions during fixes, stop redundant reruns on unchanged passing code. Include main named-indexer/Go-checker tests and new alias/telemetry/staging/re-export/missing-file/stdin tests. Run a final full post-fix gate and report exact pass/fail/ignored counts and CI coverage changes. If optional language tooling missing, record exact tests blocked and continue available checks; do not silently skip newly failing coverage.

Reproduce Falcon with the integration binary: extract slow --scip-timeout 45 --scip-cache <fresh scratch> /Users/chrishafley/projects/hafley-rs/games/blender-godot-sqlite-proof/falcon-lab. Record version/hash, exit, semantic rows, zero scip_skip, second warm run and structured logging with RUST_LOG=sprefa_extract=debug/HAFLEY_LOG_FORMAT=json. Preserve source file byte inventory and use task-owned outputs/targets. Existing source report v6/sprefa-extract/reports/1_combined_integration.md has earlier receipts. Do not label them current-main proof.

Write v6/sprefa-extract/reports/2_main_integration.md with current commit mapping, differences resolved, current tests, Falcon receipts, remaining blockers and merge readiness. Parent may sample/re-run tests before merging. Send milestone and completion via boop beep sprefa-ivm-extract-parent --as fix-extract-main-integration --no-wait. Return clean tested branch and exact final SHA. Keep going through in-scope failures; if an external blocker/new authority prevents completion report precisely and preserve work.
