---
name: project-type-shapes-prototype
description: "type arc SHIPPED in v0.6.24 (main 43852bd, tagged+pushed 2026-07-10): enum brands, shapes, type_decl_row derived shapes, json output aggs, scip occurrences; roadmap at docs/type-comptime-roadmap.md"
metadata: 
  node_type: memory
  type: project
  originSessionId: ab4a16de-586c-4508-93c0-71986c34bbfa
---

RELEASED 2026-07-10 as v0.6.24 (main 43852bd, tag pushed): all three branches merged
(type-decl-row incl. type-shapes-prototype, json-agg, scip-occurrences), doc zones
regenerated once on the merged tree, suite green (lib 251/0/1, it 594+1flake/4),
vsix parity, binary reinstalled. Feature branches + agent worktrees deleted. FOUND
AND FIXED during regen: the skill's `<!-- BEGIN: op-quickref -->` marker had been
dropped in ce66ed1, silently no-opping gen-skill-ref since then (table drifted +
partially duplicated; marker restored, respliced, -20 dup lines). json composition
gotcha worth remembering: embedding already-json TEXT into json_object/json_group_array
REQUIRES the json(x) wrapper or it double-encodes as a quoted string (the example
teaches it). Daemon restart still Chris-only. History below.

Typecheck-time type prototype, built 2026-07-09 by a delegated Opus agent, verified by Fable (independent suite runs + live smokes). UNMERGED, NOT pushed.

- Branch `feat/type-shapes-prototype`, commits 979ac0a (Phase 1) + b6305d6 (Phase 2), in worktree `~/projects/sprefa/.claude/worktrees/agent-ac9a487db14a19d08` (base = v0.6.23 / 20fcd5c).
- Phase 1: `type severity = "error" | "warn".` enum brands (rides existing BrandDecl/Brands; new `Tok::Pipe`; diag `enum-variant-unknown` w/ Levenshtein did-you-mean; `--parse-only` exits 1) + `type finding(cols).` named shapes expanding to plain RelDecls at load (`expand_shapes` in frontend + check_and_normalize; engine never sees `Item::Shape`). Storage/lower untouched; columns stay text.
- Phase 2: ambient builtin enum brands (`builtin_enum_brands()` in engine/mod.rs) on type_edge(_rev).kind, type_entity(_rev).kind, df_node(_rev).kind (21 kinds), checkout_done/checkout_plan.action; user redecl of those names = load error. Motivation: the #1 agent failure mode (literal typo -> silent zero rows) becomes a loud suggestion. New introspection rel `rel_col(rel, pos, col, ty, variants)` on CatalogKind (chosen over widening rel_catalog: dl arity exact, positional-reader sweep avoided). Enum-brand var joining plain text is silent BY DESIGN (vocabulary gate, not path-like refinement); doc_tag.tag skipped (open set).
- Suites green in worktree: lib 234/0/1, it 573/0/4. Diag span is (0,0) -> line 1 (Term::Str carries no span — known limitation).
- Phase 3 (19d8e9c): generated-doc regen via the generators (README/relations/magic-rels/syntax/examples), df_node doc-string fix; surfaced pre-existing committed-doc drift from 0.6.19/0.6.22 arcs. Phase 4 (99d6877): S3/S4 — triage found body binds ALREADY worked in derived rules (the logged complaint was the source-rule val_of path; its note enriched); shipped `+` overload (int+int add, text+text -> SQLite `||`; mixed = plus-mismatch error; FIXED silent wrong answer where "https://"+host returned 0), body-bind hardening (check_body_binds unbound diag, Pos->Cmp->Neg lower order fixing a silent wrong join for binds in negations), bind lowering = inline expr into use sites (no subquery). Fable-verified: lib 240/0/1, it 579/0/4 + smoke (chained bind + concat). The ?-then-ref intra-row self-eq machinery (294d60db) is GONE post-lift — zero grep hits.
- Sibling branch feat/scip-occurrences (34dde8b, off main v0.6.23, separate agent worktree): scip_occurrence(file,symbol,line,col,end_line,end_col,role,repo) + scip_binding(file,symbol,local_name,line,col,repo) — S1/S2 answered; importer was decoding ranges/roles but dropping them at the rel layer (occ_spans read only by propose.rs). local_name = source slice at occurrence range, WORK-only, UTF-16 cols. scip_def/ref/edge byte-identical. Doc regen deliberately unstaged (would drag sibling-branch rels into generated zones) — regen at merge time.
- Phase 5 DONE (feat/type-decl-row, 3d1352b off 99d6877): rung 2+3 "types from rels" — reserved derived-headed `type_decl_row(shape,pos,col,ty)`; end-of-tick digest-guarded persist to `_shapes` meta table (`shape:type_decl_row` _reldigest key), next-tick declare resolves deferred `rel name: shape.` via normal `declare` (column drift migrates + re-derives); rules heading a pending rel skipped that tick in BOTH tick paths; diags shape-pending(info)/shape-shadowed(warn, syntax wins)/shape-unknown-ty(warn, steady); `rel type_decl_row(...)` bails. examples/type-from-json.dl = inferred JSON schema live tick 2 + mapped-type `"partial_" + rel <- rel_col` (94 partial_* shapes). Opus agent built impl+example (killed by Chris mid-tests for cost); Fable wrote the 8 e2e + unit + CHANGELOG + commit. Suites lib 241/0/1, it 587/0/4. Fable-verified live: 4-tick migration, shadow, unknown-ty, reserved bail all smoked.
- 2026-07-10 additions on feat/type-decl-row: 1176f01 = `ty` column renamed `type` across the DSL surface (Chris rule, see [[feedback-no-ty-abbreviation]]; rel_col/type_decl_row/_shapes/diag code shape-unknown-type; Rust internals keep ty); f3f0baf = docs/type-comptime-roadmap.md (textbook ladder rungs 0-7: shipped brands/shapes/introspection/type_decl_row -> in-flight json aggs -> typeof -> reference columns -> codecs -> generics-as-desugar-over-type_decl_row -> cross-language lens; registration law = decl+check+ROWS+mintable; guardrails flat-storage + tick-boundary); 2f1df52 = skill type-decl section (17 lines, lean per Chris) + stale 3-ary type_edge fixed (is 4-ary w/ repo).
- IN FLIGHT: feat/json-agg (fresh Opus agent, brief at scratchpad json-agg-brief.md session ab4a16de, branched off 1176f01): json_group_array/json_group_object head aggs (object form is TWO-arg, extend AggFn) + json_object/json_array/json scalar fns; DETERMINISM REQUIRED (ORDER BY inside agg — flapping array order would churn rel content digests/rebuild scoping); source-rule heads refused; examples/json-out.dl corpus-free; tests/it/json_agg.rs w/ serde_json validity + determinism + stratification-diag gates.
- MERGE PLAN (Chris said GO 2026-07-10: "put all on main once its done, new gtag and push"): after json-agg verifies — merge feat/json-agg (contains type stack thru 1176f01) -> merge feat/type-decl-row (adds f3f0baf+2f1df52) -> merge feat/scip-occurrences (34dde8b; dry-run showed ONLY CHANGELOG.md conflicts, both-append-under-Unreleased, trivial union) -> regen generated doc zones ONCE on merged main (README builtin-rels, docs/reference/*, syntax.md type rows) -> full suite -> stamp 0.6.24 (CHANGELOG + Cargo.toml + scripts/build-vsix.sh parity) -> tag v0.6.24 -> push. Chris-only after: daemon restart.
- Residuals: stale "assign" in df_node doc string, maybe module_import.kind ambient brand. Chris ruled: type ladder stops at the roadmap's recorded-failure criterion ("no fuckass types"). QUEUED NEXT (Chris 2026-07-10): [[project-vscode-ext-review]]. Design lineage: [[project_doc_comment_spine]] June-30 types roadmap; value-space `def shape(...)` form deliberately NOT taken.
