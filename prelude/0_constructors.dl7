; Userland unary type constructors.
(: Partial
   (* (: source type)
      (: return type)))

(: Option
   (* (: source type)
      (: return type)))

; The zero-column product every `(Option T)` reaches for when the value is
; absent. A JSON null is this node; a key with no edge is a different graph.
(: none (*))

