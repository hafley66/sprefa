# BOARD

## Next
- Cross-file rule references (dotted `frontend.package_manifest` syntax)
- Watermarking (rowid high-water per dependency edge)
- Capture annotations: semver($VAR)

## Later
- LSP via tree-sitter
- Pluggable tree sources (TreeNode trait)
- CSS DOM mapping
- Static type graph layer
- ATTACH DATABASE for true schema isolation (if prefix approach proves insufficient)
