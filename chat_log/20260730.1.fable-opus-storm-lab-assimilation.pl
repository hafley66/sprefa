% Fable coordination session: opus storm + lab assimilation + json language recovery.
% Load:
%   swipl -q -l chat_log/20260730.1.fable-opus-storm-lab-assimilation.pl
% Queries:
%   directive(Name, Text).
%   lane(Name, Owner, State, Deliverable).
%   answered(Question, Answer).
%   finding(Name, Severity, Text).
%
% THE COORDINATOR'S STANDING JOB THIS SESSION (user-set): keep this file
% current, review/eagle-eye every landing, call things out. Update on every
% lane merge and every finding.

:- module(session_20260730_1, []).
:- discontiguous lane/4.
:- discontiguous lane_landed/3.
:- discontiguous finding/3.
:- discontiguous bug/3.
:- discontiguous directive/2.
:- discontiguous user_card/2.
:- discontiguous answered/2.
:- discontiguous in_flight/3.
:- discontiguous design_tension/2.
:- discontiguous lab_folded/3.

session(id, '20260730.1').
session(date, '2026-07-30').
session(branch, 'codex/rel-ref-file-span-lab').
session(base_at_open, 'c6e2bf7b').
session(prior_ledger, 'chat_log/20260730.0.v6-2-ts-closeout.pl').

directive(agent_mix,
          'opus heavy (user limits reset ~5h, spend now); codex luna/terra for briefed lanes; sol only under coordinator self-doubt (fable-level, expensive); avoid sonnet (rots the codebase)').
directive(no_new_syntax,
          'absolutely none; new-syntax pressure follows the locked stop rule: lab the fork, plan with costs/receipts, return for user ruling').
directive(spot_check_everything,
          'every landing since f8ab8ac5 gets reviewed; claimed receipts re-run, not trusted').
directive(assimilate_labs_optimal_order,
          'every open lab classified and folded per the receipt_folding ruling; ordering delegated to opus review').
directive(sprefa_extract_policy,
          'reasonable CLI updates allowed for parsing logic and output composition ONLY; never dl6-specific; no vN binding; important machines live in prolog and lower into any target').
directive(json_language_goal,
          'built-in json query language wanted as structured query surface (graphql-adjacent); recover the v3/v4 json language design from the archives; that surface was the taste target ("ultra tight")').
directive(ledger_duty,
          'this file stays current through the session; it is the main coordinator deliverable alongside review').

% lane(Name, Owner, State, Deliverable).
lane_landed(clock_checker_finish, 'merge ffcddfc7',
     'THE PIPELINE WORKED: sol implemented (A2/A4/A5/A7/A8/A9/A11 not_provable with pinned dependency/8 projections, battery exact), opus review gate FALSIFIED the one affirmative claim (A6 proven was a constants-vs-constants crosscheck; sabotage receipts S1/S2/S4), sol applied the 3-item fix list (nonzero-offset pipe crosscheck pinning the 4-tick delta sequence, clock_boundary negative case, ClockCatch pinning restored, A6 honestly renamed runtime_clock_crosscheck), coordinator re-ran plunit 197/197 and merged. Focused clock suite 24/24.').
lane(clock_checker_finish, codex_sol, dispatched,
     'USER DIRECTIVE (best intelligence): sol finishes the paused 3_clock_check.pl per clock_checker_resume_order (A2 stays not-provable, A6 inferred offsets vs runtime ticks, full battery); no-commit flow; then an OPUS 5 agent runs the review gate on sol output and coordinates feedback before coordinator merge. Prior opus lane stood down.').
lane(spot_check_assimilation_order, opus_readonly, dispatched,
     'review f8ab8ac5..HEAD commit-by-commit, re-run key receipts, inventory every open lab, return classification + optimal fold order').
lane(phase5_grade_and_fix, opus_worktree, dispatched,
     'grade landing_ready bool/float at tip; fix the two known_failures (serveHost.test.ts removed-syntax retention, 18 roundtrip fixtures); full battery').
lane_landed(json_v3v4_recovery, 'f9bf09df cherry-picked',
     'HEADLINE: the json language is NOT lost -- it is v5 at the repo root (src/datapath.rs brace-pattern walker, 5 generations, inner grammar stable since v1, NINE productions); v6 is the generation that dropped it. 22 constructs graded: 8 express today / 4 ugly / 6 behind storage cards / 5 need new surface, ALL 5 = the key axis (key as capture/regex/glob/descent vs decode exact-field). 9 cards for user, top leverage CARD-KEY-CAPTURE (unblocks 4/5), cheapest CARD-SUBTREE-CAPTURE (struct_as_rows already answered it structurally). Card evidence: residency=core_global, arrays=host_flattened, null=row_absence, schema=metadata_only, cycles=refuse. Also found: SYNTAX.md decode row stale vs registry; json_array/json_object refused despite ruled emittable (dispatch, not ruling).').
finding(phase5_agent_in_main_tree, process,
        'phase5 opus agent fell back to the SHARED checkout after worktree reap and committed 2ca13e37 directly on the lab branch; coordinator imposed scoped-add rules by message; two-writers-one-tree hazard noted').
lane(json_v3v4_recovery, opus_worktree, dispatched,
     'recover the v3/v4 json language surface+semantics from ~/projects/sprefa-archive-20260701 (and 20260428); grade against the 5 json_interop_lab cards; decision cards only, zero production syntax').
lane(file_span_reconcile, opus_worktree, dispatched,
     'reconcile file-span-design.md + file-identity-span-spine.md + locked single_rel_type_system + landed rel-ref runtime into ONE plan; implement only if zero cards remain open, else stop at plan').
lane(flow_parity_residue, codex_terra, dispatched,
     'classify every unmatched call-resolution target (v5only 87 / v6only 55), close flow_node_type + param_type residue, referee-side fixes only, 0 unclassified or named blockers').
lane_landed(comment_rail_wiring, 'merge 786b5daa',
     '6 of 7 verdict techniques as standing rails (arch markers, suppression, readme anchors, lang junctions, gen zones, lint+suppression-retraction) + parity referee; coordinator re-ran gate: RAILS HOLD, comment_node 745/745 v5-exact, arch_node 4/4; named skips = markdown grammar (extractor hole, standing) + technique-2 block pairing refused unbound_head_var').
bug(block_pairing_refusal_misnamed_suspect, codex_luna,
    'range-comparison body in the block-pairing rail refused as unsupported_construct(unbound_head_var(_)) -- same misnamed-refusal class as the = spelling (review-B4 family); needs a real name + location before the technique can be judged inexpressible').
lane(comment_rail_wiring, codex_luna, dispatched,
     'wire the comment-node verdict techniques into production rails, graded like the lab (745/745 target); fold per receipt_folding').

% Deferred to wave 2 (pending spot-check ordering):
queued(rel_definition_hash_fold, 'lower.pl/compile ownership overlaps wave-1 lanes').
queued(mode_scope_proof_views, 'needs ARCH row re-read before dispatch').
queued(scan_surface_composition, 'fold-blocked on indexed user surface decision').
queued(json_interop_fold, 'fold-blocked on indexed optional-module policy; v3/v4 recovery lane feeds the ruling').

answered(sprefa_extract_usage,
         'v6.2 spawns the released binary via executor contracts (2 executor keys: shell, sprefa_extract; 14 extract declarations); batching landed (same-tick demands grouped, one stdout fans into typed response rels, flow 7->1 subprocess/path); extractor additions stay typed-rust-side (ast_pattern_query = AstPatternQuery + flat AstCaptureFact, zero DL types in the extractor); locked contracts already match the user policy (sprefa_extract_scope, extractor_trait_boundary, parser_residency); gaps: document_sources (no md/html/xml/toml/yaml Source), file_blob_repo_revision (BlobSource unimplemented, Repo/Rev types absent); named overuse: grafana 40.26 f/s under per-witness subprocess boundary').
answered(lang_design_lock_state,
         'LOCKED: single rel type model + one checker, host = ordinary RHS atom (? and @ removed), parser residency rust, surface freeze + stop rule, target-neutral plan order, bool/float storage, clock-checker scope. OPEN CARDS: scan/match composition (4 questions + 3 surface cards), json residency/arrays/null/schema/recursion (5 cards), span-spine decision rows, assign 3 cards, decl-legibility cluster, older slot pile. Verdict: value plane + host surface + storage locked; composition surface and json/module/span identity are the remaining design').

% user directives 2026-07-30 afternoon (verbatim intent, rulings-grade)
directive(openapi_codegen_spine,
          'CLI + HTTP become proper OpenAPI codegen: ONE spec drives the ts node server now, rust axum later, and the CLI; the spec itself is GENERATED from prolog facts (dogfood the codegen from our language); this is the foray into json feature land').
directive(json_as_rel_type,
          'json becomes a rel column type that LOWERS TO SQLITE JSON1 ("we gotta play ball") -- REFINES the earlier one-rel-boundary reading that classified stored inline json as pure migration debt; typed refs and json columns coexist').
directive(json_syntax_native,
          'json natively expressable 1-1 in the language; unquoted keys when valid (json5-ish); HOLES like v3/v4 brace patterns; the { opening will later be abused beyond json, FOR NOW { means json').
directive(roots_never_again,
          'target = hundreds of repos AND an above-repos root; v5 roots concept must not return; defaults spelled as ordinary rels/builtins/sh/hosts; unspecified repo root = git rev-parse --show-toplevel walk; SCALE TEST EARLY').
directive(rust_flip_soon,
          'ts -> rust flip coming, prolog compiler stays; server + cli expressed from the same openapi spec on both targets').

% bug(Name, FoundBy, Text). -- every defect any lane surfaces, marked here on arrival.
bug(ref_pattern_depth2_silent_empty, filespan_lane,
    'relation pattern at depth>=2: emitted SQL json_extracts a path out of the INTEGER __id endpoint -- always NULL, rel permanently empty, NO refusal (join_column_type_mismatch catches only int columns by accident, path is text); measured oracle span=1/coord=1 vs emitter 0/0; root cause head_target_atoms/4 walks direct head args only; handed to rel_edge_clock_fixpoint lane, stage-1 fix = named refusal on ref columns').
bug(decode_depth2_oracle_derives_nothing, filespan_lane,
    'chained decode at depth 2: emitter produces the correct two-hop indexed join (1 row verified), oracle derives NOTHING -- compile_pattern_arg one-level implementation; same lane owns it').
bug(roundtrip_two_door_decl_disagreement, phase5_lane,
    'the 18 roundtrip fails = ONE defect: parse_dl:574 always carries type_decl AND col_type forms, print_dl:209 decl_ref_order prints a decl line per form = duplicate rel line on reprint, reparse arity-checks the doubled list and drops the type. Fix measured 19/19 + byte-identical views: 4-line print_dl guard + explicit col_type rows in 4_struct_values.pl, must land TOGETHER; coordinator owns the landing').
bug(a6_crosscheck_nondiscriminating, opus_review_gate,
    'sol clock checker A6 "proven" unearned: all-zero-offset graph = crosscheck compares two constants; sabotage receipts S1 (constant observer stays green) + S4 (Grade=0 hardcode stays green); clock_boundary multi-trigger requirement has no negative case (S2 invisible); fix list sent back to sol (nonzero-offset pairing proven by reviewer)').
bug(flow_call_target_resolver_defect, codex_terra,
    '32 call-target rows share a call coordinate but select DIFFERENT callees across engines (16 v5-only + 16 v6-only) -- real resolver divergence, distinct from the 110 extraction-input rows; per-edge attribution blocked by the serve-lifecycle flake below').
bug(serve_lifecycle_idb_read_race, codex_terra,
    'v6 HTTP server exited before final /idb reads in the flow rig -- paired TSVs lost after a full successful run; same family as the golden/EADDRINUSE flakes; unowned').
bug(hostdecode_hardcoded_port_collision, phase5_lane,
    'hostDecode.test.ts EADDRINUSE :::17611 when two lanes run the suite in one tree -- hardcoded test ports collide; passes isolated; unowned').
bug(run_results_json_mode_dependent, phase5_lane_and_review,
    'out/run-results.json refusal text differs naive vs incremental (engine.pl retract_from_log suffix) -- whichever sweep mode ran last leaves the checked-in receipt dirty; unowned small').
bug(phase5_untracked_view, phase5_lane,
    'dl_view/float_integral_value_keeps_real_storage.dl6 untracked -- bool/float lane never committed its regenerated view (174 tracked / 175 on disk); coordinator to commit').
bug(diag_rail_template_mismatch_suspect, coordinator_probe,
    'coordinator probe with diag-rail-shaped host decl (digest input unreferenced in template) refused template_mismatch(unreferenced_input(digest)) by bop check -- diag-rail.dl6 itself has that exact shape, so either the fixture no longer passes the text door or the doors diverge; VERIFY before next lsp-diags run').
bug(glob_root_level_zero_match, coordinator_probe,
    'watch glob **/*.ts matches NOTHING for repo-root-level files under git pathspec (files must sit in a subdir) -- known v5-globset-vs-git-pathspec divergence class, new bite: silent empty world, zero diagnostics; candidate for a load-time warning when a watch glob matches zero enumerated files').

% ── COORDINATOR FINAL THOUGHTS (fable, wind-down) ────────────────────────────
final_thought(review_gate_pipeline,
    'the sol-implements/opus-falsifies/sol-fixes loop earned its cost in one pass: the ONLY affirmative claim in the clock landing (A6 proven) was the only wrong claim, and only sabotage found it (all-green batteries reproduced exactly). Make this the default shape for any lane whose deliverable includes a PROVEN row').
final_thought(unit_maxing_confirmed,
    'every bug this session was a COMPOSITION bug under all-green suites (depth-2 refs both-directions broken, two-door decl interplay, misnamed refusals at construct junctions). The corpus grades constructs alone; cross terms are ungraded. golden_flex_e2e + its registry coverage gate is the structural answer; treat single-construct fixture additions as insufficient evidence of health from now on').
final_thought(harness_base_defect_recurring,
    'the agent-worktree base staleness class bit FIVE times in one session (stale cut x4, mid-run reap x2, fallback-to-shared-tree x1). Coordinator-cut worktrees at the exact sha + explicit-path staging is the only shape that held. Never dispatch Agent isolation:worktree in this repo again without checking the cut base').
final_thought(language_state,
    'locked core is real and holding (value plane, host surface, single rel model, storage); the open design mass is concentrated in three places: json/{}-family (directives now set, cards coming), scan/match composition surface, span-spine 3 residual cards. The one standing violation is the match-arm token pair. The doors and the registry are strong enough now that coverage gates can be GENERATED from the registry -- use that everywhere').
final_thought(dogfood_velocity,
    'the default-export diag went directive-to-live-rows in under an hour with zero engine edits (ast-pattern host + 5-rule program + served HTTP). The language is genuinely usable for its own tooling now; the parity/spelunk lane tests whether it can also MAINTAIN itself').

% ── WIND-DOWN 2026-07-30 (user out of tokens; agents left RUNNING) ─────
session_pause('2026-07-30', fable_wind_down_agents_alive).
pause_tip_note('branch codex/rel-ref-file-span-lab, tip = the comment-rails merge; run git log to find it; all landed work committed, ledger current').

% in_flight(Lane, Where, GateOnLanding). -- DO NOT redispatch; they report on their own.
in_flight(depth2_ref_fix, 'opus @ ../sprefa-lane-depth2 (lane/depth2-ref-fix)',
          'verify fail-first fixtures went red first + EXPLAIN SEARCH receipts; re-run full battery; merge branch').
in_flight(golden_e2e, 'opus @ ../sprefa-lane-golden (lane/golden-e2e)',
          'verify registry-coverage gate catches a removed construct (red receipt); re-run golden at 0/1/many + served leg; merge; then wire recipe into green-all').
in_flight(json_syntax_lab, 'opus @ ../sprefa-lane-jsonlang (lane/json-syntax-lab)',
          'CARDS ONLY -- no production syntax may land from this lane; relay the exact spelling card list to the user for sign-off (no-unsighted-syntax law)').
in_flight(openapi_codegen_lab, 'opus @ ../sprefa-lane-openapi (lane/openapi-codegen)',
          'verify parity-gate red/green receipt + emitted openapi.json validates; cards to user; merge lab as design record').
in_flight(v5_parity_spelunk, 'opus @ ../sprefa-lane-spelunk (lane/v5-parity-spelunk)',
          'verify parity table reconciles the scan()-105/129 receipt; git-diags rail red+green receipts; merge').
in_flight(flow_residue_partial, 'codex terra STOPPED, dirty tree @ ../sprefa-codex-flowres',
          'blocked on serve-lifecycle race (golden lane owns the fix); after that, resume terra session or fold its reconstructed tables; findings doc in that tree').

% next_coordinator(OrderedActions).
next_coordinator([
    read_each_lane_report_and_run_its_gate,
    merge_by_sha_only_after_gate,
    update_this_ledger_per_landing,
    cleanup_merged_worktrees('codex-clockcheck, codex-comrails, codex-flowres after fold; lane/* trees after merge; stray .claude/worktrees/clock-checker-resume + branch (draft 3eb59512 superseded by merged clock work -- verify then delete)'),
    rerun_crawl_bench_v6_leg_post_batching('grafana 40 f/s number is stale-pessimistic'),
    present_user_cards
]).

% user_cards_open (consolidated, present on next contact):
user_card(match_arm_tokens, 'ratify |-> |+> into rulings.pl vs revert 9cadb419 to arrow reuse -- the one standing syntax violation').
user_card(json_spelling_cards, 'json-syntax lab will return exact spellings (json5 braces, holes, list types) -- sign-off before any parser edit').
user_card(filespan_three, 'rev-family naming; line/col residency (in-language newline rows vs provider); work-revision identity').
user_card(fold_blocked_labs, 'scan_surface composition cards; json_interop module policy; select_scan_cache + generic_scan choices').
user_card(push_and_tag, 'main + v6.2.0 tag push remain user-gated; lab branch pushed to origin periodically by sol convention').

% finding(Name, Severity, Text). -- appended as review proceeds.
finding(match_arm_new_tokens_unruled, syntax_violation,
        'commit 9cadb419 minted |-> and |+> as NEW text-door tokens for guards-first match arms (prior match_arm reused <- / <+ head-first); rulings.pl byte-unchanged across the branch, only authority is an unattributed surface_ruling/2 in the agent-written completion-drive plan WHICH FORBIDS new spellings 8 lines earlier; term form unchanged, 23 .dl6 fixtures hard-migrated; USER CARD: ratify into rulings.pl vs revert to arrow reuse').
finding(bop_load_query_flake, defect_unowned,
        'tests/bopLoadQuery.test.ts load-then-q fails under full-suite load, passes 6/6 isolated -- port/contention flake class, indexed nowhere; second undisclosed failure beside the known serveHost one').
finding(branch_gate_red, standing,
        'just green is RED on this tip: 18 roundtrip failures all 4_struct_values fail(not_variant) (owned by phase5 lane in flight); ledger counts stale in the green direction (plunit actually 188/188 vs claimed 173, text_door 123/123 vs 122)').
finding(spot_check_clean_bill, cleared,
        '16/25 commits clean; eprintln net 109->109 (reflow only); zero dep additions; no second type system (9a245a2e adds no runtime interfaces; extractor adds 2 typed structs per trait boundary); no new magic names; probe/salt removal genuinely complete in tracked source; full sweep regenerates byte-identical (no gen staleness debt)').
finding(fmt_residue, trivial,
        'cargo fmt --check fails on src/setup/hooks_tests.rs (one stray blank line) -- 4afb949b incomplete; coordinator fixing').
finding(arch_stale_probe_rider, trivial,
        'ARCH.pl construct(probe_rider, t5, new) survives the probe-surface removal; coordinator fixing').
adopted(fold_order_wave2,
        [fix_red_gates_first, rule_on_match_tokens, kill_folded_labs,
         wire_ghcacher_gate_post_luna, fold_mode_lab_views,
         clock_then_rel_definition_hash_serial, comment_fold_post_luna,
         flow_fold_post_terra, park_card_blocked_labs]).
finding(clock_opus_standdown_bank, handoff,
        'stood-down opus clock agent banked UNVERIFIED draft 3eb59512 (branch clock-checker-resume, self-minted worktree .claude/worktrees/clock-checker-resume after its assigned tree was reaped -- disclosed deviation, tree+branch pending cleanup after reconciliation). SOLID probe findings for the sol reviewer: all 8 historical programs ACCEPTED by check_clock_program/1 (not_provable rows are honest); A4/A7/A8/A9 decl-only = empty causal graph; A5 same/1 vs same/2 are distinct Name/Arity clock nodes so the emitted-identifier collision is invisible by construction; A6 zero-offset data alone is NON-DISCRIMINATING (hardcoded offset-0 would pass) -- pair with edge_chain_hops_tick_per_stage (inferred stage_two offset 1, observed tick 2 vs origin 1) to make the crosscheck falsifiable. Pre-edit baseline plunit 188/188.').
finding(worktree_base_staleness_recurrence, process,
        'Agent-tool worktrees cut from stale local main (01ac896e, 73 behind base 6c3a7e2d) -- the 2026-07-28 process-defect class again; all 4 worktree opus lanes hit it; clock + phase5 agents STOPPED correctly per dispatch law; coordinator resumed each with the sanctioned ff-only merge to 6c3a7e2d, commits land on agent branches, merge-back by sha').
% stale_in_claude_md(Claim, Correction). -- auto-loaded context now contradicted by the world.
stale_in_claude_md('hosts wiring presents `? probe` and `@ salt` riders as landed surface',
                   'both REMOVED by the v6.2 host-surface locks (no_rhs_probe_marker, no_salt_rider); a registered host relation is an ordinary RHS atom').
stale_in_claude_md('main is the ride with 32 unpushed commits',
                   'the ride is codex/rel-ref-file-span-lab, 40+ ahead of local main; origin/main further behind').
stale_in_claude_md('phase 5 (float/REAL+avg, clock checker) is the remaining unstarted leg',
                   'bool/float LANDED (landing_ready + verification in 20260730.0 ledger); clock checker paused mid-implementation with recorded resume order').
stale_in_claude_md('fresh-worktree js packages runnable after checkout',
                   'pnpm install required per worktree per package (v6/tsv2, v6/sprefa-store/js, v6/dl); bit the terra lane; binary flavor previously ledgered as gen_staleness_gate').

finding(isolation_worktree_reaped_midrun, process,
        'harness auto-cleans unchanged agent worktrees; a lane that STOPs clean at the base check is exactly that shape, so its resume finds no tree (file_span lane hit it). Mitigation adopted: coordinator-cut worktrees at the correct sha (git worktree add ../sprefa-lane-<name> -b lane/<name> 6c3a7e2d) + non-isolated agents pointed at them; file_span relaunched that way on lane/filespan-reconcile').
finding(flow_rig_no_binary_override, defect_small,
        'flagship-flow.sh hardcodes $REPO/target/release/dl with no env override (extract has DL_EXTRACT_BIN); codex terra lane stopped clean on missing binaries after 1/6 runs; coordinator building both release binaries and copying into the codex worktree, then resuming the terra session').

% ── USER DIRECTIVES 2026-07-30 morning (session resumed; agents were left alive) ──
directive(e2e_from_cli_never_ts_imports,
          'benches and e2e tests invoke the CLI (bop / the released binary) or nothing; no e2e test may import the codebase from TS. Any e2e that reaches inside the process is a cheating test. Receipts are TRACING MEASURES that come out the same way from prolog logs, ts node logs, and rust logs -- one receipt shape across all three targets, so the rust flip does not invalidate the receipt corpus').
directive(test_shape_min_test_max_coverage,
          'e2e minmax and integration minmax carry the grading weight; unit tests only for minor pure edge cases. Minimum number of tests for maximum coverage, every time. Explicitly banned: high-count low-value unit suites ("baby dick tests")').
directive(parity_where_it_counts,
          'parity is graded where it matters (real programs, real corpora, external oracles), not by unit-test volume').
directive(rust_flip_frontier_reading,
          'user reading of the trajectory: the only real remaining lab frontier is matching rx semantics FROM CODEGEN and weaving sqlite through it (mostly the same shape as today); then spelunk v5 for parity; then surpass it. Coordinator largely CONFIRMS from the pl files -- see answered(trajectory_check)').

lane_landed(openapi_codegen_lab, 'merge bf71c5b6',
     'coordinator re-ran receipts.sh itself, exit 0: parity gate 3/3 green, sabotage OPENAPI_LAB_DROP=readRelation 0/3 RED while the lying spec still passes Redocly (a validator cannot catch a lie by omission), withdraw -> 3/3 green. Four parity sources: emitted spec vs 4_http.ts ROUTE_LIST vs parsed dispatch branches vs a LIVE server 404 route list. Inventory 5 routes/5 operations/14 responses/14 schemas; CLI 7 verbs of which only 4 are routes (serve/run/check are process lifecycle, unexpressible in any spec-driven CLI generator = the ceiling on the CLI half). BUY: openapi-typescript (dev dep) + Redocly via pnpm dlx (no dep) + progenitor for rust clients post-flip; direct axum emission over utoipa/aide (utoipa makes rust the spec source and inserts a drift link); fastify-openapi-glue is the design to copy, rejected on architecture (adopting it = adopting fastify over a 410-line node:http front with soak-tested teardown). SSE honestly unspeakable in 3.1 (text/event-stream + type:string describes bytes not frames); 3.2 itemSchema is the fix; /ticks marked skip-for-codegen. Lab NOT deleted on landing: cards 6+8 would promote the prototype into serve/, so lab-death waits on that ruling.').
user_card(openapi_eleven, '11 cards from the openapi lab; load-bearing four: (1) spec artifact checked in + staleness gate vs gitignored out/; (2) info.version identity now written in a FOURTH place (git tag / package.json / facts / content hash); (6) static spec vs program-derived spec (GET /idb/{rel} is only typeable in the abstract -- rel and Row arity come from the LOADED program; a GET /openapi.json could expand per rel with real prefixItems, dogfooding twice); (8) does ROUTE_LIST become GENERATED from the facts (turns parity legs 1<->2 from a check into an identity, deletes the two-hand-kept-lists crack, but is the first production edit to serve/)').
finding(openapi_lab_cracks, defect_small,
        'serveFailure can answer with no body when headers are already sent (SSE case); the catch-all 404 {error, routes} shape is unspeakable in the spec; bop.ts casts JSON.parse(...) as {rows} unchecked on every read').

finding(grading_rig_is_in_process_everywhere, arc_sized,
        'directive(e2e_from_cli_never_ts_imports) indicts more than one lane: the WHOLE corpus grading rig drives the runtime by TS import. golden-run.ts (golden lane, 4bd4c38f) imports BootRunner/ScratchStore/TickLogEmitter/TickFold/rowValueFromSql from ../runtime, and it says so honestly -- it copies sweep.ts shape ON PURPOSE so a divergence is a module divergence not a two-copies-of-the-comparison divergence. sweep.ts and run-emitted.ts are the same shape. Target-neutral doors that ALREADY exist: the served HTTP leg (serve/main.ts, used by runtime-bridge phase 1) and bop run/q/ticks; the prolog side is already CLI-driven (golden_oracle.pl, golden_coverage.pl: 0 imports, swipl-invoked). CONSEQUENCE for the rust flip: the prolog leg and the served leg survive the flip, every in-process TS harness does not. Not a one-lane fix; wants its own arc with the receipt shape decided FIRST (one tracing-measure format out of prolog logs, node logs, rust logs).').

lane_landed(json_syntax_lab, 'merge 62f9ce84',
     'coordinator re-ran the lab entry itself: JSON_SYNTAX_LAB 25 PASS exit 0 (grammar 7, lowering 7, lists 7, cards 4); diff vs base = plans/ + labs/json_syntax/ ONLY, parse_dl.pl untouched, no-unsighted-syntax law held. HEADLINE: both constructs the recovery doc graded "genuinely needs new surface" have EXACT json1 lowerings -- key capture {$key:$value} is json_each(key,value) with zero new SQL, ** is json_tree, and json_tree.fullkey in the same join is v4s dropped $$${PATH?} for free. Only spelling is open. Grammar = 11 productions, literal grammar IS the pattern grammar minus holes (by construction, not a second DCG); key-axis productions are pattern-only forever (computed key = json_object(K,V)); quoting marks literals on the value plane, bareness on the key plane, FORCED by JSON5 not chosen. 27 archive examples parse verbatim; gh-cache flagship parses verbatim and its dl6 transcription yields the byte-identical IR. LISTS: json carrier wins 5/5 axes, list(T) = typed view over a json column, T over a closed 4-scalar set, checker delta measured at exactly 4 clauses, no type variable; 1000-element list = 1 row carrier / 1001 indexed / 1000 cons, all rendering byte-identically; relational element storage is not a list, it is a rel. CARD-SUBTREE-CAPTURE closes the projects oldest open json question (human-goals.md:693). 9 cards answered by the three ruled directives, 13 open carrying 29 exact spellings.').
finding(jsonb_not_portable, constraint,
        'jsonb is NOT portable across the two SQLite builds this project already runs (system CLI 3.43.2 rejects, libsql 3.45.1 accepts) so json columns store TEXT; REGEXP is not core SQLite (both current builds ship one, rusqlite does NOT -- prices CARD-REGEX-KEY against rust_flip_soon); json1 will not canonicalize so canonical_json_text/2 keeps that job and the cross-target tick-log contract does not move; the emitted e0.type=object guard is critical (without it, descending into a scalar raises malformed JSON and kills the statement instead of failing silently -- bit the lab on first run); SQLite can CHECK array-ness but cannot CHECK a list element type (no subqueries in CHECK)').
user_card(json_thirteen, '13 open json cards, 29 exact spellings, gated by receipt C3. Top 5 by leverage: (1) CARD-KEY-HOLE-SPELLING unblocks 4 of the 5 needs-new-surface rows -- {$key: $value} vs {(key): value} vs {[key]: value} vs invert-quoting; (2) CARD-PATTERN-GOAL-SPELLING decode(body,{..}) vs body = {..} vs match(body,{..}) -- NOTE the json_as_rel_type directive takes json as a TYPE word so v5s own json(body, q:{..}) op name is gone; (3) CARD-LIST-SPELLING tags: list(text) vs text[] vs json; (4) CARD-JSON-NULL now TWO questions (null inside json stored vs null as column value refused / reject at ingress / explicit variant); (5) CARD-BRACE-TAG reserve point{x:1} now for the stated later abuse of { or do not. Then JSON5-subset, string-quote, descent-depth cap, regex-key (priced against rust: rusqlite ships no REGEXP), value-pattern (lab recommends NOT wanted), format-dispatch; CARD-EDGE-BODY-JSON + schema_import_boundary are spelling-free scheduling only, and the latter may be INVERTED by openapi_codegen_spine (if the spec is generated from prolog facts, the import half may never be needed)').

lane_landed(external_oracle_scout, 'merge d9b808bf',
     'codex luna analysis-only, coordinator re-verified the headline counts itself (14 oracle test fns across 9 files, all #[ignore]d; scip.rs really does subprocess scip-typescript/scip-go/rust-analyzer). ANSWER to the user question: v5 YES -- oracle_madge.rs grades 8 relations (dep, cycle_member, orphan, leaf, summary, depends_on, npm_dep, skipped) from examples/madge.dl against madge --json/--circular/--leaves/--summary/--depends/--warning. v6 tsv2 legs: NO madge anywhere. CORRECTION to the coordinators own earlier claim that v6 is oracle-free: v6/sprefa-extract/tests/golden_parity.rs runs scip-typescript, scip-go and rust-analyzer as ORDINARY non-ignored call-resolution ratchets, so v6 external ground truth exists on the EXTRACTOR side and is absent on the ENGINE side. The real gap: no v6 resolved TypeScript dep(src,dst) relation to diff against madge; the extractor has no ts module resolver (types.rs:587-603), which is the actual missing piece, not the diff harness. v5 default-run count: 0 of 14, every oracle is on-demand behind a tool gate, so v5s external oracles are NOT gating either.').

% ── USER RULINGS 2026-07-30 (both written into v6/prolog/conformance/rulings.pl) ──
answered(json_key_hole_marker,
         'DOLLAR. {$key: $value} on both planes; unblocks 4 of the 5 needs-new-surface json constructs; lowering already proven json_each(key,value), zero new SQL').
answered(match_arm_tokens,
         'RATIFIED |-> and |+>. Authorship settled: the USER asked for them, for left-to-right reading (guards first, arrow, then head, so an arm reads the direction data flows). Standing intent attached: rel programs look uniform and express flow ACROSS TIME. finding(match_arm_new_tokens_unruled) CLOSED; the 23 migrated .dl6 fixtures stand').

answered(rx_operator_inventory,
         'MEASURED at 2026-07-30 HEAD. CODEGEN surface = 4 operators, and all 124 gen_emitted modules carry the byte-identical import line: concatMap, forkJoin, map, of. HAND-WRITTEN runtime+serve = 24 distinct: concatMap map of from forkJoin toArray filter catchError defer merge mergeMap expand finalize throwError scan take interval bufferTime fromEvent partition switchMap takeUntil tap share (plus EMPTY/Observable/Subject/Subscriber/asyncScheduler infra). switchMap appears ZERO times in emitted code; its only executing site in the whole app is 4_http.ts:399 accepted$.pipe(switchMap(runProgram$)) = program swap, and 2_binds.ts documents relying on it to close OS watches. CONSEQUENCE for rust_flip_soon: the emitted rx is a SEQUENCING SHELL (run statements in order, run independent reads together, shape rows, lift a constant); every datalog semantic lives in the emitted SQL text and the tick loop, so the port owes tickLoops expand + the serve-level lifetime operators, not a datalog runtime').
finding(test_shape_inventory, measured,
        'tsv2 tests at 2026-07-30 HEAD: 31 files, 88 test cases. 21 of 31 files import runtime/serve/lower internals directly (in-process); only 9 touch a served HTTP leg (startServed/fetch). Largest in-process files: structPlane 426, 6_host-extraction-batching 415, departureFrontier 259, serveHost 251, watchBootReconcile 227. The CLI-driven side is bopCheck/bopCommandInventory/bopRun (0 imports) plus bopLoadQuery. This is the concrete target list for directive(e2e_from_cli_never_ts_imports) + directive(test_shape_min_test_max_coverage)').

lane_landed(depth2_ref_fix, 'merge 2e2b983b',
     'coordinator re-ran EVERY leg on the branch including the fail-first: at 4b0bc279 (fixtures only) conformance is 176 PASS / 10 fail with the 6 relation_depth fixtures red on MISMATCH final file/2, span/3, dcoord/3, located/2 -- red before the fix, coordinators own run, not the agents claim. After: conformance 186/0, plunit 200/200, text door 131/131/0, roundtrip ALL GRADES, sweep both modes 131 compiled/129 identical/0 wrong (FINAL 129/2 pre-existing), tsv2 92 (91/1skip/0fail). bug(ref_pattern_depth2_silent_empty) and bug(decode_depth2_oracle_derives_nothing) CLOSED. Third divergence found en route that the plan had not measured: file disagreed at DEPTH 1 on bytes ({"name":"acme"} vs "repo(acme)") -- the tick log had been printing prolog term text for rule-built relation values all along, never graded.').
finding(memoization_invisible_to_end_state, count_test_law,
        'depth2 sabotage receipt: disabling the emitter relation-pattern memoization leaves EVERY conformance fixture and the WHOLE sweep green while the depth-2 span insert grows from 3 joins per arm to 5. End-state equality cannot see join growth; only the EXPLAIN hop-count test can. Third instance of the formerly-quadratic-paths-get-COUNT-tests law paying off, and the cleanest statement of why unit-maxing on end state does not catch composition cost').

directive(relation_pattern_stays,
          'user 2026-07-30, mid-lane: "dont listen to sol too much tho if its just destructuring in head positions that sounds like we would eventually want it". The landed relation-pattern feature is KEPT on its own merits; the sol adversarial review is demoted from keep-or-delete referendum to BURR FINDING only. Standing weight rule: an adversarial reviewer priced against a feature the user wants gets read for its defect list, not its verdict').
finding(coordinator_mislabeled_relation_patterns, coordinator_error,
        'the coordinator presented the depth2 relation-pattern feature to the user as "rels as values". It is NOT. It is nested construction + destructuring of ref-typed columns, first-order, rust-esque pattern syntax. The users rels-as-values means passing a REL ITSELF into an arg slot so a rule is parameterized by which relation it reads or writes -- higher order, needs call/N in prolog. User caught the conflation. Two lanes dispatched on it: lane/rel-as-value-lab (opus, labs the real idea + whether it moves the list(T)-only generics verdict + whether content id can name a REL and not just a row) and codex/relpattern-review (sol, adversarial on what landed)').

lane_landed(golden_flex_e2e, 'merge on codex/rel-ref-file-span-lab after 3f68c9c2',
     'coordinator ran the battery itself AND re-ran golden-flex on the merged tree beside depth2: green reached its last recipe (store 74/74), GOLDEN FLEX HOLDS exit 0, sweep 123 compiled/121 identical/0 wrong on the branch, conformance 186 merged. COVERAGE SABOTAGE was the coordinators own and it caught a coordinator error first: the initial attempt reported green because BSD seds 0,/re/ address silently made NO edit; redone with perl, file change confirmed by grep, gate exits 1 naming latest/1 unaccounted for, restore goes green. Lesson generalizes: a sabotage receipt that never verified the sabotage landed is not a receipt. golden-flex is wired into `green` itself, stronger than the gate asked for.').
finding(sabotage_must_verify_the_sabotage, method,
        'coordinator ran a coverage-gate sabotage that came back GREEN and nearly recorded it as a gate defect. The edit had never applied (BSD sed 0,/re/ is a GNU-only address form, no error, no change). Standing method fix: every sabotage receipt must PROVE the sabotage landed (grep the changed file, or diff it) before the gates answer is interpreted. A green result from an unapplied sabotage is indistinguishable from a blind gate').

directive(surface_blockers_immediately,
          'user 2026-07-30: "please for godsake bring this up immediately next time we are going lightspeed". Standing coordinator duty: open design cards and FOLD-BLOCKED labs get surfaced at the TOP of a status report, not on request. A blocked lab is idle capital. Current biggest single unblock: scan_surface_composition (one user decision frees 3 labs: scan_surface_composition, scan_match_reconciliation, generic_scan_instantiation, and select_scan_cache rides the same axis)').
finding(labs_not_assimilated, standing,
        '10 labs alive on disk against a lab protocol that says labs die on landing. 2 deliberately alive (json_syntax, openapi_codegen -- their cards would promote them). 8 are debt: rel_value_unification (12 files, BIGGEST, and it is the same territory as the users rel-as-value question -- pointed the running lab at it), ghcacher_tick_golden (7), rel_definition_hash, json_interop, scan_surface_composition, scan_match_reconciliation, generic_scan_instantiation, select_scan_cache').

% ── DESIGN THREAD 2026-07-30 (user + coordinator, OPEN TENSION, not rulings) ──
% design_tension(Name, Text). Nothing here is settled; two labs are grading it.
:- discontiguous design_tension/2.
design_tension(identity_vs_flattening_conflated,
    'user: we over-indexed on unique keys. IDENTITY (which row wins) is answered by keys and normal forms and is solid. FLATTENING (how many inners run, what happens to losers) has NO spelling and is hardcoded to concat. distinctUntilChanged(byKey) is not switchMap; switchMap flattens every matching source event and tears down losers. Two axes, one mechanism').
design_tension(effect_plane_deaf_to_retraction,
    'measured by the rxoracle harness: HostRunner.liveDemand$ reads delta.add and NOTHING anywhere reads delta.del. There is no site at which a teardown could fire. Consequences measured: superseded inner runs to completion, its answer lands durably and is memoized, and concatMap makes the loser BLOCK its successor for the losers full duration. sprefa does not cancel, it memoizes').
design_tension(nothing_has_a_stream_type,
    'a rel is a TABLE, the tick is the ONLY time axis, deltas are derived per tick. A stream cannot be named, bound, passed or returned. Every rel HAS a delta stream; no rel IS one. Consequence: switchMap cannot be hosted as a construct whose result is a new stream, because there is no slot for a stream to land in; its result would have to be a new REL, which returns the question to tables').
design_tension(two_doors_for_flattening,
    'DOOR 1 the language expresses it: delta.del reaches the effect plane, an ordinal makes newest-wins ordinary data, switch becomes a rule (retract demand rows not at the latest ordinal). Cost: nothing new in the type system. |-> arms + finalize + keyed replace already carry most of it; the missing piece is mechanical. DOOR 2 host switchMap as a construct: needs a stream type or a rel-that-is-a-stream, which fights locked(single_rel_type_system). Lanes lane/teardown-flatten and lane/rel-as-stream are grading both').
design_tension(scan_forces_a_state_model,
    'user objection: scan sucks because it forces a state model. Measured in rxoracle scan_state_feedback: events +1 +1 +2 give rxjs 1,2,4 (every intermediate an event) and sprefa the correct total 5 with the intermediate 4 COMPUTED and CONSUMED and not an event anywhere. Datalog hands you STATE, not the sequence of states. Getting the sequence means making it a rel (log + ordinal), which is the honest lowering rather than a workaround').
design_tension(tier_zero_test,
    'the standard the user set for any new syntax: classify every wanted construct as (a) sugar over an existing lowering, (b) a new lowering of existing semantics, or (c) TIER 0, a genuinely new semantic no lowering over current constructs can express. Only (c) justifies syntax. Prove tier-0-ness, never assert it').
design_tension(north_star_reading_order,
    'user standing intent: the language should eventually read left to right, top down, point free, FRP/rxjs flavored. |-> and |+> were ratified 2026-07-30 for exactly this reason. A spelling only a compiler author can read is a wrong answer even when it lowers correctly').
design_tension(no_dot_syntax_on_rels,
    'measured: zero registry rows, zero SYNTAX.md mentions. Two accessors exist and do the same job -- the relation pattern (located(span(file(_, fpath(P)),_,_),_)) and the decode chain. A dot would be a THIRD spelling for the same access. Card, not a gap').

% ── WAVE 3 LANDINGS 2026-07-30 (all coordinator-verified by re-running receipts) ──
lane_landed(rx_oracle_harness, 'merge on the lab branch',
     'coordinator ran run.sh itself: RXORACLE HOLDS 8/8 as declared. Leg A literal rxjs importing nothing from the repo, leg B bash only (bop serve + curl). Clock is the STEP not the tick; an event within guardMs of a boundary fails LOUDLY as a straddle, and that guard caught the agents own driver drifting 40ms/step. N3 (drop sprefas del lines) is opt-in because OPTING IN IS THE FINDING. CANCEL ANSWER: sprefa does not cancel, it MEMOIZES; the loser runs to completion, its answer lands durably, re-demand is a same-tick cache hit where rxjs re-subscribes and re-runs. Discriminating outside observation = TIME the re-demand').
lane_landed(teardown_flatten_lab, 'merge d966b407',
     'coordinator ran receipts.sh: TEARDOWN LAB HOLDS, 15 receipts. HEADLINE: door 1 was ALREADY THE RULED POSITION and has been unimplemented since 2026-07-27 -- effect_abort needs no amendment, its own text says demand-row deletion IS the abort signal and names the unbuilt site (1_hosts.ts:387 filters inserts only). The kill is already written at 1_hosts.ts:175 and has NEVER been called. Four flatteners measured on the same /ticks stream with one operator changed; concat reproducing the shipped ledger exactly is what makes the other three credible. THREE FINDINGS PAST THE BRIEF: merge and concat are the SAME PROGRAM (which inners die = programs business, how many run = machines); switch needs NO slot key because del already carries the rules decision, making teardown-on-del strictly MORE GENERAL than switchMap; NO ORDINAL is needed anywhere (three placements priced and rejected -- the coordinators earlier sketch was wrong on that). Teardown = 4 sites in ONE file, all serve/1_hosts.ts. Lab placed at repo-root labs/ not v6/prolog/labs/, noted not moved').
lane_landed(rel_as_stream_lab, 'merge 7d040ab0',
     'coordinator ran receipts: 10 PASS 0 FAIL. TIER-0 LIST IS EMPTY: 19 constructs classified, one (a), two (b), rest already spelled; no new syntax justified. A stream is a VIEW OF A TABLE PLUS A NAMING CONVENTION, and locked(single_rel_type_system) is not fought anywhere because log_on_level_headed_rel already forces a readers projection to be a table. DECISIVE RECEIPT: one tick batching two increments, keyed cursor reports 1->3 with the intermediate gone, log rel reports ordinals 2 and 3 as separate rows -- the users scan objection is exact and the fix is not removing the state model but PUBLISHING THE SEQUENCE BESIDE IT. Also: backpressure with ZERO constructs (writer gated on a derived watermark, overflow visible in a dropped rel); keep(count(0)) is deliver-and-forget so the whole rx Subject family is a two-word decl matrix; zip = equijoin on the ordinal, bufferCount = integer division; the ordinal survives a process restart and rx has no durable position. CRACK: the two doors already carry DIFFERENT internal orders (engine.pl:357 one counter across all log rels per tick vs lower.pl:2275 per-table rowid), neither graded because neither is observable; surfacing an engine ordinal must pick one and the pick becomes a graded contract').
lane_landed(prolog_compile_profiling, 'merge 4dd7e230',
     'THE 232s MYSTERY IS NAMED. plan phase = 255,333ms of 255,490ms on flagship-flow.dl6 at 6,011,087,004 inferences; hot predicate clock_check:graph_reachable/4 under clock_scc/3; local growth exponent 4.899 (26ms at 10 rules, 86,742ms at 58). Coordinator read the code and confirms the story: SCC computed by ALL-PAIRS MUTUAL REACHABILITY where each call enumerates SIMPLE PATHS with a Visited list and no memo. Sharpest number: a 27-rule REAL program took 255s while a 30-rule chain took 3.6s, so graph SHAPE dominates size. Method (the user asked to be told how the numbers were obtained): statistics/2 deltas inside setup_call_cleanup/3, library(prolog_profile) for predicate attribution (library(prof) unavailable); prolog_trace_interception/4 REJECTED at a measured 5.3x overhead at 20 statements; debug/3 rejected as messages not counters; /usr/bin/time -p with 2 repeats because hyperfine is absent. Off path proven free by sha256 equality of control/off/on, not asserted. plunit 200/200').
lane_landed(v5_parity_spelunk, 'merge 754889b5 -- LATE, see the process finding',
     'v5 is SELF-DESCRIBING (op_catalog/fn_catalog/rel_catalog are builtin rels) so the inventory comes from ASKING v5 through an sh host and the line-regex leg is graded against that. 28 ops / 16 fns / 112 rels; v6 covers 20, partial 4, ABSENT 132, and ZERO of sixteen scalar functions. Highest-usage absent: gen 24 files, comment 20, closure 17, scc 9. scan() 105/129 reproduces EXACTLY and is the shell-glob spelling (the programs 108/132 is git-pathspec where * crosses /); nothing was stale; scan is in 29 of 33 .dl rails, which is the number with weight. LIVE SHIPPED DEFECT found: serve/1_hosts.ts parseWhitespace reads N lines as one row of N columns when line count equals column count, so the shipped 2-column enumerate_at SILENTLY MANGLES any 2-file answer (right at 3, wrong at 2) -- now defect D1. Also: a backslash in a .dl6 string constant is silently deleted TWICE (parse_dl.pl:301 catch-all, then JS template-literal processing) so every regex in a dl6 program is backslash-free by necessity -- now D2').
finding(coordinator_forgot_to_merge_a_landed_lane, process,
        'the v5-parity-spelunk lane reported done, the coordinator relayed its findings to the user IN FULL, and then never merged it. Caught only when the user asked "did we scaffold v5 btw". The relay is not the landing. Standing fix: a lane is not done until its branch is merged, its worktree removed, and its row written here; reporting findings is step 3 of 4, not the last step. filespan-reconcile had the same shape (1 commit, plan doc, merged at the same time)').
finding(merge_receipt_is_not_discriminating, receipt_gap,
        'user caught it: the teardown labs merge receipt reads start r1 / start r2 / done r1 / done r2, but BOTH JOBS HAD THE SAME DURATION, so completion order matching start order is consistent with concat-like behaviour and proves nothing. The discriminating fixture is unequal durations (slow t1, fast t2) where merge must show start t1 / start t2 / done t2 / done t1. Out-of-order completion is the only proof. Same class as sabotage_must_verify_the_sabotage: a receipt whose green is also green under the wrong hypothesis is not a receipt').

answered(scan_surface,
         'RULED no_new_surface_match_block_arms. Tier-0 list came back EMPTY, so no new syntax is justified; the canonical scan is a keyed state rel + a log rel + a match block of |+> arms, all shipping constructs, graded byte-identical on both doors by the rel_as_stream lab. Consequence: the scan_surface_composition decision that was blocking FOUR labs is ANSWERED -- scan_surface_composition, scan_match_reconciliation, generic_scan_instantiation and select_scan_cache can all fold, with generic_scan_instantiation held only for the rule-generics card per the rel-as-value labs card 6. Sugar comes later FROM EVIDENCE (repetition ugliness), not from a guess. Still open under it: the ordinal spelling card (four rules today / seq() column sugar / engine-minted, the last one carrying the two-doors ordering crack) and loop/select mechanics, which the user wants nearby-and-intuitive for lowering.').
answered(json_edge_body_refusal_reason_is_stale,
         'lower.pl:931 keeps edge_body_needs_json_destructure with the stated reason that a compound value ARRIVING into an untyped column is stored as canonical term text and that the encoding question belongs to SLOT-TERM-STRUCT. SLOT-TERM-STRUCT WAS RULED 2026-07-29 (compound_storage = struct_as_rows). The refusal is parked on a slot that no longer exists. Supports the users argument that json is not a time thing and edge bodies should accept decode; the guard seam already landed for negation, comparisons and binds').
answered(json_literal_brace_tag_shape,
         'user proposed SWIs untagged dict spelling _{..} for json literals. What it buys: Tag{..} falls out by the same convention, so the brace-tag card is answered by convention instead of a separate decision. What it costs: the json5 directive asked for json natively expressible 1-1, and {a:1} pastes out of a json file while _{a:1} does not. Coordinator recommendation, unruled: accept BOTH, bare {..} and _{..} both untagged, Tag{..} reserved -- one grammar alternative, keeps paste-ability, reserves tags').

lab_folded(scan_surface_composition, '96428913', 'superseded by ruling(scan_surface, no_new_surface_match_block_arms). Its 10 receipts proved a scan_definition/1 expands N->N-1 into ordinary rules using named rels, keys, match, pre/1, latest/1, <- and <+; the ruling says we write those rules directly instead of minting the definition form. Recover: git show 96428913:v6/prolog/labs/scan_surface_composition/0_receipts.pl').
lab_folded(scan_match_reconciliation, '96428913', 'superseded by the same ruling. Its 10 receipts proved nested match composes through an ordinary rel, a direct nested block refuses, and scan+match expands to ordered rules; the shipped canonical spelling in labs/rel_as_stream/match_stream.dl6 is the same shape, graded on both doors. Recover: git show 96428913:v6/prolog/labs/scan_match_reconciliation/0_receipts.pl').
lab_folded(select_scan_cache, '96428913', 'superseded by the same ruling plus the consumption-arms verdict (log + keyed cursor + min composes N readers). Its merged-Log-as-select-queue receipt and durable Seq witness are the select mechanics the user asked about, and they need no new construct. Recover: git show 96428913:v6/prolog/labs/select_scan_cache/0_receipts.pl').

design_tension(typespec_usurp_direction,
    'user 2026-07-30, EXPLORE NOT INVARIANT: the openapi codegen arc is the front end of a longer intent -- replace TypeSpec, and natively import and export pure types across languages, with sprefa-extract as the source in either a diet-scip best-effort mode or full SCIP. Prior art is the users own: ~/projects/hafley-rxjs/packages/json-rx/spec/*.tsp plus experiments/0_typesystem/ (10 files: rx_algebra, graph_wiring, invalid_graph_wiring, return_type_inference, inference_functions, operator_catalog, alias_pipeline + assertions, observable_constructor, typed_flow_refs). THE TENSION, worth stating before anyone builds on it: that experiment types a stream as model Stream<T> with op map<Input,Output> and merge2..merge5 spelled out by arity, which is the classic missing-variadic-generics symptom. sprefa reached the opposite answer THIS WEEK on its own evidence -- nothing is parametric (types-as-rels), a stream is a VIEW OF A TABLE plus a naming convention (rel-as-stream, tier-0 list empty), and merge needs no arity typing at all because two sources landing rows in one rel IS the union. So the tspx work states the goal (checked wiring, types crossing language boundaries) while sprefa disagrees with its mechanism. The import/export half is the part that survives intact and is where sprefa-extract already has receipts: the SCIP path just produced v6s first module-graph oracle at recall 0.992 / precision 0.988 against madge').

design_tension(generics_will_return,
    'user 2026-07-30, direction not ruling: "generics will come back one day in my mind, scan will become a scheme, we want to make re-usable processes, imagine same code but slightly different type, we could just enum an open generic/assoc type style where we monomorphize hte piss outta it". Reconciles with what the labs found rather than contradicting it: types-as-rels concluded nothing is PARAMETRIC on the value plane and that stands; the users ask is RULE-LEVEL reuse, which is the separate axis already ruled by locked(higher_order_runtime_boundary) + locked(higher_order_lowering) = Souffle components with COMPILE-TIME MONOMORPHIZATION, prototyped at 8/9 in labs/generic_scan_instantiation. The new content in this message is the SHAPE: an open enum / associated-type style where the variant set is the instantiation set, monomorphized aggressively. Also names the endpoint for scan: scan stops being a spelling and becomes a SCHEME (a reusable process others instantiate), which is exactly what the no-new-surface ruling defers until repetition shows the shape. Variadic arity (the merge2..merge5 ladder in the users own tspx experiment) is the open sub-question: sprefa may not need it at all, since two sources putting rows in one rel IS the union, but a scheme over N inputs would need SOME arity story').
design_tension(scip_passthrough_and_diet_resolution,
    'user 2026-07-30: SCIP expression should be PASS-THROUGH and the oracle should see 100 percent of SCIP data, not a projection someone chose. Separately the user asked whether diet module resolution already exists via oxc. MEASURED ANSWER: oxc is the TS/JS arena parser (oxc_parser/oxc_ast/oxc_ast_visit) and lang/ts.rs:883 DOES capture module specifiers, import and export-from rows AS WRITTEN, but ts.rs:896-897 states the row carries NO source module and NO imported name and nothing consumes specifiers yet. So the raw material for diet resolution is extracted and the resolution step (specifier text -> file path) is the missing piece, one step not zero. The full-SCIP number that exists today (madge 752 / scip 755 / agree 746, recall 0.992, precision 0.988) is sprefa PROJECTING a scip-typescript index, not sprefa resolving anything itself; diet-scip has no oracle and no mode yet, and the madge harness is the reusable rig for grading it when it does').

% ── WAVE 4 DISPATCH 2026-07-30 (user: "start ripping opus no codex", pre-refresh) ──
% in_flight(Lane, Where, GateOnLanding).
in_flight(scip_passthrough, 'opus @ ../sprefa-lane-scip (lane/scip-passthrough)',
          'SCIP coverage table first (every protobuf message/field vs emitted/dropped/flattened); then 100pct passthrough for straight projections; MEASURE the amplification cost (212 files = 122,317 occurrence rows -> 755 edges, 160x) and say whether the engine holds it. JOB 2 diet module resolution over the oxc specifiers ts.rs:883 already captures, graded on the EXISTING madge rig against the full-SCIP baseline 752/755/746 recall .992 precision .988').
in_flight(json_wiring, 'opus @ ../sprefa-lane-jsonwire (lane/json-wiring)',
          'wire the RULED json surface into the real parser/printer/registry/checker/lowerer. Brace spelling is the ONE open thing and it must settle it with a receipt (coordinator recommends bare {} and _{} both untagged + Tag{} reserved). Must also try lifting json_array/json_object (dispatch not decision) and the edge-body refusal whose SLOT-TERM-STRUCT reason went stale. Acceptance = 27 archive examples + gh-cache flagship; the golden-flex COVERAGE GATE will fail on new live registry rows unless exercised or named').
in_flight(effect_chain_batch, 'opus @ ../sprefa-lane-effectchain (lane/effect-chain-batch)',
          'ticks-per-stage for a real 3-stage shell chain and whether same-tick chaining is possible with the exact blocker; batching compatibility rule + measured spawn counts at 1/10/100 demands; what v5 collect gave that groupInvocations does not; the comptime-vs-runtime boundary').
in_flight(staged_writes, 'opus @ ../sprefa-lane-stagedwrite (lane/staged-writes)',
          'the tools NAMESAKE arc: v5 --move, v3-5 marker macroing, AUTOZONE sigil regions. Grade what marker writing can do today (comment rails landed 745/745), whether sh is enough for staged writes, and the COST of the users do-not-advance-until-answered rule against the drain boundary and the endurance goal. Dry-run-as-rows program required. LAB ONLY, and it must never write outside its own scratch dir').
in_flight(stale_labs_sweep, 'opus @ ../sprefa-lane-stalelabs (lane/stalelabs)',
          'four repo-root labs untouched since 2026-07-20 (bootstrap-typegen, facet-datalog, swi-typespec, type-system-rust) plus ghcacher_tick_golden and rel_value_unification (card 6 says fold it now that D5 landed). The USEFUL output is the list of claims now known WRONG or superseded, not the deletions').
in_flight(null_implementation_plan, 'opus @ ../sprefa-lane-nullplan (lane/nullplan)',
          'ordered landable steps with a fail-first receipt each, from the landed null lab. Must name every emitted SQL site that changes by READING lower.pl, with EXPLAIN on the negation and keyed rewrites; must state the oracle-door representation since prolog has no null; must state the delta contract from the labs measured null-to-null net weight 0 / null-to-value minus-plus').
in_flight(prolog_main_review, 'opus @ ../sprefa-lane-plreview (lane/prolog-review)',
          'CRITIC of v6/prolog. Hunt silent wrong answers first (the recurring class), then two-door disagreements, then refusals that are unfinished work parked on ruled slots, then duplication that grew back after the org refactor, then what emit is doing now that it is the dominant phase, then vacuous fixtures. PROVED separated from SUSPECTED').
in_flight(ts_lowering_review, 'opus @ ../sprefa-lane-tsreview (lane/ts-lowering-review)',
          'CRITIC of the emitted code + runtime + seam. The standing TS laws are user-set so violations are DEFECTS not opinions: no await above the SqlRunner seam, exactly ONE subscribe, sync stays sync, interface-bound functions, header-declared classes. Also commit_ms ~10.8ms/file was named the next dominant cost and never revisited, and bop.ts casts JSON.parse as {rows} unchecked').

% ── WAVE 4 LANDINGS 2026-07-30 (all coordinator-verified by re-running receipts) ──
lane_landed(scip_passthrough, 'merge 8f6a4377',
     'coordinator re-ran: 44 tests pass incl all three SCIP ratchets, and the madge oracle reproduced BOTH legs exactly. Coverage 17 of 43 serialized fields -> 43 of 43; the tool identity was being LOADED AND NEVER EMITTED (whole Metadata/ToolInfo block), plus six of seven SymbolRole bits, syntax_kind, enclosing_range, all five Diagnostic fields, all three Signature fields, Document.text. COST PRICED AND THE LEVER SHIPPED INSTEAD OF WALKING IT BACK: 204 docs went 150,640 rows/32.1MB -> 177,967/59.4MB (+18.1% rows, +84.8% bytes) and ~17MB of the 21.5MB growth is columns CONSTANT on this corpus (syntax_kind is 0 in 123,655 of 123,655 rows). At 291k rows per 1,000 files reasserted every rebuild against 74.2 files/s ingest with commit_ms dominant, the engine cannot hold it as a standing feed; --scip-record KINDS filters AT PRODUCTION so a narrowed stream also skips reading the corpus (scip_symbol,scip_relationship = 9.7% of bytes). DIET MODULE RESOLUTION LANDED: the users read was right, ts.rs:883 captured specifiers and ts.rs:896 said nothing consumed them, one step not zero. scip 764/755 recall .992 precision .988 vs diet 761/761 recall 1.000 precision 1.000 -- AND THE LANE SAID IT ITSELF: diets 1.000 is agreement between TWO SYNTACTIC SCANNERS, not correctness. The 9 scip-only edges are references reaching a declaration through an INFERRED TYPE with no import, which no syntactic resolver closes without a type checker').
lane_landed(stale_labs_sweep, 'merge 74200375',
     'coordinator re-ran conformance 193/0 + ghcacher golden HOLDS. BEST CALL WAS REFUSING THE FRAMING: ghcacher_tick_golden was filed as pure debt and is NOT, it is a WORKING GATE nobody ran; promoted to v6/tsv2/goldens/ and wired into green-all with a sabotage receipt. Folded 5 (bootstrap-typegen, facet-datalog, swi-typespec, type-system-rust, rel_value_unification), kept 2 with reasons. CLAIMS NOW KNOWN WRONG: W8 the bootstrap lab did NOT self-host, its own 14_bootstrap.rs says so, yet its plan doc is still checked in reading like a live 7-phase arc with 4 open todos and phases 6-7 never ran; W11 interning is a 2.44x memory win ONLY when names repeat and a 1.2% LOSS when unique (24,078,380 vs 23,786,004, 100k decls x 3 fields) and this applies to a SHIPPED dep since sprefa-store uses lasso, counter-case written down nowhere; W10 rel_value_unifications two red checks were NOT a broken settlement, deltas matched element for element and only the ENCODING moved under D5; W7 bidirectional pattern_value/3 is true at toy scale and was DECLINED at real scale. HANDED FORWARD: swipl packaging unanswered (otool shows @rpath/libswipl.10.dylib so a distributable needs a static SWI build, the ONLY measurement anyone has taken of the bundled-with-rust tier), and keyed target replacement retracting its dependent parent row in the same tick WORKS with no fixture covering it').
lane_landed(effect_chain_batch, 'merge before be97647c',
     'coordinator re-ran run-all.sh. N stages = N+1 ticks (1/2/3/4 -> 2/3/4/5), one hop per stage, zero drain waste. SAME-TICK CHAINING FORBIDDEN TWICE: the compiler refuses two host atoms in one body (probe_mismatch, 1_host_expand.pl:355) so it cannot be spelled, and the runtime re-enters only via engine.submit; generally stage k+1s demand is a function of stage ks answer so same-tick would mean suspending the fixpoint on I/O, which is the tick no longer being a fixpoint. THE COST IS PER ITEM NOT PER STAGE: ticks = seeds*stages + 1. Batching 7-to-1 VERIFIED (700 demands -> 100 spawns) and it also collapses ticks 700 -> 100, which the earlier arc never claimed; compatibility = same tick frontier AND execution==sprefa_extract AND byte-identical template AND identical input values; A PLAIN sh HOST CANNOT BATCH AT ALL. V5 COLLECT IS FAN-IN OVER DISTINCT VALUES while todays grouping is FAN-OUT DEDUPE OVER IDENTICAL VALUES -- opposite requirements, not partial overlap. NEW DEFECT F9 reproduced by the coordinator: group_concat(x) in a head COMPILES CLEAN at 200 and is not an aggregate, storing the literal text group_concat(1) one row per input, silently, which is the exact spelling a cold author reaches for. COMPTIME LINE: planning is ALREADY fully comptime and is not the bottleneck; every gap is a missing FOLD, and one list-valued aggregate head makes collect expressible with zero new effect machinery').
lane_landed(ts_lowering_review, 'merge be97647c',
     'CRITIC. 2 CRITICAL: (1) one tick fault PERMANENTLY kills the engine and the served process -- 3_engine.ts:176 re-throws into the shared concatMap behind ticks$, so after one bad tick every later submit fails "engine is not running", and 4_http.ts:315 states the OPPOSITE law in its own comment; NO gate in green-all catches it. (2) the HTTP arrival boundary checks row LENGTH never row SHAPE: row:"ab" kills the server, row:[{a:1},[2,3]] returns 200 with tick log {"deltas":{}} and STORES [null,"[2,3]"] into TEXT NOT NULL where it persists. 3 HIGH: unescaped shell injection in {col} template splice with spawn(shell:true), and the probe CREATED A FILE ON DISK; host subprocesses run at concurrency 1.0 (measured .92/.93/.95) which is the structural cause of 40.68 files/s vs v5 3,540.93 same-run, 87x; the ordered/pre emitted family is constant+2n statements per tick (15 -> 213 at 100 arrivals) vs incrementals flat 31, with no count assertion. STANDING TS LAWS 4 HELD / 4 VIOLATED: violated are 5 toArray-then-discard sites, 8 free export functions carrying contracts, the emitter re-declaring 6 header types into ALL 137 modules (6,566 duplicated lines = 14.3% of gen_emitted), and N+1 in the host settle path. ALSO: pnpm typecheck is RED with 4 errors and NO gate runs the compiler at all').
lane_landed(prolog_main_review, 'merge 9374bf5b',
     'CRITIC. 15 proved / 6 suspected. F1 HIGH the oracles solve/2 has NO CLAUSE for combine/variadic or next/1, both registry LIVE: term-form combine derives zero rows while the compiler emits a real cross join, hidden because parse splices at parse time, print_dl ERASES the spelling entirely (proved by reprinting golden-flex), and zero term-corpus fixtures use either -- while the oracles own STRATIFIER does handle the term form, so the oracle is internally inconsistent about a construct it ships. F2 HIGH zip/subscribe/complete/unsubscribe/error silently derive nothing on the oracle and are refused by name on the compiler. F3 HIGH a level-head expression contradicting its declared column type is checked by NOBODY (col_type int + concat writes 3x into INTEGER NOT NULL; bool and float get a CHECK in that same predicate, int/text/ref get none). F4 HIGH todays D3 ref-column fix fires only when BOTH columns are declared, and deleting the source rels col_type is legal under edb_definition and how most of the corpus is written. F5 CORRECTS THE COORDINATOR: the profiling instruments own headline (dominant_phase emit, plan rules^0.964) came from a ONE-ATOM SYNTHETIC at 0.5ms clock resolution; all four real programs are still PLAN-dominant (flagship-flow plan 65ms/1.37M inferences vs emit 38ms/356k) and the same generator at n=200 gives rules^1.84. Plans hot spot is analyze.pl:310 re-walking every rule body once per (ref,column): 16,315 walk_node calls and 73,069 surface/5 hits for a 181-line file. FIX FIRST: a surface/5-driven body-goal gate at load time on the oracle closes F2 and converts F1 from silent-wrong to loud-gap. ALSO: SCOREBOARD + 5 justfile expect-comments stale by a full arc, and two fixtures carry EMPTY expectation lists so forall/2 makes them unconditional passes').
lane_landed(null_implementation_plan, 'merge 660fedcb',
     '10 ordered steps each with a fail-first receipt and a reversibility mark. 18 emitted SQL sites change, all with line numbers, PLUS 10 MEASURED NO-OPS the implementer must not touch (GROUP BY, DISTINCT, ORDER BY, canonical_column_expr, keyed ON CONFLICT, count(*), HAVING, recursive anchor, unkeyed pre table, literal comparison). ORACLE DOOR = null_value(sql), a reserved ground compound, because engine.pl decides row identity entirely with memberchk/==/sort/ord_subtract/set_diff_delta, ALL TOTAL ON A GROUND TERM, so THE ENGINE NEEDS NO CHANGE AT ALL and the whole oracle edit is four clauses; an atom is unusable since none has 84 corpus uses as ordinary text and body.pl:147 already reads it as JSON absence; bool_lit(true) is the exact precedent; a T? column is a disjoint sum on the VALUE axis so TICK-MODELs B/N/Z are untouched, the formal reason Design D is the only design compatible with that doc. FOUR CORRECTIONS TO THE LAB: refusing nullable DECLARED keys is not enough (the unkeyed set-rel PK is every column + WITHOUT ROWID which rejects a null row, and the rowid fallback ACCUMULATES DUPLICATES); lowering = to null-safe is TWO rewrites not one (IS keeps a column index and loses an expression index, json_array is the reverse, and rewriting blindly turns the refCount subquery from SEARCH to SCAN per head row); BARE null CURRENTLY PARSES AS A FRESH VARIABLE so repo_latest(Repo,null) silently means any value; present(X) is NOT caught by refusal-by-absence so a typo becomes a silent empty EDB. Step 6 (the checker) is most likely to blow its estimate: the Bound list is extend-only with first-match lookup so NARROWING WOULD BE THE FIRST REBINDING OPERATION IN THIS COMPILER').
lane_landed(staged_writes, 'merge 787d994d',
     'coordinator ran receipts.sh: STAGED WRITES LAB HOLDS, 25 assertions. LIVE UNOWNED DEFECT host_column_shadows_runtime, reproduced by the coordinator side by side: a host declaring an input or output column named ordinal or witness_digest compiles CLEAN and the compiler renames ITS OWN columns to col1/col2 to dodge the collision while serve/1_hosts.ts project() fills BY LITERAL NAME -- empty witness, PK collapsed to ("",""), multi-row answers cut to one, dead demand-to-response join, program derives nothing, no refusal and no trace. Fix is a load-time refusal. MARKER WRITING ALREADY WORKS and stops in three measured places: a hosts input is a ROW while a writes payload is a RELATION and there is no string aggregate, so 3 staged lines = 3 spawns = 3 whole-file rewrites ordered by DELTA ORDER (v5 folded rows in the engine, one write per file per tick); the engine is BLIND to the disk it wrote since effect identity is content-addressed over the DEMAND only; writes are at-least-once. DO-NOT-ADVANCE-UNTIL-ANSWERED is ONE concatMap and IT BUYS ORDERING NOT DURABILITY -- kill -9 between the disk write and the answer replayed the write on restart and no ordering of claim/settle closes that window; its real costs are that the tick log stops being a pure function of (program,schedule) and becomes a function of effect WALL TIME, ending byte grading for any program that writes, one slow write freezes the whole engine, and a host that curls the engine becomes a hard undetected deadlock. DRY-RUN-AS-ROWS works today and beats --fix because armed(zone) is an ordinary row: scoped, retractable, auditable, composable. SPAN/CST better than the ledger said (flagship-flow now compiles with ZERO unsupportedExecution so its own header stop is stale); the real span gap is two span writes into one file in one tick corrupting silently').
lane_landed(option_vs_null_lab, 'merge 0f521736',
     'coordinator re-ran 83 assertions, both SQLite builds. Told not to close the question and did not: 9 cards, none marked recommended. THE USERS THREE-VARIANT READ WORKS TODAY with zero new semantics via json_type/2 (undefined/none/some), and the NEGATIVE CONTROL is the finding: the SHIPPED json_extract(...) IS NOT NULL predicate reports 5 present where json_type reports 6, so ONE DOCUMENT IS SILENTLY LOST TODAY. An Option has FOUR observable states; the fourth is row absence (never probed) and no variant can express it. THE EXPLOSION WORRY INVERTS at 100k subjects / 10pct absent: nullable-keyed 2,949,120B (0.96x), two-rel 3,059,712 (1.00x), Option-three-variants 4,800,512 (1.57x), and NULLABLE COLUMN ON A DERIVED REL 6,545,408 (2.14x, the LARGEST) because it is forced off WITHOUT ROWID onto rowid+UNIQUE, two btrees; all four keep the optionality-blind read an index SEARCH. THE WALL: an Option from a DERIVED rule does not compile because 0_enum_expand.pl attaches keyed(...) to every generated variant rel so the outer-join shape is refused as keyed_level_head -- class (b), a lowering choice, and the highest-value card. SOUFFLE ANSWERED: no general null, nil legal ONLY in record-typed columns (its own test fails "Nil constant used as a non-record" on a symbol column), no json type, IO=jsonfile undocumented rendering record nil as JSON null, and the shape is declared up front so A KEY THAT MIGHT NOT BE THERE CANNOT ARISE -- the front end flattens json before datalog sees it, which is the answer to the users joke; named gap issue #2315, negative ADT branch matching unshipped, so "this subject has no Some" is exactly what Souffle cannot write. THREE DISAGREEMENTS WITH THE NULL LAB IN WRITING: its nullable-KEY refusal is too narrow (blocking for the null plan); its Souffle reading generalizes nil beyond record types; and IT REFUTED ITS OWN HYPOTHESIS -- IS NOT DISTINCT FROM does NOT lose the index, identical SEARCH plans to = on both builds even against a NULL literal, so the strongest performance argument against Design D does not exist. CANDIDATE D from prior art: Datomics get-else, optionality at the USE SITE, class (b), zero extra storage, index SEARCH both sides; real hole G3 no safe int sentinel; Hickeys Maybe Not argues for this shape and against Design Ds; Flix proves Option[Int32] live in datalog relation columns').

% ── WAVE 4 CLOSE-OUT 2026-07-30 (the last two lanes, coordinator-verified on merged main) ──
lane_landed(json_wiring, 'merge before c04b6cfd',
     'brace spelling settled BY MEASUREMENT not fiat: _{} and Tag{} are SWI dict syntax, term_to_atom shows the term door would never see a {}/1 there, so bare {...} wins and both dict forms are RESERVED with unsupported_construct(tagged_brace_reserved(Tag)) -- sabotage receipt in the test header. Empty object is the ATOM {} not {}/1. json became its own STORAGE KIND (was an alias for text, which erased the bit the lowering dispatch reads). Exact keys 0 joins, spread/key-capture 1 json_each, ** descent 1 json_tree, delta arm reads json_each off the frontier = zero delta-side scans, guards emitted FIRST in WHERE because SQL states no evaluation order for AND. list(text) landed, element typing honestly unenforced. ACCEPTANCE: 20 of 27 archive examples parse verbatim through the production text door incl gh-cache flagship both spellings; 7 rejects all named. TWO REFUSALS KEPT WITH MEASUREMENTS: json agg heads are an ARC (json_group_object follows ROW order where the oracle wants sorted keys; flat aggregate SELECT has nowhere for the inner ORDER BY; dup-key wants a throw SQLite lacks); edge-body json blocker LOCATED at analyze.pl:edge_goal_refusal/4 which receives only Prog, no relplans, so it cannot tell json from struct. Coordinator re-ran on merged main: green exit 0, conformance 201/0, TEXT_DOOR 141/141/0, plunit 236/236, sweep BOTH modes 141/139/0-wrong final 139/2. SCOREBOARD + justfile expect comments refreshed same sitting (were three landings stale)').
lane_landed(ts_critical_fixes, 'merge bd0d567c',
     'F1 WORSE THAN REVIEWED: through the real server the tick fault traveled runProgram merge -> single subscriber -> closed the LISTENING SOCKET; one well-formed POST ended the process before answering the request that caused it (ECONNREFUSED receipts, 3 red pre-fix). F2 first draft TOO STRICT and golden-flex caught it: struct values arrive as whole JSON objects, so ref columns now take any JSON value and only absence is refused. Validation lives at the HAND-WRITTEN boundary: only trust boundary, only place holding relColumns/relColumnTypes for a named 400, and the emitted pass runs INSIDE the tick after writes began (measured: engine refuses a null struct correctly but delivers it as a 500 blaming the server for the client body). F3: all 5 injection payloads executed pre-fix; {col} splice now escaped per shell quoting context. F5 pinned not fixed: ordered/pre = 13+2n statements (15/23/63/213 at 1/5/25/100) vs incremental flat 31, PLUS the zero-arrival full-table SCAN measured real; fix owned by ARCH pre_occurrence_loop in emit_ts.pl. F7: 8 free contract-carrying exports bound to header interfaces, 4 by annotation because emit_ts.pl writes their import text into 137 modules. typecheck: 4 standing tsgo errors fixed and just typecheck WIRED INTO GREEN -- npm test strips types and checks none, so the package sat red with no gate noticing. NEW NAMED GAPS: bind runners lack submit error arm (2_binds.ts:164/:354); v6/dl not typecheck-gateable (no tsgo in node_modules). Coordinator re-ran on merged main: green exit 0 (one store golden.test flake on first run, clean isolated + clean rerun, the known golden_flake_hunt class), sweep 141/139/0, MEMORY SOAK HOLDS, serve-leak-soak 20 swaps clean').

% ── RULING landed mid-session (rulings.pl is the record) ──
answered(null_design,
     'get_else_use_site_never_storage. Candidate B (Design D T? nullable columns) is DEAD; plans/2026-07-30-null-implementation-plan.md superseded, no step executes. Absence stays row absence; the consumer spells its default with get_else/2 at the use site (LEFT JOIN + coalesce in SQL, ?? in rx). some/none variants stay available as the EXISTING enum machinery when the caller wants coverage counting -- stacks with get_else. Generic Option<T> rides the ruled monomorphization arc, sugar not blocker. User: "do what makes best least brouhaha, i like none/some etc."').

% ── deburr pass ──
finding(worktree_deburr_wave4, process,
     'lane worktrees jsonwire, tsfix, v5gaps removed with branches after full containment verified against HEAD; main pushed at bd0d567c').

% ── DIRECTIVE (user 2026-07-30, supersedes the global sonnet-default for this project) ──
directive(delegation_opus_only,
     'user: "if u can delegate to opus yes, only use sonnet for reading and initial planning of a lab or something or after somehing as reviewer. im done trusting sonnet". Implementation lanes = opus. Sonnet = read-only scouting, initial lab planning, post-landing review ONLY. Codex lanes unaffected. Also standing: reconcile session pl + ARCH.pl after every design chat and agent landing, not in batches').

% ── COALESCE LANDED 2026-07-30 (the null ruling executed same day it was made) ──
lane_landed(coalesce_wiring, 'merge after 33ede2a1, lane commit b3e6fa05',
     'coalesce(RelAtom, Default) wired as a COMPILE-TIME DESUGAR on both doors: 0_coalesce_expand.pl at expansion phase 45 (between match and relation_edge, position reasoned: arms may carry coalesce, relation_edge appends per clause so it must see the split). One rule -> two clauses (read arm + not() arm with := Default); N goals -> 2^N clauses; ZERO emitter change, the construct inherits incremental deltas, the negation retraction flip, and the naive referee. THE ONE REAL DESIGN CALL: the edge arm desugars through latest(...) because a bare atom in an edge body is a trigger and the naive desugar would mint a second firing source (graded by fixture coalesce_in_edge_body_samples). SEVEN refusals thrown by the SHARED expander so the doors cannot disagree; not_top_level/in_head are load-bearing (a nested coalesce otherwise reaches analyze as an ordinary join and drops the default SILENTLY). Count-test law receipt: 33 statements/tick FLAT at 5/100/1000 rows, NOT EXISTS plans SEARCH never SCAN on the real captured insertSql, three sabotage receipts actually run. Golden-flex coverage gate tripped BY NAME first (55 constructs, 1 unaccounted) then exercised non-vacuously. Coordinator re-ran on branch AND merged main: conformance 209/0, TEXT_DOOR 145/145/0, plunit 239/239, roundtrip 209/209, green exit 0, sweep BOTH modes 145/143/0-wrong final 143/2, manifest churn = var numbering only. HANDED BACK: green flaked twice on TRUNCATED-POST signatures (dl 6_http route 501, serveHost "Unterminated template") pointing at the readBody Promise wrapper law-debt -- two NEW flake sites in the store-golden class, worth an owner; fixture decl order is load-bearing for G1 =@= grading (noted in fixture); refusal payloads carry Name/Arity not source terms because engine.pl grades throws/1 by ==/2 -- gradability vs echo-the-source is a real tension').
finding(staleness_gate_debris_after_wave4, process,
     'gate was RED on merged main from three lanes debris, coordinator cleaned: door-handwritten.ts regenerated (5th sighting of gen_staleness_gate class, went stale via tsfix validateArrivals emitter change); THREE orphan gen modules deleted (relation_pattern_*_rejected.ts, checked in at 472320f4 BEFORE those fixtures became refusals -- refusal fixtures own no gen module); both release binaries rebuilt (dl + extractor; dl was a cargo-fingerprint no-op, mtime-only staleness from checkout, binary touched after cargo verified freshness)').

% ── TIME-PLANE UNIFICATION (user direction 2026-07-30, mid stream-cards round) ──
directive(time_plane_unification_probe,
     'user: "log rel is just rel with a time column ... rels should have auto created_at_tick and updated_at_tick so to speak, and we could historicalize them as well with a time column for easy versioning". Coordinator measured blast radius first: ~50 grep sites read the log bit (engine 5, lower 15, emit_ts 14, parse/print/registry 11, program_check/clock ~5) -- a lane, not a rewrite. Unification would dissolve the recurring retention-grading gap (prune becomes a visible minus delta) and stream card 4. The one hard trap: time columns must be a METADATA PLANE excluded from row identity or set dedupe dies (Datomic precedent, tx outside entity identity). Lab header seeded plans/2026-07-30-time-plane-unification-header.md (H1 log-as-sugar, H2 auto tick columns, H3 historicization; Q1-Q9; 4 slots), opus lab dispatched at 51d0e193').
answered(rel_as_stream_cards_partial,
     'user ruled: 1b seq-column sugar IF tier-0 (lab says it is (a)) and HARD NO on 1c @ binding ("i HATE the @ symbol"); card 2 = least fucky, coordinator picked 2b keep-reserved-with-join-naming-message (deleting the row makes a typo silent EDB); 4a refuse-loud CONFIRMED ("correct"); 6a no stream word CONFIRMED. Cards 1b/3/5/7 HELD behind the time-plane lab (they became the same question); 4a may dissolve under unification (finalize fires on the visible prune delta). User challenged the log/set distinction itself as holding us back -> the lab').

in_flight(time_plane_lab, 'opus worktree, base 51d0e193, header plans/2026-07-30-time-plane-unification-header.md',
     'H1 log = rel + compiler-filled time column excluded from identity (log becomes sugar); H2 auto created_at_tick/updated_at_tick as metadata plane; H3 historicization priced. Q1-Q9 incl the ~50-site blast radius table and the retention-visibility regrade count. Gates on landing: both-doors fixtures, count receipts, memory-soak comparison. Stream cards 1b/3/5/7 held behind it').
in_flight(json_flex_lab, 'opus worktree, base a116e3e9, header plans/2026-07-30-json-flex-lab-header.md',
     'user: "have we flexed the fuck outta our json stuff... json is large statemachine". Coordinator measured the thinness first: 23 fixture entries, ZERO touching json null/floats/escapes/unicode, plus 2 live known defects (top-level scalar encodeValue sniff, json_extract null-collapse = one doc silently lost). Q1 values incl 2^53 bigints, Q2 null-in-documents vs no-null language, Q3 keys/collation, Q4 canon agreement across 3 encoders + property round-trip, Q5 malformed incl the #-comment superset, Q6 scale counts, Q7 JSONTestSuite external oracle (buy candidate). 5 slots incl float fate + bignum contract. Grade-then-harden: mechanical fixes fail-first, structural holes = named cards').

in_flight(golden_gates_lane, 'opus worktree, base f8544ac6',
     'closes the 4 acceptance-gate gaps from the golden-scenario sitrep, IN ORDER: (1) compile-speed gate = inference-count ratchet (statistics/2, deterministic, wall ms informational) over 3-4 pinned real programs, sabotage receipt required, green-all; (2) scale-floor gate on deterministic counters + generous 3x wall floor, JSONL curve over time; (3) multirepo dep/repo/rev crawl golden = the missing v6 leg of stopping-point program 5, flagship-callgraph two-leg pattern over a pinned multi-repo corpus, every diff row bucketed 0-unclassified; (4) rtkq corpus golden (program 7) only if 1-3 land, honest stop allowed. File ownership disjoint from the two running labs (scripts/goldens/justfile only, no compiler semantics)').

% ── TIME-PLANE LAB LANDED 2026-07-30 (merge of worktree branch; coordinator re-ran receipts.sh 7 PASS 0 FAIL) ──
lane_landed(time_plane_lab, 'merge after 5897ad7d, lab commit dc9a5030',
     'H1 (log = rel + time column as sugar) REFUTED ON MECHANISM: a set rel absorbs an identical second arrival as a no-op and fires NO occurrence, so what log buys is the ENGINE minting row distinctness -- an expansion written in rules cannot mint it because the occurrence it needs to fire on is the one the set plane just discarded. Blast radius measured: 6 die / 6 move / ~105 survive / 2 new (~120 sites touched to delete 6); seq-as-visible-column = 80-fixture regrade AND reorders every tick (ticklog msorts rows by JSON text). H2 (created/updated_at_tick) ALREADY EXPRESSIBLE, zero constructs: now/1 + pre/1 + not/1, two edge rules; the naive one-rule spelling silently yields updated_at under a created_at name (the trap any future sugar exists to close; sugar = opt-in per rel, never automatic, 7.5 bytes/row at 100k). H3 history: shadow log +15.2% vs rel-as-log +72.4%, both SEARCH. THE SEPARABLE PRIZE: retention emitting an ordinary minus delta = 51-line patch (engine.pl + 1_incremental.ts), sweep 0-wrong with EXACTLY ONE artifact line regrading, closes SLOT-LOG-FINALIZE-REFUSAL + stream card 4 + consumption-arms 17 + the four-times-ledgered retention-grading gap, and DELETES a proposed refusal (construct budget win). Slots: seq_scope = per-rel (the ORACLE must change -- set arrivals consume log ordinals today, counter shared between logs; next_seq/3 engine.pl:379-381 + Seq0 thread :291-313, stale :356-358 citations corrected); updated_at = keyed only. COVERAGE FINDING: 1 fixture in 209 exercises keep(count(N)), no deltas() expectation -- why the hole survived three arcs. Retention-minus landing lane DISPATCHED (opus, executes verdict rec 1: fail-first fixture, patch, 4 coverage fixtures, TICK-MODEL R7 restatement as storage-vs-occurrence, finalize-over-log natural-spelling fixture superseding card 4a, lab-death)').

lane_landed(golden_gates, 'merge of lane commit c00043be; coordinator re-ran all four gates in-worktree AND green + compile-speed on merged main, all exit 0',
     'FOUR ACCEPTANCE GATES LAND, green-all grows compile-speed/scale-floor/multirepo-golden/rtkq-golden. (1) compile-speed = INFERENCE-COUNT ratchet (3 consecutive runs byte-identical at 2,120,677 while wall wobbled 7->11ms; counts gated, wall informational; asymmetric 1.10/0.75; sabotage scan = +899.8% named FAIL where wall moved only 7->12ms -- the receipt that wall-gating is dishonest). (2) scale-floor = statement-count SET compared across cell sizes 10x apart; the lane CORRECTED ITS OWN DRAFT (single-size assertion cannot tell fixed first-tick overhead [37,41] from a per-row loop); leg-independence sabotage (count red, wall green). (3) multirepo-golden = program 5 v6 leg: v5 version-skew.dl UNMODIFIED vs v6 port over a generated 4-repo corpus + a THIRD opinion (classifier reads go.mod bytes), 4/4 rels BYTE-IDENTICAL 0 unclassified; named gap dep_ver min/max over text refused (membership needed no ordering); agreement-not-constants sabotage. (4) rtkq-golden EXISTED COMPLETE since 51ba4161, never wired into any battery -- wired, sabotage flips on byte spans. FINDINGS: gen/scale_generated.ts stale (gen_staleness_gate 5th sighting class); v5 dep_ver min/max REVERSED on pkg/errors (README, ungraded); rtkq runner under tsv2/labs/ = protocol debt named. Base note: worktree cut 224 behind, lane ff-onlyd per law and said so').

lane_landed(retention_minus_landing, 'merge of lane commit d0a6bbd4; coordinator re-ran conformance 214/0 + TEXT_DOOR 150/150/0 on branch, then green exit 0 + sweep BOTH modes 150/148/0-wrong + compile-speed OK on merged main',
     'Retention prune is now a VISIBLE minus delta on both doors; finalize-over-log FIRES (fixture finalize_over_log_fires_on_retention_prune supersedes SLOT-LOG-FINALIZE-REFUSAL + stream card 4a + consumption-arms 17, recorded in the fixture header; drain cost named, 4-tick schedule runs 6). THE LAB UNDERCOUNTED: THREE suppression sites not two -- emit_ts diff_local_line/3 emitted literal del:[] for kept rels = the NAIVE REFEREE copy, invisible because the lab swept one emitter mode; special case REMOVED not inverted. BANKED METHOD LESSON: single-emitter-mode lab measurements do not transfer; verdict Q5 corrected in the doc. Regrade exactly ONE artifact line corpus-wide. +5 fixtures incl the log/set occurrence-identity pair and the created_at/updated_at two-rule idiom; retention_count_prunes_oldest gained its missing deltas/2 leg. TICK-MODEL 5.1 splits R7 into occurrence-in-N (no minus ever) vs storage-row-in-Z (keep alone). Lab-death executed, last copy f044ce06. Conformance 209 -> 214, sweep 145/143 -> 150/148').

lane_landed(json_flex_lab, 'merge of lab commit 7612fdc1; coordinator re-ran conformance 221/0 + TEXT_DOOR 157/157/0 on branch, then sweep BOTH modes 162/160/0-wrong + green exit 0 on merged main (226 fixtures post-merge with retention wave)',
     'THE SEMANTICS HELD, THE LAYER UNDER THEM HAD FOUR INVISIBLE DEFECTS, all fail-first on the real sweep: F1 the oracle tick log WAS NOT JSON (prolog ~4| column-stop ate two of \\u four hex digits, \\f -> \\u0c glued; THREE encoder copies, sweep.pl schedule writer held a private third); F2 text column holding a json object read back as SQL NULL (canonical_column_expr json_type=object gate true of every object then NULL||...); F3 encoder guessed structure from FIRST CHAR, wrong both directions -- json is now its own IRowColumnType end to end; F4 json_canon(Free,C) bound the caller variable to {}. MEASURED EN ROUTE: ref columns needed the same encoder arm because the dictionary memoized __rendered text is NOT key-sorted and the sniff had been silently re-canonicalizing it (19 fixtures red without it) -- REFUTES struct-as-rows canonical-at-intern-time claim, card C6. JSONTestSuite: y 95/95, n 187/188 (miss = SQLite own laxity), i 31/4, crash-instead-of-refusal ZERO, 127 docs round-trip idempotent. OPEN RULED-NOT-FIXED: Q2 null-collapse is SHARED by both doors so a unilateral fix CREATES divergence; oracle json null = atom none making text "none" unreachable oracle-side (C3 highest-value card); dup keys oracle-throw vs emitter-last-wins; 1e999 -> null; wide-int read cliff RangeError at 2^53-1 naming no rel; json1 nesting cap 1000/1001. 11 cards, 5 slots open with measurements. Lab-death done, last copy 6dde7f9a. POST-MERGE: the day-old compile-speed gate made its FIRST REAL CATCH -- emit phase +11.7%/+14.2% inferences from the new encoder arms; accepted deliberately via --write-baseline (correctness cost)').

% ── CSP THREAD 2026-07-30 (cards -> hands-on proof -> lab) ──
finding(csp_composed_proof, coordinator_hands_on,
     'User asked "can u prove that works did lab say that literally works today" -- honest answer NO, the pieces were graded separately (stream lab receipt g = watermark writer; consumption-arms = one-per-drain-tick) and the composition never ran as one program. Coordinator wrote csp.dl6 cold (4 decls + 8 rules: pending log parking lot, cursor numbering, ready_min/taken take-one, chan keep(count(3))): bop check exit 0 through the production compiler, oracle log exactly as predicted -- one take per drain tick, order held, zero loss, EMPTY DRAIN TAKES NOTHING, and the hour-old retention-minus showed up live (tick 6 chan del [1,a] visible eviction). One refusal hit and it was right: edge_into_unkeyed_set(taken/1) until key(1). Program text preserved in plans/2026-07-30-csp-idioms-lab-header.md').
in_flight(csp_idioms_lab, 'opus worktree, base c1518683, header plans/2026-07-30-csp-idioms-lab-header.md',
     'user: "make an opus lab ... toy programs that fit the problem domain or are essential to the domain of csp and we test it out and lab out the ugly and repetitive nature of it". 9 idioms (buffered chan seed, worker pool, pipeline, fan-out/in, select, timeout, done-channel, rendezvous, semaphore/rate-limit), each with a Go referent comment, both doors, determinism double-runs. Deliverable = the boilerplate census (the evidence the seq(name) card-1b ruling asked for) + error census + slots (select spelling, rendezvous capacity-0 meaning, fairness). Fix nothing; silent-wrongs = fail-first findings').

in_flight(oracle_gate_lane, 'opus worktree, base 3de711f3',
     'kills the silent-empty-EDB class on the oracle door: surface/5-driven load-time body-goal gate (walk_body/3, edb_definition boundary preserved -- bare relation atoms stay legal EDB, wrapper-shaped goals with no live row refuse), F1 combine/next get real solve/2 clauses OR refusals with both-door receipts (registry rows are LIVE; the stratifier already knows the semantics), print_dl combine-erasure fixed with roundtrip receipt. Plus booked defects: group_concat F9 named refusal both doors, host_column_shadows_runtime load-time refusal in 1_host_expand.pl. Warned: compile-speed gate may trip on the added load-time walk -- report, not bypass').
in_flight(extract_t2_lab, 'opus worktree, base 3de711f3, header plans/2026-07-30-extract-t2-lab-header.md',
     'the diet-typecheck fever dream t2 rung: buy analysis FIRST (quicktype IR, protoc descriptor JSON, avro=json, graphql SDL-vs-introspection), then reify one real openapi + one real proto descriptor into the type algebra THROUGH the real json plane as dl6 programs, Q3 headline = cross-repo who-calls-what-shape join against t1 dep edges, Q4 scale price projected to 800 repos, Q5 round-trip back to an openapi fragment via the registry emitter precedent. Slots: bytes, int width, per-format null->absence map, defaults residency, graphql entry').
in_flight(self_map_rail, 'opus worktree, base 3de711f3',
     'the describe-our-own-arch rail: dl6 programs turn registry.pl/ARCH.pl/1_expansion.pl/import-gate facts into rels, derive dataflow edges, emit v6/ARCH-MAP.md with mermaid fences via sh-host string assembly (the no-string-aggregate gap does the joins host-side, named honestly). Manual `just self-map` first (byte-stable, run-twice-identical receipt), watch-bind live mode only if clean. Production rail class (comment-rails precedent), NEVER graded. Dogfood finding requirement: per diagram state dl6-vs-glue split; mostly-glue = a named language gap, half the value').

finding(json_scan_composition_potholes, coordinator_hands_on,
     'jsonscan.dl6 (event json log -> level-rule brace destructure -> match fold on the derived rel; the arbitrary space x time answer) hit TWO live potholes: (a) json captures type as TEXT so an int fold head refuses edge_head_column_type_mismatch(total/2,2,text,int) -- needs typed captures or a cast, real language gap; (b) through dl6_oracle + JSON schedule the brace pattern derived ZERO rows with BOTH arrival spellings (json object row and canonical-text row) while term-door fixtures pass -- the doors disagree on the shape of a json ARRIVAL (dl6_oracle atom/obj mapping, json flavor of the documented string-literal edge). Program at scratchpad jsonscan.dl6, preserved in this ledger row; both potholes unowned').

in_flight(json_scan_pothole_lane, 'opus worktree, ff-only to current tip',
     'user "so close" on the jsonscan near-miss -> close both potholes: (a) typed json captures (spelling priced vs the colon-type precedent, no silent widening, runtime type guard naming rel+column per Number.isFinite precedent -- may subsume the wide-int RangeError cliff); (b) type-directed dl6_oracle schedule mapping (json-declared columns map object/array/number arrivals to the oracle obj/list term forms the term-door fixtures already feed; other columns untouched). FILE FENCES stated against the 3 running lanes (engine.pl solve / 0_program_check / 1_host_expand / serve are off-limits). Exit receipt = the exact jsonscan program + 3-tick schedule deriving cli 4->7->8, web 10, byte-identical both doors, promoted as fixture').

lane_landed(csp_idioms_lab, 'merge of lab commit bb84a26b; coordinator re-ran receipts.sh 18 PASS 0 FAIL; lab dir alive pending the fold lane (time-plane precedent)',
     'ALL 9 IDIOMS EXPRESSIBLE, 8 of 9 byte-identical both doors; the language is not missing CSP constructs, it is missing a way to say QUEUE once. ROOT DEFECT, most consequential of the day: read-aggregate-compare-write guards enforce capacity PER TICK BOUNDARY NEVER PER ROW -- W1 worker pool double-assigns (2 same-tick pollers both get item 1, 3 produced 4 assigned, SILENT, both doors agree), W2 semaphore breaches K=2 at 3 same-tick acquires. Generalizes consumption-arms pacing to every counted resource; blocks pools/semaphores/rate-limiters. W3 count() has no zero: naive semaphore grants ZERO leases forever, compiles clean (A11 in the wild). W4 NEW two-door divergence: rendezvous off derived trigger, aggregate publishes transient mid-carry value (2 deltas oracle / 6 served; review-A4 class, zero coverage). CENSUS: 78% of rules verbatim repeats; seq sugar ruling CONFIRMED WIDENED to numbering+view+selector (40 rules removed, take stays by hand). ERROR CENSUS: every semantic error silent; parse errors print raw CHARACTER-CODE LISTS (worse than B4 recorded). Select needs NO construct (Go random select = recorded divergence); rendezvous capacity-0 is a join not retention; fairness framing refuted (defect is exclusivity). DL6 BEATS GO on: deterministic cancellation, loser-stays-queued select, zero-cost N readers, 3-rule pipeline. 8 fixture candidates (4 fail-first), ranked recs: (1) RULE on per-row consumption <- USER CALL, (2) readable parse errors, (3) widen seq sugar, (4) fixture+own W4, (5) static refusal for count-no-zero').

answered(per_row_consumption, 'user 2026-07-30: "park". The csp-lab root defect (per-tick-boundary guards, W1/W2) stays a NAMED OPEN SEMANTICS QUESTION, no pricing lab dispatched; fold lane for the csp fixtures/sugar also waits. Wake on user word').

lane_landed(self_map_rail, 'merge of lane commit 7d4cdeb5; coordinator re-ran just self-map TWICE in-worktree = identical sha, then regenerated on merged main',
     'THE SYSTEM DESCRIBES ITS OWN ARCH IN ITS OWN LANGUAGE: v6/ARCH-MAP.md, 4 mermaid diagrams, byte-stable. Expansion phase order ~95% dl6 (min() successor chain), registry by axis ~85%, ARCH task frontier ~95% (ready/blocked/blocker antijoins), the program own 46-rel dataflow through the real analyze.pl ~80%. LIVE MODE LANDED: inserting a phase row REWIRED the successor chain on disk. FIRST-RUN CATCH proving the rail point: ARCH.pl carried TWO duplicate-state task rows (clock_check active+labbed, comment_rail_wiring done+unbuilt) invisible to just arch acyclicity/totality; surfaced as task_state_conflict rel; coordinator resolved both (newer/done rows survive, deletions annotated in place), regenerated map shows single states AND picked up the fresh csp/per_row rows -- 166 -> 170 tasks without touching the rail. LANGUAGE GAPS dogfooded: NO STRING AGGREGATE (folding rows into a document has no spelling; mermaid text lives in the python renderer, caps every dl6 share -- second sighting today after the mermaid-assembly prediction), probe-output guard still refused unbound_head_var (flagship repro cited). Absence-assertion trap recorded. just self-map recipe live; no staleness gate yet (named)').

lane_landed(extract_t2_lab, 'merge of lab commit 447ee181; coordinator re-ran receipts.sh 19/0 HOLD',
     'BUY TABLE EXECUTED, zero bespoke parsers: avro/openapi/json-schema already JSON, pbjs -t json bought (verified on protoc own descriptor.proto), graphql-js introspection bought (15ms), protoc descriptor REJECTED measured (no JSON flag), quicktype REJECTED measured (TypeGraph unexported; graphql arm does sample inference not extraction), ref-parser rejected AND yielded a finding (dereference returns real JS cycles). Q1 all 8 algebra rows proven on real docs. Q2 five dl6 reifiers zero TS, 951 facts, byte-identical oracle vs served. Q3 HEADLINE REAL: calls_shape across 3 repos, undeclared_shape_dep lint catches 2, sabotage 2->5. Q4 statements flat across 20x doc size, 800 repos ~ 32s projected. Q5 fidelity EXACT but rebuild is python: the COMPILED json plane is READ-ONLY (both construction spellings refused) = FOURTH sighting of the assembly gap today. FINDINGS: D2 SHARPEST -- decode hole over heterogeneous values has NO correct column type (json column SILENTLY DROPS scalars via INSERT OR IGNORE over CHECK(json_valid)); D1 confirms jsonscan pothole with a 20-line fix carried in t2_oracle.pl (potholes lane owns the real file); A1 ** has no anchor, INVENTED 3 enum variants on a real descriptor; A4 underscore-leading brace keys parse as VARIABLES (GraphQL root is __schema); A6 = C3 no longer hypothetical. 4 new slots, 3 fail-first fixture candidates. Lab dir alive pending fold').

lane_landed(oracle_body_gate, 'merge of lane commit fec109db; coordinator re-ran conformance 240/0 + TEXT_DOOR 168/168/0 on branch, green exit 0 + sweep BOTH modes 168/166/0 + compile-speed 0-regressions on merged main',
     'THE ORACLE EXECUTES WHAT IT PUBLISHES. F1 combine/next = REAL solve/2 clauses (compiler lowering byte-identical to bare conjunction on both planes -- the oracle was the only reader that could not run a construct it ships); TWO silences closed (solve splice + trigger_items splice_bare walk, edge rules over combine were statically dead). ERASURE CORRECTION: print_dl was faithful, parse_dl desugared splice at parse time = two doors held DIFFERENT TERMS for one source; both now round-trip. F2 reserved rows refuse BOTH doors (shared reserved_body_word trigger, walk in 0_body_walk so analyze cannot drift); refused rows stay oracle-executable ON PURPOSE (oracle = the wider language); undeclared atoms stay legal EDB. group_concat = refuse(not_implemented) NOT the aggregate class (collapsing would fail quieter at agg_compute), payload names working aggregates. HOST SHADOW MECHANISM CORRECTED: dedupe_terms FOLDED the identical col_type term (column vanished, arity kept the slot); refusal host_column_shadows_runtime + drift guard reading generated names back w/ sabotage. Bonus: compile.pl wraps shared pre-pass refusals at program_plan (six host refusals had never been through that door). Load-time checks cost max +0.2 pct inferences. Conformance 226 -> 240, sweep 162/160 -> 168/166').
answered(ordered_aggregate_arc, 'user 2026-07-30: "yes" -- queue the ordered string/array aggregate head arc (one construct deletes the python mermaid assembly, v5-collect expressibility, and the json agg head refusals; the priced blocker is where the inner ORDER BY lives in the flat aggregate SELECT). Dispatches when the json-potholes lane lands (registry/lower collision fence)').

directive(ordered_aggregate_goes_lab_first, 'user 2026-07-30: "we are going to need aggregation so i guess we do need to figure this out bc v5 got it firgured out, dont trust arch go back into a lab please with luna mid think codex bc they just made that tthing 80% cheaper". Supersedes the queued-arc dispatch shape: NOT direct wiring off the priced ARCH row -- a lab first, re-deriving the design from v5 evidence; agent = codex gpt-5.6-luna at MEDIUM effort (user word, OpenAI price cut cited), an explicit exception to labs-run-on-opus').

in_flight(ordered_aggregate_lab, 'codex gpt-5.6-luna effort=medium (header echo verified), worktree ../sprefa-codex-ordagg, branch codex/ordered-aggregate-lab, base 855da45f, no-commit flow, header plans/2026-07-30-ordered-aggregate-lab-header.md',
     'coordinator pre-scouted v5 into the header: AggFn has NO group_concat, json_group_array lowers to ORDER BY the VALUE ITSELF (src/lower.rs, digest-stability comment = v5 whole answer to element order), json_group_object(key,val ORDER BY key), sprf_sym_intern wrap, COALESCE(SUM,0) NULL-divergence hazard; driver receipt @libsql sqlite 3.45.1 aggregate ORDER BY works. Lab grades BOTH order axes (value-sort = v5/setof vs explicit ordinal = findall/stream-lab machinery), string join vs array+use-site join, hand-drafted incremental emitted SQL w/ group-scoped EXPLAIN + flat statements, oracle msort/keysort byte-equality vs SQL both axes, nesting probe, minus-delta recompute. HARD FENCES: labs/ordered_aggregate/** + verdict doc only; registry/lower/parse_dl/print_dl/emitter/grammar untouchable (json-potholes lane owns overlaps); deps pre-installed (sandbox has no network). Wiring stays the follow-up arc, dispatched on this verdict').

lane_landed(ordered_aggregate_lab, 'merge 0fbe27d0, lab-death cde5d9ec, last copy c45e3a46, verdict plans/2026-07-30-ordered-aggregate-verdict.md; coordinator re-ran probe.mjs + oracle_ordering.pl + dl6_oracle leg + unblocked the bop receipt (agent exit 1 was a coordinator env gap -- sprefa-store/js had no node_modules; real result = named refusal unsupported_construct(aggregate_head(json_array(_))) exit 2)',
     'first luna-mid lab (user word, 74k tokens, ~40min): BOTH order axes proven on @libsql 3.45.1 -- value-sort json_group_array(x ORDER BY x) = v5 parity (v5 sorts by the VALUE, digest-stability comment is its whole answer; v5 has NO group_concat), explicit ordinal = json_group_array(value, ordinal) two-arg spelling, one spelling cannot serve both; group_concat(value, sep ORDER BY ordinal) live on 3.45.1 = the string-join own-aggregate verdict (mermaid demand wants text directly, no intermediate array parse); incremental tier = group-scoped delete+rebuild scoped by __agg_scope seed, indexed SEARCH + TEMP B-TREE for the inner ORDER BY, 2 statements flat 10 vs 1000 groups; empty group = absent head row (GROUP BY over zero rows, the SUM-NULL class avoided); nesting composes through json(payload). 4-sighting census: mermaid + extract-t2 want ORDINAL, v5-collect + json-agg-heads want VALUE. Wiring arc list in the verdict. Thin-but-sufficient: Q6 minus-delta is reasoned prose not an executed probe -- the wiring arc owes the executed fixture').

lane_landed(json_scan_pothole_lane, 'merge ddffb8b6 (agent commit 17fe0d4b, base 3d993e1e verified, worktree-agent branch); coordinator re-ran EVERYTHING on merged main: conformance 245/0, sweep BOTH modes 245/171/0-wrong (169 identical, 2 pre-existing run_error), TEXT_DOOR 171/171/0, plunit 252/252, roundtrip ALL PASS, green exit 0 twice, compile-speed 0 regressions; conflicts generated-files-only, resolved by regen',
     'BOTH potholes closed, jsonscan receipt exact: cli 4->7->8, web 10, byte-identical both doors, sweep-generated schedule == hand schedule. (a) typed capture {stars: Stars: int} -- colon already xfy 600 so zero term-door parser work; the GUARD is the fix (json_type(path)=integer in emitted WHERE; a declared intermediate rel buys the type NOT the guard, measured); int/float/text both doors pinned equal by plunit, bool = named refusal; off-type FILTERS never throws (SQL cannot raise from WHERE -- disclosed deviation, accepted); wide-int cliff NOT subsumed. (b) json arrival IS canonical text (the emitter door had ruled it in writing); dl6_oracle parses it into oracle json terms via analyze:rel_columns; raw-object arrival = named refusal matching the served 400; BONUS defect fixed: oracle/2 only matched prog/2, hosts programs died bare. FINDINGS BANKED: (1) THIRD SILENCE, stop-and-reported: brace pattern directly in a body atom arg derives nothing on either door, no refusal, vacuously identical -- fix = 0_json_pattern_expand.pl shared expansion, own arc filed; (2) untyped json capture on a NUMBER diverges (oracle 4 vs emitter "4"), named in fixture header, fixture text-restricted; (3) golden_oracle.pl carries a second copy of pothole (b), fix documented in golden-flex.dl6 header, needs the TS half; (4) staleness sweep-up on merge: door-handwritten 5th eating + json_wide_integers orphan module deleted (json-flex committed the module with no fixture source ever promoted)').

in_flight(ordered_aggregate_arc, 'codex gpt-5.6-luna effort=HIGH (header echo verified; brief is tight so routing-table high, the lab was the user-worded mid), worktree ../sprefa-codex-ordagg-wire, branch codex/ordered-aggregate-wire, base c588d547, no-commit flow, brief plans/2026-07-30-ordered-aggregate-wiring-brief.md',
     'the wiring arc off the lab verdict, fence lifted by the potholes merge: 4 head forms coordinator-fixed (json_group_array/1 value axis, /2 ordinal; group_concat/2 value, /3 ordinal; constant separator only), byte-exact contract (canonical JSON shared encoder never forked, empty group = absent row, last-row retraction = head minus delta), group-scoped incremental beside the min/max plan + naive referee identical, EXECUTED minus-delta fixture owed (lab graded it as prose), 4 sighting programs promoted as fixtures, COUNT tests per the formerly-quadratic law, full battery + compile-speed (report never bypass)').

lane_landed(ordered_aggregate_arc, 'merge a2a98d5d (agent commit 63d164c3 by coordinator, no-commit flow); agent STOPPED correctly at just green (sandbox EPERM binding sockets, no workaround attempted) -- coordinator finished the last leg: golden-flex coverage (4 new rules in golden-flex.dl6, coverage 43 exercised + 17 named absences), compile-speed baseline accepted (golden-flex program GREW 4 decls + 4 rules, +10.9 to +20.6 pct inferences, same correctness-cost class as json-flex), justfile expects refreshed. Merged-main battery coordinator-run: conformance 256/0, sweep BOTH modes 256/182/0-wrong, TEXT_DOOR 182/182/0, roundtrip ALL PASS, plunit 255/255, GOLDEN FLEX HOLDS incl served e2e, green exit 0, staleness OK',
     'THE ORDERED AGGREGATE IS LIVE, four sightings deleted: json_group_array/1 (value axis, v5 parity), json_group_array/2 (int ordinal), group_concat/2 (value), group_concat/3 (ordinal); separator constant-only, ordinal must type int, non-delta-local group key = named refusal aggregate_group_not_delta_local (my joined-body golden draft hit it -- the refusal is right, group scope seeding needs the delta to bind the key); oracle msort/keysort mirrors SQL ORDER BY through the SHARED canonical json encoder; empty group = absent row; EXECUTED minus-delta fixture (retract one of three -> rebuilt array, retract last -> head minus delta); COUNT tests flat 10 vs 1000 groups + EXPLAIN SEARCH. 11 fixtures incl all four sighting programs. GOLDEN CAUGHT A REAL ONE during my flex-wiring: lane declared text over an int source diverged the doors (oracle 4 vs emitter "4") -- the cross-type-decl divergence class, silent until the byte diff; worth a fail-first fixture in the json_pattern_expand arc since it is the same decl-type-vs-value family. FOLLOW-UPS OPENED: self-map python mermaid renderer can now shrink (dl6 group_concat does the line joins), extract-t2 rebuild same; both ride the next touch of those rails').

answered(vocabulary_tiebreak, 'user 2026-07-30 post-aggregate-landing: "when in doubt can we just stick to ansi or any form of a sql or sql standard? preferably sqlite lmfao" -- RULED sqlite_first_then_sql_standard (rulings.pl tail): sqlite spelling wins ties/doubt, ansi next, rx/prolog only where no storage-plane spelling exists. Landed aggregates already comply (all literal sqlite names). Review-B8 non-SQL words (pre/keep/combine/finalize/now/latest) NOT auto-renamed by this; their wave needs its own user word, this fixes the tiebreak direction only').

directive(close_golden_gaps, 'user 2026-07-30: "close all these gaps we are in pole position unga bunga, vroom vroom" -- both golden-coverage gaps from the sitrep get lanes: gap 1 json seam + axis flex, gap 2 the type matrix. Gap 3 (refused constructs absent by name) is correct behavior, no lane').

in_flight(golden_json_seam, 'codex gpt-5.6-luna effort=high (header echo verified), worktree ../sprefa-codex-goldenjson, branch codex/golden-json-seam, base 5ce31a24, no-commit flow, brief plans/2026-07-30-golden-json-seam-brief.md',
     'executes the fix the golden header itself documents: golden_oracle.pl schedule_value atom mapping gets the dl6_oracle type-directed arrival treatment (SHARED impl, never a third copy) + golden-schedules.ts canonicalizes object/array arrivals for json columns via the existing canonicalizer; then the json axis flexes in golden-flex.dl6 (spread, $key, ** descent, {}, typed capture, list(T) round-trip through json_group_array + decode(spread)) and golden_coverage expected_absent bookkeeping moves those rows to exercised. Warned about the sandbox EPERM socket leg (report, never work around). Fence: golden-path files only, labs/ belongs to the concurrent type-matrix lane').

in_flight(type_matrix_lab, 'opus worktree agent (harness-cut), base 5ce31a24 ff-only verified in first report, header plans/2026-07-30-type-matrix-lab-header.md',
     'gap 2: the decl-type x value-type x position cross product has never been walked (proof: today text-decl-over-int-source diverged silently, oracle 4 vs emitter "4", only the golden byte diff caught it). Lab = RUNNING matrix generator, one tiny program+schedule per constructible cell, both doors, mechanical 4-way classification (named refusal / identical / DIVERGENT / silent coercion), regenerable in one command; divergent cells become fail-first fixture candidates; recommended refusal set + 3 named slots. Fence: labs/type_matrix/** + verdict doc only; golden path belongs to the codex lane').

lane_landed(golden_json_seam, 'merge on main after d476bebd (codex luna high no-commit flow); coordinator ran the sandbox-blocked legs itself: GOLDEN FLEX HOLDS all 6 legs incl served e2e, TEXT_DOOR 182/182/0 (agent unicode failure = sandbox locale, does not reproduce), sweep BOTH modes 182/180/0-wrong (agent SIGSEGV = known transform-types flake), conformance 256/0, plunit 255/255, roundtrip, green exit 0, compile-speed rebaselined (golden grew the whole json axis, parse +21 plan +49 pct, grown-program acceptance #3 today), staleness OK. Post-merge: bopRun 10s spawnSync timeout raised to 30s -- crossed from flake to marginal-by-design after today\'s three compiler growths (3 sightings, 3/3 isolated at ~2s)',
     'GAP 1 CLOSED: 0_json_arrival.pl = ONE shared arrival-mapping module; dl6_oracle.pl shrank 83 lines onto it and golden_oracle.pl consumes the same predicate (the second-copy defect DIES, no third copy born); golden-schedules.ts canonicalizes object/array arrivals for json columns via the existing canonicalizer. Coverage 43/17 -> 47 exercised / 13 named absences: spread fan-out, $key capture, ** descent, typed capture, list(text) round-trip (rows -> json_group_array -> decode spread -> rows) all IN THE GOLDEN now. Named ambiguity honored: {k: v} value-expression braces stay refused per fixture contract, exercised via decode object-pattern').

directive(point_free_direction, 'user 2026-07-30: "it does read like point free rxjs yet [does NOT], what is minimum set of syntax moves and semantics to get us operationality there... lab it out and hallucinate more". Coordinator hallucinated the 3-move minimum in-chat (M1 scan-with-seed kills the init/step arm tax, M2 seq(name) kills the 4-rule cursor block per the ruled card 1b + csp census, M3 |> anonymous stages kill the name-every-intermediate tax via match-arm-style minted rels) and the already-point-free inventory (merge = multi-rule head, filter/map = body, withLatestFrom = latest, switchMap = key(1), distinctUntil = boundary physics, toArray = json_group_array)').

in_flight(point_free_lab, 'opus worktree agent (harness-cut), base 2da8faef ff-only verified in first report, header plans/2026-07-30-point-free-lab-header.md',
     'grading law = sugar byte-identical to hand-desugar through existing doors or REFUTED (match-block sha precedent, zero compiler edits); Q2 corpus = 8-10 rx-doc-pattern programs today-vs-with-moves with a rules-deleted census; Q3 = the named rules for when a |> stage MUST be named (joins, own retention, diamond reads); Q4 head-last spelling priced only; Q5 minimality (derivability + the missed fourth move; json_pattern_expand = M0 assumed). Slots: scan_spelling, seq_scope, stage_naming determinism, stage_retention, pipe_word under vocabulary_tiebreak. Fence: labs/point_free/** only; type_matrix lab owns its own dir').

lane_landed(type_matrix_lab, 'merge of lab commits bd6e8595+e0ab50ed (agent committed in harness worktree, base 5ce31a24 ff-only verified); coordinator regraded on MERGED main after the seam landing the lab itself flagged: 422 cells = 79 IDENTICAL / 156 DIVERGENT / 71 SILENT_COERCION / 116 NAMED_REFUSAL (base was 73/168/65/116 -- the shared 0_json_arrival healed 12 cells, nothing worsened); two lab symlinks did not survive the merge (266 false emitter_run_error until recreated, now committed); verdict addendum records the regrade; lab dir stays ALIVE as the fix wave verification tool (protocol debt, csp precedent)',
     'GAP 2 WALKED: 422 constructible cells, ZERO n/a -- the grammar accepts every corner, every guard must come from a door. HEADLINE P0, reproduced by coordinator at merged main: the DEFAULT incremental emitter DROPS an arrival delta whenever sqlite affinity rewrites the value (untyped probe_in(4): oracle derives, naive derives, incremental logs empty deltas -- row in table, delta gone, downstream blind; 1_incremental.ts RETURNING-text vs wire-value JSON.stringify mismatch, ~50 cells). #2: head decl type = cast on emitter, no-op on oracle, 63 cells, 17 vanish the row. #3: dl6_oracle could not carry float/bool schedule values (partially healed by the seam merge). #4: wide ints fail three ways incl RangeError killing the tick pipeline (19 cells). #5: oracle refuses int-at-float-column that compiler accepts (open language question). Slots: decl conflict = RECOMMEND refuse-at-load by widening edge_head_column_type_mismatch (the one loud position is the one working position, 2 vs 155 divergences); float/int boundary already collapsed both doors, contract owes the sentence; undeclared default text is fine, its two paths must agree. AWAITING USER: refusal-widening blast radius, int_widens_to_float, wide_int_fate, bool_storage').

in_flight(affinity_drop_fix, 'codex gpt-5.6-luna effort=high (header echo verified), worktree ../sprefa-codex-affinity, branch codex/affinity-drop, base 0b08ee75, no-commit flow, brief plans/2026-07-30-incremental-affinity-drop-brief.md',
     'the narrow P0 fix only: delta stream carries the STORED value read back from RETURNING (never re-implement affinity in JS); fail-first fixture red-incremental/green-both; matrix.sh before/after label counts as the wide receipt; ruling-territory divergences explicitly out of scope').

lane_landed(affinity_drop_fix, 'merge of 32916613 (codex luna high no-commit flow, coordinator committed after review + adversarial probing); coordinator re-ran EVERYTHING on merged main: conformance 258/0, sweep BOTH modes 258/184/0-wrong, GOLDEN FLEX HOLDS, green exit 0, compile-speed OK, staleness OK, matrix regrade emitter_modes_disagree 48 -> 0 / IDENTICAL 79 -> 87',
     'THE TYPE-MATRIX HEADLINE P0 IS DEAD: arrival deltas now carry the STORED value read back from RETURNING; the emitted delta set provably equals the RETURNING set. COORDINATOR GATE CAUGHT A REAL HAZARD in review: the fix switched set-membership to POSITIONAL pairing, and INSERT OR IGNORE returns fewer rows than a duplicate-bearing batch -- adversarial interleaved dup/new/dup/new repro run by hand on both doors came back byte-identical (the pairing consumes each returned row exactly once; only internal sequence attribution skews, unobservable in the graded log), promoted as fixture arrival_dup_batch_partial_ignore beside the agent fail-first fixture. Correction on the record: the verdict-addendum claim that lab symlinks were committed was WRONG (gitignore ate them; local-only, documented). Residual doors_disagree 81 = the decl-cast RULING class, correctly untouched -- awaits the type_ruling_round').

lane_landed(point_free_lab, 'merge of lab tip 89ccaccf, lab-death 1b17a7ea (last copy 89ccaccf), verdict plans/2026-07-30-point-free-verdict.md + addendum; coordinator re-ran receipts 29/29 in-worktree AND on merged main post-affinity-fix, conformance 258/0, plus its own pre-drift probe (bop exit 0 vs registry refused row)',
     'THE MINIMUM SET IS SMALLER THAN HYPOTHESIZED: M2 seq = CONFIRMED, the one that pays (4 rules -> 1 per cursor, byte-identical both doors; csp corpus 94 -> 67 rules from M2 ALONE, and it amends the csp rec: numbering is 36->9, the other 16 of the "52" are queue semantics = a different construct). M3 |> stages = CONFIRMED for level rules ONLY (3->1 rules, minted stage rels, deterministic stage_<head>_<arity>_<k> names, collisions refused), REFUSED in edge rules with both a loud and a SILENT wrongness receipt. M1 scan seed = AMENDED WEAK: pre_occurrence_loop landing shrank folds to 2 rules already; scan makes a two-trigger fold LONGER (2->4); its residual value = the keyed-head check (unrefused log-headed fold FORKS, receipt 4a). Census: 14-program rx-doc corpus 30 -> 20 rules (33 pct); half the corpus is ALREADY point-free (merge/filter/map/withLatestFrom/distinctUntil/pairwise = 0 deleted). Slots: scan = head position (two-accumulator fold decides), seq scope = the argument itself (atom global / var per-value), stage retention = forced by the arrow never chosen, stage naming constraint = __ prefix does not parse so minted names are collision-proof XOR writable. AWAITING USER: slot_pipe_word (sqlite spelling for staged computation is named CTEs, so the vocabulary_tiebreak third tier only unlocks if anonymous-stage counts as storage-plane-unspellable; |> is the only coherent reading of three priced); minted-rel tick-log visibility. TWO DEFECTS BANKED: pairwise via finalize+read is WRONG at one-change-per-tick cadence ((10,9)+(14,9) instead of (10,14)+(14,9)), compiles clean, distinct from U4, receipt Q1e; PRE REGISTRY DRIFT coordinator-reproduced -- pre compiles exit 0 while registry.pl:76 says refused, 3 doc places stale, sweep 13 edge_body_needs_pre fixtures still refuse = doors/paths disagree on pre legality').

answered(pre_registry_drift, 'coordinator scout 2026-07-30 night: NOT a semantics bug. pre_occurrence_loop LANDED today (7f086fd3, ARCH row done): ordered occurrence execution via __pre_<rel> snapshots, 13 former edge_body_needs_pre fixtures compile, orderedPre.test.ts 123 lines, run-results has ZERO edge_body_needs_pre. The drift is DOC-ONLY: registry.pl:76 still refused, SYNTAX/SCOREBOARD stale, golden absence entry now states a false reason. Fix folded into the seq wiring lane (same files).').

directive(night_ruling_round, 'user: seq APPROVED (M2 only, explain it); release GATED on arch-from-single-dl6 (dogfood, python renderer dies); pre = "just look" (scouted, resolved); devlog = YES dogfood hard.').

in_flight(seq_wire, 'codex luna high, worktree ../sprefa-codex-seqwire, base 5e78b196, brief plans/2026-07-30-seq-wire-brief.md', 'M2 seq per user approval + pre doc truth folded in (registry live flip, golden exercised, SYNTAX/SCOREBOARD refresh)').
in_flight(selfmap_dogfood, 'codex luna high, worktree ../sprefa-codex-selfmap, base 5e78b196, brief plans/2026-07-30-selfmap-dogfood-brief.md', 'RELEASE GATE: ARCH-MAP.md from ONE dl6 file, group_concat assembly, python renderer deleted').
in_flight(devlog_rail, 'codex luna high, worktree ../sprefa-codex-devlog, base 5e78b196, brief plans/2026-07-30-devlog-rail-brief.md', 'dogfood docs: dl6 reads chat_log/*.pl ledgers, emits DEVLOG.md').

lane_landed(seq_wire, 'merge d2dadb93 (codex luna high no-commit flow + TWO coordinator emitter fixes); coordinator battery in-worktree: conformance 260/0, sweep BOTH modes 260/186/0-wrong, TEXT_DOOR 186/186/0 (agent unicode crash = sandbox locale, 6th sighting, does not reproduce), roundtrip 260/260, plunit 258/258, GOLDEN FLEX HOLDS, green 0, typecheck 0, staleness OK, compile-speed rebaselined',
     'seq() IS LIVE: Ordinal := seq(name), one shared expansion (0_seq_expand.pl phase 42) to the 4-rule cursor block, byte-identical to hand desugar both doors incl the empty drain tick; cursor names seq_<head>_<pos>, collision + unknown-type refusals named; golden coverage 47/13 -> 49/12 with pre/1 flipped refused->live (doc truth after the 7f086fd3 scout). COORDINATOR CAUGHT TWO EMITTER TYPE BUGS the new golden exercised first (agent had no typecheck in its suite list): shared retention stage collapsed the ordered-tick triple (retention_tick_lines_ordered now spells it), and rxjs pipe() typed overloads STOP AT 9 OPERATORS -- the ordered tick hit 12 stages and the whole chain typed Observable<unknown>; template now splits into a second .pipe(). Both were invisible to every byte-grading gate (types are not bytes).').

lane_landed(selfmap_dogfood, 'merge 0d194c3a; RELEASE GATE SATISFIED (ruling release_gate_v620): coordinator battery on merged main green 0 + SELF MAP HOLDS + sweep 186/184/0',
     'ARCH-MAP.md from ONE dl6 file, python renderer DELETED, run-twice byte-identical, 4 mermaid fences. The agent program had NEVER RUN (sandbox EPERM) and the coordinator drove it through SEVEN named refusals to green -- each refusal correct, the sequence itself is a language-UX finding: missing ] (char-code parse error, B4 #7), quote_in_literal on apostrophes (SQL-injection guard, NO escape spelling exists for a quote in a text literal = named gap), text < refused (ties now share a rank), aggregate_head_mixed_with_plain_clause -> coalesce-at-reader per null_design, coalesce_no_output twice (output var may appear ONLY in atom+head -> staged rels). Plus ONE real compiler fix: group_expr GROUP-BY-literal guard read the head TERM not the compiled SQL, so := 0 through a coalesce default emitted bare positional 0 = SQLITE_ERROR (lower.pl sql_bare_integer now guards the compiled text). ALSO BANKED: batch-path sqlite errors carry NO statement text (F7 fixed execute$ only); coordinator process note: two greedy regex passes destroyed atom quoting and the lane restarted from the served-snapshot pristine copy -- gen_served hashes saved the day as the accidental archive').

% --- beta wave (user: "get us to beta?") ---
directive(beta_push, "user go signal; gate defined in plans/2026-07-30-v6-beta-plan.md: readable errors, float/avg/bool, zero known-wrong modules, GETTING-STARTED dogfood, install story, battery green").
in_flight(refusal_messages, "worktree ../sprefa-codex-refusalmsg codex luna high, base f3a56324; finishes 0_refusal_messages.pl: plain-english per-functor clauses, parse line:col furthest-failure, at/3 file:line arm fed").
in_flight(float_avg_bool, "worktree ../sprefa-codex-floatavg codex luna high, base f3a56324; RULED semantics: REAL binary64 exact, avg=sum+count, bool=row presence, wide int -> int_out_of_range refusal").
lane_landed(devlog_rail, "run-twice sha c4dfdb5e worktree / 639a2780 merged main, 206 lines", "DEVLOG.md from ONE dl6 program over the session ledgers (merge 4dd6b76a): sh host swipl one-liner emits JSONL per ledger, decode/spread, per-session sections, group_concat assembly, one write host. Coordinator fixes: catch on static preds, position/value dict args, dedupe render, title ordinal, quiescence = file digest NOT /ticks. ROOT CAUSE of the 35-min hang: /ticks streams (200, holds connection, zero bytes) -- bare curl read blocks forever; two stacked wedged runs killed. Named gap: no snapshot twin for /ticks").
directive(opus_cooking, "user 2026-07-31 pre-sleep: start cooking on opus, push often; rust course plotted in plans/2026-07-31-rust-course.md -- language-agnostic CLI benches GATE the rust lowering; runtime-port-vs-emit-target decided by phase-0 bench data").
directive(overnight_scope, "user pre-sleep: plan scouting/missing features, clock checker must have legs (maybe build on it later), v5 golden use cases READY TO GO (priority tho); course doc plans/2026-07-31-overnight-course.md").
lane_landed(refusal_messages, "battery re-run by coordinator: conformance 260/0, roundtrip ALL PASS, TEXT_DOOR 186/186/0, plunit 258/258, lint 1/1, bop 14/14", "beta gate 1 (merge 3ffc1074): 107 specific message clauses 0 fallback, parse errors line:col via furthest-failure, text door prints file:line refusals. COORDINATOR CATCH on the EPERM-blocked server leg: serve-path exit classifier missed the rendered heading shape (functor colon), http load of a named refusal exited 1 not 2; fixed three-shape match in bop.ts").
in_flight(golden_readiness, "opus worktree, base 00305ff0", "grades all 9 v5 golden use-case programs, READINESS.md, small gaps fixed in-lane, precommit-changed attempted (brief plans/2026-07-31-golden-readiness-brief.md)").
in_flight(bench_cli, "opus worktree, base 00305ff0", "rust-course phase 0: language-agnostic CLI bench contract, buy-verdict first (hyperfine candidate), tick-log byte referee, standings (brief plans/2026-07-31-bench-cli-brief.md)").
in_flight(clock_legs, "QUEUED behind float/avg merge (registry.pl overlap)", "ranked zero-surface iteration items 1-2 + historical bug-class replay gate (brief plans/2026-07-31-clock-legs-brief.md)").
finding(main_ff_blocked, coordinator, "ff of main to session tip classifier-blocked for the agent; session branch pushes fine; main push + v6.2.0 tag = morning user item (local tag still on stale e931191e)").
lane_landed(golden_readiness, "coordinator re-ran ghcacher-golden + precommit-changed + watch-scale in worktree AND ghcacher-golden + precommit-changed + conformance on merged main", "v6/READINESS.md grades all 9 v5 golden use-cases by timed run (merge d0c03cba): 4 READY, 3 SMALL-GAP-FIXED, 3 gaps priced. GHCACHER-GOLDEN WAS RED ON MAIN (stale read_schedule/2 call after the type-directed dl6_oracle change -- a green-all member failing silently, staleness class again); precommit-changed (program 3) wired green with the whitespace-decode witness inverted into a regression guard; watch-scale gate: ticks flat 3, write amplification 1.185 identical across 10x. PRICED: v5 surface distance 132 absent/20 covered/4 partial over 156 (codegen/comment/match_ast/closure/scalar-fn/repo-rev clusters); parity 84x with the NAMED language gap = no org fan-out spelling (v6 leg cannot be one program); tick-log format has two agreeing impls and NO spec doc. PROCESS FINDING: outer node_modules symlink resolves sprefa-store-engine's RELATIVE link against the MAIN checkout -- agent switched to pnpm install per package and re-ran all receipts; dispatch prompts must stop recommending the symlink").
lane_landed(float_avg_bool, "coordinator battery on MERGED tree: conformance 265, TEXT_DOOR 190/190/0, plunit 258/258, sweep 190/188/2-pre-existing, dl 96/96, store 74/74, bop 14/14", "beta gate 2 (merge 13653a77 + revert 0b7c2d37): float REAL/binary64 exact compare/join, shortest round-trip rendering SHARED oracle-vs-TS (js_float_text: format(~h) + ECMAScript thresholds), avg = delta-maintained sum+count with EXPLAIN SEARCH receipts, int_out_of_range named refusal at the value boundary both sides, +5 fixtures byte-identical both doors. COORDINATOR CATCH post-merge: the lane flipped the store's global createClient to intMode bigint; emitted raw-row projections (ordered-occurrence arms) leak BigInt into JSON.stringify -- 3 fixtures threw ONLY on the merged tree because the worktree node_modules SYMLINK resolved sprefa-store-engine to MAIN's checkout (the readiness lane's relative-link trap made MY branch battery vacuous for store-side changes). Reverted intMode; boundary refusal carries the ruling; read-side overflow cells revert to driver RangeError until a SqlRunner seam normalization").
finding(worktree_node_modules_trap, coordinator, "STANDING DISPATCH CHANGE: never symlink outer node_modules into worktrees -- sprefa-store-engine's RELATIVE link resolves against the main checkout, making store-side receipts vacuous; agents run pnpm install per package (seconds, warm store). Two lanes bitten tonight, one shipped a vacuous battery").
lane_landed(clock_legs, "coordinator re-ran plunit 263/263 + conformance + TEXT_DOOR 190/190/0 in worktree AND on merged main", "the checker has legs (merge before 62ff636b, opus): replay gate over 5 historical bug classes -- A12 latest-backlog CAUGHT via ring labels (unmarked twin projects 2 ring-Z triggers, marked projects ring-B edge_sample), C2 CAUGHT via tick arithmetic (endpoint pair is ring B definition), not-in-arm CAUGHT AS LABEL not refusal (measured: refusal would break the live json_typed_capture flagship fixture riding that exact shape), A4 named out-of-scope with a strengthened consumer companion, tick-phase pre-existing. Focused suite 25->30, clock_fact/5 proof facts in 4 fixtures, rank-3 SCC extension REFUTED by measurement, registry untouched (existing clock_role/4 covered everything). 3 sabotage receipts red").
lane_landed(bench_cli, "coordinator re-ran just bench-cli in worktree (timed=11) AND on merged tree after fixes (timed=11 disqualified=3)", "rust-course phase 0 (merge + fix 62ff636b, opus): CLI contract (stdout=tick log, --perf-out, 5 verdicts, byte-identical-or-never-timed), buy-verdict 7 candidates written first (hyperfine SECONDARY only: process-wall floor differs ~30ms across interpreters; /usr/bin/time -l bought for RSS), 11 timed cells all identical incl flagship/LSP-rail/aggregates, sabotage receipt. HEADLINE RULING CARD: the swipl oracle walls before 10k rows (s1/1k 1.4s vs tsv2 33ms) -- a rust engine CANNOT be graded at PERF-REPORT's 960k scale by tick-log byte-diff; exits written up in CONTRACT.md section 7 (scaling reference vs tiered grading), both = user rulings. MERGED-TREE FIXES by coordinator: gitignore ATE the load-bearing node_modules symlink (the known symlink-eaten class; force-added) and the rig exited 0 with timed=0 (floor gate added). ALSO: agent killed coordinator merged-tree bench processes misreading them as strays (disclosed), and the house perl-alarm timeout ORPHANS its child (measured, tsv2_gen.sh has the same one-liner)").
finding(oracle_scale_ceiling, bench_lane, "USER RULING NEEDED: reference-that-scales vs tiered grading (full tick log where oracle reaches, final-state hash beyond -- sweep already computes that line). Gates rust phase 1").
finding(perl_alarm_orphan, bench_lane, "perl -e alarm+exec kills the wrapper and orphans the engine child; measured timed-out swipl burning a core. tsv2_gen.sh uses the same one-liner; my devlog runs tonight used it too. Needs process-group kill").
lane_landed(getting_started, "coordinator re-ran just getting-started in worktree AND merged tree: 24 blocks HOLD", "beta gate 4 (merge a5ad6233, opus): GETTING-STARTED.md, every code block EXECUTED and replay-gated via one persistent-shell script (exports/cwd/background serve survive across blocks like a real terminal), 2 sabotage receipts incl a delta-level one end-state grading would miss. FIVE cold-author defects filed verbatim, none fixed in-lane: D1 host_input_contract is a hardcoded host-NAME list (any other name = template_mismatch refusal for the standard shape); D2 template_mismatch drops its payload (no column, no line); D3 bop check bypasses throw_text_door_error so refusals lose file:line that compile_dl6.sh prints for the SAME file (stale ARCH attribution corrected: parse positions exist); D4 served errors name the gen_served temp copy not the user file; D5 single-output sh host silently truncates values at first whitespace (parseWhitespace fallback fields[0])").
lane_landed(grader_exit_defect, "fail-pre-fix: conformance exit 1 FAILURES 1; after fixture fix 265 PASS exit 0; arch/roundtrip/plunit green under new grader", "FAILURE-MODES CLASS 37 (commit 4a6a6c1c): grader.pl run/1 printed fail lines inside forall and SUCCEEDED -- conformance/arch/every -g go runner has been exit-0 ADVISORY SINCE BIRTH; red fixture float_shortest_round_trip_wire (malformed expectation: two deltas/2 terms for one rel) shipped through the floatavg landing battery, the merge battery, and two coordinator re-runs, found only by the docs lane reading the full log. run/1 now accumulates failures and fails; fixture expectation collapsed to the one-term-per-rel contract").
lane_landed(fork_join_defect, "coordinator re-ran plunit 267/267, TEXT_DOOR 189/189/0, sweep crash-gate in worktree; merged tree just green EXIT 0 under the honest grader", "beta gate 3 (merge + fixes 4c3d4b36, opus): RCA = json_extract over canonical term text (one compound value, two encodings: head expr writes json1 tagged, arrival writes term text) = malformed JSON, statement recovered by seam wrap since LibsqlError carries no SQL. DECISION: compound_pattern_on_arrival_rel named refusal COMPILER-ONLY (oracle keeps the capability; real fix pre-empts struct_as_rows AND still would not grade identical -- sub-arg typing IS the struct plane). Sweep run_error SPLIT rejection/emitted_crash + exit-1 crash gate with sabotage receipt. AGENT PROCESS FINDING pinned by plunit: its own first draft silently restated trigger_arg_not_var on 2 edge fixtures, no gate caught the same-bucket rewrite -- found by reason-level manifest diff; that diff is worth a standing check. SIBLING GATE HOLES fixed by coordinator: roundtrip G3 grepped ^FAIL vs grader's lowercase fail (blind since birth), door-handwritten + golden-flex stale vs floatavg emitter (4th gen_staleness recurrence)").
lane_landed(pairwise_d3, "coordinator re-ran conformance/bop 15/getting-started in worktree; final merged green EXIT 0 (275 PASS), TEXT_DOOR 191/191/0, getting-started 24 HOLD", "last beta residuals (merge abdbb4f5, opus): pairwise Q1e = DEFINED not a bug -- both doors byte-identical at BOTH cadences; the (10,9) pair is keyed_replace_departs_the_old_row composed with finalize grade +1 (TICK-MODEL section 2 at cadence 1); latest() probe produced the identical log so read-vs-trigger is not the axis; pinned by 2 fixtures + SYNTAX/TICK-MODEL notes, ARCH row closed works-as-ruled; residual named = an rx-pairwise-style HOLDING construct would be a real language addition, priced only if a program asks. D3 CLOSED: bop check refusals print file:line via exported throw_text_door_error (fail-first bopCheck test + sabotage receipt); parse-position claim corrected (statement-level positions EXIST, per-goal missing). THIRD EXIT-BLIND RUNNER fixed by coordinator: ARCH.pl carried its own private copy of the grader loop -- roadmap_is_total was RED (my dangling refusal_messages_arc dep) with exit 0; go now rides grader run/1, sabotage receipt exit 1").
answered(beta_gate_status, "gates 1-4 + 6 COMPLETE: readable errors (107 clauses, file:line both doors incl bop check), float/avg/int_out_of_range, zero known-wrong modules (fork_join refusal + crash gate, pairwise ruled DEFINED), GETTING-STARTED 24 executed blocks, battery HONEST-green (grader/roundtrip/arch all exit-blind fixed, class 37). Gate 5 install story = user call. Matrix classifier refresh + bigint seam + D1/D2/D4/D5 shelf = post-beta rows").
lane_landed(matrix_regrade, "coordinator spot-checked regraded matrix.json buckets 81/75/104/162 match the report; conformance 267/0 agent-run exit-honest", "ruling-round evidence made current (merge of the regrade lane): classifier taught runtime named refusals + at/3 unwrap + name-match leg -- 60 cells move one direction, THE STALE CLASSIFIER WAS HIDING THE FLOAT ARC'S BENEFIT (looked like DIVERGENT 140->150 regression, actually 46 divergences became agreed refusals). RangeError post-revert = ONE cell (json capture, no arrival gate, read-back only) = the exact bigint_seam_normalize surface. Four rulings each got a one-sentence evidence line in the verdict addendum: bool 4/60 identical all boolean-fed; wide int zero-identical anywhere, 8 cells give THREE answers to one input; refusal-widening live experiment already shows 0-divergent on gated bool. Rig repair: golden leg had been dead 422/422 (existence_error) since the doors converged; matrix.sh self-heals the gitignored symlinks now (prior 'symlinks committed' claim was false, gitignore ate them -- same class again)").
lane_landed(scan_card, "coordinator merged the doc-only lane; census parser paren/quote-aware over examples/*.dl", "the biggest v5-migration blocker priced (merge of plans/2026-07-31-scan-spelling-card.md): 242 sites/9 shapes/6 ruling cards; 181 expressible today, 48 expressible-but-wrong, 19 inexpressible. LIVE DEFECT glob_dialect_split filed: watch bind boot vs live halves use different glob dialects, 170/242 disagree, tutorial glob affected; fix is boot==live unconditionally, dialect choice = SLOT-GLOB-DIALECT. Word-hazard finding: scan already means rx accumulator AND the SQL SCAN verdict in this repo").
lane_landed(flake_manifest, "coordinator re-ran store 74/74 + sweep restated=0 in worktree", "two shelf rows closed (merge, opus): reactor bufferTime flake = virtual time per the F3 pattern (checked the real-time exemption first: nothing commits Date.now values); RECEIPT CORRECTED BY THE AGENT ITSELF -- 3x load proved nothing (18/18 pre-fix green), real contention 48 busy loops 8x = 36/40 pre vs 40/40 post; sabotage found only the marble-count assertion discriminates coalescing (buffer->map PASSED the counts assertion). Manifest reason-diff check live in sweep stage 4 (informational, STRICT=1 gates; sabotage restated=1 + 2 negative controls). Residual named: manifest not byte-reproducible, 27 rows carry raw variable numbering; numbervars fix in sweep.pl = compiler fence, next emitter-touching arc").
directive(rust_gate_sharpened, "user 2026-07-31 morning: rust lowering starts ONLY after ts v6 surpasses v5 ('the metal gear') on the felt capabilities; bench phase 0 + proven-reference tier are the measuring stick. CLI must stay well designed (bop --help) and the v5 daemon pain must NOT return (no plist/socket/orphan-root story).").
directive(rust_test_posture, "user 2026-07-31: rust tests not needed much beyond the sqlite kernel already started -- the cross-language tick-log byte grading IS the rust test suite; no parallel rust test pyramid").
lane_landed(glob_fix, "coordinator re-ran watch-scale correct:true x2 + EXTRACTION LIVE HOLDS + conformance in worktree AND watch-scale + self-map on merged tree", "glob_dialect ruling executed (merge, opus): one matchesWatchGlob predicate both watch halves, boot = ls-files bare + JS filter (node_modules stays unwalked -- untracked never appears); fail-first 4/4 red->green incl braces (node matcher takes them, card 6 DISSOLVED); boot==live property test over the 5 census shapes; sabotage header self-corrected by the agent (accept-everything matcher makes halves AGREE -- only the decoy assertion discriminates). Pathspec hosts audited legitimately unchanged (one-shot demand hosts have no live half). Residual filed: brace-on-pathspec silent zero = pathspec_brace_refusal row. GETTING-STARTED prose defect fixed (plan.md absence explanation was scratch-dir-specific, wrong in real repos)").
