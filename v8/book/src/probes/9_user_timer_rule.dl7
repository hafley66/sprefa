; expect: rc=0
; A user timer with a text column, read by a rule; --serve timer binds the Rust timer to it.
(: timer
   (* (: label text)))

(: Want (* (: label text)))

(Want "mine")

(: Seen (* (: label text)))

(<- (Seen ?Label)
    (Want ?Label)
    (timer ?Label))
