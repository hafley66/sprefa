# PaymentClient

The PaymentClient is the shared charge entry point across the Rust core, the
TypeScript gateway, and the mobile client. This heading maps to a real symbol.

## oldCharge

Deprecated path removed in v2. This heading no longer maps to any code symbol —
the planted doc-drift the doc->code seam must catch.
