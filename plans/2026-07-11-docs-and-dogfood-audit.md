# Docs story + dogfood coverage audit (2026-07-11 final hamon)

Companion chat assessments written to disk. Blindspots + graph ideas live in
plans/2026-07-11-scip-atlas.md (Dataflow frontier addendum) and
plans/2026-07-11-dataflow-research-program.md.

## Documentation story — strong core, two soft spots

STRONG (generated + self-defending): README/reference regen from RelDecl docs
(undocumented-builtins check); book + 9-lesson tutorial embedded in `dl docs`
with outputs captured from real runs; doc indexes + LANG-JUNCTION skill
GENERATED by dl from the tree with drift rails; magic-rels doc from
rel_catalog; query-output contract documented (batch 1).

SOFT SPOT 1 — CHANGELOG is hand-maintained and rots: the 2026-07-11 audit
found whole arcs missing (semi-naive, DL_CACHE_MB, engine split,
observability) until backfilled same day. Fix shape: a rail cross-checking
the commit window against CHANGELOG entries (exactly what the Sonnet audit
did manually).
<!-- todo(docs): CHANGELOG drift rail — commit window vs entries cross-check -->

SOFT SPOT 2 — book/tutorial lag the newest arcs: nothing teaches sym,
file_lines, --max-wall, code-todo convention, semi-naive semantics. Plus the
open friction item: per-language df coverage is invisible (no doc says TS
class methods emit zero df nodes — how it stayed a 3-time surprise). The
coverage table should be GENERATED (research program S-E's df-hole rail
produces it as a side effect).
<!-- todo(docs): book/tutorial refresh for sym/file_lines/--max-wall/semi-naive -->

## Dogfood coverage of language arms

| arm | state |
| --- | --- |
| scan/match/ast/sg/json, closure, aggregates, gen zones, comment_node, diag sinks, suppress directives, demand sinks, @async/@stream, every/clock, lattice | HOT — daily rails in .dl/ + shipped examples run them on this repo; the lattice hot-reload bug and the bom wedge were FOUND by dogfood (system working) |
| sym type | WARM — std/flow + df decls exercise it; no daily rail queries string/ref spine rels |
| hooks + hook_event + chat-marks + MCP serving | DARK since the 2026-07-11 outage — .claude/settings.json.disabled means zero real event traffic. Prereqs to re-enable are ALL merged (warning-severity rails, --max-wall, 35ms warm ticks). Chris's flip. |
| --move refactor | DARK — tests only, no routine use since the kernel demo |

<!-- todo(feature): dogfood coverage rail — scan in-tree .dl usage vs rel_catalog/op docs, warn on zero-usage builtins -->
<!-- todo(triage): re-enable hooks (timeout+advisory) — relights the dark event arm; Chris flips -->
