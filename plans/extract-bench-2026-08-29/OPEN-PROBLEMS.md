# sprefa-extract open problems (index, 2026-08-31)

Every open problem lives in a report section with numbers; this file is the
index. RULE for every future lane: closing or moving a problem updates this
file in the same PR; discovering one adds a row.

| # | problem | numbers | detail lives at | next action |
|---|---|---|---|---|
| 1 | rust type residual categories | re-measured 2026-08-31 at 8cb781d9d: 2,926 missing at 64.93/98.26 (recall 5,417/8,343 oracle type-rows, precision 5,417/5,513 emitted); 2,074 oracle convention or scope, 418 resolution, 224 impl-owner residual (E1 40, E1b 180, D2 4), 204 rejected on precision, 6 residual. Syntax leg alone on the same set: 59.25/92.31 | rust.REPORT.md sec 28-30 and sec 33, join script `rust.type_census.py` | see row 11 — the resolution block is one mechanism |
| 11 | rust type plane is blind to macro_rules-minted types | 255 of the 418 resolution-block rows name a dst with no top-level declaration (`TraitId` 25, `FunctionId` 23, `ImplId` 17, all `impl_intern!`); ceiling 3.06 pt. CALL plane measured 2026-08-31: 2,975 of the 13,764 CodeQL call misses (21.6%) are expansion-shaped (macro-minted caller 1,928, macro-arg site 450, unspelled operator sugar 324, unspelled macro callee 273) | rust.REPORT.md sec 30 (type), sec 31 (call census), sec 33.5 (the tier is at its ceiling here: `answer_of` needs a `DefIndex` site and no `TypeF` node exists for a macro-minted type) | MBE expansion exists for the CALL plane (sec 14); wiring the type plane is a design arc (span of a macro-minted decl, corpus-unique eligibility, cross-crate same-name) and needs the user; the call-plane block needs proc-macro/derive expansion, a different machine than MBE |
| 12 | rust checker tier answers too rarely on the call plane | CLOSED 2026-08-31 (PR pending, lane `fix-extract-rust-checker-wiring`). One loader defect, not four join legs: `CargoConfig::default()` left `sysroot` unset, so the crate graph had no core/std and rust-analyzer declined every method whose receiver type flows through one (172 of 259 method sites in one file; 2,591 of 84,627 after). `set_test` was the second field. recall 73.37 -> 84.28, precision 78.71 -> 83.64, missing 13,764 -> 8,126, contradicted 8,819 -> 7,379 | rust.REPORT.md sec 32, census `rust.call_census.py`, `tests/93_rust_checker_wiring.rs` | residual: 2,591 unanswered method sites unclassified; expansion block (M1/M3/S2/M2, 2,887 rows) needs a proc-macro server, a seam change |
| 2 | python dynamic shapes | aggregate recall 69.49% / precision 100.00% after the dynamic-shape tier (was 48.31/99.13); worst categories: dynamic 0%, builtins 25.00%, lambdas 35.71%, dicts 36.84%, generators 41.18%, lists 46.15% | py.REPORT.md (PyCG suite section) | written stops: cross-function container/param flow, call-result containers, computed keys, mutation, receiver dispatch — all need dataflow or checker tier |
| 3 | go agreed-missed residual | 351 rows: one-hop 155, multi-hop 139, alias 26, bare 12, qualified 11, promoted 8 | go.GAPS.md residual-six section | one-hop reassignment shape (x = y.M()) records no bind-plan row |
| 4 | ts madge module recall | 50.57% recall / 32.85% precision, never grinded | RATCHET.tsv ts5 module row | dependency-cruiser proposed as better oracle (PRIOR-ART.md sec 9) |
| 5 | rust checker RSS | 2.5 GB on ra corpus (foreign repos 330-934 MB) | rust.REPORT.md sec 24 | SHELVED by user 2026-08-31 ("whatevs for now") |
| 6 | corpus-scale name_resolve wrong-owner defect (go) | 13+58 wrong-owner bindings, corpus-scale dependent | GO-PARITY.REPORT.md sec 5a | folded into filed resolve defect; over 100-line bar |
| 7 | resolve is not a function of the input set | xargs chunking changed results (#600 found it; single-process forced) | rust.REPORT.md sec 27 header, PR #600 body | design-level: def index depends on file batch |
| 8 | bench protocol forks from prior-art study | BOTH LANDED (PR #619): `resolution_origin` is a closed enum on every `resolved_edge` / `resolved_type_edge`, the ratchet prints per-origin row counts per corpus, and each oracle row carries a matched / contradicted / unjudged split beside flat precision. The 4-column match protocol is unchanged and no RATCHET.tsv cell moved. | PRIOR-ART.md sec 3, `plans/extract-eval-2026-08-31/PLAN.md` sec 4, `src/types.rs` `ResolutionOrigin`, `tests/bench/mod.rs` `buckets` | closed; the open follow-up is which origins the held-out ratchet should floor separately |
| 9 | jelly oracle rejected | 35-37% agreement vs both established oracles | ts.REPORT.md jelly section, PR #603 | closed negative; keep tsv for reference |
| 10 | rust dyn-dispatch + fn-pointer kinds | rust-callgraph-benchmark: LLVM resolves dyn 0%, fn-ptr 0% (external finding) | PRIOR-ART.md sec 9 pass 1 | candidate micro-suite import like PyCG's |
| 13 | rust checker tier cost after the sysroot fix | wall 13,465 ms (was 10,653) and rss 3,756-4,434 MB (was 2,049) on rust-analyzer(873f); the sysroot crate graph lives in memory per run | rust.REPORT.md sec 32 | future lane: checker answer cache keyed by (blob, site) so the load pays once, or sysroot graph sharing across runs |
| 14 | rust checker type answers collapse on a (file, name) key | `type_ambiguous=459` on rust-analyzer(873f): 459 type names one file resolves two ways, each an answer the tier holds and hands to nobody. The CALL plane keys on the reference span and has no such collapse. Ceiling only: on a fixture where it fires the module plane binds both spellings anyway, so the recall a span key adds is unmeasured | rust.REPORT.md sec 33.3, counter at `rust_checker.rs:98`, pinned by `tests/94_rust_checker_types.rs:a_name_one_file_resolves_two_ways_falls_back_to_the_syntax_leg` | a reference span on `TypeEdgeCandidate` (`types.rs:342`), shared by the ts, go, kotlin, python and dl6 arms — a cross-lang arc, and its prize needs measuring before it is priced |
