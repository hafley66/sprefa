# rxprim duel verdict (overnight audit, 2026-08-04) — MORNING DECISION DOC

Auditor: opus, native, read all three legs + contract + rulings + COMPOSE 3-4,
verified ~30 file:line citations against the tree, RE-RAN opus's five swipl
probes at base (outputs identical). Legs: kimi a012519f, flash 20437fae,
opus ba4c344a, worktrees sprefa-plan-rxprim-{kimi,flash,opus} (kept for your
read; labs-die after your ruling).

## Verdict: fuse on the opus spine

Opus PLAN.md is the base (only leg whose claims survive re-execution, only one
with a real emitter address, decl spelling `one(Positions)` mirrors
keyed(Ref,Positions) and answers "first per WHAT" without deepening
keyed-vs-log). Its headline findings:

- The emitter landing is ONE gate: lower.pl:1560-1566 TriggerKind = arrival vs
  ordered_arrival decides arm-major (concat, today's wrong order) vs
  occurrence-major (merge). Widening it when a head carries one(_) or an arm
  negates an edge-headed ref closes one_pick_order for the new spelling AND
  every legacy negation-guard program, zero oracle change.
- 13 fixtures; F1-F4 are COMPOSE's race square; F9 pins arrival index as list
  POSITION vs the 1000/2000 stamps (the one place the doors could silently
  redefine "first" later).
- Reserved door: throttle(Positions) via the parse-time removed-word clause
  precedent. DISAGREES with the shelf's zip(tick).
- 7-row decl conflict table, every pairing a named refusal with a checker home.

## Grafts into the opus spine (audit-named)

1. kimi's `any (...) / one (...)` block sugar + desugar discipline
   (0_merge_expand.pl before analyze) IF the block ships; keep kimi's
   block-vs-match grammar-delta paragraph verbatim.
2. kimi's cascade counterfactual COUNT numbers (2 vs 6 arms = 15 edge_absence
   edges, 21 arm terms) as a second COUNT test.
3. kimi's argument that rewriting the keyed-attempt fixture is safe
   (key_same_tick_ordered_not_conflict, merge_family.pl:74-80).
4. flash's reconcile table (directive's four sketches dispositioned) as PLAN
   preamble.
5. flash's loud-loser requirement as open question: a dropped candidate is
   invisible in the delta model; visible drop channel vs tick-log-is-the-audit.

## Discards (audit-named, with reasons)

- kimi's edge_head_conflict_risk stand-down (reopens the source-arm-order
  tiebreak the ruling removes).
- kimi §5 + flash §3 typed merge (both assume enum-name column typing and
  tag-column exhaustiveness that measurably do not exist today; opus R3+S3
  replace them with four named checker edits).
- flash's COUNT-as-conformance-expectation (form the harness does not have).

## Defects noted

- opus: both doc files end with a leaked `</content>` artifact; strip before
  quoting. One unstated gap: check_no_edge_head_conflict_risk bails vacuously
  if any edge rule carries a level pre ref (analyze.pl:1334-1340); runtime
  sibling one_admission_tie covers it.
- flash: typed-merge contest key never defined; garbled token PLAN.md:43.
- kimi: four claims failed verification (ON CONFLICT DO NOTHING misread,
  keyed.set is refusal not last-write-wins, enum-in-column-type is refused
  today, match coverage checks head refs not tag columns).
- all three: INDEX.md -20 hunk from fresh-worktree pre-commit regen; drop on
  merge.

## YOUR TWO DECISIONS before anyone builds

1. Reserved door spelling: `throttle(1)` (opus, new word, parse-time
   reservation mechanism exists) vs `zip(tick)` (shelf sketch, rx-vocabulary
   law, no reservation mechanism named). One word to rule.
2. Does the `one (...)` / `any (...)` block sugar ship in the same wave as the
   decl property, or property first, block later?
