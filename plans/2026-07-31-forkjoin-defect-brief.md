# fork_join_malformed_json — brief (opus worktree)

Beta gate item 3 (plans/2026-07-30-v6-beta-plan.md). ARCH task
fork_join_malformed_json has the full statement: fixture
fork_join_error_arm_is_a_value (operators.pl) has a COMPLETE 2-tick oracle
log while the emitted module dies `SQLITE_ERROR: malformed JSON`. Compound
arrivals ok(body_one)/error(502) matched in a level body — the
SLOT-TERM-STRUCT family. It hides in the sweep's "2 pre-existing
run_error" bucket beside log_retraction_rejected, which is a GENUINE
rejection fixture (oracle throws too) — not the same class.

## Task

1. RCA first: where exactly does the compound arrival text meet json1?
   State the failing statement (batch errors carry statement text now).
2. Fix the smallest correct way; if the honest answer is a NAMED REFUSAL
   (encoding decision belongs to the struct_as_rows arc), implement that
   refusal in BOTH oracle-door analysis and compiler, with the reason
   pointing at the arc — a named refusal beats a SQL crash for beta.
   A real fix that makes the fixture identical both doors is better if it
   does not pre-empt struct_as_rows decisions.
3. Split the run_error bucket in sweep reporting: rejection-path fixtures
   (oracle also throws) vs emitted-module crashes, so nothing hides again.
4. Fail-first receipt whatever the outcome.

## Receipts

Conformance/plunit/text-door/sweep BOTH modes with counts; the fixture's
before/after bucket movement stated; no other fixture moves.

## Fences

- Touch: lower/emit/analyze as needed, sweep.ts bucket split, fixtures.
- Do NOT touch: 3_clock_check.pl + its tests, bench-cli/**,
  0_refusal_messages.pl (add refusal TERMS freely; rendering is generic),
  READINESS.md, labs/**.
- pnpm install per package, NEVER symlink outer node_modules.
- Commit per step `git commit -n`; no push.
