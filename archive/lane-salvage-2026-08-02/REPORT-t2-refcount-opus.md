(Coordinator note: agent harness blocked its own REPORT.md write; summary relayed from its final message.)

# support -> refCount rename sweep (opus lane)

- 25 files, +194/-193, zero generated files touched; all four byte-locked literals (__support_count, __support_next_<rel>, supportSql field, header comment) preserved; gen_emitted 0/196 changed after sweep regen.
- 49 jargon residuals all categorized (byte-golden locks, ruling atoms = user-decision record deliberately untouched, ARCH history rows, quoted lab finding).
- Findings: supportSql retirement = own arc (emitter + full golden regen); ledger claim "rust store identifiers" was WRONG (rust had only jargon prose; the queued identifiers are all TS); compile/out/manifest.json is non-reproducible across runs pre-existing (SWI var numbers in reason strings, proven symmetric); store G1 RSS flake seen once pre-existing; worktrees need pnpm install (dispatch template gap); cargo test must run from v6/sprefa-store cwd.
- Gates all green and identical to its own pre-edit baselines: sweep both modes 196/195/0 EXIT=0, conformance 281/0, plunit 271/271, TEXT_DOOR 196/196/0, tsv2 128/126/0/2skip, store js 74/74, rust store full matrix EXIT=0, three tsgo typechecks (the real cross-package proof), import gate, lint 1/1, dl 97/96/0/1skip.
