# cl-grph Lab Brief

Read the complete `common-lisp-logic` skill and its references. Own only this folder. Do not commit.

Upstream: `https://github.com/inconvergent/cl-grph`

Probe the immutable graph representation and Datalog query language with the shared cyclic graph. Record recursive-rule semantics, termination, indexes, fact-update model, graph algorithm coverage, serialization, and SBCL image behavior. Identify whether the query engine can accept compiler facts independently of the graph convenience API.

Write the required lab files and pin the upstream commit.
