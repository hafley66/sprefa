# logadat Lab Brief

Read the complete `common-lisp-logic` skill and its references. Own only this folder. Do not commit.

Upstream: `https://github.com/taarotman/logadat` (MIT, single file `logadat.lisp`, latest commit 2025-12-20, not in Quicklisp).

Install from source. Run the shared logic fixture through the library's public API. Trace the source implementing facts, rules, evaluation, and recursion. Probe cyclic transitive closure and its termination mechanism, duplicate answers, fact update and retraction, and standalone SBCL image creation.

Write the required lab files from the report contract. Pin the upstream commit. Keep downloaded source outside the final repository.
