# PAIProlog Lab Brief

Read the complete `common-lisp-logic` skill and its references. Own only this folder. Do not commit.

Primary upstream: `https://github.com/quek/paiprolog`

Identify its relationship to Peter Norvig's PAIP Prolog compiler and other copies. Run the shared fixture through the public compiled or interpreted API. Trace unification, clause compilation, backtracking, cut if present, occurs-check policy, cyclic recursion, update behavior, and SBCL image creation.

Write the required lab files. Pin the exact upstream commit and distinguish inherited PAIP behavior from repository changes.
