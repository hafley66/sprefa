# VivaceGraph Lab Brief

Read the complete `common-lisp-logic` skill and its references. Own only this folder. Do not commit.

Upstream: `https://github.com/kraison/vivace-graph`

Identify the current maintained branch or successor named by upstream. Probe its Prolog-like query layer with the shared graph using an isolated temporary database path. Record persistence, transaction behavior, indexes, recursion, tabling or visited-state behavior, update semantics, storage footprint, and standalone image behavior.

Do not leave database files outside this lab or the system temporary directory. Write the required lab files and pin the upstream commit.
