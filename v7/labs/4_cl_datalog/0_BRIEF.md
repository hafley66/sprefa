# cl-datalog Lab Brief

Read the complete `common-lisp-logic` skill and its references. Own only this folder. Do not commit.

Upstream: `https://github.com/thephoeron/cl-datalog`

Determine whether the project loads on SBCL 2.6.7 and whether it executes rules or primarily supplies syntax/data structures. Run the shared cyclic transitive-closure fixture when an evaluator exists. Identify bottom-up, top-down, seminaive, tabled, or another evaluation algorithm from source receipts.

Write the required lab files. Include exact load failures and missing dependencies when execution is blocked.
