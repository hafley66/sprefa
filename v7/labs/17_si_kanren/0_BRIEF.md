# si-kanren Lab Brief

Read the complete `common-lisp-logic` skill and its references. Own only this folder. Do not commit.

Upstream: `https://github.com/rgc69/si-kanren` (MIT, pure Common Lisp, latest commit 2025-12-30, Quicklisp `si-kanren` 2026-01-01).

Install from Quicklisp. Run the shared logic fixture through the library's public API. Trace the source implementing substitutions, unification, search, and the constraint store `cs = '(((s) . c) (d) (t) (a))`. Probe occurs-check policy, answer ordering, duplicate answers, cyclic transitive closure, disequality and `numbero`/`symbolo`/`absento` constraints on the fixture domain, and standalone SBCL image creation.

Write the required lab files from the report contract. Pin the upstream commit. Keep downloaded source outside the final repository.
