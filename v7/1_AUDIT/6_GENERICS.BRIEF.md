# Slice 6: generics, application, and user compiler rules

Read `0_SHARED.md` first.

Primary files:

- every file under `v6/prolog/0_generic_expand/`
- `v6/prolog/0_generic_expand.pl`
- generic and userland type-operator tests

Trace declaration, parameter ordering, `type_apply`, partial/full application,
interning or canonicalization, compiler-plane facts, type graph queries,
conformance, rewrite, and freeze boundaries. Identify the exact strata and
termination checks currently enforced.

Write `v7/1_AUDIT/results/6_GENERICS.md`.
