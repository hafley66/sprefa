[package]
name = "fixture-app"
version = "0.1.0"

[workspace]
members = ["crates/alpha", "crates/beta", "crates/gamma"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
tokio = { workspace = true }

[dev-dependencies]
insta = "1.30"
