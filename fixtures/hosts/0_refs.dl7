; One repository root; every ref it holds, then every ref that moves, is a row.
(git: (import "@std/git"))

(: Watch (* (: root str)))

(Watch "__ROOT__")

(: Head (* (: sha str)))

(<- (Head ?Sha)
    (Watch ?Root)
    (git.refs ?Root "HEAD" ?Sha))
