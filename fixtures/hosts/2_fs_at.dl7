; The tracked files of one repository at one revision.
(fs: (import "@std/fs"))

(: Pin
   (* (: root str)
      (: sha str)))

(Pin "__ROOT__" "__SHA__")

(: File
   (* (: path str)
      (: blob str)))

(<- (File ?Path ?Blob)
    (Pin ?Root ?Sha)
    (fs.at ?Root ?Sha ?Path ?Blob))
