name: CI

# v5 gates removed 2026-08-11 per user decree; v6 gates not yet wired in.

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Gate placeholder
        run: echo "v5 gates removed 2026-08-11 per user; v6 gates not yet wired into CI"
