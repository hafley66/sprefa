# sprf VS Code extension

Syntax highlighting + LSP client for `.sprf` files. Talks to the
`sprefa-lsp` binary built from `v3/crates/server`.

Install:

```bash
./install.sh
```

See `../../v3/README.md` for sprefa itself.
