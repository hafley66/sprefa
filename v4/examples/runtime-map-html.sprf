# Living runtime map.
#
# Scans source definitions that anchor the runtime calculus, writes an HTML
# diagram, and warns when an expected architecture node has no source cursor.
#
# Run from repo root:
#   cargo run --manifest-path v4/Cargo.toml --bin sprefa-run -- \
#     v4/examples/runtime-map-html.sprf --root . --show-rows

rule(:expected_runtime_nodes, NAME?);
rule(:runtime_specs, NAME?, FILE?, NOTE?);
rule(:source_type_defs, NAME?, FILE?, LO?, HI?);
rule(:runtime_nodes, NAME?, FILE?, LO?, HI?, NOTE?);
rule(:missing_runtime_nodes, NAME?);

`Cursor|v4/src/lib.rs|current cursor payload; migrating toward implicit & plus named terms
CursorValue|v4/src/lib.rs|typed payload handle for strings, byte ranges, blobs, and scalars
Term|v4/src/lib.rs|named compact term row: name, value string id, byte-range ref
QueueRow|v3/crates/effect_runtime/src/v2/queue.rs|runtime cursor row: parent, batch index, path, pipe hash, instance, depth, wake
PipeInstance|v3/crates/effect_runtime/src/v2/expand.rs|runtime pipe identity: ordered components plus pipe hash and instance id
RenderCtx|v3/crates/effect_runtime/src/v2/component.rs|dispatch context: pipe hash, depth, generation tick, event bus, diagnostics
Component|v3/crates/effect_runtime/src/v2/component.rs|operator boundary: render, render_batch, dispatch, barrier lifecycle
Node|v3/crates/effect_runtime/src/v2/node.rs|operator output algebra: emit, many, park, done`
  > split`
`
  > re`(?P<NAME>[^|]+)\|(?P<FILE>[^|]+)\|(?P<NOTE>.+)`
  > runtime_specs(NAME, FILE, NOTE)
  > expected_runtime_nodes(NAME);

`CursorTerm` > term_bind(:NAME) > expected_runtime_nodes(NAME);

`v4/src/lib.rs
v3/crates/effect_runtime/src/v2/queue.rs
v3/crates/effect_runtime/src/v2/expand.rs
v3/crates/effect_runtime/src/v2/component.rs
v3/crates/effect_runtime/src/v2/node.rs`
  > split(FILE?)`
`
  > path`${FILE}`
  > ast_yaml(:rs)`
      rule:
        any:
          - pattern: "pub struct ${NAME?} { $$$ }"
          - pattern: "pub struct ${NAME?}<$$$> { $$$ }"
          - pattern: "pub enum ${NAME?} { $$$ }"
          - pattern: "pub enum ${NAME?}<$$$> { $$$ }"
          - pattern: "pub trait ${NAME?} { $$$ }"
          - pattern: "pub trait ${NAME?}: $$$ { $$$ }"
    `
  > source_type_defs(NAME, FILE, LO, HI);

source_type_defs?(NAME?, FILE?, LO?, HI?)
  > runtime_specs?(NAME, FILE, NOTE?)
  > runtime_nodes(NAME, FILE, LO, HI, NOTE);

expected_runtime_nodes?(NAME?)
  > sql`
      SELECT input.__cursor_idx, input.NAME
      FROM input
      WHERE NOT EXISTS (
        SELECT 1
        FROM runtime_nodes
        WHERE runtime_nodes.NAME = ${NAME}
      )
    `
  > missing_runtime_nodes(NAME)
  > lsp_warn(:missing_runtime_doc)`runtime map expected source cursor is missing: ${NAME}`;

`v4/examples/runtime-map.html`
  > render.markdown`<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>sprefa runtime map</title>
  <style>
    body { font-family: ui-sans-serif, system-ui, sans-serif; margin: 32px; color: #1f2933; }
    h1 { font-size: 28px; margin: 0 0 8px; }
    p { max-width: 760px; line-height: 1.45; }
    .diagram { display: grid; gap: 12px; max-width: 980px; margin-top: 24px; }
    .node { border: 1px solid #c8d0d9; border-radius: 8px; padding: 14px 16px; background: #fbfcfd; }
    .node strong { display: block; font-size: 18px; margin-bottom: 4px; }
    .node a { color: #0f5c8c; text-decoration: none; }
    .note { color: #52616f; }
    .edge { color: #7b8794; font-family: ui-monospace, SFMono-Regular, Menlo, monospace; }
  </style>
</head>
<body>
  <h1>sprefa runtime map</h1>
  <p>This file is rendered by <code>v4/examples/runtime-map-html.sprf</code>. Missing source matches emit <code>lsp_warn(:missing_runtime_doc)</code>.</p>
  <div class="diagram">
${ runtime_nodes?(NAME?, FILE?, LO?, HI?, NOTE?)
  > render.markdown`    <section class="node">
      <strong><a href="../../${FILE}">${NAME}</a></strong>
      <div class="note">${NOTE}</div>
      <div class="edge">${FILE} byte ${LO}..${HI}</div>
    </section>
`
}
  </div>
</body>
</html>
`
  > write_file`v4/examples/runtime-map.html`;
