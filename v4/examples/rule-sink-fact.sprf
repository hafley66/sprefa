# Implemented syntax. Empty rule as replay table plus relation query.

rule(:words, WORD?);
rule(:echo_words, WORD?);

`alpha beta`
  > split(WORD?)` `
  > words.(WORD);

words(WORD?)
  > echo_words.(WORD);
