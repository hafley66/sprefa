[package]
name = "sprefa-extract"
version = "0.1.0"
edition = "2021"
description = "Language -> type/entity IR via tree-sitter. No compilers. Consumed by the reify macro op; never touches sprf."

[dependencies]
tree-sitter = "0.24"
tree-sitter-rust = "0.23"
