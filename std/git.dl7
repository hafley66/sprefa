; `@std/git`: refs and history of a repository root. Every rel here is served
; by an executor; a program that never serves one gets no rows.

(: refs
   (* (: root str)
      (: name str)
      (: sha str)))

(: refs_error
   (* (: root str)
      (: message str)))

(: history
   (* (: root str)
      (: sha str)
      (: parent str)))

(: history_error
   (* (: root str)
      (: message str)))
