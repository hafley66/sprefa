; expect: unresolved_name
; A dotted name is a path, not one atom: boop.lane reads as (. boop lane), and
; the walk stops at the first segment because no boop is bound.
(: boop.lane
   (* (: lane text)))

(boop.lane "sprefa-coordinator")
