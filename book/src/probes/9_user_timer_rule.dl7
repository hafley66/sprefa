; expect: rc=0
; A user timer with a str column, read by a rule; --serve timer binds the Rust timer to it.
(: timer
   (* (: label str)))

(: Want (* (: label str)))

(Want "mine")

(: Seen (* (: label str)))

(<- (Seen ?Label)
    (Want ?Label)
    (timer ?Label))
