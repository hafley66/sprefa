NO-COMMIT lane, worktree /Users/chrishafley/projects/sprefa-codex-cstnative.
Base check read-only: `git rev-parse HEAD` must be f76ef6f4025130c2b73664ee2a61d9d0f9811d37, else STOP into REPORT.md. Never run git merge/commit.

Read plans/2026-08-04-cst-native-syntax.md and build it: the `cst(path_var, digest_var, lang_atom) { <s-expr patterns> }` body item, parsed by v6/prolog/compile/parse_dl.pl into the structured query vocabulary that ALREADY EXISTS in conformance/fixtures/2_hosts_wiring.pl:200-233 (node/2, field/2, capture/2, group/2, quant/2, alternative/1, anonymous/1, predicate/3, capture_ref/1, wildcard, named_wildcard, string/1 -- reuse these functors EXACTLY), then serialized to the canonical tree-sitter query string and handed to the shipped ast/4 expansion (v6/prolog/0_ast_expand.pl) as if the string form had been written. Compile-time checks per the plan: cst_capture_unused / cst_variable_uncaptured refusals (follow the regexp/ast refusal pattern in 0_program_check.pl), and every #match?/#not-match? pattern goes through the existing regexp subset check. Round-trip test law: for each conformance fixture, parse(serialize(term)) == term.
Ownership: v6/prolog/** only. Gates: cd v6 && just conformance && just text-door && just plunit, outputs verbatim in REPORT-CSTNATIVE.md at worktree root (NOT REPORT.md; a root REPORT.md is stale history). Style: comment budget max 2 consecutive lines, banned words provenance/substrate/load-bearing/regime/support, descriptive dl variable names. Deliverable: dirty tree + REPORT-CSTNATIVE.md. If reality deviates from the plan or this prompt, STOP and record it.
