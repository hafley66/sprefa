# Lane: catalog duel entry

Worktree /Users/chrishafley/projects/sprefa-duel-catalog-fable, branch lab/duel-catalog-fable,
base 9557daf2. FIRST action: `git merge --ff-only 9557daf2` — failure =
STOP and write REPORT.md saying so.

Read CONTRACT.md at the worktree root; it is the whole spec and binds you.
Grounding docs referenced by the contract are at the worktree root too.

Rules: no commits; nothing written outside this worktree; never run the
full battery (green-all); deviations recorded in REPORT.md, never
improvised around; a permission denial ends that approach; no subagents.
Deliverable: REPORT.md with every gate's exact command and output.

Style: comments only for constraints code cannot show; no em dashes; never
the words provenance, substrate, load-bearing, regime; descriptive names.
