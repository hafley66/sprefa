# YOUR BRANCH: A, contribute-only

A dotted head CONTRIBUTES rules to a rel that the path's home block declares.
It does NOT create new paths from outside. A file declares a module's shape
before another file can add rules to it, which makes the module a real
interface with a declaration site.

Assume this decision went your way. Design it so it works.
