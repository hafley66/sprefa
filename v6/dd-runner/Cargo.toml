[package]
name = "dd-runner"
version = "0.1.0"
edition = "2021"

[workspace]

# bought: serde parses the compiler's JSON emission; rusqlite supplies SQLite.
[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
