// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: json_safe_integer_boundary_survives_both_doors.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lowerSql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multisetDiff } from "../runtime/diff.ts";
import { selectRows } from "../runtime/rows.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowValue,
  ISqlSeam,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demandRel: string; readonly responseRel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowValue[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowValue | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowValue[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly finalSelect: Record<string, string>; readonly hostPlans: readonly IHostPlanData[]; readonly bindPlans: readonly IBindPlanData[]; readonly queryPlans: readonly IQueryPlanData[]; readonly subscribedRels: readonly string[]; readonly relCatalog: readonly IRelCatalogRow[]; readonly unsupportedExecution: readonly string[] };

export const hostPlans: readonly IHostPlanData[] = [];
export const bindPlans: readonly IBindPlanData[] = [];
export const queryPlans: readonly IQueryPlanData[] = [];
export const subscribedRels: readonly string[] = [];
export const unsupportedExecution: readonly string[] = [];

function bindArgs(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => typeof value === "boolean" ? BigInt(value ? 1 : 0) : (typeof value === "number" && Number.isSafeInteger(value) ? BigInt(value) : value));
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wideIntegerWitness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[eE][+-]?\d+)?/g;

function wideIntegerInJsonText(value: IRowValue): boolean {
  if (typeof value !== "string") return wideIntegerWitness(value);
  const withoutStrings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of withoutStrings.match(JSON_NUMBER) ?? []) {
    if (/[.eE]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validateArrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = relColumnTypes[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = relDeclaredColumnTypes[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wideIntegerInJsonText(value)
        : type === "float" ? false
        : wideIntegerWitness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

const ddl: readonly string[] = [
  `CREATE TABLE "carried" ("name" TEXT NOT NULL, "value" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("name", "value")) WITHOUT ROWID`,
  `CREATE TABLE "measure" ("name" TEXT NOT NULL, "value" INTEGER NOT NULL, PRIMARY KEY ("name", "value")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__delta_carried" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_carried_sign" ON "__delta_carried" ("_sign")`,
  `CREATE INDEX "__delta_carried_group" ON "__delta_carried" ("name", "value")`,
  `CREATE TEMP TABLE "__frontier_carried" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_carried_phase" ON "__frontier_carried" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_carried" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_measure" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_measure_sign" ON "__delta_measure" ("_sign")`,
  `CREATE INDEX "__delta_measure_group" ON "__delta_measure" ("name", "value")`,
  `CREATE TEMP TABLE "__frontier_measure" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_measure_phase" ON "__frontier_measure" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_measure" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__support_next_carried" ("name" TEXT NOT NULL, "value" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("name", "value")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_carried" ("name" TEXT NOT NULL, "value" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "carried_zero" ON "carried" ("__refcount") WHERE "__refcount" <= 0`,
];

const relColumns: Record<string, readonly string[]> = {
  carried: ["name", "value"],
  measure: ["name", "value"],
};

const relColumnTypes: Record<string, readonly IRowColumnType[]> = {
  carried: ["text", "int"],
  measure: ["text", "int"],
};

const relCatalog: readonly IRelCatalogRow[] = [
  { relId: 1, parentId: 0, ordinal: 0, localName: "text", kind: "primitive", typeId: 0, arity: 0, moduleId: 0, hId: "", hSchema: "", hRule: "" },
  { relId: 2, parentId: 0, ordinal: 0, localName: "int", kind: "primitive", typeId: 0, arity: 0, moduleId: 0, hId: "", hSchema: "", hRule: "" },
  { relId: 3, parentId: 0, ordinal: 0, localName: "float", kind: "primitive", typeId: 0, arity: 0, moduleId: 0, hId: "", hSchema: "", hRule: "" },
  { relId: 4, parentId: 0, ordinal: 0, localName: "bool", kind: "primitive", typeId: 0, arity: 0, moduleId: 0, hId: "", hSchema: "", hRule: "" },
  { relId: 5, parentId: 0, ordinal: 0, localName: "json", kind: "primitive", typeId: 0, arity: 0, moduleId: 0, hId: "", hSchema: "", hRule: "" },
  { relId: 6, parentId: 0, ordinal: 0, localName: "json_safe_integer_boundary_survives_both_doors", kind: "module", typeId: 0, arity: 0, moduleId: 6, hId: "d402f97ee6de5eb2", hSchema: "", hRule: "" },
  { relId: 7, parentId: 6, ordinal: 0, localName: "carried", kind: "rel", typeId: 0, arity: 2, moduleId: 6, hId: "e70b5f886464b68b", hSchema: "9f4a4c9bfecfb7a1", hRule: "9b79169b084ad423" },
  { relId: 8, parentId: 7, ordinal: 1, localName: "name", kind: "column", typeId: 1, arity: 0, moduleId: 6, hId: "46aec3223a6b6723", hSchema: "", hRule: "" },
  { relId: 9, parentId: 7, ordinal: 2, localName: "value", kind: "column", typeId: 2, arity: 0, moduleId: 6, hId: "ff2a8fdc267bff4c", hSchema: "", hRule: "" },
  { relId: 10, parentId: 6, ordinal: 0, localName: "measure", kind: "rel", typeId: 0, arity: 2, moduleId: 6, hId: "37375b93861fdd9b", hSchema: "9f4a4c9bfecfb7a1", hRule: "" },
  { relId: 11, parentId: 10, ordinal: 1, localName: "name", kind: "column", typeId: 1, arity: 0, moduleId: 6, hId: "5ff4a099a4aefa03", hSchema: "", hRule: "" },
  { relId: 12, parentId: 10, ordinal: 2, localName: "value", kind: "column", typeId: 2, arity: 0, moduleId: 6, hId: "88bbc787d03e5c5b", hSchema: "", hRule: "" },
];

const relDeclaredColumnTypes: Record<string, readonly string[]> = {
  carried: ["text", "int"],
  measure: ["text", "int"],
};

const arrivalTargets: readonly string[] = ["measure"];

const boot: readonly IBootStatement[] = [
  { rel: "carried", sql: `DELETE FROM "carried"`, params: [] },
  { rel: "carried", sql: `INSERT OR IGNORE INTO "carried" ("name", "value") SELECT b0."name", b0."value" FROM "measure" b0`, params: [] },
];

type Snapshot = {
  readonly carried: readonly IRow[];
  readonly measure: readonly IRow[];
};

function readSnapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    carried: selectRows(seam, `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' AND json_type("name", '$.fn') = 'text' AND json_type("name", '$.args') = 'array' THEN json_extract("name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("name", '$.args')), '') || ')' ELSE "name" END AS "name", "value" FROM "carried"`, relColumns.carried!, relColumnTypes.carried!),
    measure: selectRows(seam, `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' AND json_type("name", '$.fn') = 'text' AND json_type("name", '$.args') = 'array' THEN json_extract("name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("name", '$.args')), '') || ')' ELSE "name" END AS "name", "value" FROM "measure"`, relColumns.measure!, relColumnTypes.measure!),
  });
}

const finalSelect: Record<string, string> = {
  carried: `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' AND json_type("name", '$.fn') = 'text' AND json_type("name", '$.args') = 'array' THEN json_extract("name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("name", '$.args')), '') || ')' ELSE "name" END AS "name", "value" FROM "carried"`,
  measure: `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' AND json_type("name", '$.fn') = 'text' AND json_type("name", '$.args') = 'array' THEN json_extract("name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("name", '$.args')), '') || ')' ELSE "name" END AS "name", "value" FROM "measure"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; addSql: string; delSql: string | null }> = {
  measure: { kind: "set", addSql: `INSERT OR IGNORE INTO "measure" ("name", "value") VALUES (?, ?)`, delSql: `DELETE FROM "measure" WHERE "name" = ? AND "value" = ?` },
};

function arrivalStatement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`json_safe_integer_boundary_survives_both_doors: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`json_safe_integer_boundary_survives_both_doors: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.delSql === null) {
      throw new Error(`json_safe_integer_boundary_survives_both_doors: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.delSql, args: bindArgs(arrival.row) };
  }
  return { sql: template.addSql, args: bindArgs(arrival.row) };
}

function applyArrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrivalStatement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "carried", kind: "set", tableName: "carried", deltaTableName: "__delta_carried", frontierTableName: "__frontier_carried", nextFrontierTableName: "__next_frontier_carried", columns: ["name", "value"], columnTypes: ["text", "int"], keyIndices: [], arrivalAddSql: null, arrivalDelSql: null, boundarySql: `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' AND json_type("name", '$.fn') = 'text' AND json_type("name", '$.args') = 'array' THEN json_extract("name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("name", '$.args')), '') || ')' ELSE "name" END AS "name", "value", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_carried" WHERE "_sign" IN (-1, 1) GROUP BY "name", "value", "_sign"`, ruleObservers: [] },
  { rel: "measure", kind: "set", tableName: "measure", deltaTableName: "__delta_measure", frontierTableName: "__frontier_measure", nextFrontierTableName: "__next_frontier_measure", columns: ["name", "value"], columnTypes: ["text", "int"], keyIndices: [], arrivalAddSql: `INSERT OR IGNORE INTO "measure" ("name", "value") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "name", "value"`, arrivalDelSql: `DELETE FROM "measure" WHERE ("name", "value") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "name", "value"`, boundarySql: `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' AND json_type("name", '$.fn') = 'text' AND json_type("name", '$.args') = 'array' THEN json_extract("name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("name", '$.args')), '') || ')' ELSE "name" END AS "name", "value", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_measure" WHERE "_sign" IN (-1, 1) GROUP BY "name", "value", "_sign"`, ruleObservers: ["carried/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { headRel: "carried", ruleId: "json_safe_integer_boundary_survives_both_doors:carried/2#1", headDeltaTableName: "__delta_carried", headColumns: ["name", "value"], insertSql: `INSERT OR IGNORE INTO "carried" ("name", "value") SELECT DISTINCT d0."name", d0."value" FROM "__frontier_measure" d0 WHERE d0."_phase" >= 0 RETURNING "name", "value"`, selectSql: `SELECT "name", "value" FROM "carried"`, recomputeSql: `DELETE FROM "carried";
INSERT OR IGNORE INTO "carried" ("name", "value") SELECT b0."name", b0."value" FROM "measure" b0`, supportSql: [`DELETE FROM "__support_next_carried"`, `INSERT INTO "__support_next_carried" ("name", "value", "__refcount") SELECT "name", "value", sum("__refcount") FROM (SELECT b0."name" AS "name", b0."value" AS "value", count(*) AS "__refcount" FROM "measure" b0 GROUP BY b0."name", b0."value") GROUP BY "name", "value"`, `UPDATE "carried" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_carried" n WHERE n."name" = h."name" AND n."value" = h."value"), 0)`, `INSERT INTO "__delta_carried" ("_sign", "_sequence", "name", "value") SELECT -1, row_number() OVER () - 1, "name", "value" FROM "carried" WHERE "__refcount" <= 0`, `DELETE FROM "carried" WHERE "__refcount" <= 0`, `DELETE FROM "__new_carried"`, `INSERT INTO "__new_carried" ("name", "value", "__refcount") SELECT n."name", n."value", n."__refcount" FROM "__support_next_carried" n LEFT JOIN "carried" h ON n."name" = h."name" AND n."value" = h."value" WHERE h."name" IS NULL`, `INSERT INTO "__delta_carried" ("_sign", "_sequence", "name", "value") SELECT 1, "rowid" - 1, "name", "value" FROM "__new_carried"`, `INSERT INTO "__frontier_carried" ("_phase", "_sequence", "name", "value") SELECT ?, "rowid" - 1, "name", "value" FROM "__new_carried"`, `INSERT INTO "__next_frontier_carried" ("_phase", "_sequence", "name", "value") SELECT ?, "rowid" - 1, "name", "value" FROM "__new_carried"`, `INSERT OR IGNORE INTO "carried" ("name", "value", "__refcount") SELECT n."name", n."value", n."__refcount" FROM "__support_next_carried" n`], expandSql: null, dredSql: null, aggregateSql: null },
];

function recomputeLevels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "carried";
INSERT OR IGNORE INTO "carried" ("name", "value") SELECT b0."name", b0."value" FROM "measure" b0`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function buildDeltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const carried = multisetDiff(before.carried, after.carried);
  const measure = multisetDiff(before.measure, after.measure);
  return {
    rels: [
      { rel: "carried", add: carried.add, del: carried.del },
      { rel: "measure", add: measure.add, del: measure.del },
    ],
    carryPending: false,
  };
}

function runNaiveTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return readSnapshot(seam).pipe(
    concatMap((before) => applyArrivals(seam, arrivals).pipe(map(() => before))),
    concatMap((before) => recomputeLevels(seam).pipe(map(() => before))),
    concatMap((before) => readSnapshot(seam).pipe(map((after) => buildDeltas(before, after)))),
  );
  // json_safe_integer_boundary_survives_both_doors: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = EMITTER_MODE;
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribedRels, arrivalTargets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribedRels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribedRels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribedRels, arrivalTargets);

function runIncrementalTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepareTick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.applyArrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyLevelsBeforeEdges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyEdges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recomputeLevelsAfterEdges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.readBoundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promoteFrontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carryPending): ITickDeltas => ({ rels, carryPending })),
    )),
  );
}

function runTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validateArrivals(arrivals);
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return runNaiveTick(seam, arrivals);
  }
  return runIncrementalTick(seam, arrivals);
}

export const incrementalPlan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcileEveryTick: RECONCILE_EVERY_TICK,
  retractionGuard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "json_safe_integer_boundary_survives_both_doors",
  ddl,
  relColumns,
  relColumnTypes,
  arrivalTargets,
  boot: SUBSCRIBED_BOOT,
  finalSelect,
  hostPlans,
  bindPlans,
  queryPlans,
  subscribedRels,
  relCatalog,
  unsupportedExecution,
  tick: runTick,
};
