// GENERATED by v6/prolog/compile/3_emit_trace_schema.pl from registry.pl.

/** One field of one DL_PERF_LOG record. `stability` says whether a second
 *  emitter (a rust runtime on `tracing`, say) has to reproduce the value:
 *  stable = yes, byte for byte; timing = a clock; host = target-specific text. */
export interface ITraceField {
  readonly key: string;
  readonly type: string;
  readonly stability: "stable" | "timing" | "host";
}

export interface ITraceEvent {
  readonly name: string;
  readonly fields: readonly ITraceField[];
}

export const TRACE_SCHEMA: readonly ITraceEvent[] = [
  { name: "tick_line", fields: [{ key: "tick", type: "int", stability: "stable" }, { key: "rels", type: "int", stability: "stable" }, { key: "rows", type: "int", stability: "stable" }, { key: "statements", type: "int", stability: "stable" }, { key: "wall_ms", type: "real", stability: "timing" }, { key: "rules", type: "list(rule)", stability: "stable" }, { key: "effects", type: "list(effect)", stability: "stable" }, { key: "binds", type: "list(bind)", stability: "stable" }, { key: "watches", type: "list(watch)", stability: "stable" }] },
  { name: "rule", fields: [{ key: "rule", type: "text", stability: "stable" }, { key: "rows", type: "int", stability: "stable" }, { key: "wall_ms", type: "real", stability: "timing" }] },
  { name: "effect", fields: [{ key: "host", type: "text", stability: "stable" }, { key: "witness_digest", type: "text", stability: "stable" }, { key: "outcome", type: "text", stability: "stable" }, { key: "rows", type: "int", stability: "stable" }, { key: "wall_ms", type: "real", stability: "timing" }, { key: "error", type: "text", stability: "host" }] },
  { name: "bind", fields: [{ key: "rel", type: "text", stability: "stable" }, { key: "period", type: "int", stability: "stable" }, { key: "bucket", type: "int", stability: "stable" }] },
  { name: "watch", fields: [{ key: "rel", type: "text", stability: "stable" }, { key: "glob", type: "text", stability: "stable" }, { key: "added", type: "int", stability: "stable" }, { key: "removed", type: "int", stability: "stable" }] },
];
