# G5 SCC result

Built `cascade::retract_scc` and `RelStore::retract_scc`. It first runs the
counting cascade, then stores the cut's forward cone in `cx_scc_scope`. The
nested PK-frontier fixpoint starts only at surviving parents outside that scope;
members omitted from the fixpoint are set to zero. `cx_scc_*` state is TEMP
SQLite state, not a Rust adjacency graph. `sqlite-count-scc` is a sixth
per-process engine. Agreement now checks it against `oracle_survivors` on 3 DAG
shapes and 15 cyclic shapes.

| shape | SCC result | firsthand read |
|---|---|---|
| CYC 60k s7 | 51,430, yes, 167.8 ms, 56 stmts, 0.10 Rust MB, 6.22 SQLite MB, 44.5 RSS MB | Matches the oracle; counting retained 52,384. |
| CYC 960k s7 | 815,240, yes, 2890.0 ms, 56 stmts, 0.16 Rust MB, 98.69 SQLite MB, 503.2 RSS MB | Matches the oracle. It is 7.1x counting's 408.7 ms and 1.22x DRed-loop's 2361.0 ms. |
| DAG 60k / 240k / 960k / 2.9M | 158.2 / 643.6 / 2690.1 / 8274.8 ms | SQLite high-water moves 6.20 / 24.39 / 97.82 / 293.12 MB and DB moves 3.76 / 15.21 / 61.46 / 184.47 MB. Rust is 0.12 / 0.12 / 0.12 / 0.17 MB because rows and frontier stay in SQLite. |

First CYC 960k fused run: 2867.9 ms, hash `25ee690520b18777`; second: 2815.3 ms,
same hash. The final full-driver row was 2890.0 ms, same survivor count and
oracle-verified hash. The repeated hashes falsify a timing-only result. The
increasing SQLite and DB measurements falsify interpreting the nearly flat Rust
column as no scale work. Fusion of nested-round batches reduced the harness count
from 74 to 56; the pre-fusion CYC 960k run was 3246.8 ms.

`cargo test --test agreement` passed. The default parallel `cargo test` run has
the pre-existing global statement-counter race in `temporal::tests` (190 rather
than 5 statements, then poisoned lock). The serialized run passed through
`head_to_head`; its later `kernel_roots` fixture cannot create
`.git/worktrees/main2` in this sandbox. No SCC child aborted under the 2048 MB
gun. The report was regenerated by the hermetic release driver.
