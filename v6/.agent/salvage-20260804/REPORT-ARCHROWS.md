# REPORT-ARCHROWS

## Rows landed

1. `task(regexp_builtin, done, [])`
2. `task(ast_op_wire, done, [])`
3. `task(cst_native_syntax, done, [])`
4. `task(comment_prod_expression, done, [])`
5. `task(comment_rail_prose_count, done, [])`
6. `task(graded_file_subprocess_fold, unbuilt, [])` (FILED as finding F6)

Appended after `task(text_door_fact_seam, ...)`.

## Gate output (verbatim)

```
PASS  sugar_grounds_out
PASS  species_are_four
PASS  graphs_refine_ast
PASS  roadmap_is_total
PASS  construct_status_closed
PASS  construct_tier_known
PASS  covers_endpoints_ground
```

## Deviations

None.
