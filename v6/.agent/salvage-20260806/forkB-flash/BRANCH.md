# YOUR BRANCH: B, create-on-write

A dotted head CREATES the path when it is absent. Any file can grow any module,
with no prior declaration, and multiple files contributing to one rel is
ordinary datalog union.

Assume this decision went your way. Design it so it works.
