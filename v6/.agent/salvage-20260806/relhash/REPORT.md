# LANE relhash REPORT — h4: module identity in `__rel`

Base commit: 86f20179 (branch lane/relhash)
Commit: 787deb9cb1d5587e7a242d0d9d5ce54364fa9f60

## Deviations

- `compile.pl` required no edit. `program_plan/2` already matches
  `fixture(Name, ...)` and puts `Name` first in `plan/7`; `lower_program/2`
  reads it and passes it to `catalog_row_ddl/4`. The module name is threaded
  end-to-end without a compile.pl change.
- tsv2 needed node_modules: `pnpm install` in `v6/tsv2` and in
  `v6/sprefa-store/js` (the tree ships without node_modules), plus the release
  extractor at `v6/sprefa-extract/target/release/extract` (built via
  `cargo build --release --features cli --bin extract`). Environment setup, not
  source edits. With the extractor present, tsv2 runs the crawl test and lands
  at 150 pass / 1 skip (the remaining skip is serveLeak, gated on SOAK_ENABLED).
- `sweep.sh` exits 1 on its final informational stage-4 refusal-reason diff vs
  HEAD: "HEAD manifest: duplicate fixture name
  enum_decl_variant_rows_round_trip_through_tag_view". Pre-existing: reproduced
  identically at base 86f20179 with my changes stashed. The RUN/FINAL receipt
  is unchanged: RUN total=422 identical=420 wrong=0 rejection=2, FINAL
  final_wrong=0 no_oracle_final=2. Not caused by this lane.
- `v6/prolog/compile/out/manifest.json` is regenerated by the mandatory
  conformance/text-door runs and differs from HEAD only in nondeterministic
  Prolog variable-number tokens inside refusal-reason strings (buckets and
  fixture names unchanged). Restored to HEAD; not committed.

The critical no-movement check holds: no conformance fixture and no `.dl6` view
names `__rel`, and `conformance/engine.pl` models no catalog rows. Conformance
is 306/0 and TEXT_DOOR is 422/422, both unchanged, so the change leaked into no
program that never uses the catalog.

## Commits

| # | sha | subject |
|---|---|---|
| 1 | 787deb9cb1d5587e7a242d0d9d5ce54364fa9f60 | catalog: module identity, one module row and an h_id per rel |

## What landed

- `module_hash/2` (SHA-256 of the module name, truncated to 16 hex) and
  `rel_h_id/4` (module hash + local name + arity) in `lower.pl`,
  `library(crypto)` added.
- `catalog_ddl_contract/2` gains `module_id-int`, `h_id-text` after `arity-int`
  (nine columns total).
- `catalog_row_ddl/4` takes ModuleName first; rel ids start at 7, module row is
  id 6, rels nest under it via `parent_id`, every row carries `module_id` and
  `h_id`.
- `analyze.pl` `catalog_mentions_atom/1` gates on arity 9.
- plunit: exact CREATE TABLE string, exact seed rows, one new test that two rels
  of the same local name in different modules get different `h_id`.
- catalogRows.test.ts: updated DDL string and rows, one query asserting exactly
  one non-empty `h_id` on `kind='module'`.

## Emitted module row (catalog_reader fixture)

```
(6,0,0,'catalog_reader','module',0,0,6,'52371c9ee530d976')
```

## Validation stdout

Rail verdicts: plunit 353/353, conformance 306/0, TEXT_DOOR 422/422,
prolog-lint findings=1 baseline=1, tsv2 150/1/0, sweep RUN/FINAL wrong=0.

### 1. plunit

```
% [106/353] catalog_g1:catalog_ids_are_positional ... passed (0.001 sec)
% [107/353] catalog_g1:catalo.._distinguishes_h_id .. passed (0.002 sec)
% [108/353] catalog_g1:refuse..ies_of_one_rel_name .. passed (0.000 sec)
% [353/353] fact_seeding:rege..int_column_compiles .. passed (0.003 sec)
```
353 / 353 passed (352 prior + 1 new `catalog_module_scope_distinguishes_h_id`).

### 2. conformance

```
PASS count: 306
FAIL count: 0
```
PASS  306 / 0 FAIL, unchanged.

### 3. TEXT_DOOR

```
TEXT_DOOR compiled=422 byte_identical=422 failures=0
```
422 / 422 byte-identical, unchanged.

### 4. prolog-lint

```
PROLOG_LINT findings=1 baseline=1 OK
```
findings=1 baseline=1, unchanged.

### 5. tsv2

```
ℹ tests 151
ℹ pass 150
ℹ fail 0
ℹ skipped 1
```
150 pass / 1 skip / 0 fail (includes the added module-row assertions).

### 6. sweep

```
RUN total=422 identical=420 wrong=0 emitted_crash=0 rejection=2 no_oracle_log=0
FINAL total=422 final_identical=420 final_wrong=0 no_oracle_final=2
```
final_wrong=0, unchanged.
