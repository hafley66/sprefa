# Brief: dl6 build, one Rust binary per program (card issues/dl6-build-single-binary, epic productionize-rust-door, size M) + ir_version (card issues/program-ir-version, size S)

Repo `~/projects/sprefa`. Base = origin/main sha the coordinator gives you (do not rebase). Worktree: `git -C ~/projects/sprefa worktree add ~/projects/sprefa-worktrees/<branch-dir> -b <branch> <sha>`; first action inside: `git log -1` = sha, tree clean; then `just boop-start` if it exists (it builds the extractor into the shared target; do not rebuild by hand). Never touch the main tree at `~/projects/sprefa`. Never `git stash`. Read `CLAUDE.md` standing laws and style laws first: no eprintln, banned words provenance/substrate/load-bearing/regime/refusal/honest/ground*, no em dashes, build-vs-buy paragraph before any hand-rolled infra, 10-second law, failure ledger, comment budget. Measure three times, never once. You MAY spawn ONE sonnet subagent for mechanical parts with disjoint files stated in its prompt; you review and test every line; no further fan-out. PR: `gh pr create --base main` with 1-3 plain sentences, `## Reading order`, `## Tests` (name, input, expectation, printed before); no words gate/leg/receipt/door/probe/refusal; do NOT merge; tick the AC you land on the card in your worktree and commit the card with the code.

Branch `feature/dl6-build-single-binary`. Runs in parallel with `feature/dl6c-saved-state`; until that lands, invoke the compiler the way `grade.sh:36` does, behind one function you can later point at `dl6c`.

## What exists
- `v6/prolog/emit_rust.pl:594-608` emits `<prog>.program.rs`: `pub const PROGRAM_JSON: &str = r#"..."#` + `pub fn program() -> ProgramJson`; `<prog>.types.rs` beside it; the runtime interprets the IR: `program.rs:49 GenProgram::from_json`, `driver.rs run_schedule`, `serve.rs` (UDS axum, `GET /rel/{name}`, `GET /rel/{name}/deltas`, `POST /arrive`). rusqlite `bundled`. No dyloading anywhere.
- Hand-done single binaries today: `src/bin/emit_rust_harness.rs` (reads the module text at runtime and extracts the JSON) and `tests/source_bind/_0_runtime.rs:63` (`include!`-style).
- Sidecar: `<prog>.adapters.json` loaded by `types.rs:631 load_program_host_adapter_rows` from `DL_ADAPTERS_DIR` or the repo-relative default.
- `incremental_safe: true` is a fossil (`emit_rust.pl:587`), no `ir_version` anywhere.

## Deliverables
1. `ir_version`: one integer constant in `emit_rust.pl` AND `emit_ts.pl` (same value, one test pins equality by grepping both), emitted into the program dict; `types.rs ProgramJson` gets `ir_version: u32` and `GenProgram::from_json` (or the bin's boot) refuses a mismatch with a named error; the TS runtime does the same at load (`v6/tsv2/runtime`, find where ProgramJson is parsed). Delete `incremental_safe` from emitter + both deserializers. Regenerate `compile/out/**` and `gen_emitted/**` (sweep) and confirm every fixture's diff is only those two keys. `docs/failure-modes.md` rail entry.
2. `dl6 build`: a subcommand of a new bin `v6/sprefa-engine-rs/src/bin/dl6.rs` (clap already a dep): `dl6 build <prog>.dl6 [--out <path>] [--adapters <file>]` = compile (rust target) -> write a cargo bin crate from a template to `target/dl6-build/<prog>/` (`Cargo.toml` with a path dep on `sprefa-engine-rs` by absolute path, `src/main.rs` = template) -> `cargo build --release` -> copy the binary to `--out`. Template is the harness minus arg parsing: `include!("program.rs")`, `include!("types.rs")`, embedded `adapters.json` via `include_str!` with `--adapters-dir` override; verbs on the built binary: `<prog> serve --socket <path>`, `<prog> run <schedule.json>` (tick log on stdout), `<prog> --version` (program name, compiler sha if available, ir_version).
3. Tests: `tests/dl6_build.rs` builds `golden-flex` and `resident-coroutine`, runs `run` on each with its `compile/out/<name>.schedule.json` and byte-diffs stdout against `emit_rust_harness` on the same input; `serve --socket` answers `GET /rel/<name>` for one rel; `--version` shows ir_version; wall time per step printed (cargo build exempt from the 10-second law, everything else not).
4. `just dl6-build prog=<path>` wrapper.

## Files owned
`v6/sprefa-engine-rs/src/bin/dl6.rs` (new), `v6/sprefa-engine-rs/src/build_template/**` (new), `v6/sprefa-engine-rs/src/types.rs` (ir_version + incremental_safe removal only), `v6/sprefa-engine-rs/src/program.rs` (same), `v6/sprefa-engine-rs/tests/dl6_build.rs`, `v6/prolog/emit_rust.pl` and `v6/prolog/emit_ts.pl` (ir_version + fossil only), the TS runtime file that parses ProgramJson (one check), `v6/prolog/compile/out/**`, `v6/tsv2/gen_emitted/**`, `justfile`, `docs/failure-modes.md`, the two cards. Do NOT touch `serve.rs` routes, `hosts.rs`, `parse_dl_dcg.pl`, `lower.pl`.

## Tests to run at the end
`cargo test -p sprefa-engine-rs`, `bash v6/sprefa-engine-rs/grade.sh` (ratchet: refresh `graded.tsv`, commit only newly clean rows), `cd v6/tsv2 && bash scripts/sweep.sh` (every fixture keeps its bucket; MANIFEST_REASON_DIFF empty), `cd v6/prolog/conformance && swipl -g go -t halt go.pl`, `swipl -g go -t halt v6/prolog/ARCH.pl`. Pre-commit needs `DL_EXTRACT_BIN=$PWD/v6/sprefa-extract/target/release/extract` and `pnpm install --frozen-lockfile` in `v6/tsv2` and `v6/sprefa-store/js` inside the worktree.
