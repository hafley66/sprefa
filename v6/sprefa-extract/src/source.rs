//! The uniform surface. Canonical definitions now live in `crate::types`; this
//! module is a re-export so `crate::source::*` import paths keep resolving.
pub use crate::types::{ExtractOutput, FamilyMask, Source};
