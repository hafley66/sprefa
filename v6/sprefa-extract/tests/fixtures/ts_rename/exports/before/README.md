# exports fixture

The Foo symbol exported by src/lib.ts is renamed across this fixture by the
tests in 4_rename_ts.rs; this file carries a prose mention of the name so the
rename verb's --text-refs report sees a carrier outside any source file.
