# prolog-lint baseline. Lines starting with # are reasons, not findings.
# `bash tools/prolog-lint.sh --write-baseline` rewrites findings only and drops
# these comments, so re-add them by hand after a refresh.
#
# ── private cross-module calls, 9 sites ──
# Rank R8 of plans/2026-07-29-prolog-org-review.md owns these. The eight PLUnit
# sites pin emitter and lowerer internals (incremental mode selection, the
# retraction guard, support SQL shape, the canonical column expression); the
# ghcacher one reaches the older src/ cluster's body walker. Each needs a
# public behavioral seam before the reach can go, not a deletion.
FINDING	private_cross_module_call	compile-(emit_ts:derived_edge_carry_required/3)-(plunit_incremental_mode-derived_edge_trigger_requires_incremental_carry_path)
FINDING	private_cross_module_call	compile-(emit_ts:derived_edge_carry_required/3)-(plunit_incremental_mode-edb_edge_trigger_keeps_naive_referee_available)
FINDING	private_cross_module_call	compile-(emit_ts:incremental_program_safe/4)-(plunit_incremental_mode-negative_level_body_uses_incremental_reconcile)
FINDING	private_cross_module_call	compile-(emit_ts:incremental_program_safe/4)-(plunit_incremental_mode-positive_edge_level_program_is_incremental)
FINDING	private_cross_module_call	compile-(emit_ts:reconcile_every_tick/2)-(plunit_incremental_mode-negative_level_body_uses_incremental_reconcile)
FINDING	private_cross_module_call	compile-(emit_ts:retraction_guard/2)-(plunit_incremental_mode-self_recursive_support_uses_recursive_cte_reseed)
FINDING	private_cross_module_call	compile-(lower:canonical_column_expr/2)-(plunit_sql_text_snapshots-canonical_column_expr_shape)
FINDING	private_cross_module_call	compile-(lower:level_support_sql/4)-(plunit_incremental_mode-self_recursive_support_uses_recursive_cte_reseed)
FINDING	private_cross_module_call	example-(checks:body_member/2)-(user-report/0)
#
# ── void declaration, 1 site, an artifact of how the gate composes clusters ──
# conformance/engine.pl declares `:- discontiguous user:fixture/5` for the
# fixture files the conformance runner loads alongside it. The compile cluster
# now loads engine.pl (the body-walk characterization test reaches the oracle's
# own traversals) but never loads a fixture file, so in THAT process the
# declaration covers zero clauses. It is not void in the conformance cluster,
# where it is exactly right. Removing it would break the conformance runner.
FINDING	void_declaration	compile-(user:fixture/5)-(discontiguous)
