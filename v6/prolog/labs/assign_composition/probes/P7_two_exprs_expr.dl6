rel division_input(label: text, numerator: int, denominator: int).
rel probe(label: text, quotient: int, remainder: int).

probe(Label, Numerator / Denominator, Numerator mod Denominator) <- division_input(Label, Numerator, Denominator).
