rel append_line(channel: text, piece: text) log keep(all).
rel log_text(channel: text, next: text) key(1).

log_text(Channel, concat([SoFar, Piece])) <+ append_line(Channel, Piece), pre(log_text(Channel, SoFar)).
