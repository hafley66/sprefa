rel bump(name: text, extra: int).
rel seen(name: text, base: int, tag: text).
rel over_budget(name: text, sum: int).

over_budget(Name, Base + Extra) <- seen(Name, Base, _), bump(Name, Extra), Base + Extra > 10.
