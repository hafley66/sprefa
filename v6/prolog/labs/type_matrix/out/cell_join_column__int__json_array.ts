// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: cell_join_column__int__json_array.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lowerSql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { multisetDiff } from "../runtime/diff.ts";
import { selectRows } from "../runtime/rows.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowValue,
  ISqlSeam,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demandRel: string; readonly responseRel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowValue[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly snapshot: "current" }

interface IBootStatement {
  sql: string;
  params: readonly IRowValue[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly finalSelect: Record<string, string>; readonly hostPlans: readonly IHostPlanData[]; readonly bindPlans: readonly IBindPlanData[]; readonly queryPlans: readonly IQueryPlanData[]; readonly unsupportedExecution: readonly string[] };

export const hostPlans: readonly IHostPlanData[] = [];
export const bindPlans: readonly IBindPlanData[] = [];
export const queryPlans: readonly IQueryPlanData[] = [];
export const unsupportedExecution: readonly string[] = [];

function bindArgs(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => typeof value === "boolean" ? BigInt(value ? 1 : 0) : (typeof value === "number" && Number.isInteger(value) ? BigInt(value) : value));
}

function validateArrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = relColumnTypes[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const row = arrival.row.map((value, index): IRowValue => {
      const type = types[index];
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`bool arrival ${arrival.rel}[${index}] requires true or false`);
        return value;
      }
      if (type === "float") {
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`float arrival ${arrival.rel}[${index}] requires a finite number`);
        return Object.is(value, -0) ? 0 : value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

const ddl: readonly string[] = [
  `CREATE TABLE "probe_left" ("value" INTEGER NOT NULL, "tag" TEXT NOT NULL, PRIMARY KEY ("value", "tag")) WITHOUT ROWID`,
  `CREATE TABLE "probe_out" ("value" INTEGER NOT NULL, "__support_count" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("value")) WITHOUT ROWID`,
  `CREATE TABLE "probe_right" ("value" INTEGER NOT NULL, "note" TEXT NOT NULL, PRIMARY KEY ("value", "note")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__delta_probe_left" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL, "tag" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_probe_left_sign" ON "__delta_probe_left" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_probe_left" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL, "tag" TEXT NOT NULL)`,
  `CREATE INDEX "__frontier_probe_left_phase" ON "__frontier_probe_left" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_probe_left" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL, "tag" TEXT NOT NULL)`,
  `CREATE INDEX "__next_frontier_probe_left_phase" ON "__next_frontier_probe_left" ("_phase")`,
  `CREATE TEMP TABLE "__delta_probe_out" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_probe_out_sign" ON "__delta_probe_out" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_probe_out" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_probe_out_phase" ON "__frontier_probe_out" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_probe_out" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__next_frontier_probe_out_phase" ON "__next_frontier_probe_out" ("_phase")`,
  `CREATE TEMP TABLE "__delta_probe_right" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL, "note" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_probe_right_sign" ON "__delta_probe_right" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_probe_right" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL, "note" TEXT NOT NULL)`,
  `CREATE INDEX "__frontier_probe_right_phase" ON "__frontier_probe_right" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_probe_right" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL, "note" TEXT NOT NULL)`,
  `CREATE INDEX "__next_frontier_probe_right_phase" ON "__next_frontier_probe_right" ("_phase")`,
  `CREATE TEMP TABLE "__support_next_probe_out" ("value" INTEGER NOT NULL, "__support_count" INTEGER NOT NULL, PRIMARY KEY ("value")) WITHOUT ROWID`,
];

const relColumns: Record<string, readonly string[]> = {
  probe_left: ["value", "tag"],
  probe_out: ["value"],
  probe_right: ["value", "note"],
};

const relColumnTypes: Record<string, readonly IRowColumnType[]> = {
  probe_left: ["int", "text"],
  probe_out: ["int"],
  probe_right: ["int", "text"],
};

const arrivalTargets: readonly string[] = ["probe_left", "probe_right"];

const boot: readonly IBootStatement[] = [
  { sql: `DELETE FROM "probe_out"`, params: [] },
  { sql: `INSERT OR IGNORE INTO "probe_out" ("value") SELECT b0."value" FROM "probe_left" b0, "probe_right" b1 WHERE b1."value" = b0."value"`, params: [] },
];

type Snapshot = {
  readonly probe_left: readonly IRow[];
  readonly probe_out: readonly IRow[];
  readonly probe_right: readonly IRow[];
};

function readSnapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    probe_left: selectRows(seam, `SELECT "value", CASE WHEN json_valid("tag") AND json_type("tag") = 'object' AND json_type("tag", '$.fn') = 'text' AND json_type("tag", '$.args') = 'array' THEN json_extract("tag", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("tag", '$.args')), '') || ')' ELSE "tag" END AS "tag" FROM "probe_left"`, relColumns.probe_left!, relColumnTypes.probe_left!),
    probe_out: selectRows(seam, `SELECT "value" FROM "probe_out"`, relColumns.probe_out!, relColumnTypes.probe_out!),
    probe_right: selectRows(seam, `SELECT "value", CASE WHEN json_valid("note") AND json_type("note") = 'object' AND json_type("note", '$.fn') = 'text' AND json_type("note", '$.args') = 'array' THEN json_extract("note", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("note", '$.args')), '') || ')' ELSE "note" END AS "note" FROM "probe_right"`, relColumns.probe_right!, relColumnTypes.probe_right!),
  });
}

const finalSelect: Record<string, string> = {
  probe_left: `SELECT "value", CASE WHEN json_valid("tag") AND json_type("tag") = 'object' AND json_type("tag", '$.fn') = 'text' AND json_type("tag", '$.args') = 'array' THEN json_extract("tag", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("tag", '$.args')), '') || ')' ELSE "tag" END AS "tag" FROM "probe_left"`,
  probe_out: `SELECT "value" FROM "probe_out"`,
  probe_right: `SELECT "value", CASE WHEN json_valid("note") AND json_type("note") = 'object' AND json_type("note", '$.fn') = 'text' AND json_type("note", '$.args') = 'array' THEN json_extract("note", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("note", '$.args')), '') || ')' ELSE "note" END AS "note" FROM "probe_right"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; addSql: string; delSql: string | null }> = {
  probe_left: { kind: "set", addSql: `INSERT OR IGNORE INTO "probe_left" ("value", "tag") VALUES (?, ?)`, delSql: `DELETE FROM "probe_left" WHERE "value" = ? AND "tag" = ?` },
  probe_right: { kind: "set", addSql: `INSERT OR IGNORE INTO "probe_right" ("value", "note") VALUES (?, ?)`, delSql: `DELETE FROM "probe_right" WHERE "value" = ? AND "note" = ?` },
};

function arrivalStatement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`cell_join_column__int__json_array: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`cell_join_column__int__json_array: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.delSql === null) {
      throw new Error(`cell_join_column__int__json_array: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.delSql, args: bindArgs(arrival.row) };
  }
  return { sql: template.addSql, args: bindArgs(arrival.row) };
}

function applyArrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrivalStatement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "probe_left", kind: "set", tableName: "probe_left", deltaTableName: "__delta_probe_left", frontierTableName: "__frontier_probe_left", nextFrontierTableName: "__next_frontier_probe_left", columns: ["value", "tag"], columnTypes: ["int", "text"], keyIndices: [], arrivalAddSql: `INSERT OR IGNORE INTO "probe_left" ("value", "tag") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "value", "tag"`, arrivalDelSql: `DELETE FROM "probe_left" WHERE ("value", "tag") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "value", "tag"`, boundarySql: `SELECT "value", CASE WHEN json_valid("tag") AND json_type("tag") = 'object' AND json_type("tag", '$.fn') = 'text' AND json_type("tag", '$.args') = 'array' THEN json_extract("tag", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("tag", '$.args')), '') || ')' ELSE "tag" END AS "tag", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_probe_left" WHERE "_sign" IN (-1, 1) GROUP BY "value", "tag", "_sign"` },
  { rel: "probe_out", kind: "set", tableName: "probe_out", deltaTableName: "__delta_probe_out", frontierTableName: "__frontier_probe_out", nextFrontierTableName: "__next_frontier_probe_out", columns: ["value"], columnTypes: ["int"], keyIndices: [], arrivalAddSql: null, arrivalDelSql: null, boundarySql: `SELECT "value", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_probe_out" WHERE "_sign" IN (-1, 1) GROUP BY "value", "_sign"` },
  { rel: "probe_right", kind: "set", tableName: "probe_right", deltaTableName: "__delta_probe_right", frontierTableName: "__frontier_probe_right", nextFrontierTableName: "__next_frontier_probe_right", columns: ["value", "note"], columnTypes: ["int", "text"], keyIndices: [], arrivalAddSql: `INSERT OR IGNORE INTO "probe_right" ("value", "note") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "value", "note"`, arrivalDelSql: `DELETE FROM "probe_right" WHERE ("value", "note") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "value", "note"`, boundarySql: `SELECT "value", CASE WHEN json_valid("note") AND json_type("note") = 'object' AND json_type("note", '$.fn') = 'text' AND json_type("note", '$.args') = 'array' THEN json_extract("note", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("note", '$.args')), '') || ')' ELSE "note" END AS "note", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_probe_right" WHERE "_sign" IN (-1, 1) GROUP BY "value", "note", "_sign"` },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { headRel: "probe_out", headDeltaTableName: "__delta_probe_out", headColumns: ["value"], insertSql: `INSERT OR IGNORE INTO "probe_out" ("value") SELECT DISTINCT d0."value" FROM "__frontier_probe_left" d0, "probe_right" b0 WHERE d0."_phase" >= 0 AND b0."value" = d0."value" UNION ALL SELECT DISTINCT d0."value" FROM "__frontier_probe_right" d0, "probe_left" b0 WHERE d0."_phase" >= 0 AND b0."value" = d0."value" RETURNING "value"`, selectSql: `SELECT "value" FROM "probe_out"`, recomputeSql: `DELETE FROM "probe_out";
INSERT OR IGNORE INTO "probe_out" ("value") SELECT b0."value" FROM "probe_left" b0, "probe_right" b1 WHERE b1."value" = b0."value"`, supportSql: [`DELETE FROM "__support_next_probe_out"`, `INSERT INTO "__support_next_probe_out" ("value", "__support_count") SELECT "value", sum("__support_count") FROM (SELECT b0."value" AS "value", count(*) AS "__support_count" FROM "probe_left" b0, "probe_right" b1 WHERE b1."value" = b0."value" GROUP BY b0."value") GROUP BY "value"`, `UPDATE "probe_out" AS h SET "__support_count" = "__support_count" - ("__support_count" - COALESCE((SELECT n."__support_count" FROM "__support_next_probe_out" n WHERE n."value" = h."value"), 0))`, `DELETE FROM "probe_out" WHERE "__support_count" <= 0 RETURNING "value"`, `INSERT INTO "probe_out" ("value", "__support_count") SELECT "value", n."__support_count" FROM "__support_next_probe_out" n WHERE NOT EXISTS (SELECT 1 FROM "probe_out" h WHERE n."value" = h."value") RETURNING "value"`], aggregateSql: null },
];

function recomputeLevels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "probe_out";
INSERT OR IGNORE INTO "probe_out" ("value") SELECT b0."value" FROM "probe_left" b0, "probe_right" b1 WHERE b1."value" = b0."value"`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function buildDeltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const probe_left = multisetDiff(before.probe_left, after.probe_left);
  const probe_out = multisetDiff(before.probe_out, after.probe_out);
  const probe_right = multisetDiff(before.probe_right, after.probe_right);
  return {
    rels: [
      { rel: "probe_left", add: probe_left.add, del: probe_left.del },
      { rel: "probe_out", add: probe_out.add, del: probe_out.del },
      { rel: "probe_right", add: probe_right.add, del: probe_right.del },
    ],
    carryPending: false,
  };
}

function runNaiveTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return readSnapshot(seam).pipe(
    concatMap((before) => applyArrivals(seam, arrivals).pipe(map(() => before))),
    concatMap((before) => recomputeLevels(seam).pipe(map(() => before))),
    concatMap((before) => readSnapshot(seam).pipe(map((after) => buildDeltas(before, after)))),
  );
  // cell_join_column__int__json_array: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

function runIncrementalTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepareTick(seam, INCREMENTAL_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.applyArrivals(seam, arrivals, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyLevelsBeforeEdges(seam, INCREMENTAL_LEVEL_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyEdges(seam, INCREMENTAL_EDGE_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recomputeLevelsAfterEdges(seam, INCREMENTAL_LEVEL_STATEMENTS, INCREMENTAL_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.readBoundary(seam, INCREMENTAL_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promoteFrontiers(seam, INCREMENTAL_RELATIONS).pipe(
      map((carryPending): ITickDeltas => ({ rels, carryPending })),
    )),
  );
}

function runTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validateArrivals(arrivals);
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return runNaiveTick(seam, arrivals);
  }
  return runIncrementalTick(seam, arrivals);
}

export const incrementalPlan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcileEveryTick: RECONCILE_EVERY_TICK,
  retractionGuard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "cell_join_column__int__json_array",
  ddl,
  relColumns,
  relColumnTypes,
  arrivalTargets,
  boot,
  finalSelect,
  hostPlans,
  bindPlans,
  queryPlans,
  unsupportedExecution,
  tick: runTick,
};
