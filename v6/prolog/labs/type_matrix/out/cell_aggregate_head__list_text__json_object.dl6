# type-matrix cell: position=aggregate_head declared=list_text value=json_object
# GENERATED by gen_cells.mjs; the lab dies on landing, do not hand-edit.

rel probe_in(group_id: int, value: json).
rel probe_out(group_id: int, value: list(text)).

probe_out(group_id, min(value)) <- probe_in(group_id, value).
