// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: departed_fires_next_tick_on_retraction.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "closed_at": [true, false],
    "mirror": [true],
    "source_row": [true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "closed_at" ("item" INTEGER NOT NULL, "tick" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt_closed_at" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."item") AS "item", t."tick" AS "tick" FROM "closed_at" t`,
  `CREATE TABLE "mirror" ("__id" INTEGER PRIMARY KEY, "item" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("item"))`,
  `CREATE TEMP VIEW "__txt_mirror" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."item") AS "item", t."__refcount" AS "__refcount" FROM "mirror" t`,
  `CREATE TABLE "source_row" ("__id" INTEGER PRIMARY KEY, "item" INTEGER NOT NULL, UNIQUE ("item"))`,
  `CREATE TEMP VIEW "__txt_source_row" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."item") AS "item" FROM "source_row" t`,
  `CREATE TEMP TABLE "__delta_closed_at" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL, "tick" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_closed_at_sign" ON "__delta_closed_at" ("_sign")`,
  `CREATE INDEX "__delta_closed_at_group" ON "__delta_closed_at" ("item", "tick")`,
  `CREATE TEMP TABLE "__frontier_closed_at" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL, "tick" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_closed_at_phase" ON "__frontier_closed_at" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_closed_at" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL, "tick" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_closed_at" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."item") AS "item", t."tick" AS "tick", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_closed_at" t`,
  `CREATE TEMP TABLE "__delta_mirror" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_mirror_sign" ON "__delta_mirror" ("_sign")`,
  `CREATE INDEX "__delta_mirror_group" ON "__delta_mirror" ("item")`,
  `CREATE TEMP TABLE "__frontier_mirror" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_mirror_phase" ON "__frontier_mirror" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_mirror" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_mirror" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."item") AS "item", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_mirror" t`,
  `CREATE TEMP TABLE "__departure_frontier_mirror" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" TEXT NOT NULL)`,
  `CREATE TEMP TABLE "__delta_source_row" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_source_row_sign" ON "__delta_source_row" ("_sign")`,
  `CREATE INDEX "__delta_source_row_group" ON "__delta_source_row" ("item")`,
  `CREATE TEMP TABLE "__frontier_source_row" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_source_row_phase" ON "__frontier_source_row" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_source_row" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_source_row" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."item") AS "item", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_source_row" t`,
  `CREATE TEMP TABLE "__support_next_mirror" ("item" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("item")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_mirror" ("item" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "mirror_zero" ON "mirror" ("__refcount") WHERE "__refcount" <= 0`,
  `CREATE TABLE "__tick" ("n" INTEGER NOT NULL)`,
  `INSERT INTO "__tick" ("n") SELECT 0 WHERE NOT EXISTS (SELECT 1 FROM "__tick")`,
];

const rel_columns: Record<string, readonly string[]> = {
  closed_at: ["item", "tick"],
  mirror: ["item"],
  source_row: ["item"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  closed_at: ["text", "int"],
  mirror: ["text"],
  source_row: ["text"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  closed_at: ["text", "int"],
  mirror: ["text"],
  source_row: ["text"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "departed_fires_next_tick_on_retraction", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "df1d95666548fc07", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "closed_at", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "3d8633a9155cc167", h_schema: "eeea42965b73609a", h_rule: "d7e7abbb49fcc224" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "item", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "dde43720d48ab42c", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 8, ordinal: 2, local_name: "tick", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "374c3966e8547e2d", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 7, ordinal: 0, local_name: "mirror", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "f6936328ad00b7a8", h_schema: "4f9957ccb96234b6", h_rule: "c8d7419b9f55c92e" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "item", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "5d82bd731b454fba", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 7, ordinal: 0, local_name: "source_row", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "890b9da0578850f7", h_schema: "4f9957ccb96234b6", h_rule: "" },
  { rel_id: 14, parent_id: 13, ordinal: 1, local_name: "item", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "e14198a0dd78942b", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 8, ordinal: 0, local_name: "__delta_closed_at", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "5cda3c02982aa513", h_schema: "440659c9e75fd496", h_rule: "" },
  { rel_id: 16, parent_id: 8, ordinal: 0, local_name: "__frontier_closed_at", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "1a6b3427d8000024", h_schema: "0f9f3ad354d3d66d", h_rule: "" },
  { rel_id: 17, parent_id: 8, ordinal: 0, local_name: "__next_frontier_closed_at", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "35de415397ca4e28", h_schema: "0f9f3ad354d3d66d", h_rule: "" },
  { rel_id: 18, parent_id: 8, ordinal: 0, local_name: "__txt_closed_at", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "a81513160c8fc259", h_schema: "eeea42965b73609a", h_rule: "" },
  { rel_id: 19, parent_id: 15, ordinal: 0, local_name: "__txt___delta_closed_at", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "a4b7b4bee2879f44", h_schema: "eeea42965b73609a", h_rule: "" },
  { rel_id: 20, parent_id: 11, ordinal: 0, local_name: "__delta_mirror", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "155787e818004bfd", h_schema: "15e84563064d7c4c", h_rule: "" },
  { rel_id: 21, parent_id: 11, ordinal: 0, local_name: "__frontier_mirror", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "eb871fbff2c9e14f", h_schema: "c35a652f9e4a4d00", h_rule: "" },
  { rel_id: 22, parent_id: 11, ordinal: 0, local_name: "__next_frontier_mirror", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "4c7e0b71c8794205", h_schema: "c35a652f9e4a4d00", h_rule: "" },
  { rel_id: 23, parent_id: 11, ordinal: 0, local_name: "__departure_frontier_mirror", kind: "departure", type_id: 0, arity: 3, module_id: 7, h_id: "26510f6ea78aaac1", h_schema: "c35a652f9e4a4d00", h_rule: "" },
  { rel_id: 24, parent_id: 11, ordinal: 0, local_name: "__txt_mirror", kind: "view", type_id: 0, arity: 1, module_id: 7, h_id: "eb6e9dbdaf11a490", h_schema: "4f9957ccb96234b6", h_rule: "" },
  { rel_id: 25, parent_id: 20, ordinal: 0, local_name: "__txt___delta_mirror", kind: "view", type_id: 0, arity: 3, module_id: 7, h_id: "1405e13bcf7e4726", h_schema: "4f9957ccb96234b6", h_rule: "" },
  { rel_id: 26, parent_id: 13, ordinal: 0, local_name: "__delta_source_row", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "4005097d007d8c89", h_schema: "15e84563064d7c4c", h_rule: "" },
  { rel_id: 27, parent_id: 13, ordinal: 0, local_name: "__frontier_source_row", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "a55c46fe48f1a201", h_schema: "c35a652f9e4a4d00", h_rule: "" },
  { rel_id: 28, parent_id: 13, ordinal: 0, local_name: "__next_frontier_source_row", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "8f53cdce4f264085", h_schema: "c35a652f9e4a4d00", h_rule: "" },
  { rel_id: 29, parent_id: 13, ordinal: 0, local_name: "__txt_source_row", kind: "view", type_id: 0, arity: 1, module_id: 7, h_id: "ff3ef38e4dad24c5", h_schema: "4f9957ccb96234b6", h_rule: "" },
  { rel_id: 30, parent_id: 26, ordinal: 0, local_name: "__txt___delta_source_row", kind: "view", type_id: 0, arity: 3, module_id: 7, h_id: "b8a5c47452078db0", h_schema: "4f9957ccb96234b6", h_rule: "" },
  { rel_id: 31, parent_id: 7, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 7, h_id: "1f891389047eba2b", h_schema: "", h_rule: "" },
  { rel_id: 32, parent_id: 11, ordinal: 0, local_name: "__support_next_mirror", kind: "refcount", type_id: 0, arity: 2, module_id: 7, h_id: "f8934029cc3e7ff2", h_schema: "", h_rule: "" },
  { rel_id: 33, parent_id: 11, ordinal: 0, local_name: "__new_mirror", kind: "refcount_staging", type_id: 0, arity: 2, module_id: 7, h_id: "87e444af14bbb021", h_schema: "", h_rule: "" },
  { rel_id: 34, parent_id: 9, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "fb03e50aa7a42222", h_schema: "", h_rule: "" },
  { rel_id: 35, parent_id: 10, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "dea7ca5066f8c8ee", h_schema: "", h_rule: "" },
  { rel_id: 36, parent_id: 12, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "23391b4fcf9b5b23", h_schema: "", h_rule: "" },
  { rel_id: 37, parent_id: 14, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "db3ffdb86b08aebd", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["source_row"];

const boot: readonly IBootStatement[] = [
  { rel: "mirror", sql: `DELETE FROM "mirror"`, params: [] },
  { rel: "mirror", sql: `INSERT OR IGNORE INTO "mirror" ("item") SELECT b0."item" FROM "source_row" b0`, params: [] },
];

const final_select: Record<string, string> = {
  closed_at: `SELECT CASE WHEN json_valid(t."item") AND json_type(t."item") = 'object' AND json_type(t."item", '$.fn') = 'text' AND json_type(t."item", '$.args') = 'array' THEN json_extract(t."item", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."item", '$.args')), '') || ')' ELSE t."item" END AS "item", t."tick" FROM "__txt_closed_at" t`,
  mirror: `SELECT CASE WHEN json_valid(t."item") AND json_type(t."item") = 'object' AND json_type(t."item", '$.fn') = 'text' AND json_type(t."item", '$.args') = 'array' THEN json_extract(t."item", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."item", '$.args')), '') || ')' ELSE t."item" END AS "item" FROM "__txt_mirror" t`,
  source_row: `SELECT CASE WHEN json_valid(t."item") AND json_type(t."item") = 'object' AND json_type(t."item", '$.fn') = 'text' AND json_type(t."item", '$.args') = 'array' THEN json_extract(t."item", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."item", '$.args')), '') || ')' ELSE t."item" END AS "item" FROM "__txt_source_row" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "closed_at", kind: "log", table_name: "closed_at", delta_table_name: "__delta_closed_at", frontier_table_name: "__frontier_closed_at", next_frontier_table_name: "__next_frontier_closed_at", columns: ["item", "tick"], column_types: ["text", "int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."item") AND json_type(t."item") = 'object' AND json_type(t."item", '$.fn') = 'text' AND json_type(t."item", '$.args') = 'array' THEN json_extract(t."item", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."item", '$.args')), '') || ')' ELSE t."item" END AS "item", t."tick", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_closed_at" t WHERE t."_sign" IN (-1, 1) GROUP BY t."item", t."tick", t."_sign"`, rule_observers: [] },
  { rel: "mirror", kind: "set", table_name: "mirror", delta_table_name: "__delta_mirror", frontier_table_name: "__frontier_mirror", next_frontier_table_name: "__next_frontier_mirror", columns: ["item"], column_types: ["text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."item") AND json_type(t."item") = 'object' AND json_type(t."item", '$.fn') = 'text' AND json_type(t."item", '$.args') = 'array' THEN json_extract(t."item", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."item", '$.args')), '') || ')' ELSE t."item" END AS "item", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_mirror" t WHERE t."_sign" IN (-1, 1) GROUP BY t."item", t."_sign"`, departure_frontier_table_name: "__departure_frontier_mirror", rule_observers: ["closed_at/2"] },
  { rel: "source_row", kind: "set", table_name: "source_row", delta_table_name: "__delta_source_row", frontier_table_name: "__frontier_source_row", next_frontier_table_name: "__next_frontier_source_row", columns: ["item"], column_types: ["text"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "source_row" ("item") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "item"`, arrival_del_sql: `DELETE FROM "source_row" WHERE ("item") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "item"`, boundary_sql: `SELECT CASE WHEN json_valid(t."item") AND json_type(t."item") = 'object' AND json_type(t."item", '$.fn') = 'text' AND json_type(t."item", '$.args') = 'array' THEN json_extract(t."item", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."item", '$.args')), '') || ')' ELSE t."item" END AS "item", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_source_row" t WHERE t."_sign" IN (-1, 1) GROUP BY t."item", t."_sign"`, rule_observers: ["mirror/1"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
  { head_rel: "closed_at", rule_id: "departed_fires_next_tick_on_retraction:closed_at/2#1", head_kind: "log", head_table_name: "closed_at", head_delta_table_name: "__delta_closed_at", head_columns: ["item", "tick"], key_indices: [], project_sql: `SELECT (SELECT s."__id" FROM "__str" s WHERE s."content" = d0."item") AS "item", (SELECT "n" FROM "__tick") AS "tick" FROM "__departure_frontier_mirror" d0 WHERE d0."_phase" >= 0 ORDER BY d0."_phase", d0."_sequence"`, intern_sql: [`INSERT OR IGNORE INTO "__str" ("content") SELECT DISTINCT d0."item" FROM "__departure_frontier_mirror" d0 WHERE d0."_phase" >= 0`] },
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "mirror", rule_id: "departed_fires_next_tick_on_retraction:mirror/1#1", head_delta_table_name: "__delta_mirror", head_columns: ["item"], insert_sql: `INSERT OR IGNORE INTO "mirror" ("item") SELECT DISTINCT d0."item" FROM "__frontier_source_row" d0 WHERE d0."_phase" >= 0 RETURNING "item"`, select_sql: `SELECT "item" FROM "mirror"`, recompute_sql: `DELETE FROM "mirror";
INSERT OR IGNORE INTO "mirror" ("item") SELECT b0."item" FROM "source_row" b0`, support_sql: [`DELETE FROM "__support_next_mirror"`, `INSERT INTO "__support_next_mirror" ("item", "__refcount") SELECT "item", sum("__refcount") FROM (SELECT b0."item" AS "item", count(*) AS "__refcount" FROM "source_row" b0 GROUP BY b0."item") GROUP BY "item"`, `UPDATE "mirror" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_mirror" n WHERE n."item" = h."item"), 0)`, `INSERT INTO "__delta_mirror" ("_sign", "_sequence", "item") SELECT -1, row_number() OVER () - 1, "item" FROM "mirror" WHERE "__refcount" <= 0`, `DELETE FROM "mirror" WHERE "__refcount" <= 0`, `DELETE FROM "__new_mirror"`, `INSERT INTO "__new_mirror" ("item", "__refcount") SELECT n."item", n."__refcount" FROM "__support_next_mirror" n LEFT JOIN "mirror" h ON n."item" = h."item" WHERE h."item" IS NULL`, `INSERT INTO "__delta_mirror" ("_sign", "_sequence", "item") SELECT 1, "rowid" - 1, "item" FROM "__new_mirror"`, `INSERT INTO "__frontier_mirror" ("_phase", "_sequence", "item") SELECT ?, "rowid" - 1, "item" FROM "__new_mirror"`, `INSERT INTO "__next_frontier_mirror" ("_phase", "_sequence", "item") SELECT ?, "rowid" - 1, "item" FROM "__new_mirror"`, `INSERT OR IGNORE INTO "mirror" ("item", "__refcount") SELECT n."item", n."__refcount" FROM "__support_next_mirror" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

function advance_tick(seam: ISqlSeam): Observable<void> {
  return seam.runner.execute(seam.db, `UPDATE "__tick" SET "n" = "n" + 1`).pipe(map(() => undefined));
}

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => advance_tick(seam)),
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.recompute_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK, arrivals)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.merge_next_into_current(seam, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.stage_departures(seam, SUBSCRIBED_RELATIONS, rels).pipe(map(() => rels))),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "departed_fires_next_tick_on_retraction",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
