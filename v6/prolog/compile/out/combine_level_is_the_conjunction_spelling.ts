// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: combine_level_is_the_conjunction_spelling.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowValue,
  ISqlSeam,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowValue[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowValue | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowValue[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => typeof value === "boolean" ? BigInt(value ? 1 : 0) : (typeof value === "number" && Number.isSafeInteger(value) ? BigInt(value) : value));
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

const ddl: readonly string[] = [
  `CREATE TABLE "pair" ("left" INTEGER NOT NULL, "right" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("left", "right")) WITHOUT ROWID`,
  `CREATE TABLE "source_a" ("left" INTEGER NOT NULL, PRIMARY KEY ("left")) WITHOUT ROWID`,
  `CREATE TABLE "source_b" ("right" INTEGER NOT NULL, PRIMARY KEY ("right")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__delta_pair" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_pair_sign" ON "__delta_pair" ("_sign")`,
  `CREATE INDEX "__delta_pair_group" ON "__delta_pair" ("left", "right")`,
  `CREATE TEMP TABLE "__frontier_pair" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_pair_phase" ON "__frontier_pair" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_pair" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_source_a" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_source_a_sign" ON "__delta_source_a" ("_sign")`,
  `CREATE INDEX "__delta_source_a_group" ON "__delta_source_a" ("left")`,
  `CREATE TEMP TABLE "__frontier_source_a" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_source_a_phase" ON "__frontier_source_a" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_source_a" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_source_b" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_source_b_sign" ON "__delta_source_b" ("_sign")`,
  `CREATE INDEX "__delta_source_b_group" ON "__delta_source_b" ("right")`,
  `CREATE TEMP TABLE "__frontier_source_b" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_source_b_phase" ON "__frontier_source_b" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_source_b" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__support_next_pair" ("left" INTEGER NOT NULL, "right" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("left", "right")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_pair" ("left" INTEGER NOT NULL, "right" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "pair_zero" ON "pair" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  pair: ["left", "right"],
  source_a: ["left"],
  source_b: ["right"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  pair: ["int", "int"],
  source_a: ["int"],
  source_b: ["int"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "combine_level_is_the_conjunction_spelling", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "7d47c0d730bb7cab", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "pair", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "ee8ebbca39c6ee4c", h_schema: "a3c2241561bccbe3", h_rule: "c44d03be214326ac" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "left", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "de3c2e99dd878542", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 7, ordinal: 2, local_name: "right", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "e9811d14f01a2a85", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 6, ordinal: 0, local_name: "source_a", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "f907d82bb4b86077", h_schema: "8e75f4ab37008087", h_rule: "" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "left", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "151c6ab94106aa71", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 6, ordinal: 0, local_name: "source_b", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "f865d4e39819c045", h_schema: "df1f2b9efcfed35f", h_rule: "" },
  { rel_id: 13, parent_id: 12, ordinal: 1, local_name: "right", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "6138fa672b747648", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 7, ordinal: 0, local_name: "__delta_pair", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "b632b5d810144de7", h_schema: "691d2aac47379567", h_rule: "" },
  { rel_id: 15, parent_id: 7, ordinal: 0, local_name: "__frontier_pair", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "a0632194fca11b44", h_schema: "e317bdafb05c6ac6", h_rule: "" },
  { rel_id: 16, parent_id: 7, ordinal: 0, local_name: "__next_frontier_pair", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "c1afcbfef5cbe4af", h_schema: "e317bdafb05c6ac6", h_rule: "" },
  { rel_id: 17, parent_id: 10, ordinal: 0, local_name: "__delta_source_a", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "c60743736e89ae8f", h_schema: "d3d9e0ceac75549f", h_rule: "" },
  { rel_id: 18, parent_id: 10, ordinal: 0, local_name: "__frontier_source_a", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "6662614ea1bae311", h_schema: "bbac68972c4eee12", h_rule: "" },
  { rel_id: 19, parent_id: 10, ordinal: 0, local_name: "__next_frontier_source_a", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "d204273c04d7b9b5", h_schema: "bbac68972c4eee12", h_rule: "" },
  { rel_id: 20, parent_id: 12, ordinal: 0, local_name: "__delta_source_b", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "5a8f2a7afc0fd755", h_schema: "ab858f006835a437", h_rule: "" },
  { rel_id: 21, parent_id: 12, ordinal: 0, local_name: "__frontier_source_b", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "d837331f752e3389", h_schema: "dfb634b09e13e15b", h_rule: "" },
  { rel_id: 22, parent_id: 12, ordinal: 0, local_name: "__next_frontier_source_b", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "2edc822f6d2d434d", h_schema: "dfb634b09e13e15b", h_rule: "" },
  { rel_id: 23, parent_id: 7, ordinal: 0, local_name: "__support_next_pair", kind: "refcount", type_id: 0, arity: 3, module_id: 6, h_id: "f9cc49ac03822dbe", h_schema: "", h_rule: "" },
  { rel_id: 24, parent_id: 7, ordinal: 0, local_name: "__new_pair", kind: "refcount_staging", type_id: 0, arity: 3, module_id: 6, h_id: "a40073a9e0128245", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 8, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "5182ce9980f163c7", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 9, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "7f3de6cea99b4818", h_schema: "", h_rule: "" },
  { rel_id: 27, parent_id: 11, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "21b475e195e299fc", h_schema: "", h_rule: "" },
  { rel_id: 28, parent_id: 13, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "d8d33bca6d3722fe", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["source_a", "source_b"];

const boot: readonly IBootStatement[] = [
  { rel: "pair", sql: `DELETE FROM "pair"`, params: [] },
  { rel: "pair", sql: `INSERT OR IGNORE INTO "pair" ("left", "right") SELECT b0."left", b1."right" FROM "source_a" b0, "source_b" b1`, params: [] },
];

type Snapshot = {
  readonly pair: readonly IRow[];
  readonly source_a: readonly IRow[];
  readonly source_b: readonly IRow[];
};

function read_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    pair: select_rows(seam, `SELECT "left", "right" FROM "pair"`, rel_columns.pair!, rel_column_types.pair!),
    source_a: select_rows(seam, `SELECT "left" FROM "source_a"`, rel_columns.source_a!, rel_column_types.source_a!),
    source_b: select_rows(seam, `SELECT "right" FROM "source_b"`, rel_columns.source_b!, rel_column_types.source_b!),
  });
}

const final_select: Record<string, string> = {
  pair: `SELECT "left", "right" FROM "pair"`,
  source_a: `SELECT "left" FROM "source_a"`,
  source_b: `SELECT "right" FROM "source_b"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; add_sql: string; del_sql: string | null }> = {
  source_a: { kind: "set", add_sql: `INSERT OR IGNORE INTO "source_a" ("left") VALUES (?)`, del_sql: `DELETE FROM "source_a" WHERE "left" = ?` },
  source_b: { kind: "set", add_sql: `INSERT OR IGNORE INTO "source_b" ("right") VALUES (?)`, del_sql: `DELETE FROM "source_b" WHERE "right" = ?` },
};

function arrival_statement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`combine_level_is_the_conjunction_spelling: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`combine_level_is_the_conjunction_spelling: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.del_sql === null) {
      throw new Error(`combine_level_is_the_conjunction_spelling: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.del_sql, args: bind_args(arrival.row) };
  }
  return { sql: template.add_sql, args: bind_args(arrival.row) };
}

function apply_arrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrival_statement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "pair", kind: "set", table_name: "pair", delta_table_name: "__delta_pair", frontier_table_name: "__frontier_pair", next_frontier_table_name: "__next_frontier_pair", columns: ["left", "right"], column_types: ["int", "int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT "left", "right", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_pair" WHERE "_sign" IN (-1, 1) GROUP BY "left", "right", "_sign"`, rule_observers: [] },
  { rel: "source_a", kind: "set", table_name: "source_a", delta_table_name: "__delta_source_a", frontier_table_name: "__frontier_source_a", next_frontier_table_name: "__next_frontier_source_a", columns: ["left"], column_types: ["int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "source_a" ("left") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "left"`, arrival_del_sql: `DELETE FROM "source_a" WHERE ("left") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "left"`, boundary_sql: `SELECT "left", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_source_a" WHERE "_sign" IN (-1, 1) GROUP BY "left", "_sign"`, rule_observers: ["pair/2"] },
  { rel: "source_b", kind: "set", table_name: "source_b", delta_table_name: "__delta_source_b", frontier_table_name: "__frontier_source_b", next_frontier_table_name: "__next_frontier_source_b", columns: ["right"], column_types: ["int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "source_b" ("right") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "right"`, arrival_del_sql: `DELETE FROM "source_b" WHERE ("right") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "right"`, boundary_sql: `SELECT "right", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_source_b" WHERE "_sign" IN (-1, 1) GROUP BY "right", "_sign"`, rule_observers: ["pair/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "pair", rule_id: "combine_level_is_the_conjunction_spelling:pair/2#1", head_delta_table_name: "__delta_pair", head_columns: ["left", "right"], insert_sql: `INSERT OR IGNORE INTO "pair" ("left", "right") SELECT DISTINCT d0."left", b0."right" FROM "__frontier_source_a" d0, "source_b" b0 WHERE d0."_phase" >= 0 UNION ALL SELECT DISTINCT b0."left", d0."right" FROM "__frontier_source_b" d0, "source_a" b0 WHERE d0."_phase" >= 0 RETURNING "left", "right"`, select_sql: `SELECT "left", "right" FROM "pair"`, recompute_sql: `DELETE FROM "pair";
INSERT OR IGNORE INTO "pair" ("left", "right") SELECT b0."left", b1."right" FROM "source_a" b0, "source_b" b1`, support_sql: [`DELETE FROM "__support_next_pair"`, `INSERT INTO "__support_next_pair" ("left", "right", "__refcount") SELECT "left", "right", sum("__refcount") FROM (SELECT b0."left" AS "left", b1."right" AS "right", count(*) AS "__refcount" FROM "source_a" b0, "source_b" b1 GROUP BY b0."left", b1."right") GROUP BY "left", "right"`, `UPDATE "pair" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_pair" n WHERE n."left" = h."left" AND n."right" = h."right"), 0)`, `INSERT INTO "__delta_pair" ("_sign", "_sequence", "left", "right") SELECT -1, row_number() OVER () - 1, "left", "right" FROM "pair" WHERE "__refcount" <= 0`, `DELETE FROM "pair" WHERE "__refcount" <= 0`, `DELETE FROM "__new_pair"`, `INSERT INTO "__new_pair" ("left", "right", "__refcount") SELECT n."left", n."right", n."__refcount" FROM "__support_next_pair" n LEFT JOIN "pair" h ON n."left" = h."left" AND n."right" = h."right" WHERE h."left" IS NULL`, `INSERT INTO "__delta_pair" ("_sign", "_sequence", "left", "right") SELECT 1, "rowid" - 1, "left", "right" FROM "__new_pair"`, `INSERT INTO "__frontier_pair" ("_phase", "_sequence", "left", "right") SELECT ?, "rowid" - 1, "left", "right" FROM "__new_pair"`, `INSERT INTO "__next_frontier_pair" ("_phase", "_sequence", "left", "right") SELECT ?, "rowid" - 1, "left", "right" FROM "__new_pair"`, `INSERT OR IGNORE INTO "pair" ("left", "right", "__refcount") SELECT n."left", n."right", n."__refcount" FROM "__support_next_pair" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

function recompute_levels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "pair";
INSERT OR IGNORE INTO "pair" ("left", "right") SELECT b0."left", b1."right" FROM "source_a" b0, "source_b" b1`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function build_deltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const pair = multiset_diff(before.pair, after.pair);
  const source_a = multiset_diff(before.source_a, after.source_a);
  const source_b = multiset_diff(before.source_b, after.source_b);
  return {
    rels: [
      { rel: "pair", add: pair.add, del: pair.del },
      { rel: "source_a", add: source_a.add, del: source_a.del },
      { rel: "source_b", add: source_b.add, del: source_b.del },
    ],
    carry_pending: false,
  };
}

function run_naive_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return read_snapshot(seam).pipe(
    concatMap((before) => apply_arrivals(seam, arrivals).pipe(map(() => before))),
    concatMap((before) => recompute_levels(seam).pipe(map(() => before))),
    concatMap((before) => read_snapshot(seam).pipe(map((after) => build_deltas(before, after)))),
  );
  // combine_level_is_the_conjunction_spelling: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = EMITTER_MODE;
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return run_naive_tick(seam, arrivals);
  }
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "combine_level_is_the_conjunction_spelling",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
