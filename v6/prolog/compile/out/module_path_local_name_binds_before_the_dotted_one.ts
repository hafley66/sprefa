// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: module_path_local_name_binds_before_the_dotted_one.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowValue,
  ISqlSeam,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowValue[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowValue | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowValue[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => typeof value === "boolean" ? BigInt(value ? 1 : 0) : (typeof value === "number" && Number.isSafeInteger(value) ? BigInt(value) : value));
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

const ddl: readonly string[] = [
  `CREATE TABLE "local_pick" ("tree_id" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("tree_id")) WITHOUT ROWID`,
  `CREATE TABLE "orchard__tree" ("tree_id" INTEGER NOT NULL, PRIMARY KEY ("tree_id")) WITHOUT ROWID`,
  `CREATE TABLE "path_pick" ("tree_id" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("tree_id")) WITHOUT ROWID`,
  `CREATE TABLE "tree" ("tree_id" INTEGER NOT NULL, PRIMARY KEY ("tree_id")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__delta_local_pick" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_local_pick_sign" ON "__delta_local_pick" ("_sign")`,
  `CREATE INDEX "__delta_local_pick_group" ON "__delta_local_pick" ("tree_id")`,
  `CREATE TEMP TABLE "__frontier_local_pick" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_local_pick_phase" ON "__frontier_local_pick" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_local_pick" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_orchard__tree" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_orchard__tree_sign" ON "__delta_orchard__tree" ("_sign")`,
  `CREATE INDEX "__delta_orchard__tree_group" ON "__delta_orchard__tree" ("tree_id")`,
  `CREATE TEMP TABLE "__frontier_orchard__tree" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_orchard__tree_phase" ON "__frontier_orchard__tree" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_orchard__tree" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_path_pick" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_path_pick_sign" ON "__delta_path_pick" ("_sign")`,
  `CREATE INDEX "__delta_path_pick_group" ON "__delta_path_pick" ("tree_id")`,
  `CREATE TEMP TABLE "__frontier_path_pick" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_path_pick_phase" ON "__frontier_path_pick" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_path_pick" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_tree" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_tree_sign" ON "__delta_tree" ("_sign")`,
  `CREATE INDEX "__delta_tree_group" ON "__delta_tree" ("tree_id")`,
  `CREATE TEMP TABLE "__frontier_tree" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_tree_phase" ON "__frontier_tree" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_tree" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__support_next_local_pick" ("tree_id" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("tree_id")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_local_pick" ("tree_id" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "local_pick_zero" ON "local_pick" ("__refcount") WHERE "__refcount" <= 0`,
  `CREATE TEMP TABLE "__support_next_path_pick" ("tree_id" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("tree_id")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_path_pick" ("tree_id" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "path_pick_zero" ON "path_pick" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  local_pick: ["tree_id"],
  orchard__tree: ["tree_id"],
  path_pick: ["tree_id"],
  tree: ["tree_id"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  local_pick: ["int"],
  orchard__tree: ["int"],
  path_pick: ["int"],
  tree: ["int"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "module_path_local_name_binds_before_the_dotted_one", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "63a424b21a04e1b0", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "local_pick", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "caeb45117307ce8c", h_schema: "f9ef23491f91e03d", h_rule: "91d1760c5b528766" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "tree_id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "6d27f11c0e721744", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 15, ordinal: 0, local_name: "tree", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "9c92063ba19796aa", h_schema: "f9ef23491f91e03d", h_rule: "" },
  { rel_id: 10, parent_id: 9, ordinal: 1, local_name: "tree_id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "a8503fd868014ba3", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 6, ordinal: 0, local_name: "path_pick", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "15b29a784970d63f", h_schema: "f9ef23491f91e03d", h_rule: "08be0c1e461433c3" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "tree_id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "bb9e8d9d4ea1852e", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 6, ordinal: 0, local_name: "tree", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "240311fa97d8a332", h_schema: "f9ef23491f91e03d", h_rule: "" },
  { rel_id: 14, parent_id: 13, ordinal: 1, local_name: "tree_id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "f009e6fabdb099f4", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 6, ordinal: 0, local_name: "orchard", kind: "rel", type_id: 0, arity: 0, module_id: 6, h_id: "2ab000809b19e507", h_schema: "", h_rule: "" },
  { rel_id: 16, parent_id: 7, ordinal: 0, local_name: "__delta_local_pick", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "a517e40f8d2122f2", h_schema: "3fac23d94c210ea6", h_rule: "" },
  { rel_id: 17, parent_id: 7, ordinal: 0, local_name: "__frontier_local_pick", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "b585096a9f3fc18f", h_schema: "459af400e4fe51d7", h_rule: "" },
  { rel_id: 18, parent_id: 7, ordinal: 0, local_name: "__next_frontier_local_pick", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "95f006766e543ac4", h_schema: "459af400e4fe51d7", h_rule: "" },
  { rel_id: 19, parent_id: 9, ordinal: 0, local_name: "__delta_orchard__tree", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "326923a19a4c42c0", h_schema: "3fac23d94c210ea6", h_rule: "" },
  { rel_id: 20, parent_id: 9, ordinal: 0, local_name: "__frontier_orchard__tree", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "62eb8123b7e5380d", h_schema: "459af400e4fe51d7", h_rule: "" },
  { rel_id: 21, parent_id: 9, ordinal: 0, local_name: "__next_frontier_orchard__tree", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "9a6fde676544bd21", h_schema: "459af400e4fe51d7", h_rule: "" },
  { rel_id: 22, parent_id: 11, ordinal: 0, local_name: "__delta_path_pick", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "544705661cd276a2", h_schema: "3fac23d94c210ea6", h_rule: "" },
  { rel_id: 23, parent_id: 11, ordinal: 0, local_name: "__frontier_path_pick", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "a40ccf83f99c147b", h_schema: "459af400e4fe51d7", h_rule: "" },
  { rel_id: 24, parent_id: 11, ordinal: 0, local_name: "__next_frontier_path_pick", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "9fc5493b6fb21ac7", h_schema: "459af400e4fe51d7", h_rule: "" },
  { rel_id: 25, parent_id: 13, ordinal: 0, local_name: "__delta_tree", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "4b439306845de982", h_schema: "3fac23d94c210ea6", h_rule: "" },
  { rel_id: 26, parent_id: 13, ordinal: 0, local_name: "__frontier_tree", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "64a156aa667a2244", h_schema: "459af400e4fe51d7", h_rule: "" },
  { rel_id: 27, parent_id: 13, ordinal: 0, local_name: "__next_frontier_tree", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "717f8f9eee6c9f90", h_schema: "459af400e4fe51d7", h_rule: "" },
  { rel_id: 28, parent_id: 7, ordinal: 0, local_name: "__support_next_local_pick", kind: "refcount", type_id: 0, arity: 2, module_id: 6, h_id: "faf5865fadd719ad", h_schema: "", h_rule: "" },
  { rel_id: 29, parent_id: 7, ordinal: 0, local_name: "__new_local_pick", kind: "refcount_staging", type_id: 0, arity: 2, module_id: 6, h_id: "4a09259ce76d9b80", h_schema: "", h_rule: "" },
  { rel_id: 30, parent_id: 11, ordinal: 0, local_name: "__support_next_path_pick", kind: "refcount", type_id: 0, arity: 2, module_id: 6, h_id: "6b0134741005565a", h_schema: "", h_rule: "" },
  { rel_id: 31, parent_id: 11, ordinal: 0, local_name: "__new_path_pick", kind: "refcount_staging", type_id: 0, arity: 2, module_id: 6, h_id: "51b37b16d40ea5d7", h_schema: "", h_rule: "" },
  { rel_id: 32, parent_id: 8, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "ea7d6b372087c746", h_schema: "", h_rule: "" },
  { rel_id: 33, parent_id: 10, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "46a1c976672f3d52", h_schema: "", h_rule: "" },
  { rel_id: 34, parent_id: 12, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "620688f6c34de0ab", h_schema: "", h_rule: "" },
  { rel_id: 35, parent_id: 14, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "39d378f7e3c574d2", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
  local_pick: ["int"],
  orchard__tree: ["int"],
  path_pick: ["int"],
  tree: ["int"],
};

const arrival_targets: readonly string[] = ["orchard__tree", "tree"];

const boot: readonly IBootStatement[] = [
  { rel: "local_pick", sql: `DELETE FROM "local_pick"`, params: [] },
  { rel: "local_pick", sql: `INSERT OR IGNORE INTO "local_pick" ("tree_id") SELECT b0."tree_id" FROM "tree" b0`, params: [] },
  { rel: "path_pick", sql: `DELETE FROM "path_pick"`, params: [] },
  { rel: "path_pick", sql: `INSERT OR IGNORE INTO "path_pick" ("tree_id") SELECT b0."tree_id" FROM "orchard__tree" b0`, params: [] },
];

type Snapshot = {
  readonly local_pick: readonly IRow[];
  readonly orchard__tree: readonly IRow[];
  readonly path_pick: readonly IRow[];
  readonly tree: readonly IRow[];
};

function read_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    local_pick: select_rows(seam, `SELECT "tree_id" FROM "local_pick"`, rel_columns.local_pick!, rel_column_types.local_pick!),
    orchard__tree: select_rows(seam, `SELECT "tree_id" FROM "orchard__tree"`, rel_columns.orchard__tree!, rel_column_types.orchard__tree!),
    path_pick: select_rows(seam, `SELECT "tree_id" FROM "path_pick"`, rel_columns.path_pick!, rel_column_types.path_pick!),
    tree: select_rows(seam, `SELECT "tree_id" FROM "tree"`, rel_columns.tree!, rel_column_types.tree!),
  });
}

const final_select: Record<string, string> = {
  local_pick: `SELECT "tree_id" FROM "local_pick"`,
  orchard__tree: `SELECT "tree_id" FROM "orchard__tree"`,
  path_pick: `SELECT "tree_id" FROM "path_pick"`,
  tree: `SELECT "tree_id" FROM "tree"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; add_sql: string; del_sql: string | null }> = {
  orchard__tree: { kind: "set", add_sql: `INSERT OR IGNORE INTO "orchard__tree" ("tree_id") VALUES (?)`, del_sql: `DELETE FROM "orchard__tree" WHERE "tree_id" = ?` },
  tree: { kind: "set", add_sql: `INSERT OR IGNORE INTO "tree" ("tree_id") VALUES (?)`, del_sql: `DELETE FROM "tree" WHERE "tree_id" = ?` },
};

function arrival_statement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`module_path_local_name_binds_before_the_dotted_one: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`module_path_local_name_binds_before_the_dotted_one: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.del_sql === null) {
      throw new Error(`module_path_local_name_binds_before_the_dotted_one: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.del_sql, args: bind_args(arrival.row) };
  }
  return { sql: template.add_sql, args: bind_args(arrival.row) };
}

function apply_arrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrival_statement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "local_pick", kind: "set", table_name: "local_pick", delta_table_name: "__delta_local_pick", frontier_table_name: "__frontier_local_pick", next_frontier_table_name: "__next_frontier_local_pick", columns: ["tree_id"], column_types: ["int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT "tree_id", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_local_pick" WHERE "_sign" IN (-1, 1) GROUP BY "tree_id", "_sign"`, rule_observers: [] },
  { rel: "orchard__tree", kind: "set", table_name: "orchard__tree", delta_table_name: "__delta_orchard__tree", frontier_table_name: "__frontier_orchard__tree", next_frontier_table_name: "__next_frontier_orchard__tree", columns: ["tree_id"], column_types: ["int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "orchard__tree" ("tree_id") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "tree_id"`, arrival_del_sql: `DELETE FROM "orchard__tree" WHERE ("tree_id") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "tree_id"`, boundary_sql: `SELECT "tree_id", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_orchard__tree" WHERE "_sign" IN (-1, 1) GROUP BY "tree_id", "_sign"`, rule_observers: ["path_pick/1"] },
  { rel: "path_pick", kind: "set", table_name: "path_pick", delta_table_name: "__delta_path_pick", frontier_table_name: "__frontier_path_pick", next_frontier_table_name: "__next_frontier_path_pick", columns: ["tree_id"], column_types: ["int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT "tree_id", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_path_pick" WHERE "_sign" IN (-1, 1) GROUP BY "tree_id", "_sign"`, rule_observers: [] },
  { rel: "tree", kind: "set", table_name: "tree", delta_table_name: "__delta_tree", frontier_table_name: "__frontier_tree", next_frontier_table_name: "__next_frontier_tree", columns: ["tree_id"], column_types: ["int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "tree" ("tree_id") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "tree_id"`, arrival_del_sql: `DELETE FROM "tree" WHERE ("tree_id") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "tree_id"`, boundary_sql: `SELECT "tree_id", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_tree" WHERE "_sign" IN (-1, 1) GROUP BY "tree_id", "_sign"`, rule_observers: ["local_pick/1"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "local_pick", rule_id: "module_path_local_name_binds_before_the_dotted_one:local_pick/1#1", head_delta_table_name: "__delta_local_pick", head_columns: ["tree_id"], insert_sql: `INSERT OR IGNORE INTO "local_pick" ("tree_id") SELECT DISTINCT d0."tree_id" FROM "__frontier_tree" d0 WHERE d0."_phase" >= 0 RETURNING "tree_id"`, select_sql: `SELECT "tree_id" FROM "local_pick"`, recompute_sql: `DELETE FROM "local_pick";
INSERT OR IGNORE INTO "local_pick" ("tree_id") SELECT b0."tree_id" FROM "tree" b0`, support_sql: [`DELETE FROM "__support_next_local_pick"`, `INSERT INTO "__support_next_local_pick" ("tree_id", "__refcount") SELECT "tree_id", sum("__refcount") FROM (SELECT b0."tree_id" AS "tree_id", count(*) AS "__refcount" FROM "tree" b0 GROUP BY b0."tree_id") GROUP BY "tree_id"`, `UPDATE "local_pick" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_local_pick" n WHERE n."tree_id" = h."tree_id"), 0)`, `INSERT INTO "__delta_local_pick" ("_sign", "_sequence", "tree_id") SELECT -1, row_number() OVER () - 1, "tree_id" FROM "local_pick" WHERE "__refcount" <= 0`, `DELETE FROM "local_pick" WHERE "__refcount" <= 0`, `DELETE FROM "__new_local_pick"`, `INSERT INTO "__new_local_pick" ("tree_id", "__refcount") SELECT n."tree_id", n."__refcount" FROM "__support_next_local_pick" n LEFT JOIN "local_pick" h ON n."tree_id" = h."tree_id" WHERE h."tree_id" IS NULL`, `INSERT INTO "__delta_local_pick" ("_sign", "_sequence", "tree_id") SELECT 1, "rowid" - 1, "tree_id" FROM "__new_local_pick"`, `INSERT INTO "__frontier_local_pick" ("_phase", "_sequence", "tree_id") SELECT ?, "rowid" - 1, "tree_id" FROM "__new_local_pick"`, `INSERT INTO "__next_frontier_local_pick" ("_phase", "_sequence", "tree_id") SELECT ?, "rowid" - 1, "tree_id" FROM "__new_local_pick"`, `INSERT OR IGNORE INTO "local_pick" ("tree_id", "__refcount") SELECT n."tree_id", n."__refcount" FROM "__support_next_local_pick" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
  { head_rel: "path_pick", rule_id: "module_path_local_name_binds_before_the_dotted_one:path_pick/1#1", head_delta_table_name: "__delta_path_pick", head_columns: ["tree_id"], insert_sql: `INSERT OR IGNORE INTO "path_pick" ("tree_id") SELECT DISTINCT d0."tree_id" FROM "__frontier_orchard__tree" d0 WHERE d0."_phase" >= 0 RETURNING "tree_id"`, select_sql: `SELECT "tree_id" FROM "path_pick"`, recompute_sql: `DELETE FROM "path_pick";
INSERT OR IGNORE INTO "path_pick" ("tree_id") SELECT b0."tree_id" FROM "orchard__tree" b0`, support_sql: [`DELETE FROM "__support_next_path_pick"`, `INSERT INTO "__support_next_path_pick" ("tree_id", "__refcount") SELECT "tree_id", sum("__refcount") FROM (SELECT b0."tree_id" AS "tree_id", count(*) AS "__refcount" FROM "orchard__tree" b0 GROUP BY b0."tree_id") GROUP BY "tree_id"`, `UPDATE "path_pick" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_path_pick" n WHERE n."tree_id" = h."tree_id"), 0)`, `INSERT INTO "__delta_path_pick" ("_sign", "_sequence", "tree_id") SELECT -1, row_number() OVER () - 1, "tree_id" FROM "path_pick" WHERE "__refcount" <= 0`, `DELETE FROM "path_pick" WHERE "__refcount" <= 0`, `DELETE FROM "__new_path_pick"`, `INSERT INTO "__new_path_pick" ("tree_id", "__refcount") SELECT n."tree_id", n."__refcount" FROM "__support_next_path_pick" n LEFT JOIN "path_pick" h ON n."tree_id" = h."tree_id" WHERE h."tree_id" IS NULL`, `INSERT INTO "__delta_path_pick" ("_sign", "_sequence", "tree_id") SELECT 1, "rowid" - 1, "tree_id" FROM "__new_path_pick"`, `INSERT INTO "__frontier_path_pick" ("_phase", "_sequence", "tree_id") SELECT ?, "rowid" - 1, "tree_id" FROM "__new_path_pick"`, `INSERT INTO "__next_frontier_path_pick" ("_phase", "_sequence", "tree_id") SELECT ?, "rowid" - 1, "tree_id" FROM "__new_path_pick"`, `INSERT OR IGNORE INTO "path_pick" ("tree_id", "__refcount") SELECT n."tree_id", n."__refcount" FROM "__support_next_path_pick" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

function recompute_levels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "local_pick";
INSERT OR IGNORE INTO "local_pick" ("tree_id") SELECT b0."tree_id" FROM "tree" b0;
DELETE FROM "path_pick";
INSERT OR IGNORE INTO "path_pick" ("tree_id") SELECT b0."tree_id" FROM "orchard__tree" b0`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function build_deltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const local_pick = multiset_diff(before.local_pick, after.local_pick);
  const orchard__tree = multiset_diff(before.orchard__tree, after.orchard__tree);
  const path_pick = multiset_diff(before.path_pick, after.path_pick);
  const tree = multiset_diff(before.tree, after.tree);
  return {
    rels: [
      { rel: "local_pick", add: local_pick.add, del: local_pick.del },
      { rel: "orchard__tree", add: orchard__tree.add, del: orchard__tree.del },
      { rel: "path_pick", add: path_pick.add, del: path_pick.del },
      { rel: "tree", add: tree.add, del: tree.del },
    ],
    carry_pending: false,
  };
}

function run_naive_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return read_snapshot(seam).pipe(
    concatMap((before) => apply_arrivals(seam, arrivals).pipe(map(() => before))),
    concatMap((before) => recompute_levels(seam).pipe(map(() => before))),
    concatMap((before) => read_snapshot(seam).pipe(map((after) => build_deltas(before, after)))),
  );
  // module_path_local_name_binds_before_the_dotted_one: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = EMITTER_MODE;
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return run_naive_tick(seam, arrivals);
  }
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "module_path_local_name_binds_before_the_dotted_one",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
