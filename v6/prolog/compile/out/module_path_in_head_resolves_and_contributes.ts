// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: module_path_in_head_resolves_and_contributes.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { EnumPlane } from "../runtime/enumPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IEnumRefColumns,
  IEnumTypePlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly ir_version: number; readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly rel_physical_names: Record<string, string>; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const ENUM_TYPES: readonly IEnumTypePlan[] = [];
export const ENUM_REF_COLUMNS: IEnumRefColumns = {};

const ddl: readonly string[] = [
  `CREATE TABLE "module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" ("__id" INTEGER PRIMARY KEY, "tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL, UNIQUE ("tree_id", "picked"))`,
  `CREATE TABLE "module_path_in_head_resolves_and_contributes_orchard__tree" ("__id" INTEGER PRIMARY KEY, "tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("tree_id", "picked"))`,
  `CREATE TEMP TABLE "__delta_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d_sign" ON "__delta_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" ("_sign")`,
  `CREATE INDEX "__delta_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d_group" ON "__delta_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" ("tree_id", "picked")`,
  `CREATE TEMP TABLE "__frontier_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d_phase" ON "__frontier_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_module_path_in_head_resolves_and_contributes_orchard__tree" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_module_path_in_head_resolves_and_contributes_orchard__tree_sign" ON "__delta_module_path_in_head_resolves_and_contributes_orchard__tree" ("_sign")`,
  `CREATE INDEX "__delta_module_path_in_head_resolves_and_contributes_orchard__tree_group" ON "__delta_module_path_in_head_resolves_and_contributes_orchard__tree" ("tree_id", "picked")`,
  `CREATE TEMP TABLE "__frontier_module_path_in_head_resolves_and_contributes_orchard__tree" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_module_path_in_head_resolves_and_contributes_orchard__tree_phase" ON "__frontier_module_path_in_head_resolves_and_contributes_orchard__tree" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_module_path_in_head_resolves_and_contributes_orchard__tree" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__support_next_module_path_in_head_resolves_and_contributes_orchard__tree" ("tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("tree_id", "picked")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_module_path_in_head_resolves_and_contributes_orchard__tree" ("tree_id" INTEGER NOT NULL, "picked" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "module_path_in_head_resolves_and_contributes_orchard__tree_zero" ON "module_path_in_head_resolves_and_contributes_orchard__tree" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  harvest: ["tree_id", "picked"],
  orchard__tree: ["tree_id", "picked"],
};

const rel_physical_names: Record<string, string> = {
  harvest: "module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d",
  orchard__tree: "module_path_in_head_resolves_and_contributes_orchard__tree",
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  harvest: ["int", "int"],
  orchard__tree: ["int", "int"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  harvest: ["int", "int"],
  orchard__tree: ["int", "int"],
};

const rel_catalog: readonly IRelCatalogRow[] = new Array<IRelCatalogRow>(
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "module_path_in_head_resolves_and_contributes", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "c191090caf985d6f", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "harvest", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "7608cd20efc21942", h_schema: "c10cd2ee5a451cce", h_rule: "" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "tree_id", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "2a95986b85e7fee5", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 8, ordinal: 2, local_name: "picked", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "5db6507b7a13569c", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 14, ordinal: 0, local_name: "tree", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "619a24ad8ae6767c", h_schema: "c10cd2ee5a451cce", h_rule: "f87f7cd5a61b41a6" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "tree_id", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "defee779cd0429d3", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 11, ordinal: 2, local_name: "picked", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "fcd2677f164e71aa", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 7, ordinal: 0, local_name: "orchard", kind: "rel", type_id: 0, arity: 0, module_id: 7, h_id: "bbcb2db844db319b", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 8, ordinal: 0, local_name: "__delta_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "893bd26c22d203fb", h_schema: "bf2e3fe79a36e009", h_rule: "" },
  { rel_id: 16, parent_id: 8, ordinal: 0, local_name: "__frontier_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "83ef6d42c0a78b4b", h_schema: "f0b09e9ac03edd62", h_rule: "" },
  { rel_id: 17, parent_id: 8, ordinal: 0, local_name: "__next_frontier_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "21af99b9aa665185", h_schema: "f0b09e9ac03edd62", h_rule: "" },
  { rel_id: 18, parent_id: 11, ordinal: 0, local_name: "__delta_module_path_in_head_resolves_and_contributes_orchard__tree", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "9fc65ce34aae76cb", h_schema: "bf2e3fe79a36e009", h_rule: "" },
  { rel_id: 19, parent_id: 11, ordinal: 0, local_name: "__frontier_module_path_in_head_resolves_and_contributes_orchard__tree", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "a7450aab8b631857", h_schema: "f0b09e9ac03edd62", h_rule: "" },
  { rel_id: 20, parent_id: 11, ordinal: 0, local_name: "__next_frontier_module_path_in_head_resolves_and_contributes_orchard__tree", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "56eedef6916b46b2", h_schema: "f0b09e9ac03edd62", h_rule: "" },
  { rel_id: 21, parent_id: 11, ordinal: 0, local_name: "__support_next_module_path_in_head_resolves_and_contributes_orchard__tree", kind: "refcount", type_id: 0, arity: 3, module_id: 7, h_id: "07c242bd0fa35f9e", h_schema: "", h_rule: "" },
  { rel_id: 22, parent_id: 11, ordinal: 0, local_name: "__new_module_path_in_head_resolves_and_contributes_orchard__tree", kind: "refcount_staging", type_id: 0, arity: 3, module_id: 7, h_id: "7b61355255241253", h_schema: "", h_rule: "" },
  { rel_id: 23, parent_id: 9, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "386137ac6d7dd449", h_schema: "", h_rule: "" },
  { rel_id: 24, parent_id: 10, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "287e3fed93f81488", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 12, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "bd4dc660bf5b06c0", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 13, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "e6645266029732d9", h_schema: "", h_rule: "" },
);

const rel_declared_column_types: Record<string, readonly string[]> = {
  harvest: ["int", "int"],
  orchard__tree: ["int", "int"],
};

const arrival_targets: readonly string[] = ["harvest"];

const boot: readonly IBootStatement[] = [
  { rel: "orchard__tree", sql: `DELETE FROM "module_path_in_head_resolves_and_contributes_orchard__tree"`, params: [] },
  { rel: "orchard__tree", sql: `INSERT OR IGNORE INTO "module_path_in_head_resolves_and_contributes_orchard__tree" ("tree_id", "picked") SELECT b0."tree_id", b0."picked" FROM "module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" b0`, params: [] },
];

const final_select: Record<string, string> = {
  harvest: `SELECT t."tree_id", t."picked" FROM "module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" t`,
  orchard__tree: `SELECT t."tree_id", t."picked" FROM "module_path_in_head_resolves_and_contributes_orchard__tree" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "harvest", kind: "set", table_name: "module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d", delta_table_name: "__delta_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d", frontier_table_name: "__frontier_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d", next_frontier_table_name: "__next_frontier_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d", columns: ["tree_id", "picked"], column_types: ["int", "int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" ("tree_id", "picked") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "tree_id", "picked"`, arrival_del_sql: `DELETE FROM "module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" WHERE ("tree_id", "picked") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "tree_id", "picked"`, boundary_sql: `SELECT t."tree_id", t."picked", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" t WHERE t."_sign" IN (-1, 1) GROUP BY t."tree_id", t."picked", t."_sign"`, rule_observers: ["orchard__tree/2"] },
  { rel: "orchard__tree", kind: "set", table_name: "module_path_in_head_resolves_and_contributes_orchard__tree", delta_table_name: "__delta_module_path_in_head_resolves_and_contributes_orchard__tree", frontier_table_name: "__frontier_module_path_in_head_resolves_and_contributes_orchard__tree", next_frontier_table_name: "__next_frontier_module_path_in_head_resolves_and_contributes_orchard__tree", columns: ["tree_id", "picked"], column_types: ["int", "int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT t."tree_id", t."picked", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_module_path_in_head_resolves_and_contributes_orchard__tree" t WHERE t."_sign" IN (-1, 1) GROUP BY t."tree_id", t."picked", t."_sign"`, rule_observers: [] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "orchard__tree", rule_id: "module_path_in_head_resolves_and_contributes:orchard__tree/2#1", head_delta_table_name: "__delta_module_path_in_head_resolves_and_contributes_orchard__tree", head_columns: ["tree_id", "picked"], insert_sql: `INSERT OR IGNORE INTO "module_path_in_head_resolves_and_contributes_orchard__tree" ("tree_id", "picked") SELECT DISTINCT d0."tree_id", d0."picked" FROM "__frontier_module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" d0 WHERE d0."_phase" >= 0 RETURNING "tree_id", "picked"`, select_sql: `SELECT "tree_id", "picked" FROM "module_path_in_head_resolves_and_contributes_orchard__tree"`, recompute_sql: `DELETE FROM "module_path_in_head_resolves_and_contributes_orchard__tree";
INSERT OR IGNORE INTO "module_path_in_head_resolves_and_contributes_orchard__tree" ("tree_id", "picked") SELECT b0."tree_id", b0."picked" FROM "module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" b0`, support_sql: [`DELETE FROM "__support_next_module_path_in_head_resolves_and_contributes_orchard__tree"`, `INSERT INTO "__support_next_module_path_in_head_resolves_and_contributes_orchard__tree" ("tree_id", "picked", "__refcount") SELECT "tree_id", "picked", sum("__refcount") FROM (SELECT b0."tree_id" AS "tree_id", b0."picked" AS "picked", count(*) AS "__refcount" FROM "module_path_in_head_resolves_and_contributes_harvest_0fc2d6a3f03d" b0 GROUP BY b0."tree_id", b0."picked") GROUP BY "tree_id", "picked"`, `UPDATE "module_path_in_head_resolves_and_contributes_orchard__tree" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_module_path_in_head_resolves_and_contributes_orchard__tree" n WHERE n."tree_id" = h."tree_id" AND n."picked" = h."picked"), 0)`, `INSERT INTO "__delta_module_path_in_head_resolves_and_contributes_orchard__tree" ("_sign", "_sequence", "tree_id", "picked") SELECT -1, row_number() OVER () - 1, "tree_id", "picked" FROM "module_path_in_head_resolves_and_contributes_orchard__tree" WHERE "__refcount" <= 0`, `DELETE FROM "module_path_in_head_resolves_and_contributes_orchard__tree" WHERE "__refcount" <= 0`, `DELETE FROM "__new_module_path_in_head_resolves_and_contributes_orchard__tree"`, `INSERT INTO "__new_module_path_in_head_resolves_and_contributes_orchard__tree" ("tree_id", "picked", "__refcount") SELECT n."tree_id", n."picked", n."__refcount" FROM "__support_next_module_path_in_head_resolves_and_contributes_orchard__tree" n LEFT JOIN "module_path_in_head_resolves_and_contributes_orchard__tree" h ON n."tree_id" = h."tree_id" AND n."picked" = h."picked" WHERE h."tree_id" IS NULL`, `INSERT INTO "__delta_module_path_in_head_resolves_and_contributes_orchard__tree" ("_sign", "_sequence", "tree_id", "picked") SELECT 1, "rowid" - 1, "tree_id", "picked" FROM "__new_module_path_in_head_resolves_and_contributes_orchard__tree"`, `INSERT INTO "__frontier_module_path_in_head_resolves_and_contributes_orchard__tree" ("_phase", "_sequence", "tree_id", "picked") SELECT ?, "rowid" - 1, "tree_id", "picked" FROM "__new_module_path_in_head_resolves_and_contributes_orchard__tree"`, `INSERT INTO "__next_frontier_module_path_in_head_resolves_and_contributes_orchard__tree" ("_phase", "_sequence", "tree_id", "picked") SELECT ?, "rowid" - 1, "tree_id", "picked" FROM "__new_module_path_in_head_resolves_and_contributes_orchard__tree"`, `INSERT OR IGNORE INTO "module_path_in_head_resolves_and_contributes_orchard__tree" ("tree_id", "picked", "__refcount") SELECT n."tree_id", n."picked", n."__refcount" FROM "__support_next_module_path_in_head_resolves_and_contributes_orchard__tree" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      concatMap((carry_pending) => EnumPlane.decode_deltas(seam, ENUM_TYPES, ENUM_REF_COLUMNS, SUBSCRIBED_RELATIONS, rels).pipe(
        map((decoded): ITickDeltas => ({ rels: decoded, carry_pending })),
      )),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return EnumPlane.intern(seam, ENUM_TYPES, ENUM_REF_COLUMNS, arrivals).pipe(
    concatMap((normalized) => run_incremental_tick(seam, validate_arrivals(normalized))),
  );
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "module_path_in_head_resolves_and_contributes",
  ir_version: 1,
  internMode: "dict",
  ddl,
  rel_columns,
  rel_physical_names,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  enum_types: ENUM_TYPES,
  enum_ref_columns: ENUM_REF_COLUMNS,
  unsupported_execution,
  tick: run_tick,
};
