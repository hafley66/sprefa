// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: aggregate_count_min_max_track_arrivals_and_retraction.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowValue,
  ISqlSeam,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowValue[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowValue | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowValue[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => typeof value === "boolean" ? BigInt(value ? 1 : 0) : (typeof value === "number" && Number.isSafeInteger(value) ? BigInt(value) : value));
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "star_row": [true, false],
    "stat": [true, false, false, false],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "star_row" ("repo" INTEGER NOT NULL, "stars" INTEGER NOT NULL, PRIMARY KEY ("repo", "stars")) WITHOUT ROWID`,
  `CREATE TEMP VIEW "__txt_star_row" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."repo") AS "repo", t."stars" AS "stars" FROM "star_row" t`,
  `CREATE TABLE "stat" ("repo" INTEGER NOT NULL, "col2" INTEGER NOT NULL, "col3" INTEGER NOT NULL, "col4" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("repo", "col2", "col3", "col4")) WITHOUT ROWID`,
  `CREATE TEMP VIEW "__txt_stat" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."repo") AS "repo", t."col2" AS "col2", t."col3" AS "col3", t."col4" AS "col4", t."__refcount" AS "__refcount" FROM "stat" t`,
  `CREATE TEMP TABLE "__delta_star_row" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "stars" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_star_row_sign" ON "__delta_star_row" ("_sign")`,
  `CREATE INDEX "__delta_star_row_group" ON "__delta_star_row" ("repo", "stars")`,
  `CREATE TEMP TABLE "__frontier_star_row" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "stars" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_star_row_phase" ON "__frontier_star_row" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_star_row" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "stars" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_star_row" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."repo") AS "repo", t."stars" AS "stars", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_star_row" t`,
  `CREATE TEMP TABLE "__delta_stat" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "col2" INTEGER NOT NULL, "col3" INTEGER NOT NULL, "col4" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_stat_sign" ON "__delta_stat" ("_sign")`,
  `CREATE INDEX "__delta_stat_group" ON "__delta_stat" ("repo", "col2", "col3", "col4")`,
  `CREATE TEMP TABLE "__frontier_stat" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "col2" INTEGER NOT NULL, "col3" INTEGER NOT NULL, "col4" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_stat_phase" ON "__frontier_stat" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_stat" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "col2" INTEGER NOT NULL, "col3" INTEGER NOT NULL, "col4" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_stat" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."repo") AS "repo", t."col2" AS "col2", t."col3" AS "col3", t."col4" AS "col4", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_stat" t`,
  `CREATE TEMP TABLE "__agg_scope_stat" ("repo" INTEGER NOT NULL, PRIMARY KEY ("repo")) WITHOUT ROWID`,
];

const rel_columns: Record<string, readonly string[]> = {
  star_row: ["repo", "stars"],
  stat: ["repo", "col2", "col3", "col4"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  star_row: ["text", "int"],
  stat: ["text", "int", "int", "int"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "aggregate_count_min_max_track_arrivals_and_retraction", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "e7306939e199b838", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "star_row", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "51c93b0804549bb6", h_schema: "903cbf6daedddd73", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "repo", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "c9a9200164d101a6", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 7, ordinal: 2, local_name: "stars", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "072b80e7808a0285", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 6, ordinal: 0, local_name: "stat", kind: "rel", type_id: 0, arity: 4, module_id: 6, h_id: "b52473ca6a9017f0", h_schema: "f419701620e4d50b", h_rule: "fc722eb4a40753a8" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "repo", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "4c42d61f268c19be", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 10, ordinal: 2, local_name: "col2", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "9ecbc71d9dc93c02", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 10, ordinal: 3, local_name: "col3", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "1672cbd691aef683", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 10, ordinal: 4, local_name: "col4", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "06c24dc62cfb3c1d", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 7, ordinal: 0, local_name: "__delta_star_row", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "8ead52a7c0c986a5", h_schema: "ca132724762dc791", h_rule: "" },
  { rel_id: 16, parent_id: 7, ordinal: 0, local_name: "__frontier_star_row", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "66f7021e9e872057", h_schema: "3a20e46913c99c26", h_rule: "" },
  { rel_id: 17, parent_id: 7, ordinal: 0, local_name: "__next_frontier_star_row", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "d107b5f99e4e3a36", h_schema: "3a20e46913c99c26", h_rule: "" },
  { rel_id: 18, parent_id: 7, ordinal: 0, local_name: "__txt_star_row", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "e7b941cd090d7493", h_schema: "903cbf6daedddd73", h_rule: "" },
  { rel_id: 19, parent_id: 15, ordinal: 0, local_name: "__txt___delta_star_row", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "f98fdd45378538e5", h_schema: "903cbf6daedddd73", h_rule: "" },
  { rel_id: 20, parent_id: 10, ordinal: 0, local_name: "__delta_stat", kind: "delta", type_id: 0, arity: 6, module_id: 6, h_id: "0b24145d6c00f2f7", h_schema: "271cc33e11442276", h_rule: "" },
  { rel_id: 21, parent_id: 10, ordinal: 0, local_name: "__frontier_stat", kind: "frontier", type_id: 0, arity: 6, module_id: 6, h_id: "1dc1753b3edfa375", h_schema: "792e1fd15da48c0f", h_rule: "" },
  { rel_id: 22, parent_id: 10, ordinal: 0, local_name: "__next_frontier_stat", kind: "next_frontier", type_id: 0, arity: 6, module_id: 6, h_id: "109a9860d401077d", h_schema: "792e1fd15da48c0f", h_rule: "" },
  { rel_id: 23, parent_id: 10, ordinal: 0, local_name: "__txt_stat", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "80e5d7003018309e", h_schema: "f419701620e4d50b", h_rule: "" },
  { rel_id: 24, parent_id: 20, ordinal: 0, local_name: "__txt___delta_stat", kind: "view", type_id: 0, arity: 6, module_id: 6, h_id: "cfec2b651f7aebac", h_schema: "f419701620e4d50b", h_rule: "" },
  { rel_id: 25, parent_id: 6, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 6, h_id: "9a6c09a8974aa3d6", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 10, ordinal: 0, local_name: "__agg_scope_stat", kind: "scope", type_id: 0, arity: 1, module_id: 6, h_id: "e2e2a50c2c424033", h_schema: "3a9d7092905caf86", h_rule: "" },
  { rel_id: 27, parent_id: 8, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "9d780a3971ce9f66", h_schema: "", h_rule: "" },
  { rel_id: 28, parent_id: 9, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "736c168ffce0e5bc", h_schema: "", h_rule: "" },
  { rel_id: 29, parent_id: 11, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "c3771fd8d1975708", h_schema: "", h_rule: "" },
  { rel_id: 30, parent_id: 12, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "1de767a27883d21f", h_schema: "", h_rule: "" },
  { rel_id: 31, parent_id: 13, ordinal: 3, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "f0aa660bb4429bb0", h_schema: "", h_rule: "" },
  { rel_id: 32, parent_id: 14, ordinal: 4, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "28d3257f700fc1a9", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["star_row"];

const boot: readonly IBootStatement[] = [
  { rel: "stat", sql: `DELETE FROM "stat"`, params: [] },
  { rel: "stat", sql: `INSERT OR IGNORE INTO "stat" ("repo", "col2", "col3", "col4") SELECT b0."repo", count(*), min(b0."stars"), max(b0."stars") FROM "star_row" b0 GROUP BY b0."repo" HAVING count(*) > 0`, params: [] },
];

type Snapshot = {
  readonly star_row: readonly IRow[];
  readonly stat: readonly IRow[];
};

function read_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    star_row: select_rows(seam, `SELECT CASE WHEN json_valid("repo") AND json_type("repo") = 'object' AND json_type("repo", '$.fn') = 'text' AND json_type("repo", '$.args') = 'array' THEN json_extract("repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("repo", '$.args')), '') || ')' ELSE "repo" END AS "repo", "stars" FROM "__txt_star_row"`, rel_columns.star_row!, rel_column_types.star_row!),
    stat: select_rows(seam, `SELECT CASE WHEN json_valid("repo") AND json_type("repo") = 'object' AND json_type("repo", '$.fn') = 'text' AND json_type("repo", '$.args') = 'array' THEN json_extract("repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("repo", '$.args')), '') || ')' ELSE "repo" END AS "repo", "col2", "col3", "col4" FROM "__txt_stat"`, rel_columns.stat!, rel_column_types.stat!),
  });
}

type Snapshots = { readonly decoded: Snapshot; readonly stored: Snapshot };

function read_stored_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    star_row: select_rows(seam, `SELECT "repo", "stars" FROM "star_row"`, rel_columns.star_row!, rel_column_types.star_row!),
    stat: select_rows(seam, `SELECT "repo", "col2", "col3", "col4" FROM "stat"`, rel_columns.stat!, rel_column_types.stat!),
  });
}

function read_snapshots(seam: ISqlSeam): Observable<Snapshots> {
  return forkJoin({ decoded: read_snapshot(seam), stored: read_stored_snapshot(seam) });
}

const final_select: Record<string, string> = {
  star_row: `SELECT CASE WHEN json_valid("repo") AND json_type("repo") = 'object' AND json_type("repo", '$.fn') = 'text' AND json_type("repo", '$.args') = 'array' THEN json_extract("repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("repo", '$.args')), '') || ')' ELSE "repo" END AS "repo", "stars" FROM "__txt_star_row"`,
  stat: `SELECT CASE WHEN json_valid("repo") AND json_type("repo") = 'object' AND json_type("repo", '$.fn') = 'text' AND json_type("repo", '$.args') = 'array' THEN json_extract("repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("repo", '$.args')), '') || ')' ELSE "repo" END AS "repo", "col2", "col3", "col4" FROM "__txt_stat"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; add_sql: string; del_sql: string | null }> = {
  star_row: { kind: "set", add_sql: `INSERT OR IGNORE INTO "star_row" ("repo", "stars") VALUES (?, ?)`, del_sql: `DELETE FROM "star_row" WHERE "repo" = ? AND "stars" = ?` },
};

function arrival_statement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`aggregate_count_min_max_track_arrivals_and_retraction: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`aggregate_count_min_max_track_arrivals_and_retraction: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.del_sql === null) {
      throw new Error(`aggregate_count_min_max_track_arrivals_and_retraction: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.del_sql, args: bind_args(arrival.row) };
  }
  return { sql: template.add_sql, args: bind_args(arrival.row) };
}

function apply_arrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrival_statement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "star_row", kind: "set", table_name: "star_row", delta_table_name: "__delta_star_row", frontier_table_name: "__frontier_star_row", next_frontier_table_name: "__next_frontier_star_row", columns: ["repo", "stars"], column_types: ["text", "int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "star_row" ("repo", "stars") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "repo", "stars"`, arrival_del_sql: `DELETE FROM "star_row" WHERE ("repo", "stars") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "repo", "stars"`, boundary_sql: `SELECT CASE WHEN json_valid("repo") AND json_type("repo") = 'object' AND json_type("repo", '$.fn') = 'text' AND json_type("repo", '$.args') = 'array' THEN json_extract("repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("repo", '$.args')), '') || ')' ELSE "repo" END AS "repo", "stars", "_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_star_row" WHERE "_sign" IN (-1, 1) GROUP BY "repo", "stars", "_sign"`, rule_observers: ["stat/4"] },
  { rel: "stat", kind: "set", table_name: "stat", delta_table_name: "__delta_stat", frontier_table_name: "__frontier_stat", next_frontier_table_name: "__next_frontier_stat", columns: ["repo", "col2", "col3", "col4"], column_types: ["text", "int", "int", "int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid("repo") AND json_type("repo") = 'object' AND json_type("repo", '$.fn') = 'text' AND json_type("repo", '$.args') = 'array' THEN json_extract("repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("repo", '$.args')), '') || ')' ELSE "repo" END AS "repo", "col2", "col3", "col4", "_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_stat" WHERE "_sign" IN (-1, 1) GROUP BY "repo", "col2", "col3", "col4", "_sign"`, rule_observers: [] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "stat", rule_id: "aggregate_count_min_max_track_arrivals_and_retraction:stat/4#1", head_delta_table_name: "__delta_stat", head_columns: ["repo", "col2", "col3", "col4"], insert_sql: null, select_sql: `SELECT "repo", "col2", "col3", "col4" FROM "stat"`, recompute_sql: `DELETE FROM "stat";
INSERT OR IGNORE INTO "stat" ("repo", "col2", "col3", "col4") SELECT b0."repo", count(*), min(b0."stars"), max(b0."stars") FROM "star_row" b0 GROUP BY b0."repo" HAVING count(*) > 0`, support_sql: null, expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: { scope_clear_sql: `DELETE FROM "__agg_scope_stat"`, scope_seed_sql: [`INSERT OR IGNORE INTO "__agg_scope_stat" ("repo") SELECT DISTINCT d0."repo" FROM "__delta_star_row" d0 WHERE d0."_sign" IN (-1, 1)`], delete_scoped_sql: `DELETE FROM "stat" WHERE ("repo") IN (SELECT "repo" FROM "__agg_scope_stat") RETURNING "repo", "col2", "col3", "col4"`, insert_scoped_sql: [`INSERT OR IGNORE INTO "stat" ("repo", "col2", "col3", "col4") SELECT b0."repo", count(*), min(b0."stars"), max(b0."stars") FROM "star_row" b0 WHERE (b0."repo") IN (SELECT "repo" FROM "__agg_scope_stat") GROUP BY b0."repo" HAVING count(*) > 0 RETURNING "repo", "col2", "col3", "col4"`], delta_maintained: false } },
];

function recompute_levels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "stat";
INSERT OR IGNORE INTO "stat" ("repo", "col2", "col3", "col4") SELECT b0."repo", count(*), min(b0."stars"), max(b0."stars") FROM "star_row" b0 GROUP BY b0."repo" HAVING count(*) > 0`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function build_deltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const star_row = multiset_diff(before.star_row, after.star_row);
  const stat = multiset_diff(before.stat, after.stat);
  return {
    rels: [
      { rel: "star_row", add: star_row.add, del: star_row.del },
      { rel: "stat", add: stat.add, del: stat.del },
    ],
    carry_pending: false,
  };
}

function run_naive_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return read_snapshot(seam).pipe(
    concatMap((before) => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; return before; }))),
    concatMap((before) => apply_arrivals(seam, arrivals).pipe(map(() => before))),
  ).pipe(
    concatMap((before) => recompute_levels(seam).pipe(map(() => before))),
    concatMap((before) => read_snapshot(seam).pipe(map((after) => build_deltas(before, after)))),
  );
  // aggregate_count_min_max_track_arrivals_and_retraction: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = EMITTER_MODE;
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return run_naive_tick(seam, arrivals);
  }
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "aggregate_count_min_max_track_arrivals_and_retraction",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
