// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: sum_min_max_group_by_plain_columns.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { EnumPlane } from "../runtime/enumPlane.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IEnumRefColumns,
  IEnumTypePlan,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly rel_physical_names: Record<string, string>; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const ENUM_TYPES: readonly IEnumTypePlan[] = [];
export const ENUM_REF_COLUMNS: IEnumRefColumns = {};

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "star_row": [true, false],
    "stat": [true, false, false, false],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("__id" INTEGER PRIMARY KEY, "repo" INTEGER NOT NULL, "stars" INTEGER NOT NULL, UNIQUE ("repo", "stars"))`,
  `CREATE TEMP VIEW "__txt_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."repo") AS "repo", t."stars" AS "stars" FROM "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" t`,
  `CREATE TABLE "sum_min_max_group_by_plain_columns_stat" ("__id" INTEGER PRIMARY KEY, "repo" INTEGER NOT NULL, "col2" INTEGER NOT NULL, "col3" INTEGER NOT NULL, "col4" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("repo", "col2", "col3", "col4"))`,
  `CREATE TEMP VIEW "__txt_sum_min_max_group_by_plain_columns_stat" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."repo") AS "repo", t."col2" AS "col2", t."col3" AS "col3", t."col4" AS "col4", t."__refcount" AS "__refcount" FROM "sum_min_max_group_by_plain_columns_stat" t`,
  `CREATE TEMP TABLE "__delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "stars" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d_sign" ON "__delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("_sign")`,
  `CREATE INDEX "__delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d_group" ON "__delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("repo", "stars")`,
  `CREATE TEMP TABLE "__frontier_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "stars" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d_phase" ON "__frontier_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "stars" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."repo") AS "repo", t."stars" AS "stars", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" t`,
  `CREATE TEMP TABLE "__delta_sum_min_max_group_by_plain_columns_stat" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "col2" INTEGER NOT NULL, "col3" INTEGER NOT NULL, "col4" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_sum_min_max_group_by_plain_columns_stat_sign" ON "__delta_sum_min_max_group_by_plain_columns_stat" ("_sign")`,
  `CREATE INDEX "__delta_sum_min_max_group_by_plain_columns_stat_group" ON "__delta_sum_min_max_group_by_plain_columns_stat" ("repo", "col2", "col3", "col4")`,
  `CREATE TEMP TABLE "__frontier_sum_min_max_group_by_plain_columns_stat" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "col2" INTEGER NOT NULL, "col3" INTEGER NOT NULL, "col4" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_sum_min_max_group_by_plain_columns_stat_phase" ON "__frontier_sum_min_max_group_by_plain_columns_stat" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_sum_min_max_group_by_plain_columns_stat" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo" INTEGER NOT NULL, "col2" INTEGER NOT NULL, "col3" INTEGER NOT NULL, "col4" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_sum_min_max_group_by_plain_columns_stat" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."repo") AS "repo", t."col2" AS "col2", t."col3" AS "col3", t."col4" AS "col4", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_sum_min_max_group_by_plain_columns_stat" t`,
  `CREATE TEMP TABLE "__agg_scope_sum_min_max_group_by_plain_columns_stat" ("repo" INTEGER NOT NULL, PRIMARY KEY ("repo")) WITHOUT ROWID`,
];

const rel_columns: Record<string, readonly string[]> = {
  star_row: ["repo", "stars"],
  stat: ["repo", "col2", "col3", "col4"],
};

const rel_physical_names: Record<string, string> = {
  star_row: "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d",
  stat: "sum_min_max_group_by_plain_columns_stat",
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  star_row: ["text", "int"],
  stat: ["text", "int", "int", "int"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  star_row: ["text", "int"],
  stat: ["text", "int", "int", "int"],
};

const rel_catalog: readonly IRelCatalogRow[] = new Array<IRelCatalogRow>(
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "sum_min_max_group_by_plain_columns", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "cf405352c9ac95e1", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "star_row", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "68c99c59435ad7ed", h_schema: "903cbf6daedddd73", h_rule: "" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "repo", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "f9109e46774ef86a", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 8, ordinal: 2, local_name: "stars", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "155938d3c6d51fb4", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 7, ordinal: 0, local_name: "stat", kind: "rel", type_id: 0, arity: 4, module_id: 7, h_id: "51458ea9a88f1319", h_schema: "f419701620e4d50b", h_rule: "3f7e18580fff25ad" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "repo", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "42ced8a36704195d", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 11, ordinal: 2, local_name: "col2", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "ee98f06650abd065", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 11, ordinal: 3, local_name: "col3", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "598800666a7df7c4", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 11, ordinal: 4, local_name: "col4", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "ca6708297766fcf7", h_schema: "", h_rule: "" },
  { rel_id: 16, parent_id: 8, ordinal: 0, local_name: "__delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "aef0bab4e1611d31", h_schema: "ca132724762dc791", h_rule: "" },
  { rel_id: 17, parent_id: 8, ordinal: 0, local_name: "__frontier_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "10e8705dfbb6e309", h_schema: "3a20e46913c99c26", h_rule: "" },
  { rel_id: 18, parent_id: 8, ordinal: 0, local_name: "__next_frontier_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "e4f1da01abf4973c", h_schema: "3a20e46913c99c26", h_rule: "" },
  { rel_id: 19, parent_id: 8, ordinal: 0, local_name: "__txt_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "c240a8c84906bbdf", h_schema: "903cbf6daedddd73", h_rule: "" },
  { rel_id: 20, parent_id: 16, ordinal: 0, local_name: "__txt___delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "d030aabc3a48cd14", h_schema: "903cbf6daedddd73", h_rule: "" },
  { rel_id: 21, parent_id: 11, ordinal: 0, local_name: "__delta_sum_min_max_group_by_plain_columns_stat", kind: "delta", type_id: 0, arity: 6, module_id: 7, h_id: "359baed6ee762d37", h_schema: "271cc33e11442276", h_rule: "" },
  { rel_id: 22, parent_id: 11, ordinal: 0, local_name: "__frontier_sum_min_max_group_by_plain_columns_stat", kind: "frontier", type_id: 0, arity: 6, module_id: 7, h_id: "09cb2d83ded31f4c", h_schema: "792e1fd15da48c0f", h_rule: "" },
  { rel_id: 23, parent_id: 11, ordinal: 0, local_name: "__next_frontier_sum_min_max_group_by_plain_columns_stat", kind: "next_frontier", type_id: 0, arity: 6, module_id: 7, h_id: "f76daa21ac6e419a", h_schema: "792e1fd15da48c0f", h_rule: "" },
  { rel_id: 24, parent_id: 11, ordinal: 0, local_name: "__txt_sum_min_max_group_by_plain_columns_stat", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "2089e7257b394677", h_schema: "f419701620e4d50b", h_rule: "" },
  { rel_id: 25, parent_id: 21, ordinal: 0, local_name: "__txt___delta_sum_min_max_group_by_plain_columns_stat", kind: "view", type_id: 0, arity: 6, module_id: 7, h_id: "311d5191f9c94636", h_schema: "f419701620e4d50b", h_rule: "" },
  { rel_id: 26, parent_id: 7, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 7, h_id: "edfba325b75a60f4", h_schema: "", h_rule: "" },
  { rel_id: 27, parent_id: 11, ordinal: 0, local_name: "__agg_scope_sum_min_max_group_by_plain_columns_stat", kind: "scope", type_id: 0, arity: 1, module_id: 7, h_id: "4ea60a1b39a89f21", h_schema: "3a9d7092905caf86", h_rule: "" },
  { rel_id: 28, parent_id: 9, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "f92abfd6e887b8fb", h_schema: "", h_rule: "" },
  { rel_id: 29, parent_id: 10, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "00ac20d280cdeec3", h_schema: "", h_rule: "" },
  { rel_id: 30, parent_id: 12, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "e0eda12539209c67", h_schema: "", h_rule: "" },
  { rel_id: 31, parent_id: 13, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "4c30ed137cf6eddc", h_schema: "", h_rule: "" },
  { rel_id: 32, parent_id: 14, ordinal: 3, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "b1bf978dec8a7a15", h_schema: "", h_rule: "" },
  { rel_id: 33, parent_id: 15, ordinal: 4, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "9a2457a47e9ef1fc", h_schema: "", h_rule: "" },
);

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["star_row"];

const boot: readonly IBootStatement[] = [
  { rel: "star_row", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["cli"] },
  { rel: "star_row", sql: `INSERT OR IGNORE INTO "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("repo", "stars") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["cli", 4] },
  { rel: "star_row", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["cli"] },
  { rel: "star_row", sql: `INSERT OR IGNORE INTO "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("repo", "stars") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["cli", 10] },
  { rel: "star_row", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["shell"] },
  { rel: "star_row", sql: `INSERT OR IGNORE INTO "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("repo", "stars") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["shell", 7] },
  { rel: "stat", sql: `DELETE FROM "sum_min_max_group_by_plain_columns_stat"`, params: [] },
  { rel: "stat", sql: `INSERT OR IGNORE INTO "sum_min_max_group_by_plain_columns_stat" ("repo", "col2", "col3", "col4") SELECT b0."repo", sum(b0."stars"), min(b0."stars"), max(b0."stars") FROM "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" b0 GROUP BY b0."repo" HAVING count(*) > 0`, params: [] },
];

const final_select: Record<string, string> = {
  star_row: `SELECT CASE WHEN json_valid(t."repo") AND json_type(t."repo") = 'object' AND json_type(t."repo", '$.fn') = 'text' AND json_type(t."repo", '$.args') = 'array' THEN json_extract(t."repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."repo", '$.args')), '') || ')' ELSE t."repo" END AS "repo", t."stars" FROM "__txt_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" t`,
  stat: `SELECT CASE WHEN json_valid(t."repo") AND json_type(t."repo") = 'object' AND json_type(t."repo", '$.fn') = 'text' AND json_type(t."repo", '$.args') = 'array' THEN json_extract(t."repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."repo", '$.args')), '') || ')' ELSE t."repo" END AS "repo", t."col2", t."col3", t."col4" FROM "__txt_sum_min_max_group_by_plain_columns_stat" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "star_row", kind: "set", table_name: "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d", delta_table_name: "__delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d", frontier_table_name: "__frontier_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d", next_frontier_table_name: "__next_frontier_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d", columns: ["repo", "stars"], column_types: ["text", "int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" ("repo", "stars") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "repo", "stars"`, arrival_del_sql: `DELETE FROM "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" WHERE ("repo", "stars") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "repo", "stars"`, boundary_sql: `SELECT CASE WHEN json_valid(t."repo") AND json_type(t."repo") = 'object' AND json_type(t."repo", '$.fn') = 'text' AND json_type(t."repo", '$.args') = 'array' THEN json_extract(t."repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."repo", '$.args')), '') || ')' ELSE t."repo" END AS "repo", t."stars", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" t WHERE t."_sign" IN (-1, 1) GROUP BY t."repo", t."stars", t."_sign"`, rule_observers: ["stat/4"] },
  { rel: "stat", kind: "set", table_name: "sum_min_max_group_by_plain_columns_stat", delta_table_name: "__delta_sum_min_max_group_by_plain_columns_stat", frontier_table_name: "__frontier_sum_min_max_group_by_plain_columns_stat", next_frontier_table_name: "__next_frontier_sum_min_max_group_by_plain_columns_stat", columns: ["repo", "col2", "col3", "col4"], column_types: ["text", "int", "int", "int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."repo") AND json_type(t."repo") = 'object' AND json_type(t."repo", '$.fn') = 'text' AND json_type(t."repo", '$.args') = 'array' THEN json_extract(t."repo", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."repo", '$.args')), '') || ')' ELSE t."repo" END AS "repo", t."col2", t."col3", t."col4", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_sum_min_max_group_by_plain_columns_stat" t WHERE t."_sign" IN (-1, 1) GROUP BY t."repo", t."col2", t."col3", t."col4", t."_sign"`, rule_observers: [] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "stat", rule_id: "sum_min_max_group_by_plain_columns:stat/4#1", head_delta_table_name: "__delta_sum_min_max_group_by_plain_columns_stat", head_columns: ["repo", "col2", "col3", "col4"], insert_sql: null, select_sql: `SELECT "repo", "col2", "col3", "col4" FROM "sum_min_max_group_by_plain_columns_stat"`, recompute_sql: `DELETE FROM "sum_min_max_group_by_plain_columns_stat";
INSERT OR IGNORE INTO "sum_min_max_group_by_plain_columns_stat" ("repo", "col2", "col3", "col4") SELECT b0."repo", sum(b0."stars"), min(b0."stars"), max(b0."stars") FROM "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" b0 GROUP BY b0."repo" HAVING count(*) > 0`, support_sql: null, expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: { scope_clear_sql: `DELETE FROM "__agg_scope_sum_min_max_group_by_plain_columns_stat"`, scope_seed_sql: [`INSERT OR IGNORE INTO "__agg_scope_sum_min_max_group_by_plain_columns_stat" ("repo") SELECT DISTINCT d0."repo" FROM "__delta_sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" d0 WHERE d0."_sign" IN (-1, 1)`], delete_scoped_sql: `DELETE FROM "sum_min_max_group_by_plain_columns_stat" WHERE ("repo") IN (SELECT "repo" FROM "__agg_scope_sum_min_max_group_by_plain_columns_stat") RETURNING "repo", "col2", "col3", "col4"`, insert_scoped_sql: [`INSERT OR IGNORE INTO "sum_min_max_group_by_plain_columns_stat" ("repo", "col2", "col3", "col4") SELECT b0."repo", sum(b0."stars"), min(b0."stars"), max(b0."stars") FROM "sum_min_max_group_by_plain_columns_star_row_6a98371bc62d" b0 WHERE (b0."repo") IN (SELECT "repo" FROM "__agg_scope_sum_min_max_group_by_plain_columns_stat") GROUP BY b0."repo" HAVING count(*) > 0 RETURNING "repo", "col2", "col3", "col4"`], delta_maintained: false } },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      concatMap((carry_pending) => EnumPlane.decode_deltas(seam, ENUM_TYPES, ENUM_REF_COLUMNS, SUBSCRIBED_RELATIONS, rels).pipe(
        map((decoded): ITickDeltas => ({ rels: decoded, carry_pending })),
      )),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return EnumPlane.intern(seam, ENUM_TYPES, ENUM_REF_COLUMNS, arrivals).pipe(
    concatMap((normalized) => run_incremental_tick(seam, validate_arrivals(normalized))),
  );
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "sum_min_max_group_by_plain_columns",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_physical_names,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  enum_types: ENUM_TYPES,
  enum_ref_columns: ENUM_REF_COLUMNS,
  unsupported_execution,
  tick: run_tick,
};
