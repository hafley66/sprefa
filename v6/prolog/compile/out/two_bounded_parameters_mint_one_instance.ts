// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: two_bounded_parameters_mint_one_instance.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { StructPlane } from "../runtime/structPlane.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IStructRefColumns,
  IStructTypePlan,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    throw new Error("a list value reached a SQL parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const STRUCT_TYPES: readonly IStructTypePlan[] = [
  { name: "__gen__span_int_text_e5126de851365aff", columns: ["start", "label"], refs: [null, null], key_indices: [0, 1], conflict_sql: `SELECT i.value AS "__requested", json_array(t."start", t."label") AS "__stored" FROM json_each(?) i JOIN "__gen__span_int_text_e5126de851365aff" t ON t."start" = json_extract(i.value, '$[0]') AND t."label" = json_extract(i.value, '$[1]') WHERE json_array(t."start", t."label") <> i.value`, intern_sql: `INSERT OR IGNORE INTO "__gen__span_int_text_e5126de851365aff" ("start", "label") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)`, lookup_sql: `SELECT i.value AS "__lookup", t."__id", json_array(t."start", t."label") AS "__stored" FROM json_each(?) i JOIN "__gen__span_int_text_e5126de851365aff" t ON t."start" = json_extract(i.value, '$[0]') AND t."label" = json_extract(i.value, '$[1]')` },
];

export const STRUCT_REF_COLUMNS: IStructRefColumns = {
  "carry": [null, "__gen__span_int_text_e5126de851365aff"],
  "marker": [null, "__gen__span_int_text_e5126de851365aff"],
};

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "__gen__span_int_text_e5126de851365aff": [false, true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "__gen__span_int_text_e5126de851365aff" ("__id" INTEGER PRIMARY KEY, "start" INTEGER NOT NULL, "label" INTEGER NOT NULL, UNIQUE ("start", "label"))`,
  `CREATE TEMP VIEW "__ref___gen__span_int_text_e5126de851365aff" AS SELECT t."__id", "start", "label", json_object('start', t."start", 'label', (SELECT s."content" FROM "__str" s WHERE s."__id" = t."label")) AS "__rendered" FROM "__gen__span_int_text_e5126de851365aff" t`,
  `CREATE TEMP VIEW "__txt___gen__span_int_text_e5126de851365aff" AS SELECT t."start" AS "start", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."label") AS "label", t."__id" AS "__id" FROM "__gen__span_int_text_e5126de851365aff" t`,
  `CREATE TABLE "carry" ("__id" INTEGER PRIMARY KEY, "id" INTEGER NOT NULL, "extent" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("id", "extent"))`,
  `CREATE TABLE "marker" ("__id" INTEGER PRIMARY KEY, "id" INTEGER NOT NULL, "extent" INTEGER NOT NULL, UNIQUE ("id"))`,
  `CREATE TEMP TABLE "__delta___gen__span_int_text_e5126de851365aff" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "start" INTEGER NOT NULL, "label" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta___gen__span_int_text_e5126de851365aff_sign" ON "__delta___gen__span_int_text_e5126de851365aff" ("_sign")`,
  `CREATE INDEX "__delta___gen__span_int_text_e5126de851365aff_group" ON "__delta___gen__span_int_text_e5126de851365aff" ("start", "label")`,
  `CREATE TEMP TABLE "__frontier___gen__span_int_text_e5126de851365aff" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "start" INTEGER NOT NULL, "label" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier___gen__span_int_text_e5126de851365aff_phase" ON "__frontier___gen__span_int_text_e5126de851365aff" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier___gen__span_int_text_e5126de851365aff" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "start" INTEGER NOT NULL, "label" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta___gen__span_int_text_e5126de851365aff" AS SELECT t."start" AS "start", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."label") AS "label", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta___gen__span_int_text_e5126de851365aff" t`,
  `CREATE TEMP TABLE "__delta_carry" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "extent" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_carry_sign" ON "__delta_carry" ("_sign")`,
  `CREATE INDEX "__delta_carry_group" ON "__delta_carry" ("id", "extent")`,
  `CREATE TEMP TABLE "__frontier_carry" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "extent" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_carry_phase" ON "__frontier_carry" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_carry" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "extent" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_marker" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "extent" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_marker_sign" ON "__delta_marker" ("_sign")`,
  `CREATE INDEX "__delta_marker_group" ON "__delta_marker" ("id", "extent")`,
  `CREATE TEMP TABLE "__frontier_marker" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "extent" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_marker_phase" ON "__frontier_marker" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_marker" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "extent" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__support_next_carry" ("id" INTEGER NOT NULL, "extent" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("id", "extent")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_carry" ("id" INTEGER NOT NULL, "extent" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "carry_zero" ON "carry" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  __gen__span_int_text_e5126de851365aff: ["start", "label"],
  carry: ["id", "extent"],
  marker: ["id", "extent"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  __gen__span_int_text_e5126de851365aff: ["int", "text"],
  carry: ["int", "ref"],
  marker: ["int", "ref"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "two_bounded_parameters_mint_one_instance", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "fab8dbc66384e671", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "__gen__span_int_text_e5126de851365aff", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "46396f78b28446e2", h_schema: "1802e1918b35216d", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "start", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "b2acb9cf95ac2086", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 7, ordinal: 2, local_name: "label", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "f895ab75c2f16e61", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 6, ordinal: 0, local_name: "carry", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "d976caa6edd82021", h_schema: "4980b6c81fc87ebb", h_rule: "6b3ccdada75b025b" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "56d0c07dbc63e166", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 10, ordinal: 2, local_name: "extent", kind: "column", type_id: 7, arity: 0, module_id: 6, h_id: "5950ed32b7c25725", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 6, ordinal: 0, local_name: "marker", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "77577e252e9ce643", h_schema: "f6b4346353558fff", h_rule: "" },
  { rel_id: 14, parent_id: 13, ordinal: 1, local_name: "id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "f11aef135fdfbe74", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 13, ordinal: 2, local_name: "extent", kind: "column", type_id: 7, arity: 0, module_id: 6, h_id: "3f6aecc7a00fa746", h_schema: "", h_rule: "" },
  { rel_id: 16, parent_id: 6, ordinal: 0, local_name: "json_encodable", kind: "interface", type_id: 0, arity: 0, module_id: 6, h_id: "", h_schema: "decl:interface:json_encodable", h_rule: "" },
  { rel_id: 17, parent_id: 6, ordinal: 0, local_name: "span", kind: "generic_rel", type_id: 0, arity: 0, module_id: 6, h_id: "", h_schema: "decl:relation:span", h_rule: "" },
  { rel_id: 18, parent_id: 17, ordinal: 1, local_name: "Start", kind: "type_parameter", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "decl:relation:span:param:1:Start", h_rule: "" },
  { rel_id: 19, parent_id: 18, ordinal: 1, local_name: "json_encodable", kind: "constraint", type_id: 16, arity: 0, module_id: 0, h_id: "", h_schema: "decl:relation:span:param:1:Start:constraint:decl:interface:json_encodable", h_rule: "" },
  { rel_id: 20, parent_id: 17, ordinal: 2, local_name: "Label", kind: "type_parameter", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "decl:relation:span:param:2:Label", h_rule: "" },
  { rel_id: 21, parent_id: 20, ordinal: 1, local_name: "json_encodable", kind: "constraint", type_id: 16, arity: 0, module_id: 0, h_id: "", h_schema: "decl:relation:span:param:2:Label:constraint:decl:interface:json_encodable", h_rule: "" },
  { rel_id: 22, parent_id: 17, ordinal: 1, local_name: "start", kind: "generic_column", type_id: 18, arity: 0, module_id: 0, h_id: "", h_schema: "decl:relation:span:member:1:start", h_rule: "" },
  { rel_id: 23, parent_id: 7, ordinal: 0, local_name: "__gen__span_int_text_e5126de851365aff", kind: "concrete_type", type_id: 17, arity: 0, module_id: 0, h_id: "", h_schema: "decl:relation:__gen__span_int_text_e5126de851365aff", h_rule: "" },
  { rel_id: 24, parent_id: 23, ordinal: 1, local_name: "argument", kind: "type_argument", type_id: 2, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 23, ordinal: 2, local_name: "argument", kind: "type_argument", type_id: 1, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 7, ordinal: 0, local_name: "__delta___gen__span_int_text_e5126de851365aff", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "01fcc1c7e3f84f88", h_schema: "ba2bff6b9a09d462", h_rule: "" },
  { rel_id: 27, parent_id: 7, ordinal: 0, local_name: "__frontier___gen__span_int_text_e5126de851365aff", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "57dd29837a0f4a0c", h_schema: "5f201970064ae2bd", h_rule: "" },
  { rel_id: 28, parent_id: 7, ordinal: 0, local_name: "__next_frontier___gen__span_int_text_e5126de851365aff", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "60ae80beaa6860f7", h_schema: "5f201970064ae2bd", h_rule: "" },
  { rel_id: 29, parent_id: 7, ordinal: 0, local_name: "__txt___gen__span_int_text_e5126de851365aff", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "8f8c437aa9559112", h_schema: "1802e1918b35216d", h_rule: "" },
  { rel_id: 30, parent_id: 26, ordinal: 0, local_name: "__txt___delta___gen__span_int_text_e5126de851365aff", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "cac963aedef87423", h_schema: "1802e1918b35216d", h_rule: "" },
  { rel_id: 31, parent_id: 10, ordinal: 0, local_name: "__delta_carry", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "cfa500cc70baaeec", h_schema: "06cefb475c1a00d8", h_rule: "" },
  { rel_id: 32, parent_id: 10, ordinal: 0, local_name: "__frontier_carry", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "0d9510c37de98d88", h_schema: "0d07506d1e252281", h_rule: "" },
  { rel_id: 33, parent_id: 10, ordinal: 0, local_name: "__next_frontier_carry", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "ec442c3c5eca3764", h_schema: "0d07506d1e252281", h_rule: "" },
  { rel_id: 34, parent_id: 13, ordinal: 0, local_name: "__delta_marker", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "f6440bb56d59ddec", h_schema: "06cefb475c1a00d8", h_rule: "" },
  { rel_id: 35, parent_id: 13, ordinal: 0, local_name: "__frontier_marker", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "e54291782be2d71b", h_schema: "0d07506d1e252281", h_rule: "" },
  { rel_id: 36, parent_id: 13, ordinal: 0, local_name: "__next_frontier_marker", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "ed536673ed183961", h_schema: "0d07506d1e252281", h_rule: "" },
  { rel_id: 37, parent_id: 6, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 6, h_id: "e84ea870d8db1063", h_schema: "", h_rule: "" },
  { rel_id: 38, parent_id: 6, ordinal: 0, local_name: "__ref___gen__span_int_text_e5126de851365aff", kind: "dictionary", type_id: 7, arity: 4, module_id: 6, h_id: "5e13e673ebbf979c", h_schema: "1802e1918b35216d", h_rule: "" },
  { rel_id: 39, parent_id: 10, ordinal: 0, local_name: "__support_next_carry", kind: "refcount", type_id: 0, arity: 3, module_id: 6, h_id: "637736f221e56ccb", h_schema: "", h_rule: "" },
  { rel_id: 40, parent_id: 10, ordinal: 0, local_name: "__new_carry", kind: "refcount_staging", type_id: 0, arity: 3, module_id: 6, h_id: "59bdf525761887a8", h_schema: "", h_rule: "" },
  { rel_id: 41, parent_id: 8, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "e4479d683dd4c65a", h_schema: "", h_rule: "" },
  { rel_id: 42, parent_id: 9, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "ae6e71a8dd76f519", h_schema: "", h_rule: "" },
  { rel_id: 43, parent_id: 11, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "dc29d5e10b5c092f", h_schema: "", h_rule: "" },
  { rel_id: 44, parent_id: 12, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "d9458b6805b0f64b", h_schema: "", h_rule: "" },
  { rel_id: 45, parent_id: 14, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "2d001837812d6f94", h_schema: "", h_rule: "" },
  { rel_id: 46, parent_id: 15, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "6416d3730529b859", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
  __gen__span_int_text_e5126de851365aff: ["int", "text"],
  marker: ["int", "other"],
};

const arrival_targets: readonly string[] = ["__gen__span_int_text_e5126de851365aff", "marker"];

const boot: readonly IBootStatement[] = [
  { rel: "carry", sql: `DELETE FROM "carry"`, params: [] },
  { rel: "carry", sql: `INSERT OR IGNORE INTO "carry" ("id", "extent") SELECT b0."id", b0."extent" FROM "marker" b0`, params: [] },
];

type Snapshot = {
  readonly __gen__span_int_text_e5126de851365aff: readonly IRow[];
  readonly carry: readonly IRow[];
  readonly marker: readonly IRow[];
};

function read_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    __gen__span_int_text_e5126de851365aff: select_rows(seam, `SELECT t."start", CASE WHEN json_valid(t."label") AND json_type(t."label") = 'object' AND json_type(t."label", '$.fn') = 'text' AND json_type(t."label", '$.args') = 'array' THEN json_extract(t."label", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."label", '$.args')), '') || ')' ELSE t."label" END AS "label" FROM "__txt___gen__span_int_text_e5126de851365aff" t`, rel_columns.__gen__span_int_text_e5126de851365aff!, rel_column_types.__gen__span_int_text_e5126de851365aff!),
    carry: select_rows(seam, `SELECT t."id", (SELECT d."__rendered" FROM "__ref___gen__span_int_text_e5126de851365aff" d WHERE d."__id" = t."extent") AS "extent" FROM "carry" t`, rel_columns.carry!, rel_column_types.carry!),
    marker: select_rows(seam, `SELECT t."id", (SELECT d."__rendered" FROM "__ref___gen__span_int_text_e5126de851365aff" d WHERE d."__id" = t."extent") AS "extent" FROM "marker" t`, rel_columns.marker!, rel_column_types.marker!),
  });
}

type Snapshots = { readonly decoded: Snapshot; readonly stored: Snapshot };

function read_stored_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    __gen__span_int_text_e5126de851365aff: select_rows(seam, `SELECT "start", "label" FROM "__gen__span_int_text_e5126de851365aff"`, rel_columns.__gen__span_int_text_e5126de851365aff!, rel_column_types.__gen__span_int_text_e5126de851365aff!),
    carry: select_rows(seam, `SELECT "id", "extent" FROM "carry"`, rel_columns.carry!, rel_column_types.carry!),
    marker: select_rows(seam, `SELECT "id", "extent" FROM "marker"`, rel_columns.marker!, rel_column_types.marker!),
  });
}

function read_snapshots(seam: ISqlSeam): Observable<Snapshots> {
  return forkJoin({ decoded: read_snapshot(seam), stored: read_stored_snapshot(seam) });
}

const final_select: Record<string, string> = {
  __gen__span_int_text_e5126de851365aff: `SELECT t."start", CASE WHEN json_valid(t."label") AND json_type(t."label") = 'object' AND json_type(t."label", '$.fn') = 'text' AND json_type(t."label", '$.args') = 'array' THEN json_extract(t."label", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."label", '$.args')), '') || ')' ELSE t."label" END AS "label" FROM "__txt___gen__span_int_text_e5126de851365aff" t`,
  carry: `SELECT t."id", (SELECT d."__rendered" FROM "__ref___gen__span_int_text_e5126de851365aff" d WHERE d."__id" = t."extent") AS "extent" FROM "carry" t`,
  marker: `SELECT t."id", (SELECT d."__rendered" FROM "__ref___gen__span_int_text_e5126de851365aff" d WHERE d."__id" = t."extent") AS "extent" FROM "marker" t`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; add_sql: string; del_sql: string | null }> = {
  __gen__span_int_text_e5126de851365aff: { kind: "set", add_sql: `INSERT OR IGNORE INTO "__gen__span_int_text_e5126de851365aff" ("start", "label") VALUES (?, ?)`, del_sql: `DELETE FROM "__gen__span_int_text_e5126de851365aff" WHERE "start" = ? AND "label" = ?` },
  marker: { kind: "set", add_sql: `INSERT INTO "marker" ("id", "extent") VALUES (?, ?) ON CONFLICT ("id") DO UPDATE SET "extent" = excluded."extent"`, del_sql: `DELETE FROM "marker" WHERE "id" = ? AND "extent" = ?` },
};

function arrival_statement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`two_bounded_parameters_mint_one_instance: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`two_bounded_parameters_mint_one_instance: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.del_sql === null) {
      throw new Error(`two_bounded_parameters_mint_one_instance: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.del_sql, args: bind_args(arrival.row) };
  }
  return { sql: template.add_sql, args: bind_args(arrival.row) };
}

function apply_arrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrival_statement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "__gen__span_int_text_e5126de851365aff", kind: "set", table_name: "__gen__span_int_text_e5126de851365aff", delta_table_name: "__delta___gen__span_int_text_e5126de851365aff", frontier_table_name: "__frontier___gen__span_int_text_e5126de851365aff", next_frontier_table_name: "__next_frontier___gen__span_int_text_e5126de851365aff", columns: ["start", "label"], column_types: ["int", "text"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "__gen__span_int_text_e5126de851365aff" ("start", "label") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "start", "label"`, arrival_del_sql: `DELETE FROM "__gen__span_int_text_e5126de851365aff" WHERE ("start", "label") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "start", "label"`, boundary_sql: `SELECT t."start", CASE WHEN json_valid(t."label") AND json_type(t."label") = 'object' AND json_type(t."label", '$.fn') = 'text' AND json_type(t."label", '$.args') = 'array' THEN json_extract(t."label", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."label", '$.args')), '') || ')' ELSE t."label" END AS "label", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta___gen__span_int_text_e5126de851365aff" t WHERE t."_sign" IN (-1, 1) GROUP BY t."start", t."label", t."_sign"`, rule_observers: [] },
  { rel: "carry", kind: "set", table_name: "carry", delta_table_name: "__delta_carry", frontier_table_name: "__frontier_carry", next_frontier_table_name: "__next_frontier_carry", columns: ["id", "extent"], column_types: ["int", "ref"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT t."id", (SELECT d."__rendered" FROM "__ref___gen__span_int_text_e5126de851365aff" d WHERE d."__id" = t."extent") AS "extent", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_carry" t WHERE t."_sign" IN (-1, 1) GROUP BY t."id", t."extent", t."_sign"`, rule_observers: [] },
  { rel: "marker", kind: "set", table_name: "marker", delta_table_name: "__delta_marker", frontier_table_name: "__frontier_marker", next_frontier_table_name: "__next_frontier_marker", columns: ["id", "extent"], column_types: ["int", "ref"], key_indices: [0], arrival_add_sql: `INSERT INTO "marker" ("id", "extent") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) WHERE true ON CONFLICT ("id") DO UPDATE SET "extent" = excluded."extent" RETURNING "id", "extent"`, arrival_del_sql: `DELETE FROM "marker" WHERE ("id", "extent") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "id", "extent"`, boundary_sql: `SELECT t."id", (SELECT d."__rendered" FROM "__ref___gen__span_int_text_e5126de851365aff" d WHERE d."__id" = t."extent") AS "extent", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_marker" t WHERE t."_sign" IN (-1, 1) GROUP BY t."id", t."extent", t."_sign"`, rule_observers: ["carry/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "carry", rule_id: "two_bounded_parameters_mint_one_instance:carry/2#1", head_delta_table_name: "__delta_carry", head_columns: ["id", "extent"], insert_sql: `INSERT OR IGNORE INTO "carry" ("id", "extent") SELECT DISTINCT d0."id", d0."extent" FROM "__frontier_marker" d0 WHERE d0."_phase" >= 0 RETURNING "id", "extent"`, select_sql: `SELECT "id", "extent" FROM "carry"`, recompute_sql: `DELETE FROM "carry";
INSERT OR IGNORE INTO "carry" ("id", "extent") SELECT b0."id", b0."extent" FROM "marker" b0`, support_sql: [`DELETE FROM "__support_next_carry"`, `INSERT INTO "__support_next_carry" ("id", "extent", "__refcount") SELECT "id", "extent", sum("__refcount") FROM (SELECT b0."id" AS "id", b0."extent" AS "extent", count(*) AS "__refcount" FROM "marker" b0 GROUP BY b0."id", b0."extent") GROUP BY "id", "extent"`, `UPDATE "carry" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_carry" n WHERE n."id" = h."id" AND n."extent" = h."extent"), 0)`, `INSERT INTO "__delta_carry" ("_sign", "_sequence", "id", "extent") SELECT -1, row_number() OVER () - 1, "id", "extent" FROM "carry" WHERE "__refcount" <= 0`, `DELETE FROM "carry" WHERE "__refcount" <= 0`, `DELETE FROM "__new_carry"`, `INSERT INTO "__new_carry" ("id", "extent", "__refcount") SELECT n."id", n."extent", n."__refcount" FROM "__support_next_carry" n LEFT JOIN "carry" h ON n."id" = h."id" AND n."extent" = h."extent" WHERE h."id" IS NULL`, `INSERT INTO "__delta_carry" ("_sign", "_sequence", "id", "extent") SELECT 1, "rowid" - 1, "id", "extent" FROM "__new_carry"`, `INSERT INTO "__frontier_carry" ("_phase", "_sequence", "id", "extent") SELECT ?, "rowid" - 1, "id", "extent" FROM "__new_carry"`, `INSERT INTO "__next_frontier_carry" ("_phase", "_sequence", "id", "extent") SELECT ?, "rowid" - 1, "id", "extent" FROM "__new_carry"`, `INSERT OR IGNORE INTO "carry" ("id", "extent", "__refcount") SELECT n."id", n."extent", n."__refcount" FROM "__support_next_carry" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

function recompute_levels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "carry";
INSERT OR IGNORE INTO "carry" ("id", "extent") SELECT b0."id", b0."extent" FROM "marker" b0`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function build_deltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const __gen__span_int_text_e5126de851365aff = multiset_diff(before.__gen__span_int_text_e5126de851365aff, after.__gen__span_int_text_e5126de851365aff);
  const carry = multiset_diff(before.carry, after.carry);
  const marker = multiset_diff(before.marker, after.marker);
  return {
    rels: [
      { rel: "__gen__span_int_text_e5126de851365aff", add: __gen__span_int_text_e5126de851365aff.add, del: __gen__span_int_text_e5126de851365aff.del },
      { rel: "carry", add: carry.add, del: carry.del },
      { rel: "marker", add: marker.add, del: marker.del },
    ],
    carry_pending: false,
  };
}

function run_naive_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return read_snapshot(seam).pipe(
    concatMap((before) => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; return before; }))),
    concatMap((before) => StructPlane.intern(seam, STRUCT_TYPES, STRUCT_REF_COLUMNS, arrivals,
      (targets) => apply_arrivals(seam, targets), TEXT_INTERN_PLAN,
    ).pipe(map((normalized) => { arrivals = normalized; return before; }))),
    concatMap((before) => apply_arrivals(seam, arrivals).pipe(map(() => before))),
  ).pipe(
    concatMap((before) => recompute_levels(seam).pipe(map(() => before))),
    concatMap((before) => read_snapshot(seam).pipe(map((after) => build_deltas(before, after)))),
  );
  // two_bounded_parameters_mint_one_instance: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = EMITTER_MODE;
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => StructPlane.intern(seam, STRUCT_TYPES, STRUCT_REF_COLUMNS, arrivals,
      (targets) => IncrementalRuntime.apply_arrivals(seam, targets, SUBSCRIBED_RELATIONS), TEXT_INTERN_PLAN,
    ).pipe(map((normalized) => { arrivals = normalized; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return run_naive_tick(seam, arrivals);
  }
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "two_bounded_parameters_mint_one_instance",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
