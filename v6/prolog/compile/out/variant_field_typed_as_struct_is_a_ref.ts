// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: variant_field_typed_as_struct_is_a_ref.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { StructPlane } from "../runtime/structPlane.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowValue,
  ISqlSeam,
  IStructRefColumns,
  IStructTypePlan,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowValue[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowValue | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowValue[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => typeof value === "boolean" ? BigInt(value ? 1 : 0) : (typeof value === "number" && Number.isSafeInteger(value) ? BigInt(value) : value));
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const STRUCT_TYPES: readonly IStructTypePlan[] = [
  { name: "span", columns: ["lo", "hi"], refs: [null, null], key_indices: [0, 1], conflict_sql: `SELECT i.value AS "__requested", json_array(t."lo", t."hi") AS "__stored" FROM json_each(?) i JOIN "span" t ON t."lo" = json_extract(i.value, '$[0]') AND t."hi" = json_extract(i.value, '$[1]') WHERE json_array(t."lo", t."hi") <> i.value`, intern_sql: `INSERT OR IGNORE INTO "span" ("lo", "hi") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)`, lookup_sql: `SELECT i.value AS "__lookup", t."__id", json_array(t."lo", t."hi") AS "__stored" FROM json_each(?) i JOIN "span" t ON t."lo" = json_extract(i.value, '$[0]') AND t."hi" = json_extract(i.value, '$[1]')` },
];

export const STRUCT_REF_COLUMNS: IStructRefColumns = {
  "holder": ["span"],
  "loc_here": [null, "span"],
};

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "loc_elsewhere": [false, true],
    "loc_tag": [false, true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `INSERT OR IGNORE INTO "__str" ("content") VALUES ('elsewhere'), ('here')`,
  `CREATE TABLE "holder" ("__id" INTEGER PRIMARY KEY, "item" INTEGER NOT NULL, UNIQUE ("item"))`,
  `CREATE TABLE "loc_elsewhere" ("__id" INTEGER PRIMARY KEY, "id" INTEGER NOT NULL, "note" INTEGER NOT NULL, UNIQUE ("note"))`,
  `CREATE TEMP VIEW "__txt_loc_elsewhere" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."note") AS "note" FROM "loc_elsewhere" t`,
  `CREATE TABLE "loc_here" ("__id" INTEGER PRIMARY KEY, "id" INTEGER NOT NULL, "at" INTEGER NOT NULL, UNIQUE ("at"))`,
  `CREATE TABLE "loc_tag" ("__id" INTEGER PRIMARY KEY, "id" INTEGER NOT NULL, "tag" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("id", "tag"))`,
  `CREATE TEMP VIEW "__txt_loc_tag" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."tag") AS "tag", t."__refcount" AS "__refcount" FROM "loc_tag" t`,
  `CREATE TABLE "span" ("__id" INTEGER PRIMARY KEY, "lo" INTEGER NOT NULL, "hi" INTEGER NOT NULL, UNIQUE ("lo", "hi"))`,
  `CREATE TEMP VIEW "__ref_span" AS SELECT t."__id", "lo", "hi", json_object('lo', t."lo", 'hi', t."hi") AS "__rendered" FROM "span" t`,
  `CREATE TEMP TABLE "__delta_holder" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_holder_sign" ON "__delta_holder" ("_sign")`,
  `CREATE INDEX "__delta_holder_group" ON "__delta_holder" ("item")`,
  `CREATE TEMP TABLE "__frontier_holder" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_holder_phase" ON "__frontier_holder" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_holder" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_loc_elsewhere" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "note" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_loc_elsewhere_sign" ON "__delta_loc_elsewhere" ("_sign")`,
  `CREATE INDEX "__delta_loc_elsewhere_group" ON "__delta_loc_elsewhere" ("id", "note")`,
  `CREATE TEMP TABLE "__frontier_loc_elsewhere" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "note" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_loc_elsewhere_phase" ON "__frontier_loc_elsewhere" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_loc_elsewhere" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "note" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_loc_elsewhere" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."note") AS "note", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_loc_elsewhere" t`,
  `CREATE TEMP TABLE "__delta_loc_here" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "at" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_loc_here_sign" ON "__delta_loc_here" ("_sign")`,
  `CREATE INDEX "__delta_loc_here_group" ON "__delta_loc_here" ("id", "at")`,
  `CREATE TEMP TABLE "__frontier_loc_here" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "at" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_loc_here_phase" ON "__frontier_loc_here" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_loc_here" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "at" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_loc_tag" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "tag" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_loc_tag_sign" ON "__delta_loc_tag" ("_sign")`,
  `CREATE INDEX "__delta_loc_tag_group" ON "__delta_loc_tag" ("id", "tag")`,
  `CREATE TEMP TABLE "__frontier_loc_tag" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "tag" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_loc_tag_phase" ON "__frontier_loc_tag" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_loc_tag" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "tag" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_loc_tag" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."tag") AS "tag", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_loc_tag" t`,
  `CREATE TEMP TABLE "__delta_span" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "lo" INTEGER NOT NULL, "hi" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_span_sign" ON "__delta_span" ("_sign")`,
  `CREATE INDEX "__delta_span_group" ON "__delta_span" ("lo", "hi")`,
  `CREATE TEMP TABLE "__frontier_span" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "lo" INTEGER NOT NULL, "hi" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_span_phase" ON "__frontier_span" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_span" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "lo" INTEGER NOT NULL, "hi" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__support_next_loc_tag" ("id" INTEGER NOT NULL, "tag" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("id", "tag")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_loc_tag" ("id" INTEGER NOT NULL, "tag" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "loc_tag_zero" ON "loc_tag" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  holder: ["item"],
  loc_elsewhere: ["id", "note"],
  loc_here: ["id", "at"],
  loc_tag: ["id", "tag"],
  span: ["lo", "hi"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  holder: ["ref"],
  loc_elsewhere: ["int", "text"],
  loc_here: ["int", "ref"],
  loc_tag: ["int", "text"],
  span: ["int", "int"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "variant_field_typed_as_struct_is_a_ref", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "a85c19b5964d3537", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "holder", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "ee3d9e7110c58629", h_schema: "96e2c3c8ff2ef8a6", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "item", kind: "column", type_id: 18, arity: 0, module_id: 6, h_id: "c66608532906a2e7", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 6, ordinal: 0, local_name: "loc_elsewhere", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "c9fa758bda5d6557", h_schema: "affd4fc0dff8e270", h_rule: "" },
  { rel_id: 10, parent_id: 9, ordinal: 1, local_name: "id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "457853974628dd03", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 9, ordinal: 2, local_name: "note", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "60abb5dbfc6bf30c", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 6, ordinal: 0, local_name: "loc_here", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "d987efb951fffc36", h_schema: "04e0efa9e78e1dcf", h_rule: "" },
  { rel_id: 13, parent_id: 12, ordinal: 1, local_name: "id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "34da4351c7287cfe", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 12, ordinal: 2, local_name: "at", kind: "column", type_id: 18, arity: 0, module_id: 6, h_id: "8702af6cc76f4fd5", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 6, ordinal: 0, local_name: "loc_tag", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "c2d8b1a31a41025a", h_schema: "2901e6f8122ebf0c", h_rule: "e37ffbe4d9802e10" },
  { rel_id: 16, parent_id: 15, ordinal: 1, local_name: "id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "1ea3528e6e135de8", h_schema: "", h_rule: "" },
  { rel_id: 17, parent_id: 15, ordinal: 2, local_name: "tag", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "3da0f4b774286063", h_schema: "", h_rule: "" },
  { rel_id: 18, parent_id: 6, ordinal: 0, local_name: "span", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "7b382086db7f809d", h_schema: "1bb797f3f0491e34", h_rule: "" },
  { rel_id: 19, parent_id: 18, ordinal: 1, local_name: "lo", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "57e76e474614fb16", h_schema: "", h_rule: "" },
  { rel_id: 20, parent_id: 18, ordinal: 2, local_name: "hi", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "8b9ddc48254b45f4", h_schema: "", h_rule: "" },
  { rel_id: 21, parent_id: 7, ordinal: 0, local_name: "__delta_holder", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "4b817fd073a8579e", h_schema: "477e6242d73ef91b", h_rule: "" },
  { rel_id: 22, parent_id: 7, ordinal: 0, local_name: "__frontier_holder", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "8a1050a51ca11bbe", h_schema: "3a3bb2eddd502b8e", h_rule: "" },
  { rel_id: 23, parent_id: 7, ordinal: 0, local_name: "__next_frontier_holder", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "6330c102137d1257", h_schema: "3a3bb2eddd502b8e", h_rule: "" },
  { rel_id: 24, parent_id: 9, ordinal: 0, local_name: "__delta_loc_elsewhere", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "c3ab8aa2979a9309", h_schema: "550dc6cc72eb7287", h_rule: "" },
  { rel_id: 25, parent_id: 9, ordinal: 0, local_name: "__frontier_loc_elsewhere", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "9d1a38691411b5d2", h_schema: "024889550a300304", h_rule: "" },
  { rel_id: 26, parent_id: 9, ordinal: 0, local_name: "__next_frontier_loc_elsewhere", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "1c64e8abd3aeeaf2", h_schema: "024889550a300304", h_rule: "" },
  { rel_id: 27, parent_id: 9, ordinal: 0, local_name: "__txt_loc_elsewhere", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "58098447550c46d5", h_schema: "8a162511b2eac02f", h_rule: "" },
  { rel_id: 28, parent_id: 24, ordinal: 0, local_name: "__txt___delta_loc_elsewhere", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "98d2e5e48a7363b3", h_schema: "8a162511b2eac02f", h_rule: "" },
  { rel_id: 29, parent_id: 12, ordinal: 0, local_name: "__delta_loc_here", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "7955e20225f87fe7", h_schema: "e460df57b27397fc", h_rule: "" },
  { rel_id: 30, parent_id: 12, ordinal: 0, local_name: "__frontier_loc_here", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "57bd8dd1c9da8b47", h_schema: "3c8a4689d4be7442", h_rule: "" },
  { rel_id: 31, parent_id: 12, ordinal: 0, local_name: "__next_frontier_loc_here", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "2c96eee9cecdc66d", h_schema: "3c8a4689d4be7442", h_rule: "" },
  { rel_id: 32, parent_id: 15, ordinal: 0, local_name: "__delta_loc_tag", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "ae8283dea5100c02", h_schema: "4ac3c01192c9872c", h_rule: "" },
  { rel_id: 33, parent_id: 15, ordinal: 0, local_name: "__frontier_loc_tag", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "dddc269413722679", h_schema: "373ae2761a729e21", h_rule: "" },
  { rel_id: 34, parent_id: 15, ordinal: 0, local_name: "__next_frontier_loc_tag", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "299ea7ee63e4f951", h_schema: "373ae2761a729e21", h_rule: "" },
  { rel_id: 35, parent_id: 15, ordinal: 0, local_name: "__txt_loc_tag", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "5727c049123d2cce", h_schema: "2901e6f8122ebf0c", h_rule: "" },
  { rel_id: 36, parent_id: 32, ordinal: 0, local_name: "__txt___delta_loc_tag", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "b3e32018dad82da8", h_schema: "2901e6f8122ebf0c", h_rule: "" },
  { rel_id: 37, parent_id: 18, ordinal: 0, local_name: "__delta_span", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "29f7ffbb4e2eb5b2", h_schema: "d3af9c77324e2796", h_rule: "" },
  { rel_id: 38, parent_id: 18, ordinal: 0, local_name: "__frontier_span", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "2fbd45d89f398780", h_schema: "f27bf5b2081eb9c3", h_rule: "" },
  { rel_id: 39, parent_id: 18, ordinal: 0, local_name: "__next_frontier_span", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "1cf1ddd58130f9f2", h_schema: "f27bf5b2081eb9c3", h_rule: "" },
  { rel_id: 40, parent_id: 6, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 6, h_id: "94a24000c4d129bf", h_schema: "", h_rule: "" },
  { rel_id: 41, parent_id: 6, ordinal: 0, local_name: "__ref_span", kind: "dictionary", type_id: 18, arity: 4, module_id: 6, h_id: "c4203f7cd42619c4", h_schema: "1bb797f3f0491e34", h_rule: "" },
  { rel_id: 42, parent_id: 15, ordinal: 0, local_name: "__support_next_loc_tag", kind: "refcount", type_id: 0, arity: 3, module_id: 6, h_id: "9428b32ef565eb2b", h_schema: "", h_rule: "" },
  { rel_id: 43, parent_id: 15, ordinal: 0, local_name: "__new_loc_tag", kind: "refcount_staging", type_id: 0, arity: 3, module_id: 6, h_id: "241b7b6157e8b1eb", h_schema: "", h_rule: "" },
  { rel_id: 44, parent_id: 8, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "7ea8f76dc7d3c5d7", h_schema: "", h_rule: "" },
  { rel_id: 45, parent_id: 10, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "802f72245aee956a", h_schema: "", h_rule: "" },
  { rel_id: 46, parent_id: 11, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "cd81c260a1c46ef7", h_schema: "", h_rule: "" },
  { rel_id: 47, parent_id: 13, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "a2f7beeec3ea54c3", h_schema: "", h_rule: "" },
  { rel_id: 48, parent_id: 14, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "07e628071b76e78f", h_schema: "", h_rule: "" },
  { rel_id: 49, parent_id: 16, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "4f870ada4fd0c03c", h_schema: "", h_rule: "" },
  { rel_id: 50, parent_id: 17, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "2cfcb73ec5146a82", h_schema: "", h_rule: "" },
  { rel_id: 51, parent_id: 19, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "975d4707d495bf96", h_schema: "", h_rule: "" },
  { rel_id: 52, parent_id: 20, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "d54767f46d23152f", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
  holder: ["other"],
  loc_elsewhere: ["int", "text"],
  loc_here: ["int", "other"],
  loc_tag: ["int", "text"],
  span: ["int", "int"],
};

const arrival_targets: readonly string[] = ["holder", "loc_elsewhere", "loc_here", "span"];

const boot: readonly IBootStatement[] = [
  { rel: "loc_tag", sql: `DELETE FROM "loc_tag"`, params: [] },
  { rel: "loc_tag", sql: `INSERT OR IGNORE INTO "loc_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'here') FROM "loc_here" b0`, params: [] },
  { rel: "loc_tag", sql: `INSERT OR IGNORE INTO "loc_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'elsewhere') FROM "loc_elsewhere" b0`, params: [] },
];

type Snapshot = {
  readonly holder: readonly IRow[];
  readonly loc_elsewhere: readonly IRow[];
  readonly loc_here: readonly IRow[];
  readonly loc_tag: readonly IRow[];
  readonly span: readonly IRow[];
};

function read_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    holder: select_rows(seam, `SELECT (SELECT d."__rendered" FROM "__ref_span" d WHERE d."__id" = "item") AS "item" FROM "holder"`, rel_columns.holder!, rel_column_types.holder!),
    loc_elsewhere: select_rows(seam, `SELECT "id", CASE WHEN json_valid("note") AND json_type("note") = 'object' AND json_type("note", '$.fn') = 'text' AND json_type("note", '$.args') = 'array' THEN json_extract("note", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("note", '$.args')), '') || ')' ELSE "note" END AS "note" FROM "__txt_loc_elsewhere"`, rel_columns.loc_elsewhere!, rel_column_types.loc_elsewhere!),
    loc_here: select_rows(seam, `SELECT "id", (SELECT d."__rendered" FROM "__ref_span" d WHERE d."__id" = "at") AS "at" FROM "loc_here"`, rel_columns.loc_here!, rel_column_types.loc_here!),
    loc_tag: select_rows(seam, `SELECT "id", CASE WHEN json_valid("tag") AND json_type("tag") = 'object' AND json_type("tag", '$.fn') = 'text' AND json_type("tag", '$.args') = 'array' THEN json_extract("tag", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("tag", '$.args')), '') || ')' ELSE "tag" END AS "tag" FROM "__txt_loc_tag"`, rel_columns.loc_tag!, rel_column_types.loc_tag!),
    span: select_rows(seam, `SELECT "lo", "hi" FROM "span"`, rel_columns.span!, rel_column_types.span!),
  });
}

type Snapshots = { readonly decoded: Snapshot; readonly stored: Snapshot };

function read_stored_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    holder: select_rows(seam, `SELECT "item" FROM "holder"`, rel_columns.holder!, rel_column_types.holder!),
    loc_elsewhere: select_rows(seam, `SELECT "id", "note" FROM "loc_elsewhere"`, rel_columns.loc_elsewhere!, rel_column_types.loc_elsewhere!),
    loc_here: select_rows(seam, `SELECT "id", "at" FROM "loc_here"`, rel_columns.loc_here!, rel_column_types.loc_here!),
    loc_tag: select_rows(seam, `SELECT "id", "tag" FROM "loc_tag"`, rel_columns.loc_tag!, rel_column_types.loc_tag!),
    span: select_rows(seam, `SELECT "lo", "hi" FROM "span"`, rel_columns.span!, rel_column_types.span!),
  });
}

function read_snapshots(seam: ISqlSeam): Observable<Snapshots> {
  return forkJoin({ decoded: read_snapshot(seam), stored: read_stored_snapshot(seam) });
}

const final_select: Record<string, string> = {
  holder: `SELECT (SELECT d."__rendered" FROM "__ref_span" d WHERE d."__id" = "item") AS "item" FROM "holder"`,
  loc_elsewhere: `SELECT "id", CASE WHEN json_valid("note") AND json_type("note") = 'object' AND json_type("note", '$.fn') = 'text' AND json_type("note", '$.args') = 'array' THEN json_extract("note", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("note", '$.args')), '') || ')' ELSE "note" END AS "note" FROM "__txt_loc_elsewhere"`,
  loc_here: `SELECT "id", (SELECT d."__rendered" FROM "__ref_span" d WHERE d."__id" = "at") AS "at" FROM "loc_here"`,
  loc_tag: `SELECT "id", CASE WHEN json_valid("tag") AND json_type("tag") = 'object' AND json_type("tag", '$.fn') = 'text' AND json_type("tag", '$.args') = 'array' THEN json_extract("tag", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("tag", '$.args')), '') || ')' ELSE "tag" END AS "tag" FROM "__txt_loc_tag"`,
  span: `SELECT "lo", "hi" FROM "span"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; add_sql: string; del_sql: string | null }> = {
  holder: { kind: "set", add_sql: `INSERT OR IGNORE INTO "holder" ("item") VALUES (?)`, del_sql: `DELETE FROM "holder" WHERE "item" = ?` },
  loc_elsewhere: { kind: "set", add_sql: `INSERT INTO "loc_elsewhere" ("id", "note") VALUES (?, ?) ON CONFLICT ("note") DO UPDATE SET "id" = excluded."id"`, del_sql: `DELETE FROM "loc_elsewhere" WHERE "id" = ? AND "note" = ?` },
  loc_here: { kind: "set", add_sql: `INSERT INTO "loc_here" ("id", "at") VALUES (?, ?) ON CONFLICT ("at") DO UPDATE SET "id" = excluded."id"`, del_sql: `DELETE FROM "loc_here" WHERE "id" = ? AND "at" = ?` },
  span: { kind: "set", add_sql: `INSERT OR IGNORE INTO "span" ("lo", "hi") VALUES (?, ?)`, del_sql: `DELETE FROM "span" WHERE "lo" = ? AND "hi" = ?` },
};

function arrival_statement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`variant_field_typed_as_struct_is_a_ref: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`variant_field_typed_as_struct_is_a_ref: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.del_sql === null) {
      throw new Error(`variant_field_typed_as_struct_is_a_ref: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.del_sql, args: bind_args(arrival.row) };
  }
  return { sql: template.add_sql, args: bind_args(arrival.row) };
}

function apply_arrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrival_statement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "holder", kind: "set", table_name: "holder", delta_table_name: "__delta_holder", frontier_table_name: "__frontier_holder", next_frontier_table_name: "__next_frontier_holder", columns: ["item"], column_types: ["ref"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "holder" ("item") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "item"`, arrival_del_sql: `DELETE FROM "holder" WHERE ("item") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "item"`, boundary_sql: `SELECT (SELECT d."__rendered" FROM "__ref_span" d WHERE d."__id" = "item") AS "item", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_holder" WHERE "_sign" IN (-1, 1) GROUP BY "item", "_sign"`, rule_observers: [] },
  { rel: "loc_elsewhere", kind: "set", table_name: "loc_elsewhere", delta_table_name: "__delta_loc_elsewhere", frontier_table_name: "__frontier_loc_elsewhere", next_frontier_table_name: "__next_frontier_loc_elsewhere", columns: ["id", "note"], column_types: ["int", "text"], key_indices: [1], arrival_add_sql: `INSERT INTO "loc_elsewhere" ("id", "note") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) WHERE true ON CONFLICT ("note") DO UPDATE SET "id" = excluded."id" RETURNING "id", "note"`, arrival_del_sql: `DELETE FROM "loc_elsewhere" WHERE ("id", "note") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "id", "note"`, boundary_sql: `SELECT "id", CASE WHEN json_valid("note") AND json_type("note") = 'object' AND json_type("note", '$.fn') = 'text' AND json_type("note", '$.args') = 'array' THEN json_extract("note", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("note", '$.args')), '') || ')' ELSE "note" END AS "note", "_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_loc_elsewhere" WHERE "_sign" IN (-1, 1) GROUP BY "id", "note", "_sign"`, rule_observers: ["loc_tag/2"] },
  { rel: "loc_here", kind: "set", table_name: "loc_here", delta_table_name: "__delta_loc_here", frontier_table_name: "__frontier_loc_here", next_frontier_table_name: "__next_frontier_loc_here", columns: ["id", "at"], column_types: ["int", "ref"], key_indices: [1], arrival_add_sql: `INSERT INTO "loc_here" ("id", "at") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) WHERE true ON CONFLICT ("at") DO UPDATE SET "id" = excluded."id" RETURNING "id", "at"`, arrival_del_sql: `DELETE FROM "loc_here" WHERE ("id", "at") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "id", "at"`, boundary_sql: `SELECT "id", (SELECT d."__rendered" FROM "__ref_span" d WHERE d."__id" = "at") AS "at", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_loc_here" WHERE "_sign" IN (-1, 1) GROUP BY "id", "at", "_sign"`, rule_observers: ["loc_tag/2"] },
  { rel: "loc_tag", kind: "set", table_name: "loc_tag", delta_table_name: "__delta_loc_tag", frontier_table_name: "__frontier_loc_tag", next_frontier_table_name: "__next_frontier_loc_tag", columns: ["id", "tag"], column_types: ["int", "text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT "id", CASE WHEN json_valid("tag") AND json_type("tag") = 'object' AND json_type("tag", '$.fn') = 'text' AND json_type("tag", '$.args') = 'array' THEN json_extract("tag", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("tag", '$.args')), '') || ')' ELSE "tag" END AS "tag", "_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_loc_tag" WHERE "_sign" IN (-1, 1) GROUP BY "id", "tag", "_sign"`, rule_observers: [] },
  { rel: "span", kind: "set", table_name: "span", delta_table_name: "__delta_span", frontier_table_name: "__frontier_span", next_frontier_table_name: "__next_frontier_span", columns: ["lo", "hi"], column_types: ["int", "int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "span" ("lo", "hi") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "lo", "hi"`, arrival_del_sql: `DELETE FROM "span" WHERE ("lo", "hi") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "lo", "hi"`, boundary_sql: `SELECT "lo", "hi", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_span" WHERE "_sign" IN (-1, 1) GROUP BY "lo", "hi", "_sign"`, rule_observers: [] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "loc_tag", rule_id: "variant_field_typed_as_struct_is_a_ref:loc_tag/2#1", head_delta_table_name: "__delta_loc_tag", head_columns: ["id", "tag"], insert_sql: `INSERT OR IGNORE INTO "loc_tag" ("id", "tag") SELECT DISTINCT d0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'here') FROM "__frontier_loc_here" d0 WHERE d0."_phase" >= 0 UNION ALL SELECT DISTINCT d0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'elsewhere') FROM "__frontier_loc_elsewhere" d0 WHERE d0."_phase" >= 0 RETURNING "id", "tag"`, select_sql: `SELECT "id", "tag" FROM "loc_tag"`, recompute_sql: `DELETE FROM "loc_tag";
INSERT OR IGNORE INTO "loc_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'here') FROM "loc_here" b0;
INSERT OR IGNORE INTO "loc_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'elsewhere') FROM "loc_elsewhere" b0`, support_sql: [`DELETE FROM "__support_next_loc_tag"`, `INSERT INTO "__support_next_loc_tag" ("id", "tag", "__refcount") SELECT "id", "tag", sum("__refcount") FROM (SELECT b0."id" AS "id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'here') AS "tag", count(*) AS "__refcount" FROM "loc_here" b0 GROUP BY b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'here') UNION ALL SELECT b0."id" AS "id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'elsewhere') AS "tag", count(*) AS "__refcount" FROM "loc_elsewhere" b0 GROUP BY b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'elsewhere')) GROUP BY "id", "tag"`, `UPDATE "loc_tag" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_loc_tag" n WHERE n."id" = h."id" AND n."tag" = h."tag"), 0)`, `INSERT INTO "__delta_loc_tag" ("_sign", "_sequence", "id", "tag") SELECT -1, row_number() OVER () - 1, "id", "tag" FROM "loc_tag" WHERE "__refcount" <= 0`, `DELETE FROM "loc_tag" WHERE "__refcount" <= 0`, `DELETE FROM "__new_loc_tag"`, `INSERT INTO "__new_loc_tag" ("id", "tag", "__refcount") SELECT n."id", n."tag", n."__refcount" FROM "__support_next_loc_tag" n LEFT JOIN "loc_tag" h ON n."id" = h."id" AND n."tag" = h."tag" WHERE h."id" IS NULL`, `INSERT INTO "__delta_loc_tag" ("_sign", "_sequence", "id", "tag") SELECT 1, "rowid" - 1, "id", "tag" FROM "__new_loc_tag"`, `INSERT INTO "__frontier_loc_tag" ("_phase", "_sequence", "id", "tag") SELECT ?, "rowid" - 1, "id", "tag" FROM "__new_loc_tag"`, `INSERT INTO "__next_frontier_loc_tag" ("_phase", "_sequence", "id", "tag") SELECT ?, "rowid" - 1, "id", "tag" FROM "__new_loc_tag"`, `INSERT OR IGNORE INTO "loc_tag" ("id", "tag", "__refcount") SELECT n."id", n."tag", n."__refcount" FROM "__support_next_loc_tag" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

function recompute_levels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "loc_tag";
INSERT OR IGNORE INTO "loc_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'here') FROM "loc_here" b0;
INSERT OR IGNORE INTO "loc_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'elsewhere') FROM "loc_elsewhere" b0`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function build_deltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const holder = multiset_diff(before.holder, after.holder);
  const loc_elsewhere = multiset_diff(before.loc_elsewhere, after.loc_elsewhere);
  const loc_here = multiset_diff(before.loc_here, after.loc_here);
  const loc_tag = multiset_diff(before.loc_tag, after.loc_tag);
  const span = multiset_diff(before.span, after.span);
  return {
    rels: [
      { rel: "holder", add: holder.add, del: holder.del },
      { rel: "loc_elsewhere", add: loc_elsewhere.add, del: loc_elsewhere.del },
      { rel: "loc_here", add: loc_here.add, del: loc_here.del },
      { rel: "loc_tag", add: loc_tag.add, del: loc_tag.del },
      { rel: "span", add: span.add, del: span.del },
    ],
    carry_pending: false,
  };
}

function run_naive_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return read_snapshot(seam).pipe(
    concatMap((before) => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; return before; }))),
    concatMap((before) => StructPlane.intern(seam, STRUCT_TYPES, STRUCT_REF_COLUMNS, arrivals,
      (targets) => apply_arrivals(seam, targets), TEXT_INTERN_PLAN,
    ).pipe(map((normalized) => { arrivals = normalized; return before; }))),
    concatMap((before) => apply_arrivals(seam, arrivals).pipe(map(() => before))),
  ).pipe(
    concatMap((before) => recompute_levels(seam).pipe(map(() => before))),
    concatMap((before) => read_snapshot(seam).pipe(map((after) => build_deltas(before, after)))),
  );
  // variant_field_typed_as_struct_is_a_ref: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = EMITTER_MODE;
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => StructPlane.intern(seam, STRUCT_TYPES, STRUCT_REF_COLUMNS, arrivals,
      (targets) => IncrementalRuntime.apply_arrivals(seam, targets, SUBSCRIBED_RELATIONS), TEXT_INTERN_PLAN,
    ).pipe(map((normalized) => { arrivals = normalized; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return run_naive_tick(seam, arrivals);
  }
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "variant_field_typed_as_struct_is_a_ref",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
