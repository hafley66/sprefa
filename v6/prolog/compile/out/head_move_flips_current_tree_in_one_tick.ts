// GENERATED by v6/prolog/compile (tsv2 emitter P1). Do not
// hand-edit; recompile. Program: head_move_flips_current_tree_in_one_tick.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lowerSql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted delta-side joins for positive acyclic level rules, and
// computes boundary changes from the staged stream. The snapshot recompute
// path remains selectable with SPREFA_TSV2_EMITTER_MODE=naive and is used
// automatically for P1-unsafe shapes and ticks containing retractions.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { multisetDiff } from "../runtime/diff.ts";
import { selectRows } from "../runtime/rows.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelDelta,
  IRow,
  IRowValue,
  ISqlSeam,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IBootStatement {
  sql: string;
  params: readonly (string | number)[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[] };

function bindArgs(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => (typeof value === "number" && Number.isInteger(value) ? BigInt(value) : value));
}

function triggerOccurrences(
  kind: "log" | "set",
  relName: string,
  beforeRows: readonly IRow[],
  arrivals: IArrivalBatch,
): IArrivalBatch {
  if (kind === "log") return arrivals.filter((arrival) => arrival.rel === relName && arrival.sign === "add");
  const seen = new Set<string>(beforeRows.map((row) => JSON.stringify(row)));
  const occurrences: IArrivalRow[] = [];
  for (const arrival of arrivals) {
    if (arrival.rel !== relName || arrival.sign !== "add") continue;
    const key = JSON.stringify(arrival.row);
    if (seen.has(key)) continue;
    seen.add(key);
    occurrences.push(arrival);
  }
  return occurrences;
}

const ddl: readonly string[] = [
  `CREATE TABLE "current_tree" ("path" TEXT NOT NULL, "digest" TEXT NOT NULL, PRIMARY KEY ("path", "digest")) WITHOUT ROWID`,
  `CREATE TABLE "head" ("repo_id" INTEGER NOT NULL, "rev_id" INTEGER NOT NULL, PRIMARY KEY ("repo_id")) WITHOUT ROWID`,
  `CREATE TABLE "head_move" ("repo_id" INTEGER NOT NULL, "rev_id" INTEGER NOT NULL)`,
  `CREATE TABLE "tree_file" ("rev_id" INTEGER NOT NULL, "path" TEXT NOT NULL, "digest" TEXT NOT NULL, PRIMARY KEY ("rev_id", "path", "digest")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__delta_current_tree" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "digest" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_current_tree_sign" ON "__delta_current_tree" ("_sign")`,
  `CREATE TEMP TABLE "__delta_head" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo_id" INTEGER NOT NULL, "rev_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_head_sign" ON "__delta_head" ("_sign")`,
  `CREATE TEMP TABLE "__delta_head_move" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "repo_id" INTEGER NOT NULL, "rev_id" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_head_move_sign" ON "__delta_head_move" ("_sign")`,
  `CREATE TEMP TABLE "__delta_tree_file" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "rev_id" INTEGER NOT NULL, "path" TEXT NOT NULL, "digest" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_tree_file_sign" ON "__delta_tree_file" ("_sign")`,
];

const relColumns: Record<string, readonly string[]> = {
  current_tree: ["path", "digest"],
  head: ["repo_id", "rev_id"],
  head_move: ["repo_id", "rev_id"],
  tree_file: ["rev_id", "path", "digest"],
};

const arrivalTargets: readonly string[] = ["head_move", "tree_file"];

const boot: readonly IBootStatement[] = [
  { sql: `INSERT OR IGNORE INTO "head" ("repo_id", "rev_id") VALUES (?, ?)`, params: [1, 100] },
  { sql: `INSERT OR IGNORE INTO "tree_file" ("rev_id", "path", "digest") VALUES (?, ?, ?)`, params: [100, "src/a.rs", "sha_a1"] },
  { sql: `INSERT OR IGNORE INTO "tree_file" ("rev_id", "path", "digest") VALUES (?, ?, ?)`, params: [100, "src/b.rs", "sha_b1"] },
  { sql: `INSERT OR IGNORE INTO "tree_file" ("rev_id", "path", "digest") VALUES (?, ?, ?)`, params: [200, "src/a.rs", "sha_a2"] },
  { sql: `INSERT OR IGNORE INTO "tree_file" ("rev_id", "path", "digest") VALUES (?, ?, ?)`, params: [200, "src/b.rs", "sha_b2"] },
  { sql: `INSERT OR IGNORE INTO "tree_file" ("rev_id", "path", "digest") VALUES (?, ?, ?)`, params: [200, "src/c.rs", "sha_c1"] },
  { sql: `DELETE FROM "current_tree"`, params: [] },
  { sql: `INSERT OR IGNORE INTO "current_tree" ("path", "digest") SELECT b1."path", b1."digest" FROM "head" b0, "tree_file" b1 WHERE b1."rev_id" = b0."rev_id"`, params: [] },
];

type Snapshot = {
  readonly current_tree: readonly IRow[];
  readonly head: readonly IRow[];
  readonly head_move: readonly IRow[];
  readonly tree_file: readonly IRow[];
};

function readSnapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    current_tree: selectRows(seam, `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("digest") AND json_type("digest") = 'object' THEN json_extract("digest", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("digest", '$.args')) || ')' ELSE "digest" END AS "digest" FROM "current_tree"`, relColumns.current_tree!),
    head: selectRows(seam, `SELECT "repo_id", "rev_id" FROM "head"`, relColumns.head!),
    head_move: selectRows(seam, `SELECT "repo_id", "rev_id" FROM "head_move"`, relColumns.head_move!),
    tree_file: selectRows(seam, `SELECT "rev_id", CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("digest") AND json_type("digest") = 'object' THEN json_extract("digest", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("digest", '$.args')) || ')' ELSE "digest" END AS "digest" FROM "tree_file"`, relColumns.tree_file!),
  });
}

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; addSql: string; delSql: string | null }> = {
  head_move: { kind: "log", addSql: `INSERT INTO "head_move" ("repo_id", "rev_id") VALUES (?, ?)`, delSql: null },
  tree_file: { kind: "set", addSql: `INSERT OR IGNORE INTO "tree_file" ("rev_id", "path", "digest") VALUES (?, ?, ?)`, delSql: `DELETE FROM "tree_file" WHERE "rev_id" = ? AND "path" = ? AND "digest" = ?` },
};

function arrivalStatement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`head_move_flips_current_tree_in_one_tick: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`head_move_flips_current_tree_in_one_tick: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.delSql === null) {
      throw new Error(`head_move_flips_current_tree_in_one_tick: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.delSql, args: bindArgs(arrival.row) };
  }
  return { sql: template.addSql, args: bindArgs(arrival.row) };
}

function applyArrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrivalStatement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "current_tree", kind: "set", tableName: "current_tree", deltaTableName: "__delta_current_tree", columns: ["path", "digest"], arrivalAddSql: null, boundarySql: `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("digest") AND json_type("digest") = 'object' THEN json_extract("digest", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("digest", '$.args')) || ')' ELSE "digest" END AS "digest", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_current_tree" WHERE "_sign" IN (-1, 1) GROUP BY "path", "digest", "_sign"` },
  { rel: "head", kind: "set", tableName: "head", deltaTableName: "__delta_head", columns: ["repo_id", "rev_id"], arrivalAddSql: null, boundarySql: `SELECT "repo_id", "rev_id", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_head" WHERE "_sign" IN (-1, 1) GROUP BY "repo_id", "rev_id", "_sign"` },
  { rel: "head_move", kind: "log", tableName: "head_move", deltaTableName: "__delta_head_move", columns: ["repo_id", "rev_id"], arrivalAddSql: `INSERT INTO "head_move" ("repo_id", "rev_id") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "repo_id", "rev_id"`, boundarySql: `SELECT "repo_id", "rev_id", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_head_move" WHERE "_sign" IN (-1, 1) GROUP BY "repo_id", "rev_id", "_sign"` },
  { rel: "tree_file", kind: "set", tableName: "tree_file", deltaTableName: "__delta_tree_file", columns: ["rev_id", "path", "digest"], arrivalAddSql: `INSERT OR IGNORE INTO "tree_file" ("rev_id", "path", "digest") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]'), json_extract(value, '$[2]') FROM json_each(?) RETURNING "rev_id", "path", "digest"`, boundarySql: `SELECT "rev_id", CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("digest") AND json_type("digest") = 'object' THEN json_extract("digest", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("digest", '$.args')) || ')' ELSE "digest" END AS "digest", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_tree_file" WHERE "_sign" IN (-1, 1) GROUP BY "rev_id", "path", "digest", "_sign"` },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
  { headRel: "head", headKind: "set", headTableName: "head", headDeltaTableName: "__delta_head", headColumns: ["repo_id", "rev_id"], keyIndices: [0], projectSql: `SELECT d0."repo_id" AS "repo_id", d0."rev_id" AS "rev_id" FROM "__delta_head_move" d0 WHERE d0."_sign" = 1 ORDER BY d0."_sequence"` },
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { headRel: "current_tree", headDeltaTableName: "__delta_current_tree", headColumns: ["path", "digest"], insertSql: `INSERT OR IGNORE INTO "current_tree" ("path", "digest") SELECT DISTINCT b0."path", b0."digest" FROM "__delta_head" d0, "tree_file" b0 WHERE d0."_sign" = 1 AND b0."rev_id" = d0."rev_id" UNION ALL SELECT DISTINCT d0."path", d0."digest" FROM "__delta_tree_file" d0, "head" b0 WHERE d0."_sign" = 1 AND b0."rev_id" = d0."rev_id" RETURNING "path", "digest"` },
];

const EDGE_HEAD_0_PROJECT_SQL = `SELECT ?1 AS "repo_id", ?2 AS "rev_id"`;
const EDGE_HEAD_0_WRITE_SQL = `INSERT INTO "head" ("repo_id", "rev_id") VALUES (?, ?) ON CONFLICT("repo_id") DO UPDATE SET "rev_id" = excluded."rev_id"`;
const EDGE_HEAD_0_HEAD_COLUMNS: readonly string[] = ["repo_id", "rev_id"];
const EDGE_HEAD_0_KEY_INDICES: readonly number[] = [0];

function resolveHead_0Writes(seam: ISqlSeam, before: Snapshot, arrivals: IArrivalBatch): Observable<readonly SqlStatement[]> {
  const triggerRows = triggerOccurrences("log", "head_move", before.head_move, arrivals);
  if (triggerRows.length === 0) return of([]);
  return forkJoin(triggerRows.map((arrival) => seam.runner.execute(seam.db, { sql: EDGE_HEAD_0_PROJECT_SQL, args: bindArgs(arrival.row) }))).pipe(
    map((results) => {
      const resolved = new Map<string, IRow>();
      for (const result of results) {
        const projectedRows = result.rows.map((row) => EDGE_HEAD_0_HEAD_COLUMNS.map((column) => row[column] as IRowValue) as IRow);
        for (const projectedRow of projectedRows) {
          const key = JSON.stringify(EDGE_HEAD_0_KEY_INDICES.map((index) => projectedRow[index]));
          resolved.set(key, projectedRow);
        }
      }
      return [...resolved.values()].map((row): SqlStatement => ({ sql: EDGE_HEAD_0_WRITE_SQL, args: bindArgs(row) }));
    }),
  );
}

function recomputeLevels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "current_tree";\nINSERT OR IGNORE INTO "current_tree" ("path", "digest") SELECT b1."path", b1."digest" FROM "head" b0, "tree_file" b1 WHERE b1."rev_id" = b0."rev_id"`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function buildDeltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const current_tree = multisetDiff(before.current_tree, after.current_tree);
  const head = multisetDiff(before.head, after.head);
  const head_move = multisetDiff(before.head_move, after.head_move);
  const tree_file = multisetDiff(before.tree_file, after.tree_file);
  return {
    rels: [
      { rel: "current_tree", add: current_tree.add, del: current_tree.del },
      { rel: "head", add: head.add, del: head.del },
      { rel: "head_move", add: head_move.add, del: head_move.del },
      { rel: "tree_file", add: tree_file.add, del: tree_file.del },
    ],
    carryPending: head.add.length > 0 || head.del.length > 0,
  };
}

function runNaiveTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return readSnapshot(seam).pipe(
    concatMap((before) => applyArrivals(seam, arrivals).pipe(map(() => before))),
    concatMap((before) =>
      resolveHead_0Writes(seam, before, arrivals).pipe(
        concatMap((statements) => seam.runner.batch(seam.db, statements)),
        map(() => before),
      ),
    ),
    concatMap((before) => recomputeLevels(seam).pipe(map(() => before))),
    concatMap((before) => readSnapshot(seam).pipe(map((after) => buildDeltas(before, after)))),
  );
  // head_move_flips_current_tree_in_one_tick: engine.pl process_occurrences -> level_closure -> boundary_deltas.
}

const INCREMENTAL_PROGRAM_SAFE = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

function runIncrementalTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.clearDeltas(seam, INCREMENTAL_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.applyArrivals(seam, arrivals, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyEdges(seam, INCREMENTAL_EDGE_STATEMENTS)),
    concatMap(() => IncrementalRuntime.applyLevels(seam, INCREMENTAL_LEVEL_STATEMENTS)),
    concatMap(() => IncrementalRuntime.readBoundary(seam, INCREMENTAL_RELATIONS)),
    map((rels): ITickDeltas => ({
      rels,
      carryPending: rels.some((delta) => ["head"].includes(delta.rel) && (delta.add.length > 0 || delta.del.length > 0)),
    })),
  );
}

function runTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  const hasRetraction = arrivals.some((arrival) => arrival.sign === "del");
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE || hasRetraction) {
    return runNaiveTick(seam, arrivals);
  }
  return runIncrementalTick(seam, arrivals);
}

export const incrementalPlan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "head_move_flips_current_tree_in_one_tick",
  ddl,
  relColumns,
  arrivalTargets,
  boot,
  tick: runTick,
};
