// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: head_expression_evaluates_derived_column.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lowerSql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { multisetDiff } from "../runtime/diff.ts";
import { selectRows } from "../runtime/rows.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelDelta,
  IRow,
  IRowValue,
  ISqlSeam,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IBootStatement {
  sql: string;
  params: readonly (string | number)[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly finalSelect: Record<string, string> };

function bindArgs(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => (typeof value === "number" && Number.isInteger(value) ? BigInt(value) : value));
}

const ddl: readonly string[] = [
  `CREATE TABLE "callee_set_size" ("left" TEXT NOT NULL, "left_size" INTEGER NOT NULL, PRIMARY KEY ("left", "left_size")) WITHOUT ROWID`,
  `CREATE TABLE "shared_count" ("left" TEXT NOT NULL, "right" TEXT NOT NULL, "shared" INTEGER NOT NULL, PRIMARY KEY ("left", "right", "shared")) WITHOUT ROWID`,
  `CREATE TABLE "union_size" ("left" TEXT NOT NULL, "right" TEXT NOT NULL, "col3" INTEGER NOT NULL, "__support_count" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("left", "right", "col3")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__delta_callee_set_size" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" TEXT NOT NULL, "left_size" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_callee_set_size_sign" ON "__delta_callee_set_size" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_callee_set_size" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" TEXT NOT NULL, "left_size" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_callee_set_size_phase" ON "__frontier_callee_set_size" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_callee_set_size" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" TEXT NOT NULL, "left_size" INTEGER NOT NULL)`,
  `CREATE INDEX "__next_frontier_callee_set_size_phase" ON "__next_frontier_callee_set_size" ("_phase")`,
  `CREATE TEMP TABLE "__delta_shared_count" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" TEXT NOT NULL, "right" TEXT NOT NULL, "shared" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_shared_count_sign" ON "__delta_shared_count" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_shared_count" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" TEXT NOT NULL, "right" TEXT NOT NULL, "shared" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_shared_count_phase" ON "__frontier_shared_count" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_shared_count" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" TEXT NOT NULL, "right" TEXT NOT NULL, "shared" INTEGER NOT NULL)`,
  `CREATE INDEX "__next_frontier_shared_count_phase" ON "__next_frontier_shared_count" ("_phase")`,
  `CREATE TEMP TABLE "__delta_union_size" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" TEXT NOT NULL, "right" TEXT NOT NULL, "col3" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_union_size_sign" ON "__delta_union_size" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_union_size" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" TEXT NOT NULL, "right" TEXT NOT NULL, "col3" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_union_size_phase" ON "__frontier_union_size" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_union_size" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" TEXT NOT NULL, "right" TEXT NOT NULL, "col3" INTEGER NOT NULL)`,
  `CREATE INDEX "__next_frontier_union_size_phase" ON "__next_frontier_union_size" ("_phase")`,
  `CREATE TEMP TABLE "__support_next_union_size" ("left" TEXT NOT NULL, "right" TEXT NOT NULL, "col3" INTEGER NOT NULL, "__support_count" INTEGER NOT NULL, PRIMARY KEY ("left", "right", "col3")) WITHOUT ROWID`,
];

const relColumns: Record<string, readonly string[]> = {
  callee_set_size: ["left", "left_size"],
  shared_count: ["left", "right", "shared"],
  union_size: ["left", "right", "col3"],
};

const arrivalTargets: readonly string[] = ["callee_set_size", "shared_count"];

const boot: readonly IBootStatement[] = [
  { sql: `INSERT OR IGNORE INTO "callee_set_size" ("left", "left_size") VALUES (?, ?)`, params: ["db.rs", 10] },
  { sql: `INSERT OR IGNORE INTO "callee_set_size" ("left", "left_size") VALUES (?, ?)`, params: ["cli.rs", 8] },
  { sql: `INSERT OR IGNORE INTO "callee_set_size" ("left", "left_size") VALUES (?, ?)`, params: ["ui.rs", 4] },
  { sql: `INSERT OR IGNORE INTO "shared_count" ("left", "right", "shared") VALUES (?, ?, ?)`, params: ["db.rs", "cli.rs", 6] },
  { sql: `INSERT OR IGNORE INTO "shared_count" ("left", "right", "shared") VALUES (?, ?, ?)`, params: ["db.rs", "ui.rs", 1] },
  { sql: `DELETE FROM "union_size"`, params: [] },
  { sql: `INSERT OR IGNORE INTO "union_size" ("left", "right", "col3") SELECT b0."left", b1."left", ((b0."left_size" + b1."left_size") - b2."shared") FROM "callee_set_size" b0, "callee_set_size" b1, "shared_count" b2 WHERE b2."left" = b0."left" AND b2."right" = b1."left"`, params: [] },
];

type Snapshot = {
  readonly callee_set_size: readonly IRow[];
  readonly shared_count: readonly IRow[];
  readonly union_size: readonly IRow[];
};

function readSnapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    callee_set_size: selectRows(seam, `SELECT CASE WHEN json_valid("left") AND json_type("left") = 'object' THEN json_extract("left", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("left", '$.args')) || ')' ELSE "left" END AS "left", "left_size" FROM "callee_set_size"`, relColumns.callee_set_size!),
    shared_count: selectRows(seam, `SELECT CASE WHEN json_valid("left") AND json_type("left") = 'object' THEN json_extract("left", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("left", '$.args')) || ')' ELSE "left" END AS "left", CASE WHEN json_valid("right") AND json_type("right") = 'object' THEN json_extract("right", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("right", '$.args')) || ')' ELSE "right" END AS "right", "shared" FROM "shared_count"`, relColumns.shared_count!),
    union_size: selectRows(seam, `SELECT CASE WHEN json_valid("left") AND json_type("left") = 'object' THEN json_extract("left", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("left", '$.args')) || ')' ELSE "left" END AS "left", CASE WHEN json_valid("right") AND json_type("right") = 'object' THEN json_extract("right", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("right", '$.args')) || ')' ELSE "right" END AS "right", "col3" FROM "union_size"`, relColumns.union_size!),
  });
}

const finalSelect: Record<string, string> = {
  callee_set_size: `SELECT CASE WHEN json_valid("left") AND json_type("left") = 'object' THEN json_extract("left", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("left", '$.args')) || ')' ELSE "left" END AS "left", "left_size" FROM "callee_set_size"`,
  shared_count: `SELECT CASE WHEN json_valid("left") AND json_type("left") = 'object' THEN json_extract("left", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("left", '$.args')) || ')' ELSE "left" END AS "left", CASE WHEN json_valid("right") AND json_type("right") = 'object' THEN json_extract("right", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("right", '$.args')) || ')' ELSE "right" END AS "right", "shared" FROM "shared_count"`,
  union_size: `SELECT CASE WHEN json_valid("left") AND json_type("left") = 'object' THEN json_extract("left", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("left", '$.args')) || ')' ELSE "left" END AS "left", CASE WHEN json_valid("right") AND json_type("right") = 'object' THEN json_extract("right", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("right", '$.args')) || ')' ELSE "right" END AS "right", "col3" FROM "union_size"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; addSql: string; delSql: string | null }> = {
  callee_set_size: { kind: "set", addSql: `INSERT OR IGNORE INTO "callee_set_size" ("left", "left_size") VALUES (?, ?)`, delSql: `DELETE FROM "callee_set_size" WHERE "left" = ? AND "left_size" = ?` },
  shared_count: { kind: "set", addSql: `INSERT OR IGNORE INTO "shared_count" ("left", "right", "shared") VALUES (?, ?, ?)`, delSql: `DELETE FROM "shared_count" WHERE "left" = ? AND "right" = ? AND "shared" = ?` },
};

function arrivalStatement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`head_expression_evaluates_derived_column: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`head_expression_evaluates_derived_column: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.delSql === null) {
      throw new Error(`head_expression_evaluates_derived_column: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.delSql, args: bindArgs(arrival.row) };
  }
  return { sql: template.addSql, args: bindArgs(arrival.row) };
}

function applyArrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrivalStatement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "callee_set_size", kind: "set", tableName: "callee_set_size", deltaTableName: "__delta_callee_set_size", frontierTableName: "__frontier_callee_set_size", nextFrontierTableName: "__next_frontier_callee_set_size", columns: ["left", "left_size"], arrivalAddSql: `INSERT OR IGNORE INTO "callee_set_size" ("left", "left_size") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "left", "left_size"`, arrivalDelSql: `DELETE FROM "callee_set_size" WHERE ("left", "left_size") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "left", "left_size"`, boundarySql: `SELECT CASE WHEN json_valid("left") AND json_type("left") = 'object' THEN json_extract("left", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("left", '$.args')) || ')' ELSE "left" END AS "left", "left_size", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_callee_set_size" WHERE "_sign" IN (-1, 1) GROUP BY "left", "left_size", "_sign"` },
  { rel: "shared_count", kind: "set", tableName: "shared_count", deltaTableName: "__delta_shared_count", frontierTableName: "__frontier_shared_count", nextFrontierTableName: "__next_frontier_shared_count", columns: ["left", "right", "shared"], arrivalAddSql: `INSERT OR IGNORE INTO "shared_count" ("left", "right", "shared") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]'), json_extract(value, '$[2]') FROM json_each(?) RETURNING "left", "right", "shared"`, arrivalDelSql: `DELETE FROM "shared_count" WHERE ("left", "right", "shared") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]'), json_extract(value, '$[2]') FROM json_each(?)) RETURNING "left", "right", "shared"`, boundarySql: `SELECT CASE WHEN json_valid("left") AND json_type("left") = 'object' THEN json_extract("left", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("left", '$.args')) || ')' ELSE "left" END AS "left", CASE WHEN json_valid("right") AND json_type("right") = 'object' THEN json_extract("right", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("right", '$.args')) || ')' ELSE "right" END AS "right", "shared", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_shared_count" WHERE "_sign" IN (-1, 1) GROUP BY "left", "right", "shared", "_sign"` },
  { rel: "union_size", kind: "set", tableName: "union_size", deltaTableName: "__delta_union_size", frontierTableName: "__frontier_union_size", nextFrontierTableName: "__next_frontier_union_size", columns: ["left", "right", "col3"], arrivalAddSql: null, arrivalDelSql: null, boundarySql: `SELECT CASE WHEN json_valid("left") AND json_type("left") = 'object' THEN json_extract("left", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("left", '$.args')) || ')' ELSE "left" END AS "left", CASE WHEN json_valid("right") AND json_type("right") = 'object' THEN json_extract("right", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("right", '$.args')) || ')' ELSE "right" END AS "right", "col3", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_union_size" WHERE "_sign" IN (-1, 1) GROUP BY "left", "right", "col3", "_sign"` },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { headRel: "union_size", headDeltaTableName: "__delta_union_size", headColumns: ["left", "right", "col3"], insertSql: `INSERT OR IGNORE INTO "union_size" ("left", "right", "col3") SELECT DISTINCT d0."left", b0."left", ((d0."left_size" + b0."left_size") - b1."shared") FROM "__frontier_callee_set_size" d0, "callee_set_size" b0, "shared_count" b1 WHERE d0."_phase" >= 0 AND b1."left" = d0."left" AND b1."right" = b0."left" UNION ALL SELECT DISTINCT b0."left", d0."left", ((b0."left_size" + d0."left_size") - b1."shared") FROM "__frontier_callee_set_size" d0, "callee_set_size" b0, "shared_count" b1 WHERE d0."_phase" >= 0 AND b1."left" = b0."left" AND b1."right" = d0."left" UNION ALL SELECT DISTINCT d0."left", d0."right", ((b0."left_size" + b1."left_size") - d0."shared") FROM "__frontier_shared_count" d0, "callee_set_size" b0, "callee_set_size" b1 WHERE d0."_phase" >= 0 AND b0."left" = d0."left" AND b1."left" = d0."right" RETURNING "left", "right", "col3"`, selectSql: `SELECT "left", "right", "col3" FROM "union_size"`, recomputeSql: `DELETE FROM "union_size";\nINSERT OR IGNORE INTO "union_size" ("left", "right", "col3") SELECT b0."left", b1."left", ((b0."left_size" + b1."left_size") - b2."shared") FROM "callee_set_size" b0, "callee_set_size" b1, "shared_count" b2 WHERE b2."left" = b0."left" AND b2."right" = b1."left"`, supportSql: [`DELETE FROM "__support_next_union_size"`, `INSERT INTO "__support_next_union_size" ("left", "right", "col3", "__support_count") SELECT "left", "right", "col3", sum("__support_count") FROM (SELECT b0."left" AS "left", b1."left" AS "right", ((b0."left_size" + b1."left_size") - b2."shared") AS "col3", count(*) AS "__support_count" FROM "callee_set_size" b0, "callee_set_size" b1, "shared_count" b2 WHERE b2."left" = b0."left" AND b2."right" = b1."left" GROUP BY b0."left", b1."left", ((b0."left_size" + b1."left_size") - b2."shared")) GROUP BY "left", "right", "col3"`, `UPDATE "union_size" AS h SET "__support_count" = "__support_count" - ("__support_count" - COALESCE((SELECT n."__support_count" FROM "__support_next_union_size" n WHERE n."left" = h."left" AND n."right" = h."right" AND n."col3" = h."col3"), 0))`, `DELETE FROM "union_size" WHERE "__support_count" <= 0 RETURNING "left", "right", "col3"`, `INSERT INTO "union_size" ("left", "right", "col3", "__support_count") SELECT "left", "right", "col3", n."__support_count" FROM "__support_next_union_size" n WHERE NOT EXISTS (SELECT 1 FROM "union_size" h WHERE n."left" = h."left" AND n."right" = h."right" AND n."col3" = h."col3") RETURNING "left", "right", "col3"`], aggregateSql: null },
];

function recomputeLevels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "union_size";\nINSERT OR IGNORE INTO "union_size" ("left", "right", "col3") SELECT b0."left", b1."left", ((b0."left_size" + b1."left_size") - b2."shared") FROM "callee_set_size" b0, "callee_set_size" b1, "shared_count" b2 WHERE b2."left" = b0."left" AND b2."right" = b1."left"`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function buildDeltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const callee_set_size = multisetDiff(before.callee_set_size, after.callee_set_size);
  const shared_count = multisetDiff(before.shared_count, after.shared_count);
  const union_size = multisetDiff(before.union_size, after.union_size);
  return {
    rels: [
      { rel: "callee_set_size", add: callee_set_size.add, del: callee_set_size.del },
      { rel: "shared_count", add: shared_count.add, del: shared_count.del },
      { rel: "union_size", add: union_size.add, del: union_size.del },
    ],
    carryPending: false,
  };
}

function runNaiveTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return readSnapshot(seam).pipe(
    concatMap((before) => applyArrivals(seam, arrivals).pipe(map(() => before))),
    concatMap((before) => recomputeLevels(seam).pipe(map(() => before))),
    concatMap((before) => readSnapshot(seam).pipe(map((after) => buildDeltas(before, after)))),
  );
  // head_expression_evaluates_derived_column: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

function runIncrementalTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepareTick(seam, INCREMENTAL_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.applyArrivals(seam, arrivals, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyLevelsBeforeEdges(seam, INCREMENTAL_LEVEL_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyEdges(seam, INCREMENTAL_EDGE_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recomputeLevelsAfterEdges(seam, INCREMENTAL_LEVEL_STATEMENTS, INCREMENTAL_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.readBoundary(seam, INCREMENTAL_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promoteFrontiers(seam, INCREMENTAL_RELATIONS).pipe(
      map((carryPending): ITickDeltas => ({ rels, carryPending })),
    )),
  );
}

function runTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return runNaiveTick(seam, arrivals);
  }
  return runIncrementalTick(seam, arrivals);
}

export const incrementalPlan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcileEveryTick: RECONCILE_EVERY_TICK,
  retractionGuard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "head_expression_evaluates_derived_column",
  ddl,
  relColumns,
  arrivalTargets,
  boot,
  finalSelect,
  tick: runTick,
};
