// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: callgraph_derivation_over_extraction.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lowerSql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { multisetDiff } from "../runtime/diff.ts";
import { selectRows } from "../runtime/rows.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelDelta,
  IRow,
  IRowValue,
  ISqlSeam,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demandRel: string; readonly responseRel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly (string | number)[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly snapshot: "current" }

interface IBootStatement {
  sql: string;
  params: readonly (string | number)[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly finalSelect: Record<string, string>; readonly hostPlans: readonly IHostPlanData[]; readonly bindPlans: readonly IBindPlanData[]; readonly queryPlans: readonly IQueryPlanData[]; readonly unsupportedExecution: readonly string[] };

export const hostPlans: readonly IHostPlanData[] = [];
export const bindPlans: readonly IBindPlanData[] = [];
export const queryPlans: readonly IQueryPlanData[] = [];
export const unsupportedExecution: readonly string[] = [];

function bindArgs(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => (typeof value === "number" && Number.isInteger(value) ? BigInt(value) : value));
}

const ddl: readonly string[] = [
  `CREATE TABLE "call" ("path" TEXT NOT NULL, "callee" TEXT NOT NULL, PRIMARY KEY ("path", "callee")) WITHOUT ROWID`,
  `CREATE TABLE "calls" ("caller" TEXT NOT NULL, "callee" TEXT NOT NULL, "__support_count" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("caller", "callee")) WITHOUT ROWID`,
  `CREATE TABLE "def" ("path" TEXT NOT NULL, "name" TEXT NOT NULL, "kind" TEXT NOT NULL, "__support_count" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("path", "name", "kind")) WITHOUT ROWID`,
  `CREATE TABLE "node_fact" ("path" TEXT NOT NULL, "record" TEXT NOT NULL, "kind" TEXT NOT NULL, "name" TEXT NOT NULL, PRIMARY KEY ("path", "record", "kind", "name")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__delta_call" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "callee" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_call_sign" ON "__delta_call" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_call" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "callee" TEXT NOT NULL)`,
  `CREATE INDEX "__frontier_call_phase" ON "__frontier_call" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_call" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "callee" TEXT NOT NULL)`,
  `CREATE INDEX "__next_frontier_call_phase" ON "__next_frontier_call" ("_phase")`,
  `CREATE TEMP TABLE "__delta_calls" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "caller" TEXT NOT NULL, "callee" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_calls_sign" ON "__delta_calls" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_calls" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "caller" TEXT NOT NULL, "callee" TEXT NOT NULL)`,
  `CREATE INDEX "__frontier_calls_phase" ON "__frontier_calls" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_calls" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "caller" TEXT NOT NULL, "callee" TEXT NOT NULL)`,
  `CREATE INDEX "__next_frontier_calls_phase" ON "__next_frontier_calls" ("_phase")`,
  `CREATE TEMP TABLE "__delta_def" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "name" TEXT NOT NULL, "kind" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_def_sign" ON "__delta_def" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_def" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "name" TEXT NOT NULL, "kind" TEXT NOT NULL)`,
  `CREATE INDEX "__frontier_def_phase" ON "__frontier_def" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_def" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "name" TEXT NOT NULL, "kind" TEXT NOT NULL)`,
  `CREATE INDEX "__next_frontier_def_phase" ON "__next_frontier_def" ("_phase")`,
  `CREATE TEMP TABLE "__delta_node_fact" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "record" TEXT NOT NULL, "kind" TEXT NOT NULL, "name" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_node_fact_sign" ON "__delta_node_fact" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_node_fact" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "record" TEXT NOT NULL, "kind" TEXT NOT NULL, "name" TEXT NOT NULL)`,
  `CREATE INDEX "__frontier_node_fact_phase" ON "__frontier_node_fact" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_node_fact" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" TEXT NOT NULL, "record" TEXT NOT NULL, "kind" TEXT NOT NULL, "name" TEXT NOT NULL)`,
  `CREATE INDEX "__next_frontier_node_fact_phase" ON "__next_frontier_node_fact" ("_phase")`,
  `CREATE TEMP TABLE "__support_next_def" ("path" TEXT NOT NULL, "name" TEXT NOT NULL, "kind" TEXT NOT NULL, "__support_count" INTEGER NOT NULL, PRIMARY KEY ("path", "name", "kind")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__support_next_calls" ("caller" TEXT NOT NULL, "callee" TEXT NOT NULL, "__support_count" INTEGER NOT NULL, PRIMARY KEY ("caller", "callee")) WITHOUT ROWID`,
];

const relColumns: Record<string, readonly string[]> = {
  call: ["path", "callee"],
  calls: ["caller", "callee"],
  def: ["path", "name", "kind"],
  node_fact: ["path", "record", "kind", "name"],
};

const arrivalTargets: readonly string[] = ["call", "node_fact"];

const boot: readonly IBootStatement[] = [
  { sql: `DELETE FROM "def"`, params: [] },
  { sql: `INSERT OR IGNORE INTO "def" ("path", "name", "kind") SELECT b0."path", b0."name", b0."kind" FROM "node_fact" b0 WHERE b0."record" = 'node'`, params: [] },
  { sql: `DELETE FROM "calls"`, params: [] },
  { sql: `INSERT OR IGNORE INTO "calls" ("caller", "callee") SELECT b0."name", b1."callee" FROM "def" b0, "call" b1, "def" b2 WHERE b1."path" = b0."path" AND b2."name" = b1."callee" AND (b0."name" <> b1."callee")`, params: [] },
];

type Snapshot = {
  readonly call: readonly IRow[];
  readonly calls: readonly IRow[];
  readonly def: readonly IRow[];
  readonly node_fact: readonly IRow[];
};

function readSnapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    call: selectRows(seam, `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("callee") AND json_type("callee") = 'object' THEN json_extract("callee", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("callee", '$.args')) || ')' ELSE "callee" END AS "callee" FROM "call"`, relColumns.call!),
    calls: selectRows(seam, `SELECT CASE WHEN json_valid("caller") AND json_type("caller") = 'object' THEN json_extract("caller", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("caller", '$.args')) || ')' ELSE "caller" END AS "caller", CASE WHEN json_valid("callee") AND json_type("callee") = 'object' THEN json_extract("callee", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("callee", '$.args')) || ')' ELSE "callee" END AS "callee" FROM "calls"`, relColumns.calls!),
    def: selectRows(seam, `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", CASE WHEN json_valid("kind") AND json_type("kind") = 'object' THEN json_extract("kind", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("kind", '$.args')) || ')' ELSE "kind" END AS "kind" FROM "def"`, relColumns.def!),
    node_fact: selectRows(seam, `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("record") AND json_type("record") = 'object' THEN json_extract("record", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("record", '$.args')) || ')' ELSE "record" END AS "record", CASE WHEN json_valid("kind") AND json_type("kind") = 'object' THEN json_extract("kind", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("kind", '$.args')) || ')' ELSE "kind" END AS "kind", CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name" FROM "node_fact"`, relColumns.node_fact!),
  });
}

const finalSelect: Record<string, string> = {
  call: `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("callee") AND json_type("callee") = 'object' THEN json_extract("callee", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("callee", '$.args')) || ')' ELSE "callee" END AS "callee" FROM "call"`,
  calls: `SELECT CASE WHEN json_valid("caller") AND json_type("caller") = 'object' THEN json_extract("caller", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("caller", '$.args')) || ')' ELSE "caller" END AS "caller", CASE WHEN json_valid("callee") AND json_type("callee") = 'object' THEN json_extract("callee", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("callee", '$.args')) || ')' ELSE "callee" END AS "callee" FROM "calls"`,
  def: `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", CASE WHEN json_valid("kind") AND json_type("kind") = 'object' THEN json_extract("kind", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("kind", '$.args')) || ')' ELSE "kind" END AS "kind" FROM "def"`,
  node_fact: `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("record") AND json_type("record") = 'object' THEN json_extract("record", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("record", '$.args')) || ')' ELSE "record" END AS "record", CASE WHEN json_valid("kind") AND json_type("kind") = 'object' THEN json_extract("kind", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("kind", '$.args')) || ')' ELSE "kind" END AS "kind", CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name" FROM "node_fact"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; addSql: string; delSql: string | null }> = {
  call: { kind: "set", addSql: `INSERT OR IGNORE INTO "call" ("path", "callee") VALUES (?, ?)`, delSql: `DELETE FROM "call" WHERE "path" = ? AND "callee" = ?` },
  node_fact: { kind: "set", addSql: `INSERT OR IGNORE INTO "node_fact" ("path", "record", "kind", "name") VALUES (?, ?, ?, ?)`, delSql: `DELETE FROM "node_fact" WHERE "path" = ? AND "record" = ? AND "kind" = ? AND "name" = ?` },
};

function arrivalStatement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`callgraph_derivation_over_extraction: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`callgraph_derivation_over_extraction: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.delSql === null) {
      throw new Error(`callgraph_derivation_over_extraction: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.delSql, args: bindArgs(arrival.row) };
  }
  return { sql: template.addSql, args: bindArgs(arrival.row) };
}

function applyArrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrivalStatement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "call", kind: "set", tableName: "call", deltaTableName: "__delta_call", frontierTableName: "__frontier_call", nextFrontierTableName: "__next_frontier_call", columns: ["path", "callee"], keyIndices: [], arrivalAddSql: `INSERT OR IGNORE INTO "call" ("path", "callee") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "path", "callee"`, arrivalDelSql: `DELETE FROM "call" WHERE ("path", "callee") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "path", "callee"`, boundarySql: `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("callee") AND json_type("callee") = 'object' THEN json_extract("callee", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("callee", '$.args')) || ')' ELSE "callee" END AS "callee", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_call" WHERE "_sign" IN (-1, 1) GROUP BY "path", "callee", "_sign"` },
  { rel: "calls", kind: "set", tableName: "calls", deltaTableName: "__delta_calls", frontierTableName: "__frontier_calls", nextFrontierTableName: "__next_frontier_calls", columns: ["caller", "callee"], keyIndices: [], arrivalAddSql: null, arrivalDelSql: null, boundarySql: `SELECT CASE WHEN json_valid("caller") AND json_type("caller") = 'object' THEN json_extract("caller", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("caller", '$.args')) || ')' ELSE "caller" END AS "caller", CASE WHEN json_valid("callee") AND json_type("callee") = 'object' THEN json_extract("callee", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("callee", '$.args')) || ')' ELSE "callee" END AS "callee", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_calls" WHERE "_sign" IN (-1, 1) GROUP BY "caller", "callee", "_sign"` },
  { rel: "def", kind: "set", tableName: "def", deltaTableName: "__delta_def", frontierTableName: "__frontier_def", nextFrontierTableName: "__next_frontier_def", columns: ["path", "name", "kind"], keyIndices: [], arrivalAddSql: null, arrivalDelSql: null, boundarySql: `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", CASE WHEN json_valid("kind") AND json_type("kind") = 'object' THEN json_extract("kind", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("kind", '$.args')) || ')' ELSE "kind" END AS "kind", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_def" WHERE "_sign" IN (-1, 1) GROUP BY "path", "name", "kind", "_sign"` },
  { rel: "node_fact", kind: "set", tableName: "node_fact", deltaTableName: "__delta_node_fact", frontierTableName: "__frontier_node_fact", nextFrontierTableName: "__next_frontier_node_fact", columns: ["path", "record", "kind", "name"], keyIndices: [], arrivalAddSql: `INSERT OR IGNORE INTO "node_fact" ("path", "record", "kind", "name") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]'), json_extract(value, '$[2]'), json_extract(value, '$[3]') FROM json_each(?) RETURNING "path", "record", "kind", "name"`, arrivalDelSql: `DELETE FROM "node_fact" WHERE ("path", "record", "kind", "name") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]'), json_extract(value, '$[2]'), json_extract(value, '$[3]') FROM json_each(?)) RETURNING "path", "record", "kind", "name"`, boundarySql: `SELECT CASE WHEN json_valid("path") AND json_type("path") = 'object' THEN json_extract("path", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("path", '$.args')) || ')' ELSE "path" END AS "path", CASE WHEN json_valid("record") AND json_type("record") = 'object' THEN json_extract("record", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("record", '$.args')) || ')' ELSE "record" END AS "record", CASE WHEN json_valid("kind") AND json_type("kind") = 'object' THEN json_extract("kind", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("kind", '$.args')) || ')' ELSE "kind" END AS "kind", CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_node_fact" WHERE "_sign" IN (-1, 1) GROUP BY "path", "record", "kind", "name", "_sign"` },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { headRel: "def", headDeltaTableName: "__delta_def", headColumns: ["path", "name", "kind"], insertSql: `INSERT OR IGNORE INTO "def" ("path", "name", "kind") SELECT DISTINCT d0."path", d0."name", d0."kind" FROM "__frontier_node_fact" d0 WHERE d0."_phase" >= 0 AND d0."record" = 'node' RETURNING "path", "name", "kind"`, selectSql: `SELECT "path", "name", "kind" FROM "def"`, recomputeSql: `DELETE FROM "def";\nINSERT OR IGNORE INTO "def" ("path", "name", "kind") SELECT b0."path", b0."name", b0."kind" FROM "node_fact" b0 WHERE b0."record" = 'node'`, supportSql: [`DELETE FROM "__support_next_def"`, `INSERT INTO "__support_next_def" ("path", "name", "kind", "__support_count") SELECT "path", "name", "kind", sum("__support_count") FROM (SELECT b0."path" AS "path", b0."name" AS "name", b0."kind" AS "kind", count(*) AS "__support_count" FROM "node_fact" b0 WHERE b0."record" = 'node' GROUP BY b0."path", b0."name", b0."kind") GROUP BY "path", "name", "kind"`, `UPDATE "def" AS h SET "__support_count" = "__support_count" - ("__support_count" - COALESCE((SELECT n."__support_count" FROM "__support_next_def" n WHERE n."path" = h."path" AND n."name" = h."name" AND n."kind" = h."kind"), 0))`, `DELETE FROM "def" WHERE "__support_count" <= 0 RETURNING "path", "name", "kind"`, `INSERT INTO "def" ("path", "name", "kind", "__support_count") SELECT "path", "name", "kind", n."__support_count" FROM "__support_next_def" n WHERE NOT EXISTS (SELECT 1 FROM "def" h WHERE n."path" = h."path" AND n."name" = h."name" AND n."kind" = h."kind") RETURNING "path", "name", "kind"`], aggregateSql: null },
  { headRel: "calls", headDeltaTableName: "__delta_calls", headColumns: ["caller", "callee"], insertSql: `INSERT OR IGNORE INTO "calls" ("caller", "callee") SELECT DISTINCT d0."name", b0."callee" FROM "__frontier_def" d0, "call" b0, "def" b1 WHERE d0."_phase" >= 0 AND b0."path" = d0."path" AND b1."name" = b0."callee" AND (d0."name" <> b0."callee") UNION ALL SELECT DISTINCT b0."name", d0."callee" FROM "__frontier_call" d0, "def" b0, "def" b1 WHERE d0."_phase" >= 0 AND b0."path" = d0."path" AND b1."name" = d0."callee" AND (b0."name" <> d0."callee") UNION ALL SELECT DISTINCT b0."name", d0."name" FROM "__frontier_def" d0, "def" b0, "call" b1 WHERE d0."_phase" >= 0 AND b1."path" = b0."path" AND b1."callee" = d0."name" AND (b0."name" <> d0."name") RETURNING "caller", "callee"`, selectSql: `SELECT "caller", "callee" FROM "calls"`, recomputeSql: `DELETE FROM "calls";\nINSERT OR IGNORE INTO "calls" ("caller", "callee") SELECT b0."name", b1."callee" FROM "def" b0, "call" b1, "def" b2 WHERE b1."path" = b0."path" AND b2."name" = b1."callee" AND (b0."name" <> b1."callee")`, supportSql: [`DELETE FROM "__support_next_calls"`, `INSERT INTO "__support_next_calls" ("caller", "callee", "__support_count") SELECT "caller", "callee", sum("__support_count") FROM (SELECT b0."name" AS "caller", b1."callee" AS "callee", count(*) AS "__support_count" FROM "def" b0, "call" b1, "def" b2 WHERE b1."path" = b0."path" AND b2."name" = b1."callee" AND (b0."name" <> b1."callee") GROUP BY b0."name", b1."callee") GROUP BY "caller", "callee"`, `UPDATE "calls" AS h SET "__support_count" = "__support_count" - ("__support_count" - COALESCE((SELECT n."__support_count" FROM "__support_next_calls" n WHERE n."caller" = h."caller" AND n."callee" = h."callee"), 0))`, `DELETE FROM "calls" WHERE "__support_count" <= 0 RETURNING "caller", "callee"`, `INSERT INTO "calls" ("caller", "callee", "__support_count") SELECT "caller", "callee", n."__support_count" FROM "__support_next_calls" n WHERE NOT EXISTS (SELECT 1 FROM "calls" h WHERE n."caller" = h."caller" AND n."callee" = h."callee") RETURNING "caller", "callee"`], aggregateSql: null },
];

function recomputeLevels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "def";\nINSERT OR IGNORE INTO "def" ("path", "name", "kind") SELECT b0."path", b0."name", b0."kind" FROM "node_fact" b0 WHERE b0."record" = 'node';\nDELETE FROM "calls";\nINSERT OR IGNORE INTO "calls" ("caller", "callee") SELECT b0."name", b1."callee" FROM "def" b0, "call" b1, "def" b2 WHERE b1."path" = b0."path" AND b2."name" = b1."callee" AND (b0."name" <> b1."callee")`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function buildDeltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const call = multisetDiff(before.call, after.call);
  const calls = multisetDiff(before.calls, after.calls);
  const def = multisetDiff(before.def, after.def);
  const node_fact = multisetDiff(before.node_fact, after.node_fact);
  return {
    rels: [
      { rel: "call", add: call.add, del: call.del },
      { rel: "calls", add: calls.add, del: calls.del },
      { rel: "def", add: def.add, del: def.del },
      { rel: "node_fact", add: node_fact.add, del: node_fact.del },
    ],
    carryPending: false,
  };
}

function runNaiveTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return readSnapshot(seam).pipe(
    concatMap((before) => applyArrivals(seam, arrivals).pipe(map(() => before))),
    concatMap((before) => recomputeLevels(seam).pipe(map(() => before))),
    concatMap((before) => readSnapshot(seam).pipe(map((after) => buildDeltas(before, after)))),
  );
  // callgraph_derivation_over_extraction: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

function runIncrementalTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepareTick(seam, INCREMENTAL_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.applyArrivals(seam, arrivals, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyLevelsBeforeEdges(seam, INCREMENTAL_LEVEL_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyEdges(seam, INCREMENTAL_EDGE_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recomputeLevelsAfterEdges(seam, INCREMENTAL_LEVEL_STATEMENTS, INCREMENTAL_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.readBoundary(seam, INCREMENTAL_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promoteFrontiers(seam, INCREMENTAL_RELATIONS).pipe(
      map((carryPending): ITickDeltas => ({ rels, carryPending })),
    )),
  );
}

function runTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return runNaiveTick(seam, arrivals);
  }
  return runIncrementalTick(seam, arrivals);
}

export const incrementalPlan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcileEveryTick: RECONCILE_EVERY_TICK,
  retractionGuard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "callgraph_derivation_over_extraction",
  ddl,
  relColumns,
  arrivalTargets,
  boot,
  finalSelect,
  hostPlans,
  bindPlans,
  queryPlans,
  unsupportedExecution,
  tick: runTick,
};
