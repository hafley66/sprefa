// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: keyed_replace_departs_the_old_row.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { EnumPlane } from "../runtime/enumPlane.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IEnumRefColumns,
  IEnumTypePlan,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly ir_version: number; readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly rel_physical_names: Record<string, string>; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const ENUM_TYPES: readonly IEnumTypePlan[] = [];
export const ENUM_REF_COLUMNS: IEnumRefColumns = {};

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "from_poll": [true, true],
    "latest": [true, true],
    "replaced_value": [true, true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" ("key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value" FROM "keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" t`,
  `CREATE TABLE "keyed_replace_departs_the_old_row_latest" ("__id" INTEGER PRIMARY KEY, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL, UNIQUE ("key"))`,
  `CREATE TEMP VIEW "__txt_keyed_replace_departs_the_old_row_latest" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value" FROM "keyed_replace_departs_the_old_row_latest" t`,
  `CREATE TABLE "keyed_replace_departs_the_old_row_replaced_value" ("key" INTEGER NOT NULL, "old_value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt_keyed_replace_departs_the_old_row_replaced_value" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."old_value") AS "old_value" FROM "keyed_replace_departs_the_old_row_replaced_value" t`,
  `CREATE TEMP TABLE "__delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a_sign" ON "__delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" ("_sign")`,
  `CREATE INDEX "__delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a_group" ON "__delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" ("key", "value")`,
  `CREATE TEMP TABLE "__frontier_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a_phase" ON "__frontier_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" t`,
  `CREATE TEMP TABLE "__delta_keyed_replace_departs_the_old_row_latest" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_keyed_replace_departs_the_old_row_latest_sign" ON "__delta_keyed_replace_departs_the_old_row_latest" ("_sign")`,
  `CREATE INDEX "__delta_keyed_replace_departs_the_old_row_latest_group" ON "__delta_keyed_replace_departs_the_old_row_latest" ("key", "value")`,
  `CREATE TEMP TABLE "__frontier_keyed_replace_departs_the_old_row_latest" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_keyed_replace_departs_the_old_row_latest_phase" ON "__frontier_keyed_replace_departs_the_old_row_latest" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_keyed_replace_departs_the_old_row_latest" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_keyed_replace_departs_the_old_row_latest" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_keyed_replace_departs_the_old_row_latest" t`,
  `CREATE TEMP TABLE "__departure_frontier_keyed_replace_departs_the_old_row_latest" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" TEXT NOT NULL, "value" TEXT NOT NULL)`,
  `CREATE TEMP TABLE "__delta_keyed_replace_departs_the_old_row_replaced_value" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "old_value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_keyed_replace_departs_the_old_row_replaced_value_sign" ON "__delta_keyed_replace_departs_the_old_row_replaced_value" ("_sign")`,
  `CREATE INDEX "__delta_keyed_replace_departs_the_old_row_replaced_value_group" ON "__delta_keyed_replace_departs_the_old_row_replaced_value" ("key", "old_value")`,
  `CREATE TEMP TABLE "__frontier_keyed_replace_departs_the_old_row_replaced_value" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "old_value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_keyed_replace_departs_the_old_row_replaced_value_phase" ON "__frontier_keyed_replace_departs_the_old_row_replaced_value" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_keyed_replace_departs_the_old_row_replaced_value" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "old_value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_keyed_replace_departs_the_old_row_replaced_value" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."old_value") AS "old_value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_keyed_replace_departs_the_old_row_replaced_value" t`,
];

const rel_columns: Record<string, readonly string[]> = {
  from_poll: ["key", "value"],
  latest: ["key", "value"],
  replaced_value: ["key", "old_value"],
};

const rel_physical_names: Record<string, string> = {
  from_poll: "keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a",
  latest: "keyed_replace_departs_the_old_row_latest",
  replaced_value: "keyed_replace_departs_the_old_row_replaced_value",
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  from_poll: ["text", "text"],
  latest: ["text", "text"],
  replaced_value: ["text", "text"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  from_poll: ["text", "text"],
  latest: ["text", "text"],
  replaced_value: ["text", "text"],
};

const rel_catalog: readonly IRelCatalogRow[] = new Array<IRelCatalogRow>(
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "keyed_replace_departs_the_old_row", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "ec3c2be9d41764a8", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "from_poll", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "a3701f9a0a3459ab", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "key", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "a1165d6de492cdff", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 8, ordinal: 2, local_name: "value", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "5e3eb30d65f3e654", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 7, ordinal: 0, local_name: "latest", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "cfaca1224e60491d", h_schema: "87ce2f5d4367440d", h_rule: "efbd520524bfc7cd" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "key", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "bbc8048ba3ca7a09", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 11, ordinal: 2, local_name: "value", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "4c7d07ecad6e319a", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 7, ordinal: 0, local_name: "replaced_value", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "36e714c7cbe877cd", h_schema: "f51fb77eb3672279", h_rule: "793993b429d1f40a" },
  { rel_id: 15, parent_id: 14, ordinal: 1, local_name: "key", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "2c46580ad76ada15", h_schema: "", h_rule: "" },
  { rel_id: 16, parent_id: 14, ordinal: 2, local_name: "old_value", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "d4558329927e36b4", h_schema: "", h_rule: "" },
  { rel_id: 17, parent_id: 8, ordinal: 0, local_name: "__delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "303988accbff6c0d", h_schema: "fdf6b6ef55287c94", h_rule: "" },
  { rel_id: 18, parent_id: 8, ordinal: 0, local_name: "__frontier_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "a7bc8260af46bbdf", h_schema: "9dceee669bf5f906", h_rule: "" },
  { rel_id: 19, parent_id: 8, ordinal: 0, local_name: "__next_frontier_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "ad4447fee79697b3", h_schema: "9dceee669bf5f906", h_rule: "" },
  { rel_id: 20, parent_id: 8, ordinal: 0, local_name: "__txt_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "85be6eb76bbfe7ec", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 21, parent_id: 17, ordinal: 0, local_name: "__txt___delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "2c96615e39f88164", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 22, parent_id: 11, ordinal: 0, local_name: "__delta_keyed_replace_departs_the_old_row_latest", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "f8942c550b8550b3", h_schema: "fdf6b6ef55287c94", h_rule: "" },
  { rel_id: 23, parent_id: 11, ordinal: 0, local_name: "__frontier_keyed_replace_departs_the_old_row_latest", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "50f46ada4b76fb81", h_schema: "9dceee669bf5f906", h_rule: "" },
  { rel_id: 24, parent_id: 11, ordinal: 0, local_name: "__next_frontier_keyed_replace_departs_the_old_row_latest", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "59d1477a0719ec1b", h_schema: "9dceee669bf5f906", h_rule: "" },
  { rel_id: 25, parent_id: 11, ordinal: 0, local_name: "__departure_frontier_keyed_replace_departs_the_old_row_latest", kind: "departure", type_id: 0, arity: 4, module_id: 7, h_id: "4f35f563157927b6", h_schema: "9dceee669bf5f906", h_rule: "" },
  { rel_id: 26, parent_id: 11, ordinal: 0, local_name: "__txt_keyed_replace_departs_the_old_row_latest", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "0d48dbd5f2318aac", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 27, parent_id: 22, ordinal: 0, local_name: "__txt___delta_keyed_replace_departs_the_old_row_latest", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "dc2dbbc4aa3e3de1", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 28, parent_id: 14, ordinal: 0, local_name: "__delta_keyed_replace_departs_the_old_row_replaced_value", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "e3d6e746f94662b1", h_schema: "bfba1e5956f8aa9c", h_rule: "" },
  { rel_id: 29, parent_id: 14, ordinal: 0, local_name: "__frontier_keyed_replace_departs_the_old_row_replaced_value", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "47e8876cf460fdd3", h_schema: "d6af49ff198a1492", h_rule: "" },
  { rel_id: 30, parent_id: 14, ordinal: 0, local_name: "__next_frontier_keyed_replace_departs_the_old_row_replaced_value", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "d2cc32340944b3d0", h_schema: "d6af49ff198a1492", h_rule: "" },
  { rel_id: 31, parent_id: 14, ordinal: 0, local_name: "__txt_keyed_replace_departs_the_old_row_replaced_value", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "9e3de246a6a169e1", h_schema: "f51fb77eb3672279", h_rule: "" },
  { rel_id: 32, parent_id: 28, ordinal: 0, local_name: "__txt___delta_keyed_replace_departs_the_old_row_replaced_value", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "e3bcd1323760425a", h_schema: "f51fb77eb3672279", h_rule: "" },
  { rel_id: 33, parent_id: 7, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 7, h_id: "bde92debeb84abc0", h_schema: "", h_rule: "" },
  { rel_id: 34, parent_id: 9, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "c1f7f27c3f7c921c", h_schema: "", h_rule: "" },
  { rel_id: 35, parent_id: 10, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "71fe809c59394bc8", h_schema: "", h_rule: "" },
  { rel_id: 36, parent_id: 12, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "134889f95a784808", h_schema: "", h_rule: "" },
  { rel_id: 37, parent_id: 13, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "d2e278dbbef8ad30", h_schema: "", h_rule: "" },
  { rel_id: 38, parent_id: 15, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "e248186a243ebb8f", h_schema: "", h_rule: "" },
  { rel_id: 39, parent_id: 16, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "017a016c2c17c3c7", h_schema: "", h_rule: "" },
);

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["from_poll"];

const boot: readonly IBootStatement[] = [
];

const final_select: Record<string, string> = {
  from_poll: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value" FROM "__txt_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" t`,
  latest: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value" FROM "__txt_keyed_replace_departs_the_old_row_latest" t`,
  replaced_value: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."old_value") AND json_type(t."old_value") = 'object' AND json_type(t."old_value", '$.fn') = 'text' AND json_type(t."old_value", '$.args') = 'array' THEN json_extract(t."old_value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."old_value", '$.args')), '') || ')' ELSE t."old_value" END AS "old_value" FROM "__txt_keyed_replace_departs_the_old_row_replaced_value" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "from_poll", kind: "log", table_name: "keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a", delta_table_name: "__delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a", frontier_table_name: "__frontier_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a", next_frontier_table_name: "__next_frontier_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a", columns: ["key", "value"], column_types: ["text", "text"], key_indices: [], arrival_add_sql: `INSERT INTO "keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" ("key", "value") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "key", "value"`, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" t WHERE t."_sign" IN (-1, 1) GROUP BY t."key", t."value", t."_sign"`, rule_observers: ["latest/2"] },
  { rel: "latest", kind: "set", table_name: "keyed_replace_departs_the_old_row_latest", delta_table_name: "__delta_keyed_replace_departs_the_old_row_latest", frontier_table_name: "__frontier_keyed_replace_departs_the_old_row_latest", next_frontier_table_name: "__next_frontier_keyed_replace_departs_the_old_row_latest", columns: ["key", "value"], column_types: ["text", "text"], key_indices: [0], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_keyed_replace_departs_the_old_row_latest" t WHERE t."_sign" IN (-1, 1) GROUP BY t."key", t."value", t."_sign"`, departure_frontier_table_name: "__departure_frontier_keyed_replace_departs_the_old_row_latest", rule_observers: ["replaced_value/2"] },
  { rel: "replaced_value", kind: "log", table_name: "keyed_replace_departs_the_old_row_replaced_value", delta_table_name: "__delta_keyed_replace_departs_the_old_row_replaced_value", frontier_table_name: "__frontier_keyed_replace_departs_the_old_row_replaced_value", next_frontier_table_name: "__next_frontier_keyed_replace_departs_the_old_row_replaced_value", columns: ["key", "old_value"], column_types: ["text", "text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."old_value") AND json_type(t."old_value") = 'object' AND json_type(t."old_value", '$.fn') = 'text' AND json_type(t."old_value", '$.args') = 'array' THEN json_extract(t."old_value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."old_value", '$.args')), '') || ')' ELSE t."old_value" END AS "old_value", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_keyed_replace_departs_the_old_row_replaced_value" t WHERE t."_sign" IN (-1, 1) GROUP BY t."key", t."old_value", t."_sign"`, rule_observers: [] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
  { head_rel: "latest", rule_id: "keyed_replace_departs_the_old_row:latest/2#1", head_kind: "set", head_table_name: "keyed_replace_departs_the_old_row_latest", head_delta_table_name: "__delta_keyed_replace_departs_the_old_row_latest", head_columns: ["key", "value"], key_indices: [0], project_sql: `SELECT d0."key" AS "key", d0."value" AS "value" FROM "__frontier_keyed_replace_departs_the_old_row_from_poll_e7a2a44c403a" d0 WHERE d0."_phase" >= 0 ORDER BY d0."_phase", d0."_sequence"` },
  { head_rel: "replaced_value", rule_id: "keyed_replace_departs_the_old_row:replaced_value/2#1", head_kind: "log", head_table_name: "keyed_replace_departs_the_old_row_replaced_value", head_delta_table_name: "__delta_keyed_replace_departs_the_old_row_replaced_value", head_columns: ["key", "old_value"], key_indices: [], project_sql: `SELECT (SELECT s."__id" FROM "__str" s WHERE s."content" = d0."key") AS "key", (SELECT s."__id" FROM "__str" s WHERE s."content" = d0."value") AS "old_value" FROM "__departure_frontier_keyed_replace_departs_the_old_row_latest" d0 WHERE d0."_phase" >= 0 ORDER BY d0."_phase", d0."_sequence"`, intern_sql: [`INSERT OR IGNORE INTO "__str" ("content") SELECT DISTINCT d0."key" FROM "__departure_frontier_keyed_replace_departs_the_old_row_latest" d0 WHERE d0."_phase" >= 0 UNION SELECT DISTINCT d0."value" FROM "__departure_frontier_keyed_replace_departs_the_old_row_latest" d0 WHERE d0."_phase" >= 0`] },
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.recompute_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK, arrivals)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.merge_next_into_current(seam, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.stage_departures(seam, SUBSCRIBED_RELATIONS, rels).pipe(map(() => rels))),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      concatMap((carry_pending) => EnumPlane.decode_deltas(seam, ENUM_TYPES, ENUM_REF_COLUMNS, SUBSCRIBED_RELATIONS, rels).pipe(
        map((decoded): ITickDeltas => ({ rels: decoded, carry_pending })),
      )),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return EnumPlane.intern(seam, ENUM_TYPES, ENUM_REF_COLUMNS, arrivals).pipe(
    concatMap((normalized) => run_incremental_tick(seam, validate_arrivals(normalized))),
  );
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "keyed_replace_departs_the_old_row",
  ir_version: 1,
  internMode: "dict",
  ddl,
  rel_columns,
  rel_physical_names,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  enum_types: ENUM_TYPES,
  enum_ref_columns: ENUM_REF_COLUMNS,
  unsupported_execution,
  tick: run_tick,
};
