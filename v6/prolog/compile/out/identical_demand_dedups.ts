// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: identical_demand_dedups.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { EnumPlane } from "../runtime/enumPlane.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IEnumRefColumns,
  IEnumTypePlan,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly ir_version: number; readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly rel_physical_names: Record<string, string>; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const ENUM_TYPES: readonly IEnumTypePlan[] = [];
export const ENUM_REF_COLUMNS: IEnumRefColumns = {};

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "demand": [true, true],
    "fill": [true, true, true],
    "response": [true, true, true],
    "watch_request": [true, true, true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "identical_demand_dedups_demand" ("__id" INTEGER PRIMARY KEY, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL, UNIQUE ("args", "salt"))`,
  `CREATE TEMP VIEW "__txt_identical_demand_dedups_demand" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."args") AS "args", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."salt") AS "salt" FROM "identical_demand_dedups_demand" t`,
  `CREATE TABLE "identical_demand_dedups_fill_3d9eb2ac63c4" ("args" INTEGER NOT NULL, "salt" INTEGER NOT NULL, "payload" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt_identical_demand_dedups_fill_3d9eb2ac63c4" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."args") AS "args", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."salt") AS "salt", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."payload") AS "payload" FROM "identical_demand_dedups_fill_3d9eb2ac63c4" t`,
  `CREATE TABLE "identical_demand_dedups_response" ("args" INTEGER NOT NULL, "salt" INTEGER NOT NULL, "payload" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt_identical_demand_dedups_response" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."args") AS "args", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."salt") AS "salt", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."payload") AS "payload" FROM "identical_demand_dedups_response" t`,
  `CREATE TABLE "identical_demand_dedups_watch_request_f39914cb1980" ("col1" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt_identical_demand_dedups_watch_request_f39914cb1980" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."col1") AS "col1", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."args") AS "args", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."salt") AS "salt" FROM "identical_demand_dedups_watch_request_f39914cb1980" t`,
  `CREATE TEMP TABLE "__delta_identical_demand_dedups_demand" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_identical_demand_dedups_demand_sign" ON "__delta_identical_demand_dedups_demand" ("_sign")`,
  `CREATE INDEX "__delta_identical_demand_dedups_demand_group" ON "__delta_identical_demand_dedups_demand" ("args", "salt")`,
  `CREATE TEMP TABLE "__frontier_identical_demand_dedups_demand" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_identical_demand_dedups_demand_phase" ON "__frontier_identical_demand_dedups_demand" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_identical_demand_dedups_demand" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_identical_demand_dedups_demand" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."args") AS "args", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."salt") AS "salt", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_identical_demand_dedups_demand" t`,
  `CREATE TEMP TABLE "__delta_identical_demand_dedups_fill_3d9eb2ac63c4" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL, "payload" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_identical_demand_dedups_fill_3d9eb2ac63c4_sign" ON "__delta_identical_demand_dedups_fill_3d9eb2ac63c4" ("_sign")`,
  `CREATE INDEX "__delta_identical_demand_dedups_fill_3d9eb2ac63c4_group" ON "__delta_identical_demand_dedups_fill_3d9eb2ac63c4" ("args", "salt", "payload")`,
  `CREATE TEMP TABLE "__frontier_identical_demand_dedups_fill_3d9eb2ac63c4" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL, "payload" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_identical_demand_dedups_fill_3d9eb2ac63c4_phase" ON "__frontier_identical_demand_dedups_fill_3d9eb2ac63c4" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_identical_demand_dedups_fill_3d9eb2ac63c4" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL, "payload" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_identical_demand_dedups_fill_3d9eb2ac63c4" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."args") AS "args", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."salt") AS "salt", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."payload") AS "payload", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_identical_demand_dedups_fill_3d9eb2ac63c4" t`,
  `CREATE TEMP TABLE "__delta_identical_demand_dedups_response" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL, "payload" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_identical_demand_dedups_response_sign" ON "__delta_identical_demand_dedups_response" ("_sign")`,
  `CREATE INDEX "__delta_identical_demand_dedups_response_group" ON "__delta_identical_demand_dedups_response" ("args", "salt", "payload")`,
  `CREATE TEMP TABLE "__frontier_identical_demand_dedups_response" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL, "payload" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_identical_demand_dedups_response_phase" ON "__frontier_identical_demand_dedups_response" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_identical_demand_dedups_response" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL, "payload" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_identical_demand_dedups_response" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."args") AS "args", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."salt") AS "salt", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."payload") AS "payload", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_identical_demand_dedups_response" t`,
  `CREATE TEMP TABLE "__delta_identical_demand_dedups_watch_request_f39914cb1980" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "col1" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_identical_demand_dedups_watch_request_f39914cb1980_sign" ON "__delta_identical_demand_dedups_watch_request_f39914cb1980" ("_sign")`,
  `CREATE INDEX "__delta_identical_demand_dedups_watch_request_f39914cb1980_group" ON "__delta_identical_demand_dedups_watch_request_f39914cb1980" ("col1", "args", "salt")`,
  `CREATE TEMP TABLE "__frontier_identical_demand_dedups_watch_request_f39914cb1980" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "col1" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_identical_demand_dedups_watch_request_f39914cb1980_phase" ON "__frontier_identical_demand_dedups_watch_request_f39914cb1980" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_identical_demand_dedups_watch_request_f39914cb1980" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "col1" INTEGER NOT NULL, "args" INTEGER NOT NULL, "salt" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_identical_demand_dedups_watch_request_f39914cb1980" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."col1") AS "col1", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."args") AS "args", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."salt") AS "salt", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_identical_demand_dedups_watch_request_f39914cb1980" t`,
];

const rel_columns: Record<string, readonly string[]> = {
  demand: ["args", "salt"],
  fill: ["args", "salt", "payload"],
  response: ["args", "salt", "payload"],
  watch_request: ["col1", "args", "salt"],
};

const rel_physical_names: Record<string, string> = {
  demand: "identical_demand_dedups_demand",
  fill: "identical_demand_dedups_fill_3d9eb2ac63c4",
  response: "identical_demand_dedups_response",
  watch_request: "identical_demand_dedups_watch_request_f39914cb1980",
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  demand: ["text", "text"],
  fill: ["text", "text", "text"],
  response: ["text", "text", "text"],
  watch_request: ["text", "text", "text"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  demand: ["text", "text"],
  fill: ["text", "text", "text"],
  response: ["text", "text", "text"],
  watch_request: ["text", "text", "text"],
};

const rel_catalog: readonly IRelCatalogRow[] = new Array<IRelCatalogRow>(
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "identical_demand_dedups", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "b1da6808eafd3558", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "demand", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "8818bdee49dad899", h_schema: "a16807198159b160", h_rule: "e9c48490a723aabe" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "args", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "e49af0fcad744c81", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 8, ordinal: 2, local_name: "salt", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "b03db12264aa55f8", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 7, ordinal: 0, local_name: "fill", kind: "rel", type_id: 0, arity: 3, module_id: 7, h_id: "86669d6763ec16e8", h_schema: "8c7c3d9812b62fe0", h_rule: "" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "args", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "a435a354b4afd808", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 11, ordinal: 2, local_name: "salt", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "75bb021f2f1adea7", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 11, ordinal: 3, local_name: "payload", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "b3438d3215d85da0", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 7, ordinal: 0, local_name: "response", kind: "rel", type_id: 0, arity: 3, module_id: 7, h_id: "3e6da3956f7711f3", h_schema: "8c7c3d9812b62fe0", h_rule: "1a31aad3b6f76504" },
  { rel_id: 16, parent_id: 15, ordinal: 1, local_name: "args", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "0fca15d9f9ef88d8", h_schema: "", h_rule: "" },
  { rel_id: 17, parent_id: 15, ordinal: 2, local_name: "salt", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "ba5bd10dd27f8960", h_schema: "", h_rule: "" },
  { rel_id: 18, parent_id: 15, ordinal: 3, local_name: "payload", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "4b6b7dcbd60b9e62", h_schema: "", h_rule: "" },
  { rel_id: 19, parent_id: 7, ordinal: 0, local_name: "watch_request", kind: "rel", type_id: 0, arity: 3, module_id: 7, h_id: "107d8d3d3f24e7bb", h_schema: "7d5b45f1b8a3817c", h_rule: "" },
  { rel_id: 20, parent_id: 19, ordinal: 1, local_name: "col1", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "cda7b3f95e54a3c8", h_schema: "", h_rule: "" },
  { rel_id: 21, parent_id: 19, ordinal: 2, local_name: "args", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "f39e8a5ede2d28b0", h_schema: "", h_rule: "" },
  { rel_id: 22, parent_id: 19, ordinal: 3, local_name: "salt", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "a6c726f03b169946", h_schema: "", h_rule: "" },
  { rel_id: 23, parent_id: 8, ordinal: 0, local_name: "__delta_identical_demand_dedups_demand", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "18b637b5950e0c7b", h_schema: "2d594188e6dfac0c", h_rule: "" },
  { rel_id: 24, parent_id: 8, ordinal: 0, local_name: "__frontier_identical_demand_dedups_demand", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "d9e097befe809282", h_schema: "443233f7304e2965", h_rule: "" },
  { rel_id: 25, parent_id: 8, ordinal: 0, local_name: "__next_frontier_identical_demand_dedups_demand", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "c2496e30cf0297cc", h_schema: "443233f7304e2965", h_rule: "" },
  { rel_id: 26, parent_id: 8, ordinal: 0, local_name: "__txt_identical_demand_dedups_demand", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "840b77e49535e4ec", h_schema: "80381414cee1b243", h_rule: "" },
  { rel_id: 27, parent_id: 23, ordinal: 0, local_name: "__txt___delta_identical_demand_dedups_demand", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "8d10cae4fed18800", h_schema: "80381414cee1b243", h_rule: "" },
  { rel_id: 28, parent_id: 11, ordinal: 0, local_name: "__delta_identical_demand_dedups_fill_3d9eb2ac63c4", kind: "delta", type_id: 0, arity: 5, module_id: 7, h_id: "4ec72c03d22ce492", h_schema: "06b9ff5a9431e181", h_rule: "" },
  { rel_id: 29, parent_id: 11, ordinal: 0, local_name: "__frontier_identical_demand_dedups_fill_3d9eb2ac63c4", kind: "frontier", type_id: 0, arity: 5, module_id: 7, h_id: "6b7fa0cdeafe8735", h_schema: "3c583d1d3b2d1333", h_rule: "" },
  { rel_id: 30, parent_id: 11, ordinal: 0, local_name: "__next_frontier_identical_demand_dedups_fill_3d9eb2ac63c4", kind: "next_frontier", type_id: 0, arity: 5, module_id: 7, h_id: "6b0abf8c4918969f", h_schema: "3c583d1d3b2d1333", h_rule: "" },
  { rel_id: 31, parent_id: 11, ordinal: 0, local_name: "__txt_identical_demand_dedups_fill_3d9eb2ac63c4", kind: "view", type_id: 0, arity: 3, module_id: 7, h_id: "579825e4663bda44", h_schema: "8c7c3d9812b62fe0", h_rule: "" },
  { rel_id: 32, parent_id: 28, ordinal: 0, local_name: "__txt___delta_identical_demand_dedups_fill_3d9eb2ac63c4", kind: "view", type_id: 0, arity: 5, module_id: 7, h_id: "275ba7603d421751", h_schema: "8c7c3d9812b62fe0", h_rule: "" },
  { rel_id: 33, parent_id: 15, ordinal: 0, local_name: "__delta_identical_demand_dedups_response", kind: "delta", type_id: 0, arity: 5, module_id: 7, h_id: "97a093aa28259a5d", h_schema: "06b9ff5a9431e181", h_rule: "" },
  { rel_id: 34, parent_id: 15, ordinal: 0, local_name: "__frontier_identical_demand_dedups_response", kind: "frontier", type_id: 0, arity: 5, module_id: 7, h_id: "74d6695626737cc8", h_schema: "3c583d1d3b2d1333", h_rule: "" },
  { rel_id: 35, parent_id: 15, ordinal: 0, local_name: "__next_frontier_identical_demand_dedups_response", kind: "next_frontier", type_id: 0, arity: 5, module_id: 7, h_id: "3c2cd17a43ead074", h_schema: "3c583d1d3b2d1333", h_rule: "" },
  { rel_id: 36, parent_id: 15, ordinal: 0, local_name: "__txt_identical_demand_dedups_response", kind: "view", type_id: 0, arity: 3, module_id: 7, h_id: "23c1af7f30c63db5", h_schema: "8c7c3d9812b62fe0", h_rule: "" },
  { rel_id: 37, parent_id: 33, ordinal: 0, local_name: "__txt___delta_identical_demand_dedups_response", kind: "view", type_id: 0, arity: 5, module_id: 7, h_id: "7bdf578cf679326f", h_schema: "8c7c3d9812b62fe0", h_rule: "" },
  { rel_id: 38, parent_id: 19, ordinal: 0, local_name: "__delta_identical_demand_dedups_watch_request_f39914cb1980", kind: "delta", type_id: 0, arity: 5, module_id: 7, h_id: "7861c9e1418d8c3f", h_schema: "74868f2d6ae23ab0", h_rule: "" },
  { rel_id: 39, parent_id: 19, ordinal: 0, local_name: "__frontier_identical_demand_dedups_watch_request_f39914cb1980", kind: "frontier", type_id: 0, arity: 5, module_id: 7, h_id: "a4fe6fe699e575d1", h_schema: "5697538c1f9262c1", h_rule: "" },
  { rel_id: 40, parent_id: 19, ordinal: 0, local_name: "__next_frontier_identical_demand_dedups_watch_request_f39914cb1980", kind: "next_frontier", type_id: 0, arity: 5, module_id: 7, h_id: "c4048bb0cf6b7319", h_schema: "5697538c1f9262c1", h_rule: "" },
  { rel_id: 41, parent_id: 19, ordinal: 0, local_name: "__txt_identical_demand_dedups_watch_request_f39914cb1980", kind: "view", type_id: 0, arity: 3, module_id: 7, h_id: "c62996d16f4c6bea", h_schema: "7d5b45f1b8a3817c", h_rule: "" },
  { rel_id: 42, parent_id: 38, ordinal: 0, local_name: "__txt___delta_identical_demand_dedups_watch_request_f39914cb1980", kind: "view", type_id: 0, arity: 5, module_id: 7, h_id: "089643758e6034fc", h_schema: "7d5b45f1b8a3817c", h_rule: "" },
  { rel_id: 43, parent_id: 7, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 7, h_id: "01295488700b3252", h_schema: "", h_rule: "" },
  { rel_id: 44, parent_id: 9, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "38b6d5cfa6fb4df7", h_schema: "", h_rule: "" },
  { rel_id: 45, parent_id: 10, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "af92e9051a0f584d", h_schema: "", h_rule: "" },
  { rel_id: 46, parent_id: 12, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "d49e771f453b5c23", h_schema: "", h_rule: "" },
  { rel_id: 47, parent_id: 13, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "6786e20ca2394841", h_schema: "", h_rule: "" },
  { rel_id: 48, parent_id: 14, ordinal: 3, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "39cb484be3e2e959", h_schema: "", h_rule: "" },
  { rel_id: 49, parent_id: 16, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "ef8c8cb0e74cfe6a", h_schema: "", h_rule: "" },
  { rel_id: 50, parent_id: 17, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "1bbe7d0edcd8b1f7", h_schema: "", h_rule: "" },
  { rel_id: 51, parent_id: 18, ordinal: 3, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "cd3c644c748137b5", h_schema: "", h_rule: "" },
  { rel_id: 52, parent_id: 20, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "d44f69a02698c540", h_schema: "", h_rule: "" },
  { rel_id: 53, parent_id: 21, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "e92d1824f82e1f3e", h_schema: "", h_rule: "" },
  { rel_id: 54, parent_id: 22, ordinal: 3, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "933af4ce2a0517b0", h_schema: "", h_rule: "" },
);

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["fill", "watch_request"];

const boot: readonly IBootStatement[] = [
];

const final_select: Record<string, string> = {
  demand: `SELECT CASE WHEN json_valid(t."args") AND json_type(t."args") = 'object' AND json_type(t."args", '$.fn') = 'text' AND json_type(t."args", '$.args') = 'array' THEN json_extract(t."args", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."args", '$.args')), '') || ')' ELSE t."args" END AS "args", CASE WHEN json_valid(t."salt") AND json_type(t."salt") = 'object' AND json_type(t."salt", '$.fn') = 'text' AND json_type(t."salt", '$.args') = 'array' THEN json_extract(t."salt", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."salt", '$.args')), '') || ')' ELSE t."salt" END AS "salt" FROM "__txt_identical_demand_dedups_demand" t`,
  fill: `SELECT CASE WHEN json_valid(t."args") AND json_type(t."args") = 'object' AND json_type(t."args", '$.fn') = 'text' AND json_type(t."args", '$.args') = 'array' THEN json_extract(t."args", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."args", '$.args')), '') || ')' ELSE t."args" END AS "args", CASE WHEN json_valid(t."salt") AND json_type(t."salt") = 'object' AND json_type(t."salt", '$.fn') = 'text' AND json_type(t."salt", '$.args') = 'array' THEN json_extract(t."salt", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."salt", '$.args')), '') || ')' ELSE t."salt" END AS "salt", CASE WHEN json_valid(t."payload") AND json_type(t."payload") = 'object' AND json_type(t."payload", '$.fn') = 'text' AND json_type(t."payload", '$.args') = 'array' THEN json_extract(t."payload", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."payload", '$.args')), '') || ')' ELSE t."payload" END AS "payload" FROM "__txt_identical_demand_dedups_fill_3d9eb2ac63c4" t`,
  response: `SELECT CASE WHEN json_valid(t."args") AND json_type(t."args") = 'object' AND json_type(t."args", '$.fn') = 'text' AND json_type(t."args", '$.args') = 'array' THEN json_extract(t."args", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."args", '$.args')), '') || ')' ELSE t."args" END AS "args", CASE WHEN json_valid(t."salt") AND json_type(t."salt") = 'object' AND json_type(t."salt", '$.fn') = 'text' AND json_type(t."salt", '$.args') = 'array' THEN json_extract(t."salt", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."salt", '$.args')), '') || ')' ELSE t."salt" END AS "salt", CASE WHEN json_valid(t."payload") AND json_type(t."payload") = 'object' AND json_type(t."payload", '$.fn') = 'text' AND json_type(t."payload", '$.args') = 'array' THEN json_extract(t."payload", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."payload", '$.args')), '') || ')' ELSE t."payload" END AS "payload" FROM "__txt_identical_demand_dedups_response" t`,
  watch_request: `SELECT CASE WHEN json_valid(t."col1") AND json_type(t."col1") = 'object' AND json_type(t."col1", '$.fn') = 'text' AND json_type(t."col1", '$.args') = 'array' THEN json_extract(t."col1", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."col1", '$.args')), '') || ')' ELSE t."col1" END AS "col1", CASE WHEN json_valid(t."args") AND json_type(t."args") = 'object' AND json_type(t."args", '$.fn') = 'text' AND json_type(t."args", '$.args') = 'array' THEN json_extract(t."args", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."args", '$.args')), '') || ')' ELSE t."args" END AS "args", CASE WHEN json_valid(t."salt") AND json_type(t."salt") = 'object' AND json_type(t."salt", '$.fn') = 'text' AND json_type(t."salt", '$.args') = 'array' THEN json_extract(t."salt", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."salt", '$.args')), '') || ')' ELSE t."salt" END AS "salt" FROM "__txt_identical_demand_dedups_watch_request_f39914cb1980" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "demand", kind: "set", table_name: "identical_demand_dedups_demand", delta_table_name: "__delta_identical_demand_dedups_demand", frontier_table_name: "__frontier_identical_demand_dedups_demand", next_frontier_table_name: "__next_frontier_identical_demand_dedups_demand", columns: ["args", "salt"], column_types: ["text", "text"], key_indices: [0, 1], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."args") AND json_type(t."args") = 'object' AND json_type(t."args", '$.fn') = 'text' AND json_type(t."args", '$.args') = 'array' THEN json_extract(t."args", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."args", '$.args')), '') || ')' ELSE t."args" END AS "args", CASE WHEN json_valid(t."salt") AND json_type(t."salt") = 'object' AND json_type(t."salt", '$.fn') = 'text' AND json_type(t."salt", '$.args') = 'array' THEN json_extract(t."salt", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."salt", '$.args')), '') || ')' ELSE t."salt" END AS "salt", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_identical_demand_dedups_demand" t WHERE t."_sign" IN (-1, 1) GROUP BY t."args", t."salt", t."_sign"`, rule_observers: [] },
  { rel: "fill", kind: "log", table_name: "identical_demand_dedups_fill_3d9eb2ac63c4", delta_table_name: "__delta_identical_demand_dedups_fill_3d9eb2ac63c4", frontier_table_name: "__frontier_identical_demand_dedups_fill_3d9eb2ac63c4", next_frontier_table_name: "__next_frontier_identical_demand_dedups_fill_3d9eb2ac63c4", columns: ["args", "salt", "payload"], column_types: ["text", "text", "text"], key_indices: [], arrival_add_sql: `INSERT INTO "identical_demand_dedups_fill_3d9eb2ac63c4" ("args", "salt", "payload") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]'), json_extract(value, '$[2]') FROM json_each(?) RETURNING "args", "salt", "payload"`, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."args") AND json_type(t."args") = 'object' AND json_type(t."args", '$.fn') = 'text' AND json_type(t."args", '$.args') = 'array' THEN json_extract(t."args", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."args", '$.args')), '') || ')' ELSE t."args" END AS "args", CASE WHEN json_valid(t."salt") AND json_type(t."salt") = 'object' AND json_type(t."salt", '$.fn') = 'text' AND json_type(t."salt", '$.args') = 'array' THEN json_extract(t."salt", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."salt", '$.args')), '') || ')' ELSE t."salt" END AS "salt", CASE WHEN json_valid(t."payload") AND json_type(t."payload") = 'object' AND json_type(t."payload", '$.fn') = 'text' AND json_type(t."payload", '$.args') = 'array' THEN json_extract(t."payload", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."payload", '$.args')), '') || ')' ELSE t."payload" END AS "payload", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_identical_demand_dedups_fill_3d9eb2ac63c4" t WHERE t."_sign" IN (-1, 1) GROUP BY t."args", t."salt", t."payload", t."_sign"`, rule_observers: ["response/3"] },
  { rel: "response", kind: "log", table_name: "identical_demand_dedups_response", delta_table_name: "__delta_identical_demand_dedups_response", frontier_table_name: "__frontier_identical_demand_dedups_response", next_frontier_table_name: "__next_frontier_identical_demand_dedups_response", columns: ["args", "salt", "payload"], column_types: ["text", "text", "text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."args") AND json_type(t."args") = 'object' AND json_type(t."args", '$.fn') = 'text' AND json_type(t."args", '$.args') = 'array' THEN json_extract(t."args", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."args", '$.args')), '') || ')' ELSE t."args" END AS "args", CASE WHEN json_valid(t."salt") AND json_type(t."salt") = 'object' AND json_type(t."salt", '$.fn') = 'text' AND json_type(t."salt", '$.args') = 'array' THEN json_extract(t."salt", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."salt", '$.args')), '') || ')' ELSE t."salt" END AS "salt", CASE WHEN json_valid(t."payload") AND json_type(t."payload") = 'object' AND json_type(t."payload", '$.fn') = 'text' AND json_type(t."payload", '$.args') = 'array' THEN json_extract(t."payload", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."payload", '$.args')), '') || ')' ELSE t."payload" END AS "payload", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_identical_demand_dedups_response" t WHERE t."_sign" IN (-1, 1) GROUP BY t."args", t."salt", t."payload", t."_sign"`, rule_observers: [] },
  { rel: "watch_request", kind: "log", table_name: "identical_demand_dedups_watch_request_f39914cb1980", delta_table_name: "__delta_identical_demand_dedups_watch_request_f39914cb1980", frontier_table_name: "__frontier_identical_demand_dedups_watch_request_f39914cb1980", next_frontier_table_name: "__next_frontier_identical_demand_dedups_watch_request_f39914cb1980", columns: ["col1", "args", "salt"], column_types: ["text", "text", "text"], key_indices: [], arrival_add_sql: `INSERT INTO "identical_demand_dedups_watch_request_f39914cb1980" ("col1", "args", "salt") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]'), json_extract(value, '$[2]') FROM json_each(?) RETURNING "col1", "args", "salt"`, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."col1") AND json_type(t."col1") = 'object' AND json_type(t."col1", '$.fn') = 'text' AND json_type(t."col1", '$.args') = 'array' THEN json_extract(t."col1", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."col1", '$.args')), '') || ')' ELSE t."col1" END AS "col1", CASE WHEN json_valid(t."args") AND json_type(t."args") = 'object' AND json_type(t."args", '$.fn') = 'text' AND json_type(t."args", '$.args') = 'array' THEN json_extract(t."args", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."args", '$.args')), '') || ')' ELSE t."args" END AS "args", CASE WHEN json_valid(t."salt") AND json_type(t."salt") = 'object' AND json_type(t."salt", '$.fn') = 'text' AND json_type(t."salt", '$.args') = 'array' THEN json_extract(t."salt", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."salt", '$.args')), '') || ')' ELSE t."salt" END AS "salt", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_identical_demand_dedups_watch_request_f39914cb1980" t WHERE t."_sign" IN (-1, 1) GROUP BY t."col1", t."args", t."salt", t."_sign"`, rule_observers: ["demand/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
  { head_rel: "demand", rule_id: "identical_demand_dedups:demand/2#1", head_kind: "set", head_table_name: "identical_demand_dedups_demand", head_delta_table_name: "__delta_identical_demand_dedups_demand", head_columns: ["args", "salt"], key_indices: [0, 1], project_sql: `SELECT d0."args" AS "args", d0."salt" AS "salt" FROM "__frontier_identical_demand_dedups_watch_request_f39914cb1980" d0 WHERE d0."_phase" >= 0 ORDER BY d0."_phase", d0."_sequence"` },
  { head_rel: "response", rule_id: "identical_demand_dedups:response/3#1", head_kind: "log", head_table_name: "identical_demand_dedups_response", head_delta_table_name: "__delta_identical_demand_dedups_response", head_columns: ["args", "salt", "payload"], key_indices: [], project_sql: `SELECT d0."args" AS "args", d0."salt" AS "salt", d0."payload" AS "payload" FROM "__frontier_identical_demand_dedups_fill_3d9eb2ac63c4" d0, "identical_demand_dedups_demand" b0 WHERE d0."_phase" >= 0 AND b0."args" = d0."args" AND b0."salt" = d0."salt" ORDER BY d0."_phase", d0."_sequence"` },
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.recompute_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK, arrivals)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.merge_next_into_current(seam, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      concatMap((carry_pending) => EnumPlane.decode_deltas(seam, ENUM_TYPES, ENUM_REF_COLUMNS, SUBSCRIBED_RELATIONS, rels).pipe(
        map((decoded): ITickDeltas => ({ rels: decoded, carry_pending })),
      )),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return EnumPlane.intern(seam, ENUM_TYPES, ENUM_REF_COLUMNS, arrivals).pipe(
    concatMap((normalized) => run_incremental_tick(seam, validate_arrivals(normalized))),
  );
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "identical_demand_dedups",
  ir_version: 1,
  internMode: "dict",
  ddl,
  rel_columns,
  rel_physical_names,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  enum_types: ENUM_TYPES,
  enum_ref_columns: ENUM_REF_COLUMNS,
  unsupported_execution,
  tick: run_tick,
};
