// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: json_object_groups_rows_into_documents.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowValue,
  ISqlSeam,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowValue[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowValue | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowValue[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => typeof value === "boolean" ? BigInt(value ? 1 : 0) : (typeof value === "number" && Number.isSafeInteger(value) ? BigInt(value) : value));
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "item": [true, true, false],
    "metadata": [true, false],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "item" ("__id" INTEGER PRIMARY KEY, "group" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" TEXT NOT NULL CHECK (json_valid("value")), UNIQUE ("group", "key", "value"))`,
  `CREATE TEMP VIEW "__txt_item" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."group") AS "group", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", t."value" AS "value" FROM "item" t`,
  `CREATE TABLE "metadata" ("__id" INTEGER PRIMARY KEY, "group" INTEGER NOT NULL, "col2" TEXT NOT NULL CHECK (json_valid("col2")), "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("group", "col2"))`,
  `CREATE TEMP VIEW "__txt_metadata" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."group") AS "group", t."col2" AS "col2", t."__refcount" AS "__refcount" FROM "metadata" t`,
  `CREATE TEMP TABLE "__delta_item" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" TEXT NOT NULL CHECK (json_valid("value")))`,
  `CREATE INDEX "__delta_item_sign" ON "__delta_item" ("_sign")`,
  `CREATE INDEX "__delta_item_group" ON "__delta_item" ("group", "key", "value")`,
  `CREATE TEMP TABLE "__frontier_item" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" TEXT NOT NULL CHECK (json_valid("value")))`,
  `CREATE INDEX "__frontier_item_phase" ON "__frontier_item" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_item" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" TEXT NOT NULL CHECK (json_valid("value")))`,
  `CREATE TEMP VIEW "__txt___delta_item" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."group") AS "group", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", t."value" AS "value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_item" t`,
  `CREATE TEMP TABLE "__delta_metadata" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "col2" TEXT NOT NULL CHECK (json_valid("col2")))`,
  `CREATE INDEX "__delta_metadata_sign" ON "__delta_metadata" ("_sign")`,
  `CREATE INDEX "__delta_metadata_group" ON "__delta_metadata" ("group", "col2")`,
  `CREATE TEMP TABLE "__frontier_metadata" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "col2" TEXT NOT NULL CHECK (json_valid("col2")))`,
  `CREATE INDEX "__frontier_metadata_phase" ON "__frontier_metadata" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_metadata" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "col2" TEXT NOT NULL CHECK (json_valid("col2")))`,
  `CREATE TEMP VIEW "__txt___delta_metadata" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."group") AS "group", t."col2" AS "col2", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_metadata" t`,
  `CREATE TEMP TABLE "__agg_scope_metadata" ("group" INTEGER NOT NULL, PRIMARY KEY ("group")) WITHOUT ROWID`,
];

const rel_columns: Record<string, readonly string[]> = {
  item: ["group", "key", "value"],
  metadata: ["group", "col2"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  item: ["text", "text", "json"],
  metadata: ["text", "json"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "json_object_groups_rows_into_documents", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "b4c52076e976b950", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "item", kind: "rel", type_id: 0, arity: 3, module_id: 6, h_id: "542317d47863df1a", h_schema: "854017b7e9e71595", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "group", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "e7c9de165fccbdef", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 7, ordinal: 2, local_name: "key", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "0b7f7776ba64022e", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 7, ordinal: 3, local_name: "value", kind: "column", type_id: 5, arity: 0, module_id: 6, h_id: "056ecfad8ed0bc50", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 6, ordinal: 0, local_name: "metadata", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "f6d52847b404fa91", h_schema: "599d26db6e75d77a", h_rule: "c407802db67df3cd" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "group", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "051a3b2ef20a79d3", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 11, ordinal: 2, local_name: "col2", kind: "column", type_id: 5, arity: 0, module_id: 6, h_id: "04428feca61bd5f2", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 7, ordinal: 0, local_name: "__delta_item", kind: "delta", type_id: 0, arity: 5, module_id: 6, h_id: "f04f2251ef7921ff", h_schema: "f669e58a1ed6e368", h_rule: "" },
  { rel_id: 15, parent_id: 7, ordinal: 0, local_name: "__frontier_item", kind: "frontier", type_id: 0, arity: 5, module_id: 6, h_id: "430894ea92d08943", h_schema: "342fe61f45f3212e", h_rule: "" },
  { rel_id: 16, parent_id: 7, ordinal: 0, local_name: "__next_frontier_item", kind: "next_frontier", type_id: 0, arity: 5, module_id: 6, h_id: "963f82dc4f31a4b6", h_schema: "342fe61f45f3212e", h_rule: "" },
  { rel_id: 17, parent_id: 7, ordinal: 0, local_name: "__txt_item", kind: "view", type_id: 0, arity: 3, module_id: 6, h_id: "aff6c088964a94cb", h_schema: "854017b7e9e71595", h_rule: "" },
  { rel_id: 18, parent_id: 14, ordinal: 0, local_name: "__txt___delta_item", kind: "view", type_id: 0, arity: 5, module_id: 6, h_id: "bd643c24972514b1", h_schema: "854017b7e9e71595", h_rule: "" },
  { rel_id: 19, parent_id: 11, ordinal: 0, local_name: "__delta_metadata", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "eda2e8a53b8de337", h_schema: "f2509ecb8733d404", h_rule: "" },
  { rel_id: 20, parent_id: 11, ordinal: 0, local_name: "__frontier_metadata", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "b2f844ec84e80aee", h_schema: "ccb1c2f26ce378b1", h_rule: "" },
  { rel_id: 21, parent_id: 11, ordinal: 0, local_name: "__next_frontier_metadata", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "ec2c913d6e4ac395", h_schema: "ccb1c2f26ce378b1", h_rule: "" },
  { rel_id: 22, parent_id: 11, ordinal: 0, local_name: "__txt_metadata", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "6535dac57fa2496c", h_schema: "599d26db6e75d77a", h_rule: "" },
  { rel_id: 23, parent_id: 19, ordinal: 0, local_name: "__txt___delta_metadata", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "6b04addbbe71cdd5", h_schema: "599d26db6e75d77a", h_rule: "" },
  { rel_id: 24, parent_id: 6, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 6, h_id: "7411ca6cbf4a643b", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 11, ordinal: 0, local_name: "__agg_scope_metadata", kind: "scope", type_id: 0, arity: 1, module_id: 6, h_id: "aa6c4066516aa67f", h_schema: "af8b07b10d8881d5", h_rule: "" },
  { rel_id: 26, parent_id: 8, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "1c6eed7bbc9d643f", h_schema: "", h_rule: "" },
  { rel_id: 27, parent_id: 9, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "30067c88444c5683", h_schema: "", h_rule: "" },
  { rel_id: 28, parent_id: 10, ordinal: 3, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "90f9848c7384b6cd", h_schema: "", h_rule: "" },
  { rel_id: 29, parent_id: 12, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "31f742d19ee50dbd", h_schema: "", h_rule: "" },
  { rel_id: 30, parent_id: 13, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "7c91e4361bc34caa", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["item"];

const boot: readonly IBootStatement[] = [
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["north"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["name"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "item" ("group", "key", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), (SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["north", "name", "\"pear\""] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["north"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["count"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "item" ("group", "key", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), (SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["north", "count", "2"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["south"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["name"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "item" ("group", "key", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), (SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["south", "name", "\"apple\""] },
  { rel: "metadata", sql: `DELETE FROM "metadata"`, params: [] },
  { rel: "metadata", sql: `INSERT OR IGNORE INTO "metadata" ("group", "col2") SELECT b0."group", CASE WHEN count(DISTINCT json_array((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key"), json(b0."value"))) = count(DISTINCT (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key")) THEN json_group_object((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key"), json(b0."value") ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key")) ELSE json('json_object_dup_key') END FROM "item" b0 GROUP BY b0."group" HAVING count(*) > 0`, params: [] },
];

type Snapshot = {
  readonly item: readonly IRow[];
  readonly metadata: readonly IRow[];
};

function read_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    item: select_rows(seam, `SELECT CASE WHEN json_valid("group") AND json_type("group") = 'object' AND json_type("group", '$.fn') = 'text' AND json_type("group", '$.args') = 'array' THEN json_extract("group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("group", '$.args')), '') || ')' ELSE "group" END AS "group", CASE WHEN json_valid("key") AND json_type("key") = 'object' AND json_type("key", '$.fn') = 'text' AND json_type("key", '$.args') = 'array' THEN json_extract("key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("key", '$.args')), '') || ')' ELSE "key" END AS "key", "value" FROM "__txt_item"`, rel_columns.item!, rel_column_types.item!),
    metadata: select_rows(seam, `SELECT CASE WHEN json_valid("group") AND json_type("group") = 'object' AND json_type("group", '$.fn') = 'text' AND json_type("group", '$.args') = 'array' THEN json_extract("group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("group", '$.args')), '') || ')' ELSE "group" END AS "group", "col2" FROM "__txt_metadata"`, rel_columns.metadata!, rel_column_types.metadata!),
  });
}

type Snapshots = { readonly decoded: Snapshot; readonly stored: Snapshot };

function read_stored_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    item: select_rows(seam, `SELECT "group", "key", "value" FROM "item"`, rel_columns.item!, rel_column_types.item!),
    metadata: select_rows(seam, `SELECT "group", "col2" FROM "metadata"`, rel_columns.metadata!, rel_column_types.metadata!),
  });
}

function read_snapshots(seam: ISqlSeam): Observable<Snapshots> {
  return forkJoin({ decoded: read_snapshot(seam), stored: read_stored_snapshot(seam) });
}

const final_select: Record<string, string> = {
  item: `SELECT CASE WHEN json_valid("group") AND json_type("group") = 'object' AND json_type("group", '$.fn') = 'text' AND json_type("group", '$.args') = 'array' THEN json_extract("group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("group", '$.args')), '') || ')' ELSE "group" END AS "group", CASE WHEN json_valid("key") AND json_type("key") = 'object' AND json_type("key", '$.fn') = 'text' AND json_type("key", '$.args') = 'array' THEN json_extract("key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("key", '$.args')), '') || ')' ELSE "key" END AS "key", "value" FROM "__txt_item"`,
  metadata: `SELECT CASE WHEN json_valid("group") AND json_type("group") = 'object' AND json_type("group", '$.fn') = 'text' AND json_type("group", '$.args') = 'array' THEN json_extract("group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("group", '$.args')), '') || ')' ELSE "group" END AS "group", "col2" FROM "__txt_metadata"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; add_sql: string; del_sql: string | null }> = {
  item: { kind: "set", add_sql: `INSERT OR IGNORE INTO "item" ("group", "key", "value") VALUES (?, ?, ?)`, del_sql: `DELETE FROM "item" WHERE "group" = ? AND "key" = ? AND "value" = ?` },
};

function arrival_statement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`json_object_groups_rows_into_documents: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`json_object_groups_rows_into_documents: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.del_sql === null) {
      throw new Error(`json_object_groups_rows_into_documents: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.del_sql, args: bind_args(arrival.row) };
  }
  return { sql: template.add_sql, args: bind_args(arrival.row) };
}

function apply_arrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrival_statement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "item", kind: "set", table_name: "item", delta_table_name: "__delta_item", frontier_table_name: "__frontier_item", next_frontier_table_name: "__next_frontier_item", columns: ["group", "key", "value"], column_types: ["text", "text", "json"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "item" ("group", "key", "value") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]'), json_extract(value, '$[2]') FROM json_each(?) RETURNING "group", "key", "value"`, arrival_del_sql: `DELETE FROM "item" WHERE ("group", "key", "value") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]'), json_extract(value, '$[2]') FROM json_each(?)) RETURNING "group", "key", "value"`, boundary_sql: `SELECT CASE WHEN json_valid("group") AND json_type("group") = 'object' AND json_type("group", '$.fn') = 'text' AND json_type("group", '$.args') = 'array' THEN json_extract("group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("group", '$.args')), '') || ')' ELSE "group" END AS "group", CASE WHEN json_valid("key") AND json_type("key") = 'object' AND json_type("key", '$.fn') = 'text' AND json_type("key", '$.args') = 'array' THEN json_extract("key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("key", '$.args')), '') || ')' ELSE "key" END AS "key", "value", "_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_item" WHERE "_sign" IN (-1, 1) GROUP BY "group", "key", "value", "_sign"`, rule_observers: ["metadata/2"] },
  { rel: "metadata", kind: "set", table_name: "metadata", delta_table_name: "__delta_metadata", frontier_table_name: "__frontier_metadata", next_frontier_table_name: "__next_frontier_metadata", columns: ["group", "col2"], column_types: ["text", "json"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid("group") AND json_type("group") = 'object' AND json_type("group", '$.fn') = 'text' AND json_type("group", '$.args') = 'array' THEN json_extract("group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each("group", '$.args')), '') || ')' ELSE "group" END AS "group", "col2", "_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_metadata" WHERE "_sign" IN (-1, 1) GROUP BY "group", "col2", "_sign"`, rule_observers: [] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "metadata", rule_id: "json_object_groups_rows_into_documents:metadata/2#1", head_delta_table_name: "__delta_metadata", head_columns: ["group", "col2"], insert_sql: null, select_sql: `SELECT "group", "col2" FROM "metadata"`, recompute_sql: `DELETE FROM "metadata";
INSERT OR IGNORE INTO "metadata" ("group", "col2") SELECT b0."group", CASE WHEN count(DISTINCT json_array((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key"), json(b0."value"))) = count(DISTINCT (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key")) THEN json_group_object((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key"), json(b0."value") ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key")) ELSE json('json_object_dup_key') END FROM "item" b0 GROUP BY b0."group" HAVING count(*) > 0`, support_sql: null, expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: { scope_clear_sql: `DELETE FROM "__agg_scope_metadata"`, scope_seed_sql: [`INSERT OR IGNORE INTO "__agg_scope_metadata" ("group") SELECT DISTINCT d0."group" FROM "__delta_item" d0 WHERE d0."_sign" IN (-1, 1)`], delete_scoped_sql: `DELETE FROM "metadata" WHERE ("group") IN (SELECT "group" FROM "__agg_scope_metadata") RETURNING "group", "col2"`, insert_scoped_sql: [`INSERT OR IGNORE INTO "metadata" ("group", "col2") SELECT b0."group", CASE WHEN count(DISTINCT json_array((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key"), json(b0."value"))) = count(DISTINCT (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key")) THEN json_group_object((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key"), json(b0."value") ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key")) ELSE json('json_object_dup_key') END FROM "item" b0 WHERE (b0."group") IN (SELECT "group" FROM "__agg_scope_metadata") GROUP BY b0."group" HAVING count(*) > 0 RETURNING "group", "col2"`], delta_maintained: false } },
];

function recompute_levels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "metadata";
INSERT OR IGNORE INTO "metadata" ("group", "col2") SELECT b0."group", CASE WHEN count(DISTINCT json_array((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key"), json(b0."value"))) = count(DISTINCT (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key")) THEN json_group_object((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key"), json(b0."value") ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."key")) ELSE json('json_object_dup_key') END FROM "item" b0 GROUP BY b0."group" HAVING count(*) > 0`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function build_deltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const item = multiset_diff(before.item, after.item);
  const metadata = multiset_diff(before.metadata, after.metadata);
  return {
    rels: [
      { rel: "item", add: item.add, del: item.del },
      { rel: "metadata", add: metadata.add, del: metadata.del },
    ],
    carry_pending: false,
  };
}

function run_naive_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return read_snapshot(seam).pipe(
    concatMap((before) => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; return before; }))),
    concatMap((before) => apply_arrivals(seam, arrivals).pipe(map(() => before))),
  ).pipe(
    concatMap((before) => recompute_levels(seam).pipe(map(() => before))),
    concatMap((before) => read_snapshot(seam).pipe(map((after) => build_deltas(before, after)))),
  );
  // json_object_groups_rows_into_documents: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = EMITTER_MODE;
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return run_naive_tick(seam, arrivals);
  }
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "json_object_groups_rows_into_documents",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
