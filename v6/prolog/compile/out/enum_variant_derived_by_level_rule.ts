// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: enum_variant_derived_by_level_rule.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "review_done": [false, true],
    "review_tag": [false, true],
    "submission": [false, true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `INSERT OR IGNORE INTO "__str" ("content") VALUES ('done'), ('pending')`,
  `CREATE TABLE "review_done" ("__id" INTEGER PRIMARY KEY, "id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("id", "verdict"))`,
  `CREATE TEMP VIEW "__txt_review_done" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."verdict") AS "verdict", t."__refcount" AS "__refcount" FROM "review_done" t`,
  `CREATE TABLE "review_pending" ("__id" INTEGER PRIMARY KEY, "id" INTEGER NOT NULL, UNIQUE ("id"))`,
  `CREATE TABLE "review_tag" ("__id" INTEGER PRIMARY KEY, "id" INTEGER NOT NULL, "tag" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("id", "tag"))`,
  `CREATE TEMP VIEW "__txt_review_tag" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."tag") AS "tag", t."__refcount" AS "__refcount" FROM "review_tag" t`,
  `CREATE TABLE "submission" ("__id" INTEGER PRIMARY KEY, "id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL, UNIQUE ("id"))`,
  `CREATE TEMP VIEW "__txt_submission" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."verdict") AS "verdict" FROM "submission" t`,
  `CREATE TEMP TABLE "__delta_review_done" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_review_done_sign" ON "__delta_review_done" ("_sign")`,
  `CREATE INDEX "__delta_review_done_group" ON "__delta_review_done" ("id", "verdict")`,
  `CREATE TEMP TABLE "__frontier_review_done" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_review_done_phase" ON "__frontier_review_done" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_review_done" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_review_done" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."verdict") AS "verdict", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_review_done" t`,
  `CREATE TEMP TABLE "__delta_review_pending" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_review_pending_sign" ON "__delta_review_pending" ("_sign")`,
  `CREATE INDEX "__delta_review_pending_group" ON "__delta_review_pending" ("id")`,
  `CREATE TEMP TABLE "__frontier_review_pending" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_review_pending_phase" ON "__frontier_review_pending" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_review_pending" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_review_tag" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "tag" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_review_tag_sign" ON "__delta_review_tag" ("_sign")`,
  `CREATE INDEX "__delta_review_tag_group" ON "__delta_review_tag" ("id", "tag")`,
  `CREATE TEMP TABLE "__frontier_review_tag" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "tag" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_review_tag_phase" ON "__frontier_review_tag" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_review_tag" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "tag" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_review_tag" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."tag") AS "tag", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_review_tag" t`,
  `CREATE TEMP TABLE "__delta_submission" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_submission_sign" ON "__delta_submission" ("_sign")`,
  `CREATE INDEX "__delta_submission_group" ON "__delta_submission" ("id", "verdict")`,
  `CREATE TEMP TABLE "__frontier_submission" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_submission_phase" ON "__frontier_submission" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_submission" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_submission" AS SELECT t."id" AS "id", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."verdict") AS "verdict", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_submission" t`,
  `CREATE TEMP TABLE "__support_next_review_done" ("id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("id", "verdict")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_review_done" ("id" INTEGER NOT NULL, "verdict" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "review_done_zero" ON "review_done" ("__refcount") WHERE "__refcount" <= 0`,
  `CREATE TEMP TABLE "__support_next_review_tag" ("id" INTEGER NOT NULL, "tag" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("id", "tag")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_review_tag" ("id" INTEGER NOT NULL, "tag" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "review_tag_zero" ON "review_tag" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  review_done: ["id", "verdict"],
  review_pending: ["id"],
  review_tag: ["id", "tag"],
  submission: ["id", "verdict"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  review_done: ["int", "text"],
  review_pending: ["int"],
  review_tag: ["int", "text"],
  submission: ["int", "text"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  review_done: ["int", "text"],
  review_pending: ["int"],
  review_tag: ["int", "text"],
  submission: ["int", "text"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "enum_variant_derived_by_level_rule", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "b4e0dcd3a1cdf5e9", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "review_done", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "7bbdc1302a0c41ba", h_schema: "04e7c9b45b3adda1", h_rule: "95601ed1522e95f9" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "d1a10fe68fd505d0", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 7, ordinal: 2, local_name: "verdict", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "b1d3b82039fadf5a", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 6, ordinal: 0, local_name: "review_pending", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "910e32bb38e131bd", h_schema: "a910993d429fe6c3", h_rule: "" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "aa87a49d705881aa", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 6, ordinal: 0, local_name: "review_tag", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "363268a0c270daf0", h_schema: "2901e6f8122ebf0c", h_rule: "a6eaad5cf6a80082" },
  { rel_id: 13, parent_id: 12, ordinal: 1, local_name: "id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "9586dab9a6bacab0", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 12, ordinal: 2, local_name: "tag", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "1228e34dd84d2f8d", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 6, ordinal: 0, local_name: "submission", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "d1cc31753bf4b3f4", h_schema: "b0028b79fd5d4e18", h_rule: "" },
  { rel_id: 16, parent_id: 15, ordinal: 1, local_name: "id", kind: "column", type_id: 2, arity: 0, module_id: 6, h_id: "886afabb1ef7dd52", h_schema: "", h_rule: "" },
  { rel_id: 17, parent_id: 15, ordinal: 2, local_name: "verdict", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "79ffb22b4d3e25f2", h_schema: "", h_rule: "" },
  { rel_id: 18, parent_id: 7, ordinal: 0, local_name: "__delta_review_done", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "c3938dc5a25f8a9c", h_schema: "634a1dbec1cdfeda", h_rule: "" },
  { rel_id: 19, parent_id: 7, ordinal: 0, local_name: "__frontier_review_done", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "8fdbf4c3f9d0fdfe", h_schema: "b29581a97a657ca5", h_rule: "" },
  { rel_id: 20, parent_id: 7, ordinal: 0, local_name: "__next_frontier_review_done", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "1672a746b0d05d80", h_schema: "b29581a97a657ca5", h_rule: "" },
  { rel_id: 21, parent_id: 7, ordinal: 0, local_name: "__txt_review_done", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "9d3cb90723e0f80f", h_schema: "04e7c9b45b3adda1", h_rule: "" },
  { rel_id: 22, parent_id: 18, ordinal: 0, local_name: "__txt___delta_review_done", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "55b7afd5b071066b", h_schema: "04e7c9b45b3adda1", h_rule: "" },
  { rel_id: 23, parent_id: 10, ordinal: 0, local_name: "__delta_review_pending", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "d93712c9c76a18f6", h_schema: "d63e416beccf4892", h_rule: "" },
  { rel_id: 24, parent_id: 10, ordinal: 0, local_name: "__frontier_review_pending", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "d5b835bbc86d502a", h_schema: "a186680c4c0c99e7", h_rule: "" },
  { rel_id: 25, parent_id: 10, ordinal: 0, local_name: "__next_frontier_review_pending", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "a01763f79fd4e5dc", h_schema: "a186680c4c0c99e7", h_rule: "" },
  { rel_id: 26, parent_id: 12, ordinal: 0, local_name: "__delta_review_tag", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "dc113e4800c193d3", h_schema: "4ac3c01192c9872c", h_rule: "" },
  { rel_id: 27, parent_id: 12, ordinal: 0, local_name: "__frontier_review_tag", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "11fd3cc2bb30e6f0", h_schema: "373ae2761a729e21", h_rule: "" },
  { rel_id: 28, parent_id: 12, ordinal: 0, local_name: "__next_frontier_review_tag", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "fef812449b462353", h_schema: "373ae2761a729e21", h_rule: "" },
  { rel_id: 29, parent_id: 12, ordinal: 0, local_name: "__txt_review_tag", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "fee1be538ab66e10", h_schema: "2901e6f8122ebf0c", h_rule: "" },
  { rel_id: 30, parent_id: 26, ordinal: 0, local_name: "__txt___delta_review_tag", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "e15746333fdde5a5", h_schema: "2901e6f8122ebf0c", h_rule: "" },
  { rel_id: 31, parent_id: 15, ordinal: 0, local_name: "__delta_submission", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "e77e20959b3d2318", h_schema: "634a1dbec1cdfeda", h_rule: "" },
  { rel_id: 32, parent_id: 15, ordinal: 0, local_name: "__frontier_submission", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "67ab4e4d12e199b7", h_schema: "b29581a97a657ca5", h_rule: "" },
  { rel_id: 33, parent_id: 15, ordinal: 0, local_name: "__next_frontier_submission", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "cc77c7c90d15192c", h_schema: "b29581a97a657ca5", h_rule: "" },
  { rel_id: 34, parent_id: 15, ordinal: 0, local_name: "__txt_submission", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "515ca156c68a802c", h_schema: "04e7c9b45b3adda1", h_rule: "" },
  { rel_id: 35, parent_id: 31, ordinal: 0, local_name: "__txt___delta_submission", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "ac0f8ddf03376c1f", h_schema: "04e7c9b45b3adda1", h_rule: "" },
  { rel_id: 36, parent_id: 6, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 6, h_id: "c4988a8131179fec", h_schema: "", h_rule: "" },
  { rel_id: 37, parent_id: 7, ordinal: 0, local_name: "__support_next_review_done", kind: "refcount", type_id: 0, arity: 3, module_id: 6, h_id: "78fead025ffb95bd", h_schema: "", h_rule: "" },
  { rel_id: 38, parent_id: 7, ordinal: 0, local_name: "__new_review_done", kind: "refcount_staging", type_id: 0, arity: 3, module_id: 6, h_id: "ee53e6d0a7c6a72b", h_schema: "", h_rule: "" },
  { rel_id: 39, parent_id: 12, ordinal: 0, local_name: "__support_next_review_tag", kind: "refcount", type_id: 0, arity: 3, module_id: 6, h_id: "84b9944c9ecde2ba", h_schema: "", h_rule: "" },
  { rel_id: 40, parent_id: 12, ordinal: 0, local_name: "__new_review_tag", kind: "refcount_staging", type_id: 0, arity: 3, module_id: 6, h_id: "e058f6e243364804", h_schema: "", h_rule: "" },
  { rel_id: 41, parent_id: 8, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "1732b65e5b0f201f", h_schema: "", h_rule: "" },
  { rel_id: 42, parent_id: 9, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "188e3fe643bbc62a", h_schema: "", h_rule: "" },
  { rel_id: 43, parent_id: 11, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "6cbfbdb8fc064c2a", h_schema: "", h_rule: "" },
  { rel_id: 44, parent_id: 13, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "d220623e93ca7e25", h_schema: "", h_rule: "" },
  { rel_id: 45, parent_id: 14, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "ae728ce018b8a19c", h_schema: "", h_rule: "" },
  { rel_id: 46, parent_id: 16, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "0afdfa8edfd44cff", h_schema: "", h_rule: "" },
  { rel_id: 47, parent_id: 17, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "fbb29225ddc0dcd7", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
  review_done: ["int", "text"],
  review_pending: ["int"],
  review_tag: ["int", "text"],
  submission: ["int", "text"],
};

const arrival_targets: readonly string[] = ["review_pending", "submission"];

const boot: readonly IBootStatement[] = [
  { rel: "review_done", sql: `DELETE FROM "review_done"`, params: [] },
  { rel: "review_done", sql: `INSERT OR IGNORE INTO "review_done" ("id", "verdict") SELECT b0."id", b0."verdict" FROM "submission" b0`, params: [] },
  { rel: "review_tag", sql: `DELETE FROM "review_tag"`, params: [] },
  { rel: "review_tag", sql: `INSERT OR IGNORE INTO "review_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'pending') FROM "review_pending" b0`, params: [] },
  { rel: "review_tag", sql: `INSERT OR IGNORE INTO "review_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'done') FROM "review_done" b0`, params: [] },
];

const final_select: Record<string, string> = {
  review_done: `SELECT t."id", CASE WHEN json_valid(t."verdict") AND json_type(t."verdict") = 'object' AND json_type(t."verdict", '$.fn') = 'text' AND json_type(t."verdict", '$.args') = 'array' THEN json_extract(t."verdict", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."verdict", '$.args')), '') || ')' ELSE t."verdict" END AS "verdict" FROM "__txt_review_done" t`,
  review_pending: `SELECT t."id" FROM "review_pending" t`,
  review_tag: `SELECT t."id", CASE WHEN json_valid(t."tag") AND json_type(t."tag") = 'object' AND json_type(t."tag", '$.fn') = 'text' AND json_type(t."tag", '$.args') = 'array' THEN json_extract(t."tag", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."tag", '$.args')), '') || ')' ELSE t."tag" END AS "tag" FROM "__txt_review_tag" t`,
  submission: `SELECT t."id", CASE WHEN json_valid(t."verdict") AND json_type(t."verdict") = 'object' AND json_type(t."verdict", '$.fn') = 'text' AND json_type(t."verdict", '$.args') = 'array' THEN json_extract(t."verdict", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."verdict", '$.args')), '') || ')' ELSE t."verdict" END AS "verdict" FROM "__txt_submission" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "review_done", kind: "set", table_name: "review_done", delta_table_name: "__delta_review_done", frontier_table_name: "__frontier_review_done", next_frontier_table_name: "__next_frontier_review_done", columns: ["id", "verdict"], column_types: ["int", "text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT t."id", CASE WHEN json_valid(t."verdict") AND json_type(t."verdict") = 'object' AND json_type(t."verdict", '$.fn') = 'text' AND json_type(t."verdict", '$.args') = 'array' THEN json_extract(t."verdict", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."verdict", '$.args')), '') || ')' ELSE t."verdict" END AS "verdict", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_review_done" t WHERE t."_sign" IN (-1, 1) GROUP BY t."id", t."verdict", t."_sign"`, rule_observers: ["review_tag/2"] },
  { rel: "review_pending", kind: "set", table_name: "review_pending", delta_table_name: "__delta_review_pending", frontier_table_name: "__frontier_review_pending", next_frontier_table_name: "__next_frontier_review_pending", columns: ["id"], column_types: ["int"], key_indices: [0], arrival_add_sql: `INSERT INTO "review_pending" ("id") SELECT json_extract(value, '$[0]') FROM json_each(?) WHERE true ON CONFLICT ("id") DO NOTHING RETURNING "id"`, arrival_del_sql: `DELETE FROM "review_pending" WHERE ("id") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "id"`, boundary_sql: `SELECT t."id", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_review_pending" t WHERE t."_sign" IN (-1, 1) GROUP BY t."id", t."_sign"`, rule_observers: ["review_tag/2"] },
  { rel: "review_tag", kind: "set", table_name: "review_tag", delta_table_name: "__delta_review_tag", frontier_table_name: "__frontier_review_tag", next_frontier_table_name: "__next_frontier_review_tag", columns: ["id", "tag"], column_types: ["int", "text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT t."id", CASE WHEN json_valid(t."tag") AND json_type(t."tag") = 'object' AND json_type(t."tag", '$.fn') = 'text' AND json_type(t."tag", '$.args') = 'array' THEN json_extract(t."tag", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."tag", '$.args')), '') || ')' ELSE t."tag" END AS "tag", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_review_tag" t WHERE t."_sign" IN (-1, 1) GROUP BY t."id", t."tag", t."_sign"`, rule_observers: [] },
  { rel: "submission", kind: "set", table_name: "submission", delta_table_name: "__delta_submission", frontier_table_name: "__frontier_submission", next_frontier_table_name: "__next_frontier_submission", columns: ["id", "verdict"], column_types: ["int", "text"], key_indices: [0], arrival_add_sql: `INSERT INTO "submission" ("id", "verdict") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) WHERE true ON CONFLICT ("id") DO UPDATE SET "verdict" = excluded."verdict" RETURNING "id", "verdict"`, arrival_del_sql: `DELETE FROM "submission" WHERE ("id", "verdict") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "id", "verdict"`, boundary_sql: `SELECT t."id", CASE WHEN json_valid(t."verdict") AND json_type(t."verdict") = 'object' AND json_type(t."verdict", '$.fn') = 'text' AND json_type(t."verdict", '$.args') = 'array' THEN json_extract(t."verdict", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."verdict", '$.args')), '') || ')' ELSE t."verdict" END AS "verdict", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_submission" t WHERE t."_sign" IN (-1, 1) GROUP BY t."id", t."verdict", t."_sign"`, rule_observers: ["review_done/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "review_done", rule_id: "enum_variant_derived_by_level_rule:review_done/2#1", head_delta_table_name: "__delta_review_done", head_columns: ["id", "verdict"], insert_sql: `INSERT OR IGNORE INTO "review_done" ("id", "verdict") SELECT DISTINCT d0."id", d0."verdict" FROM "__frontier_submission" d0 WHERE d0."_phase" >= 0 RETURNING "id", "verdict"`, select_sql: `SELECT "id", "verdict" FROM "review_done"`, recompute_sql: `DELETE FROM "review_done";
INSERT OR IGNORE INTO "review_done" ("id", "verdict") SELECT b0."id", b0."verdict" FROM "submission" b0`, support_sql: [`DELETE FROM "__support_next_review_done"`, `INSERT INTO "__support_next_review_done" ("id", "verdict", "__refcount") SELECT "id", "verdict", sum("__refcount") FROM (SELECT b0."id" AS "id", b0."verdict" AS "verdict", count(*) AS "__refcount" FROM "submission" b0 GROUP BY b0."id", b0."verdict") GROUP BY "id", "verdict"`, `UPDATE "review_done" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_review_done" n WHERE n."id" = h."id" AND n."verdict" = h."verdict"), 0)`, `INSERT INTO "__delta_review_done" ("_sign", "_sequence", "id", "verdict") SELECT -1, row_number() OVER () - 1, "id", "verdict" FROM "review_done" WHERE "__refcount" <= 0`, `DELETE FROM "review_done" WHERE "__refcount" <= 0`, `DELETE FROM "__new_review_done"`, `INSERT INTO "__new_review_done" ("id", "verdict", "__refcount") SELECT n."id", n."verdict", n."__refcount" FROM "__support_next_review_done" n LEFT JOIN "review_done" h ON n."id" = h."id" AND n."verdict" = h."verdict" WHERE h."id" IS NULL`, `INSERT INTO "__delta_review_done" ("_sign", "_sequence", "id", "verdict") SELECT 1, "rowid" - 1, "id", "verdict" FROM "__new_review_done"`, `INSERT INTO "__frontier_review_done" ("_phase", "_sequence", "id", "verdict") SELECT ?, "rowid" - 1, "id", "verdict" FROM "__new_review_done"`, `INSERT INTO "__next_frontier_review_done" ("_phase", "_sequence", "id", "verdict") SELECT ?, "rowid" - 1, "id", "verdict" FROM "__new_review_done"`, `INSERT OR IGNORE INTO "review_done" ("id", "verdict", "__refcount") SELECT n."id", n."verdict", n."__refcount" FROM "__support_next_review_done" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
  { head_rel: "review_tag", rule_id: "enum_variant_derived_by_level_rule:review_tag/2#1", head_delta_table_name: "__delta_review_tag", head_columns: ["id", "tag"], insert_sql: `INSERT OR IGNORE INTO "review_tag" ("id", "tag") SELECT DISTINCT d0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'pending') FROM "__frontier_review_pending" d0 WHERE d0."_phase" >= 0 UNION ALL SELECT DISTINCT d0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'done') FROM "__frontier_review_done" d0 WHERE d0."_phase" >= 0 RETURNING "id", "tag"`, select_sql: `SELECT "id", "tag" FROM "review_tag"`, recompute_sql: `DELETE FROM "review_tag";
INSERT OR IGNORE INTO "review_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'pending') FROM "review_pending" b0;
INSERT OR IGNORE INTO "review_tag" ("id", "tag") SELECT b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'done') FROM "review_done" b0`, support_sql: [`DELETE FROM "__support_next_review_tag"`, `INSERT INTO "__support_next_review_tag" ("id", "tag", "__refcount") SELECT "id", "tag", sum("__refcount") FROM (SELECT b0."id" AS "id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'pending') AS "tag", count(*) AS "__refcount" FROM "review_pending" b0 GROUP BY b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'pending') UNION ALL SELECT b0."id" AS "id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'done') AS "tag", count(*) AS "__refcount" FROM "review_done" b0 GROUP BY b0."id", (SELECT s."__id" FROM "__str" s WHERE s."content" = 'done')) GROUP BY "id", "tag"`, `UPDATE "review_tag" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_review_tag" n WHERE n."id" = h."id" AND n."tag" = h."tag"), 0)`, `INSERT INTO "__delta_review_tag" ("_sign", "_sequence", "id", "tag") SELECT -1, row_number() OVER () - 1, "id", "tag" FROM "review_tag" WHERE "__refcount" <= 0`, `DELETE FROM "review_tag" WHERE "__refcount" <= 0`, `DELETE FROM "__new_review_tag"`, `INSERT INTO "__new_review_tag" ("id", "tag", "__refcount") SELECT n."id", n."tag", n."__refcount" FROM "__support_next_review_tag" n LEFT JOIN "review_tag" h ON n."id" = h."id" AND n."tag" = h."tag" WHERE h."id" IS NULL`, `INSERT INTO "__delta_review_tag" ("_sign", "_sequence", "id", "tag") SELECT 1, "rowid" - 1, "id", "tag" FROM "__new_review_tag"`, `INSERT INTO "__frontier_review_tag" ("_phase", "_sequence", "id", "tag") SELECT ?, "rowid" - 1, "id", "tag" FROM "__new_review_tag"`, `INSERT INTO "__next_frontier_review_tag" ("_phase", "_sequence", "id", "tag") SELECT ?, "rowid" - 1, "id", "tag" FROM "__new_review_tag"`, `INSERT OR IGNORE INTO "review_tag" ("id", "tag", "__refcount") SELECT n."id", n."tag", n."__refcount" FROM "__support_next_review_tag" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "enum_variant_derived_by_level_rule",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
