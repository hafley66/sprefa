// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: match_edge_arm_keeps_edge_semantics.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "cache": [true, true],
    "poll_result": [true, true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "cache" ("__id" INTEGER PRIMARY KEY, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL, UNIQUE ("key"))`,
  `CREATE TEMP VIEW "__txt_cache" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value" FROM "cache" t`,
  `CREATE TABLE "poll_result" ("key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt_poll_result" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value" FROM "poll_result" t`,
  `CREATE TEMP TABLE "__delta_cache" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_cache_sign" ON "__delta_cache" ("_sign")`,
  `CREATE INDEX "__delta_cache_group" ON "__delta_cache" ("key", "value")`,
  `CREATE TEMP TABLE "__frontier_cache" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_cache_phase" ON "__frontier_cache" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_cache" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_cache" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_cache" t`,
  `CREATE TEMP TABLE "__delta_poll_result" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_poll_result_sign" ON "__delta_poll_result" ("_sign")`,
  `CREATE INDEX "__delta_poll_result_group" ON "__delta_poll_result" ("key", "value")`,
  `CREATE TEMP TABLE "__frontier_poll_result" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_poll_result_phase" ON "__frontier_poll_result" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_poll_result" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "key" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_poll_result" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."key") AS "key", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_poll_result" t`,
];

const rel_columns: Record<string, readonly string[]> = {
  cache: ["key", "value"],
  poll_result: ["key", "value"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  cache: ["text", "text"],
  poll_result: ["text", "text"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "match_edge_arm_keeps_edge_semantics", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "7f5554a3b4cb46d1", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "cache", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "97dfe62e0eab306d", h_schema: "87ce2f5d4367440d", h_rule: "bd5544498a9cf554" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "key", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "58937c3bf25e225c", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 7, ordinal: 2, local_name: "value", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "afeb115ba4133d95", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 6, ordinal: 0, local_name: "poll_result", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "f4eced3335207a7d", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "key", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "44e0c1965530709a", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 10, ordinal: 2, local_name: "value", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "0ae1a730e2bed817", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 7, ordinal: 0, local_name: "__delta_cache", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "92a38f1ffa7a6720", h_schema: "fdf6b6ef55287c94", h_rule: "" },
  { rel_id: 14, parent_id: 7, ordinal: 0, local_name: "__frontier_cache", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "598e8017a4df8f62", h_schema: "9dceee669bf5f906", h_rule: "" },
  { rel_id: 15, parent_id: 7, ordinal: 0, local_name: "__next_frontier_cache", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "cdd7f6e251dc39db", h_schema: "9dceee669bf5f906", h_rule: "" },
  { rel_id: 16, parent_id: 7, ordinal: 0, local_name: "__txt_cache", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "2330a0bef0a7a335", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 17, parent_id: 13, ordinal: 0, local_name: "__txt___delta_cache", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "629ff9799891a932", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 18, parent_id: 10, ordinal: 0, local_name: "__delta_poll_result", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "2f61182689d78f61", h_schema: "fdf6b6ef55287c94", h_rule: "" },
  { rel_id: 19, parent_id: 10, ordinal: 0, local_name: "__frontier_poll_result", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "e182b521c797cf56", h_schema: "9dceee669bf5f906", h_rule: "" },
  { rel_id: 20, parent_id: 10, ordinal: 0, local_name: "__next_frontier_poll_result", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "7a6945d2e77ce891", h_schema: "9dceee669bf5f906", h_rule: "" },
  { rel_id: 21, parent_id: 10, ordinal: 0, local_name: "__txt_poll_result", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "180f27a4ab07091c", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 22, parent_id: 18, ordinal: 0, local_name: "__txt___delta_poll_result", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "0bc48b746c0dbdfc", h_schema: "7d4194af9c268071", h_rule: "" },
  { rel_id: 23, parent_id: 6, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 6, h_id: "02aed998af5a9466", h_schema: "", h_rule: "" },
  { rel_id: 24, parent_id: 8, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "c5559fae90f4a449", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 9, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "cc2d521f6ea0ef87", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 11, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "66f328a09d0ef09c", h_schema: "", h_rule: "" },
  { rel_id: 27, parent_id: 12, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "260fc9fbcc8d107f", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["poll_result"];

const boot: readonly IBootStatement[] = [
];

const final_select: Record<string, string> = {
  cache: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value" FROM "__txt_cache" t`,
  poll_result: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value" FROM "__txt_poll_result" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "cache", kind: "set", table_name: "cache", delta_table_name: "__delta_cache", frontier_table_name: "__frontier_cache", next_frontier_table_name: "__next_frontier_cache", columns: ["key", "value"], column_types: ["text", "text"], key_indices: [0], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_cache" t WHERE t."_sign" IN (-1, 1) GROUP BY t."key", t."value", t."_sign"`, rule_observers: [] },
  { rel: "poll_result", kind: "log", table_name: "poll_result", delta_table_name: "__delta_poll_result", frontier_table_name: "__frontier_poll_result", next_frontier_table_name: "__next_frontier_poll_result", columns: ["key", "value"], column_types: ["text", "text"], key_indices: [], arrival_add_sql: `INSERT INTO "poll_result" ("key", "value") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "key", "value"`, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."key") AND json_type(t."key") = 'object' AND json_type(t."key", '$.fn') = 'text' AND json_type(t."key", '$.args') = 'array' THEN json_extract(t."key", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."key", '$.args')), '') || ')' ELSE t."key" END AS "key", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_poll_result" t WHERE t."_sign" IN (-1, 1) GROUP BY t."key", t."value", t."_sign"`, rule_observers: ["cache/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
  { head_rel: "cache", rule_id: "match_edge_arm_keeps_edge_semantics:cache/2#1", head_kind: "set", head_table_name: "cache", head_delta_table_name: "__delta_cache", head_columns: ["key", "value"], key_indices: [0], project_sql: `SELECT d0."key" AS "key", d0."value" AS "value" FROM "__frontier_poll_result" d0 WHERE d0."_phase" >= 0 ORDER BY d0."_phase", d0."_sequence"` },
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.recompute_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK, arrivals)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.merge_next_into_current(seam, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "match_edge_arm_keeps_edge_semantics",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
