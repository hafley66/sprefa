// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: json_untyped_capture_binds_without_a_type.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "seen": [true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "json_untyped_capture_binds_without_a_type_event" ("payload" TEXT NOT NULL CHECK (json_valid("payload")))`,
  `CREATE TABLE "json_untyped_capture_binds_without_a_type_seen" ("__id" INTEGER PRIMARY KEY, "value" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("value"))`,
  `CREATE TEMP VIEW "__txt_json_untyped_capture_binds_without_a_type_seen" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value", t."__refcount" AS "__refcount" FROM "json_untyped_capture_binds_without_a_type_seen" t`,
  `CREATE TEMP TABLE "__delta_json_untyped_capture_binds_without_a_type_event" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "payload" TEXT NOT NULL CHECK (json_valid("payload")))`,
  `CREATE INDEX "__delta_json_untyped_capture_binds_without_a_type_event_sign" ON "__delta_json_untyped_capture_binds_without_a_type_event" ("_sign")`,
  `CREATE INDEX "__delta_json_untyped_capture_binds_without_a_type_event_group" ON "__delta_json_untyped_capture_binds_without_a_type_event" ("payload")`,
  `CREATE TEMP TABLE "__frontier_json_untyped_capture_binds_without_a_type_event" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "payload" TEXT NOT NULL CHECK (json_valid("payload")))`,
  `CREATE INDEX "__frontier_json_untyped_capture_binds_without_a_type_event_phase" ON "__frontier_json_untyped_capture_binds_without_a_type_event" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_json_untyped_capture_binds_without_a_type_event" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "payload" TEXT NOT NULL CHECK (json_valid("payload")))`,
  `CREATE TEMP TABLE "__delta_json_untyped_capture_binds_without_a_type_seen" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_json_untyped_capture_binds_without_a_type_seen_sign" ON "__delta_json_untyped_capture_binds_without_a_type_seen" ("_sign")`,
  `CREATE INDEX "__delta_json_untyped_capture_binds_without_a_type_seen_group" ON "__delta_json_untyped_capture_binds_without_a_type_seen" ("value")`,
  `CREATE TEMP TABLE "__frontier_json_untyped_capture_binds_without_a_type_seen" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_json_untyped_capture_binds_without_a_type_seen_phase" ON "__frontier_json_untyped_capture_binds_without_a_type_seen" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_json_untyped_capture_binds_without_a_type_seen" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_json_untyped_capture_binds_without_a_type_seen" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_json_untyped_capture_binds_without_a_type_seen" t`,
  `CREATE TEMP TABLE "__support_next_json_untyped_capture_binds_without_a_type_seen" ("value" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("value")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_json_untyped_capture_binds_without_a_type_seen" ("value" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "json_untyped_capture_binds_without_a_type_seen_zero" ON "json_untyped_capture_binds_without_a_type_seen" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  event: ["payload"],
  seen: ["value"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  event: ["json"],
  seen: ["text"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  event: ["json"],
  seen: ["text"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "json_untyped_capture_binds_without_a_type", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "7c11ffae6e27178f", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "event", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "da6e49491f34a7ec", h_schema: "ecc5212a70222ba6", h_rule: "" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "payload", kind: "column", type_id: 5, arity: 0, module_id: 7, h_id: "971f54baf6915d7d", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 7, ordinal: 0, local_name: "seen", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "4494cab0b583a5ce", h_schema: "84914f3feaa2025d", h_rule: "989f999615c0aa10" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "value", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "dbac28abd9ad8d9a", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 8, ordinal: 0, local_name: "__delta_event", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "c66e6c65f415bc6f", h_schema: "d83ff414a0fb00fe", h_rule: "" },
  { rel_id: 13, parent_id: 8, ordinal: 0, local_name: "__frontier_event", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "cb2f58e76c54dc19", h_schema: "f51b34af319a07fe", h_rule: "" },
  { rel_id: 14, parent_id: 8, ordinal: 0, local_name: "__next_frontier_event", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "94276e32988605cb", h_schema: "f51b34af319a07fe", h_rule: "" },
  { rel_id: 15, parent_id: 10, ordinal: 0, local_name: "__delta_seen", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "f32ebbe5bbe1d2c6", h_schema: "6c1465af2fd8064e", h_rule: "" },
  { rel_id: 16, parent_id: 10, ordinal: 0, local_name: "__frontier_seen", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "58f4c6220e7f6879", h_schema: "188c68a9f79671d5", h_rule: "" },
  { rel_id: 17, parent_id: 10, ordinal: 0, local_name: "__next_frontier_seen", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "43012fb6bbeeffec", h_schema: "188c68a9f79671d5", h_rule: "" },
  { rel_id: 18, parent_id: 10, ordinal: 0, local_name: "__txt_json_untyped_capture_binds_without_a_type_seen", kind: "view", type_id: 0, arity: 1, module_id: 7, h_id: "ee2991ac06ea2ec5", h_schema: "84914f3feaa2025d", h_rule: "" },
  { rel_id: 19, parent_id: 15, ordinal: 0, local_name: "__txt___delta_json_untyped_capture_binds_without_a_type_seen", kind: "view", type_id: 0, arity: 3, module_id: 7, h_id: "32860eed52151c1f", h_schema: "84914f3feaa2025d", h_rule: "" },
  { rel_id: 20, parent_id: 7, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 7, h_id: "b732a1251d6b25a8", h_schema: "", h_rule: "" },
  { rel_id: 21, parent_id: 10, ordinal: 0, local_name: "__support_next_seen", kind: "refcount", type_id: 0, arity: 2, module_id: 7, h_id: "a229c085b718279c", h_schema: "", h_rule: "" },
  { rel_id: 22, parent_id: 10, ordinal: 0, local_name: "__new_seen", kind: "refcount_staging", type_id: 0, arity: 2, module_id: 7, h_id: "8bc36b16227684ee", h_schema: "", h_rule: "" },
  { rel_id: 23, parent_id: 9, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "bec14a7a919e63de", h_schema: "", h_rule: "" },
  { rel_id: 24, parent_id: 11, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "2bbadcd63b9bd1e1", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
  event: ["json"],
  seen: ["text"],
};

const arrival_targets: readonly string[] = ["event"];

const boot: readonly IBootStatement[] = [
  { rel: "seen", sql: `DELETE FROM "json_untyped_capture_binds_without_a_type_seen"`, params: [] },
  { rel: "seen", sql: `INSERT OR IGNORE INTO "__str" ("content") SELECT DISTINCT json_extract(b0."payload", '$."stars"') FROM "json_untyped_capture_binds_without_a_type_event" b0 WHERE json_type(b0."payload", '$') = 'object' AND json_extract(b0."payload", '$."stars"') IS NOT NULL`, params: [] },
  { rel: "seen", sql: `INSERT OR IGNORE INTO "json_untyped_capture_binds_without_a_type_seen" ("value") SELECT (SELECT s."__id" FROM "__str" s WHERE s."content" = json_extract(b0."payload", '$."stars"')) FROM "json_untyped_capture_binds_without_a_type_event" b0 WHERE json_type(b0."payload", '$') = 'object' AND json_extract(b0."payload", '$."stars"') IS NOT NULL`, params: [] },
];

const final_select: Record<string, string> = {
  event: `SELECT t."payload" FROM "json_untyped_capture_binds_without_a_type_event" t`,
  seen: `SELECT CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value" FROM "__txt_json_untyped_capture_binds_without_a_type_seen" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "event", kind: "log", table_name: "json_untyped_capture_binds_without_a_type_event", delta_table_name: "__delta_json_untyped_capture_binds_without_a_type_event", frontier_table_name: "__frontier_json_untyped_capture_binds_without_a_type_event", next_frontier_table_name: "__next_frontier_json_untyped_capture_binds_without_a_type_event", columns: ["payload"], column_types: ["json"], key_indices: [], arrival_add_sql: `INSERT INTO "json_untyped_capture_binds_without_a_type_event" ("payload") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "payload"`, arrival_del_sql: null, boundary_sql: `SELECT t."payload", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_json_untyped_capture_binds_without_a_type_event" t WHERE t."_sign" IN (-1, 1) GROUP BY t."payload", t."_sign"`, rule_observers: ["seen/1"] },
  { rel: "seen", kind: "set", table_name: "json_untyped_capture_binds_without_a_type_seen", delta_table_name: "__delta_json_untyped_capture_binds_without_a_type_seen", frontier_table_name: "__frontier_json_untyped_capture_binds_without_a_type_seen", next_frontier_table_name: "__next_frontier_json_untyped_capture_binds_without_a_type_seen", columns: ["value"], column_types: ["text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_json_untyped_capture_binds_without_a_type_seen" t WHERE t."_sign" IN (-1, 1) GROUP BY t."value", t."_sign"`, rule_observers: [] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "seen", rule_id: "json_untyped_capture_binds_without_a_type:seen/1#1", head_delta_table_name: "__delta_json_untyped_capture_binds_without_a_type_seen", head_columns: ["value"], insert_sql: `INSERT OR IGNORE INTO "json_untyped_capture_binds_without_a_type_seen" ("value") SELECT DISTINCT (SELECT s."__id" FROM "__str" s WHERE s."content" = json_extract(d0."payload", '$."stars"')) FROM "__frontier_json_untyped_capture_binds_without_a_type_event" d0 WHERE d0."_phase" >= 0 AND json_type(d0."payload", '$') = 'object' AND json_extract(d0."payload", '$."stars"') IS NOT NULL RETURNING "value"`, select_sql: `SELECT "value" FROM "json_untyped_capture_binds_without_a_type_seen"`, recompute_sql: `DELETE FROM "json_untyped_capture_binds_without_a_type_seen";
INSERT OR IGNORE INTO "__str" ("content") SELECT DISTINCT json_extract(b0."payload", '$."stars"') FROM "json_untyped_capture_binds_without_a_type_event" b0 WHERE json_type(b0."payload", '$') = 'object' AND json_extract(b0."payload", '$."stars"') IS NOT NULL;
INSERT OR IGNORE INTO "json_untyped_capture_binds_without_a_type_seen" ("value") SELECT (SELECT s."__id" FROM "__str" s WHERE s."content" = json_extract(b0."payload", '$."stars"')) FROM "json_untyped_capture_binds_without_a_type_event" b0 WHERE json_type(b0."payload", '$') = 'object' AND json_extract(b0."payload", '$."stars"') IS NOT NULL`, support_sql: [`DELETE FROM "__support_next_json_untyped_capture_binds_without_a_type_seen"`, `INSERT INTO "__support_next_json_untyped_capture_binds_without_a_type_seen" ("value", "__refcount") SELECT "value", sum("__refcount") FROM (SELECT (SELECT s."__id" FROM "__str" s WHERE s."content" = json_extract(b0."payload", '$."stars"')) AS "value", count(*) AS "__refcount" FROM "json_untyped_capture_binds_without_a_type_event" b0 WHERE json_type(b0."payload", '$') = 'object' AND json_extract(b0."payload", '$."stars"') IS NOT NULL GROUP BY json_extract(b0."payload", '$."stars"')) GROUP BY "value"`, `UPDATE "json_untyped_capture_binds_without_a_type_seen" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_json_untyped_capture_binds_without_a_type_seen" n WHERE n."value" = h."value"), 0)`, `INSERT INTO "__delta_json_untyped_capture_binds_without_a_type_seen" ("_sign", "_sequence", "value") SELECT -1, row_number() OVER () - 1, "value" FROM "json_untyped_capture_binds_without_a_type_seen" WHERE "__refcount" <= 0`, `DELETE FROM "json_untyped_capture_binds_without_a_type_seen" WHERE "__refcount" <= 0`, `DELETE FROM "__new_json_untyped_capture_binds_without_a_type_seen"`, `INSERT INTO "__new_json_untyped_capture_binds_without_a_type_seen" ("value", "__refcount") SELECT n."value", n."__refcount" FROM "__support_next_json_untyped_capture_binds_without_a_type_seen" n LEFT JOIN "json_untyped_capture_binds_without_a_type_seen" h ON n."value" = h."value" WHERE h."value" IS NULL`, `INSERT INTO "__delta_json_untyped_capture_binds_without_a_type_seen" ("_sign", "_sequence", "value") SELECT 1, "rowid" - 1, "value" FROM "__new_json_untyped_capture_binds_without_a_type_seen"`, `INSERT INTO "__frontier_json_untyped_capture_binds_without_a_type_seen" ("_phase", "_sequence", "value") SELECT ?, "rowid" - 1, "value" FROM "__new_json_untyped_capture_binds_without_a_type_seen"`, `INSERT INTO "__next_frontier_json_untyped_capture_binds_without_a_type_seen" ("_phase", "_sequence", "value") SELECT ?, "rowid" - 1, "value" FROM "__new_json_untyped_capture_binds_without_a_type_seen"`, `INSERT OR IGNORE INTO "json_untyped_capture_binds_without_a_type_seen" ("value", "__refcount") SELECT n."value", n."__refcount" FROM "__support_next_json_untyped_capture_binds_without_a_type_seen" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null, intern_sql: [`INSERT OR IGNORE INTO "__str" ("content") SELECT DISTINCT json_extract(d0."payload", '$."stars"') FROM "__frontier_json_untyped_capture_binds_without_a_type_event" d0 WHERE d0."_phase" >= 0 AND json_type(d0."payload", '$') = 'object' AND json_extract(d0."payload", '$."stars"') IS NOT NULL`], support_intern_sql: [`INSERT OR IGNORE INTO "__str" ("content") SELECT DISTINCT json_extract(b0."payload", '$."stars"') FROM "json_untyped_capture_binds_without_a_type_event" b0 WHERE json_type(b0."payload", '$') = 'object' AND json_extract(b0."payload", '$."stars"') IS NOT NULL`] },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "json_untyped_capture_binds_without_a_type",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
