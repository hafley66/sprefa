// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: filter_map_is_a_level_rule.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lowerSql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { multisetDiff } from "../runtime/diff.ts";
import { selectRows } from "../runtime/rows.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelDelta,
  IRow,
  IRowValue,
  ISqlSeam,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: "int" | "text" | "json" }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demandRel: string; readonly responseRel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly snapshot: "current" }

interface IBootStatement {
  sql: string;
  params: readonly (string | number)[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly finalSelect: Record<string, string>; readonly hostPlans: readonly IHostPlanData[]; readonly bindPlans: readonly IBindPlanData[]; readonly queryPlans: readonly IQueryPlanData[]; readonly unsupportedExecution: readonly string[] };

export const hostPlans: readonly IHostPlanData[] = [];
export const bindPlans: readonly IBindPlanData[] = [];
export const queryPlans: readonly IQueryPlanData[] = [];
export const unsupportedExecution: readonly string[] = [];

function bindArgs(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => (typeof value === "number" && Number.isInteger(value) ? BigInt(value) : value));
}

const ddl: readonly string[] = [
  `CREATE TABLE "doubled" ("name" TEXT NOT NULL, "out" INTEGER NOT NULL, "__support_count" INTEGER NOT NULL DEFAULT 1, PRIMARY KEY ("name", "out")) WITHOUT ROWID`,
  `CREATE TABLE "reading" ("name" TEXT NOT NULL, "value" INTEGER NOT NULL, PRIMARY KEY ("name", "value")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__delta_doubled" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "out" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_doubled_sign" ON "__delta_doubled" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_doubled" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "out" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_doubled_phase" ON "__frontier_doubled" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_doubled" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "out" INTEGER NOT NULL)`,
  `CREATE INDEX "__next_frontier_doubled_phase" ON "__next_frontier_doubled" ("_phase")`,
  `CREATE TEMP TABLE "__delta_reading" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_reading_sign" ON "__delta_reading" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_reading" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_reading_phase" ON "__frontier_reading" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_reading" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" TEXT NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__next_frontier_reading_phase" ON "__next_frontier_reading" ("_phase")`,
  `CREATE TEMP TABLE "__support_next_doubled" ("name" TEXT NOT NULL, "out" INTEGER NOT NULL, "__support_count" INTEGER NOT NULL, PRIMARY KEY ("name", "out")) WITHOUT ROWID`,
];

const relColumns: Record<string, readonly string[]> = {
  doubled: ["name", "out"],
  reading: ["name", "value"],
};

const arrivalTargets: readonly string[] = ["reading"];

const boot: readonly IBootStatement[] = [
  { sql: `INSERT OR IGNORE INTO "reading" ("name", "value") VALUES (?, ?)`, params: ["cpu", 12] },
  { sql: `INSERT OR IGNORE INTO "reading" ("name", "value") VALUES (?, ?)`, params: ["disk", 4] },
  { sql: `DELETE FROM "doubled"`, params: [] },
  { sql: `INSERT OR IGNORE INTO "doubled" ("name", "out") SELECT b0."name", (b0."value" * 2) FROM "reading" b0 WHERE (b0."value" >= 10)`, params: [] },
];

type Snapshot = {
  readonly doubled: readonly IRow[];
  readonly reading: readonly IRow[];
};

function readSnapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    doubled: selectRows(seam, `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", "out" FROM "doubled"`, relColumns.doubled!),
    reading: selectRows(seam, `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", "value" FROM "reading"`, relColumns.reading!),
  });
}

const finalSelect: Record<string, string> = {
  doubled: `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", "out" FROM "doubled"`,
  reading: `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", "value" FROM "reading"`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; addSql: string; delSql: string | null }> = {
  reading: { kind: "set", addSql: `INSERT OR IGNORE INTO "reading" ("name", "value") VALUES (?, ?)`, delSql: `DELETE FROM "reading" WHERE "name" = ? AND "value" = ?` },
};

function arrivalStatement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`filter_map_is_a_level_rule: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`filter_map_is_a_level_rule: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.delSql === null) {
      throw new Error(`filter_map_is_a_level_rule: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.delSql, args: bindArgs(arrival.row) };
  }
  return { sql: template.addSql, args: bindArgs(arrival.row) };
}

function applyArrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrivalStatement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "doubled", kind: "set", tableName: "doubled", deltaTableName: "__delta_doubled", frontierTableName: "__frontier_doubled", nextFrontierTableName: "__next_frontier_doubled", columns: ["name", "out"], keyIndices: [], arrivalAddSql: null, arrivalDelSql: null, boundarySql: `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", "out", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_doubled" WHERE "_sign" IN (-1, 1) GROUP BY "name", "out", "_sign"` },
  { rel: "reading", kind: "set", tableName: "reading", deltaTableName: "__delta_reading", frontierTableName: "__frontier_reading", nextFrontierTableName: "__next_frontier_reading", columns: ["name", "value"], keyIndices: [], arrivalAddSql: `INSERT OR IGNORE INTO "reading" ("name", "value") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "name", "value"`, arrivalDelSql: `DELETE FROM "reading" WHERE ("name", "value") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "name", "value"`, boundarySql: `SELECT CASE WHEN json_valid("name") AND json_type("name") = 'object' THEN json_extract("name", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("name", '$.args')) || ')' ELSE "name" END AS "name", "value", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_reading" WHERE "_sign" IN (-1, 1) GROUP BY "name", "value", "_sign"` },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { headRel: "doubled", headDeltaTableName: "__delta_doubled", headColumns: ["name", "out"], insertSql: `INSERT OR IGNORE INTO "doubled" ("name", "out") SELECT DISTINCT d0."name", (d0."value" * 2) FROM "__frontier_reading" d0 WHERE d0."_phase" >= 0 AND (d0."value" >= 10) RETURNING "name", "out"`, selectSql: `SELECT "name", "out" FROM "doubled"`, recomputeSql: `DELETE FROM "doubled";\nINSERT OR IGNORE INTO "doubled" ("name", "out") SELECT b0."name", (b0."value" * 2) FROM "reading" b0 WHERE (b0."value" >= 10)`, supportSql: [`DELETE FROM "__support_next_doubled"`, `INSERT INTO "__support_next_doubled" ("name", "out", "__support_count") SELECT "name", "out", sum("__support_count") FROM (SELECT b0."name" AS "name", (b0."value" * 2) AS "out", count(*) AS "__support_count" FROM "reading" b0 WHERE (b0."value" >= 10) GROUP BY b0."name", (b0."value" * 2)) GROUP BY "name", "out"`, `UPDATE "doubled" AS h SET "__support_count" = "__support_count" - ("__support_count" - COALESCE((SELECT n."__support_count" FROM "__support_next_doubled" n WHERE n."name" = h."name" AND n."out" = h."out"), 0))`, `DELETE FROM "doubled" WHERE "__support_count" <= 0 RETURNING "name", "out"`, `INSERT INTO "doubled" ("name", "out", "__support_count") SELECT "name", "out", n."__support_count" FROM "__support_next_doubled" n WHERE NOT EXISTS (SELECT 1 FROM "doubled" h WHERE n."name" = h."name" AND n."out" = h."out") RETURNING "name", "out"`], aggregateSql: null },
];

function recomputeLevels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "doubled";\nINSERT OR IGNORE INTO "doubled" ("name", "out") SELECT b0."name", (b0."value" * 2) FROM "reading" b0 WHERE (b0."value" >= 10)`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function buildDeltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const doubled = multisetDiff(before.doubled, after.doubled);
  const reading = multisetDiff(before.reading, after.reading);
  return {
    rels: [
      { rel: "doubled", add: doubled.add, del: doubled.del },
      { rel: "reading", add: reading.add, del: reading.del },
    ],
    carryPending: false,
  };
}

function runNaiveTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return readSnapshot(seam).pipe(
    concatMap((before) => applyArrivals(seam, arrivals).pipe(map(() => before))),
    concatMap((before) => recomputeLevels(seam).pipe(map(() => before))),
    concatMap((before) => readSnapshot(seam).pipe(map((after) => buildDeltas(before, after)))),
  );
  // filter_map_is_a_level_rule: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

function runIncrementalTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepareTick(seam, INCREMENTAL_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.applyArrivals(seam, arrivals, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyLevelsBeforeEdges(seam, INCREMENTAL_LEVEL_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyEdges(seam, INCREMENTAL_EDGE_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recomputeLevelsAfterEdges(seam, INCREMENTAL_LEVEL_STATEMENTS, INCREMENTAL_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.readBoundary(seam, INCREMENTAL_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promoteFrontiers(seam, INCREMENTAL_RELATIONS).pipe(
      map((carryPending): ITickDeltas => ({ rels, carryPending })),
    )),
  );
}

function runTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return runNaiveTick(seam, arrivals);
  }
  return runIncrementalTick(seam, arrivals);
}

export const incrementalPlan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcileEveryTick: RECONCILE_EVERY_TICK,
  retractionGuard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "filter_map_is_a_level_rule",
  ddl,
  relColumns,
  arrivalTargets,
  boot,
  finalSelect,
  hostPlans,
  bindPlans,
  queryPlans,
  unsupportedExecution,
  tick: runTick,
};
