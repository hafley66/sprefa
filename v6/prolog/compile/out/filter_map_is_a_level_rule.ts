// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: filter_map_is_a_level_rule.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "doubled": [true, false],
    "reading": [true, false],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "filter_map_is_a_level_rule_doubled" ("__id" INTEGER PRIMARY KEY, "name" INTEGER NOT NULL, "out" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("name", "out"))`,
  `CREATE TEMP VIEW "__txt_filter_map_is_a_level_rule_doubled" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."out" AS "out", t."__refcount" AS "__refcount" FROM "filter_map_is_a_level_rule_doubled" t`,
  `CREATE TABLE "filter_map_is_a_level_rule_reading" ("__id" INTEGER PRIMARY KEY, "name" INTEGER NOT NULL, "value" INTEGER NOT NULL, UNIQUE ("name", "value"))`,
  `CREATE TEMP VIEW "__txt_filter_map_is_a_level_rule_reading" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."value" AS "value" FROM "filter_map_is_a_level_rule_reading" t`,
  `CREATE TEMP TABLE "__delta_filter_map_is_a_level_rule_doubled" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "out" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_filter_map_is_a_level_rule_doubled_sign" ON "__delta_filter_map_is_a_level_rule_doubled" ("_sign")`,
  `CREATE INDEX "__delta_filter_map_is_a_level_rule_doubled_group" ON "__delta_filter_map_is_a_level_rule_doubled" ("name", "out")`,
  `CREATE TEMP TABLE "__frontier_filter_map_is_a_level_rule_doubled" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "out" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_filter_map_is_a_level_rule_doubled_phase" ON "__frontier_filter_map_is_a_level_rule_doubled" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_filter_map_is_a_level_rule_doubled" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "out" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_filter_map_is_a_level_rule_doubled" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."out" AS "out", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_filter_map_is_a_level_rule_doubled" t`,
  `CREATE TEMP TABLE "__delta_filter_map_is_a_level_rule_reading" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_filter_map_is_a_level_rule_reading_sign" ON "__delta_filter_map_is_a_level_rule_reading" ("_sign")`,
  `CREATE INDEX "__delta_filter_map_is_a_level_rule_reading_group" ON "__delta_filter_map_is_a_level_rule_reading" ("name", "value")`,
  `CREATE TEMP TABLE "__frontier_filter_map_is_a_level_rule_reading" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_filter_map_is_a_level_rule_reading_phase" ON "__frontier_filter_map_is_a_level_rule_reading" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_filter_map_is_a_level_rule_reading" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_filter_map_is_a_level_rule_reading" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."value" AS "value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_filter_map_is_a_level_rule_reading" t`,
  `CREATE TEMP TABLE "__support_next_filter_map_is_a_level_rule_doubled" ("name" INTEGER NOT NULL, "out" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("name", "out")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_filter_map_is_a_level_rule_doubled" ("name" INTEGER NOT NULL, "out" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "filter_map_is_a_level_rule_doubled_zero" ON "filter_map_is_a_level_rule_doubled" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  doubled: ["name", "out"],
  reading: ["name", "value"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  doubled: ["text", "int"],
  reading: ["text", "int"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  doubled: ["text", "int"],
  reading: ["text", "int"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "filter_map_is_a_level_rule", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "8d3263b5964a89d5", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "doubled", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "435c9857d8a71229", h_schema: "30cbb35eb438f4ba", h_rule: "85b2d829198fea59" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "name", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "1985ceee4d61c536", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 8, ordinal: 2, local_name: "out", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "9f2f0cb426f971b7", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 7, ordinal: 0, local_name: "reading", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "837a4f347c59b56e", h_schema: "9f4a4c9bfecfb7a1", h_rule: "" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "name", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "aa4ab7c984d20db7", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 11, ordinal: 2, local_name: "value", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "a067610939aa071c", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 8, ordinal: 0, local_name: "__delta_doubled", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "deb632a2edb46921", h_schema: "aad4c05e592804c1", h_rule: "" },
  { rel_id: 15, parent_id: 8, ordinal: 0, local_name: "__frontier_doubled", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "c3f4c9675c0c6c36", h_schema: "a6e6e50d98759a98", h_rule: "" },
  { rel_id: 16, parent_id: 8, ordinal: 0, local_name: "__next_frontier_doubled", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "f64ca58fce3cf26c", h_schema: "a6e6e50d98759a98", h_rule: "" },
  { rel_id: 17, parent_id: 8, ordinal: 0, local_name: "__txt_filter_map_is_a_level_rule_doubled", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "fb99fe1cae1bf60b", h_schema: "30cbb35eb438f4ba", h_rule: "" },
  { rel_id: 18, parent_id: 14, ordinal: 0, local_name: "__txt___delta_filter_map_is_a_level_rule_doubled", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "aa87e015f90f9e03", h_schema: "30cbb35eb438f4ba", h_rule: "" },
  { rel_id: 19, parent_id: 11, ordinal: 0, local_name: "__delta_reading", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "baaa1a5b754eb2c1", h_schema: "56ad53f6153c2295", h_rule: "" },
  { rel_id: 20, parent_id: 11, ordinal: 0, local_name: "__frontier_reading", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "353cd295c000d29b", h_schema: "852deffd8189d1f7", h_rule: "" },
  { rel_id: 21, parent_id: 11, ordinal: 0, local_name: "__next_frontier_reading", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "a586b9e4da01ffcb", h_schema: "852deffd8189d1f7", h_rule: "" },
  { rel_id: 22, parent_id: 11, ordinal: 0, local_name: "__txt_filter_map_is_a_level_rule_reading", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "806a795188d44cc1", h_schema: "9f4a4c9bfecfb7a1", h_rule: "" },
  { rel_id: 23, parent_id: 19, ordinal: 0, local_name: "__txt___delta_filter_map_is_a_level_rule_reading", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "2d3b257185ccafd0", h_schema: "9f4a4c9bfecfb7a1", h_rule: "" },
  { rel_id: 24, parent_id: 7, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 7, h_id: "8817b20308da2cb5", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 8, ordinal: 0, local_name: "__support_next_doubled", kind: "refcount", type_id: 0, arity: 3, module_id: 7, h_id: "bb392aed92e6acce", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 8, ordinal: 0, local_name: "__new_doubled", kind: "refcount_staging", type_id: 0, arity: 3, module_id: 7, h_id: "b492dd2b54f38f24", h_schema: "", h_rule: "" },
  { rel_id: 27, parent_id: 9, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "5d553fa548a5b98a", h_schema: "", h_rule: "" },
  { rel_id: 28, parent_id: 10, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "4a4ea0edf4dde260", h_schema: "", h_rule: "" },
  { rel_id: 29, parent_id: 12, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "95932a49800a5fb9", h_schema: "", h_rule: "" },
  { rel_id: 30, parent_id: 13, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "7c441675cdb10aad", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["reading"];

const boot: readonly IBootStatement[] = [
  { rel: "reading", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["cpu"] },
  { rel: "reading", sql: `INSERT OR IGNORE INTO "filter_map_is_a_level_rule_reading" ("name", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["cpu", 12] },
  { rel: "reading", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["disk"] },
  { rel: "reading", sql: `INSERT OR IGNORE INTO "filter_map_is_a_level_rule_reading" ("name", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["disk", 4] },
  { rel: "doubled", sql: `DELETE FROM "filter_map_is_a_level_rule_doubled"`, params: [] },
  { rel: "doubled", sql: `INSERT OR IGNORE INTO "filter_map_is_a_level_rule_doubled" ("name", "out") SELECT b0."name", (b0."value" * 2) FROM "filter_map_is_a_level_rule_reading" b0 WHERE (b0."value" >= 10)`, params: [] },
];

const final_select: Record<string, string> = {
  doubled: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."out" FROM "__txt_filter_map_is_a_level_rule_doubled" t`,
  reading: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."value" FROM "__txt_filter_map_is_a_level_rule_reading" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "doubled", kind: "set", table_name: "filter_map_is_a_level_rule_doubled", delta_table_name: "__delta_filter_map_is_a_level_rule_doubled", frontier_table_name: "__frontier_filter_map_is_a_level_rule_doubled", next_frontier_table_name: "__next_frontier_filter_map_is_a_level_rule_doubled", columns: ["name", "out"], column_types: ["text", "int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."out", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_filter_map_is_a_level_rule_doubled" t WHERE t."_sign" IN (-1, 1) GROUP BY t."name", t."out", t."_sign"`, rule_observers: [] },
  { rel: "reading", kind: "set", table_name: "filter_map_is_a_level_rule_reading", delta_table_name: "__delta_filter_map_is_a_level_rule_reading", frontier_table_name: "__frontier_filter_map_is_a_level_rule_reading", next_frontier_table_name: "__next_frontier_filter_map_is_a_level_rule_reading", columns: ["name", "value"], column_types: ["text", "int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "filter_map_is_a_level_rule_reading" ("name", "value") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "name", "value"`, arrival_del_sql: `DELETE FROM "filter_map_is_a_level_rule_reading" WHERE ("name", "value") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "name", "value"`, boundary_sql: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."value", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_filter_map_is_a_level_rule_reading" t WHERE t."_sign" IN (-1, 1) GROUP BY t."name", t."value", t."_sign"`, rule_observers: ["doubled/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "doubled", rule_id: "filter_map_is_a_level_rule:doubled/2#1", head_delta_table_name: "__delta_filter_map_is_a_level_rule_doubled", head_columns: ["name", "out"], insert_sql: `INSERT OR IGNORE INTO "filter_map_is_a_level_rule_doubled" ("name", "out") SELECT DISTINCT d0."name", (d0."value" * 2) FROM "__frontier_filter_map_is_a_level_rule_reading" d0 WHERE d0."_phase" >= 0 AND (d0."value" >= 10) RETURNING "name", "out"`, select_sql: `SELECT "name", "out" FROM "filter_map_is_a_level_rule_doubled"`, recompute_sql: `DELETE FROM "filter_map_is_a_level_rule_doubled";
INSERT OR IGNORE INTO "filter_map_is_a_level_rule_doubled" ("name", "out") SELECT b0."name", (b0."value" * 2) FROM "filter_map_is_a_level_rule_reading" b0 WHERE (b0."value" >= 10)`, support_sql: [`DELETE FROM "__support_next_filter_map_is_a_level_rule_doubled"`, `INSERT INTO "__support_next_filter_map_is_a_level_rule_doubled" ("name", "out", "__refcount") SELECT "name", "out", sum("__refcount") FROM (SELECT b0."name" AS "name", (b0."value" * 2) AS "out", count(*) AS "__refcount" FROM "filter_map_is_a_level_rule_reading" b0 WHERE (b0."value" >= 10) GROUP BY b0."name", (b0."value" * 2)) GROUP BY "name", "out"`, `UPDATE "filter_map_is_a_level_rule_doubled" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_filter_map_is_a_level_rule_doubled" n WHERE n."name" = h."name" AND n."out" = h."out"), 0)`, `INSERT INTO "__delta_filter_map_is_a_level_rule_doubled" ("_sign", "_sequence", "name", "out") SELECT -1, row_number() OVER () - 1, "name", "out" FROM "filter_map_is_a_level_rule_doubled" WHERE "__refcount" <= 0`, `DELETE FROM "filter_map_is_a_level_rule_doubled" WHERE "__refcount" <= 0`, `DELETE FROM "__new_filter_map_is_a_level_rule_doubled"`, `INSERT INTO "__new_filter_map_is_a_level_rule_doubled" ("name", "out", "__refcount") SELECT n."name", n."out", n."__refcount" FROM "__support_next_filter_map_is_a_level_rule_doubled" n LEFT JOIN "filter_map_is_a_level_rule_doubled" h ON n."name" = h."name" AND n."out" = h."out" WHERE h."name" IS NULL`, `INSERT INTO "__delta_filter_map_is_a_level_rule_doubled" ("_sign", "_sequence", "name", "out") SELECT 1, "rowid" - 1, "name", "out" FROM "__new_filter_map_is_a_level_rule_doubled"`, `INSERT INTO "__frontier_filter_map_is_a_level_rule_doubled" ("_phase", "_sequence", "name", "out") SELECT ?, "rowid" - 1, "name", "out" FROM "__new_filter_map_is_a_level_rule_doubled"`, `INSERT INTO "__next_frontier_filter_map_is_a_level_rule_doubled" ("_phase", "_sequence", "name", "out") SELECT ?, "rowid" - 1, "name", "out" FROM "__new_filter_map_is_a_level_rule_doubled"`, `INSERT OR IGNORE INTO "filter_map_is_a_level_rule_doubled" ("name", "out", "__refcount") SELECT n."name", n."out", n."__refcount" FROM "__support_next_filter_map_is_a_level_rule_doubled" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "filter_map_is_a_level_rule",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
