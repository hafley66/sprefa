// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: json_array_spread_skips_non_matching_elements.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { EnumPlane } from "../runtime/enumPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IEnumRefColumns,
  IEnumTypePlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly rel_physical_names: Record<string, string>; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const ENUM_TYPES: readonly IEnumTypePlan[] = [];
export const ENUM_REF_COLUMNS: IEnumRefColumns = {};

const ddl: readonly string[] = [
  `CREATE TABLE "json_array_spread_skips_non_matching_elements_numbered" ("__id" INTEGER PRIMARY KEY, "number" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("number"))`,
  `CREATE TABLE "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" ("__id" INTEGER PRIMARY KEY, "body" TEXT NOT NULL CHECK (json_valid("body")), UNIQUE ("body"))`,
  `CREATE TEMP TABLE "__delta_json_array_spread_skips_non_matching_elements_numbered" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "number" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_json_array_spread_skips_non_matching_elements_numbered_sign" ON "__delta_json_array_spread_skips_non_matching_elements_numbered" ("_sign")`,
  `CREATE INDEX "__delta_json_array_spread_skips_non_matching_elements_numbered_group" ON "__delta_json_array_spread_skips_non_matching_elements_numbered" ("number")`,
  `CREATE TEMP TABLE "__frontier_json_array_spread_skips_non_matching_elements_numbered" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "number" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_json_array_spread_skips_non_matching_elements_numbered_phase" ON "__frontier_json_array_spread_skips_non_matching_elements_numbered" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_json_array_spread_skips_non_matching_elements_numbered" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "number" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "body" TEXT NOT NULL CHECK (json_valid("body")))`,
  `CREATE INDEX "__delta_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b_sign" ON "__delta_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" ("_sign")`,
  `CREATE INDEX "__delta_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b_group" ON "__delta_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" ("body")`,
  `CREATE TEMP TABLE "__frontier_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "body" TEXT NOT NULL CHECK (json_valid("body")))`,
  `CREATE INDEX "__frontier_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b_phase" ON "__frontier_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "body" TEXT NOT NULL CHECK (json_valid("body")))`,
  `CREATE TEMP TABLE "__support_next_json_array_spread_skips_non_matching_elements_numbered" ("number" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("number")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_json_array_spread_skips_non_matching_elements_numbered" ("number" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "json_array_spread_skips_non_matching_elements_numbered_zero" ON "json_array_spread_skips_non_matching_elements_numbered" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  numbered: ["number"],
  resp: ["body"],
};

const rel_physical_names: Record<string, string> = {
  numbered: "json_array_spread_skips_non_matching_elements_numbered",
  resp: "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b",
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  numbered: ["int"],
  resp: ["json"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  numbered: ["int"],
  resp: ["json"],
};

const rel_catalog: readonly IRelCatalogRow[] = new Array<IRelCatalogRow>(
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "json_array_spread_skips_non_matching_elements", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "a3d6c380a7680a14", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "numbered", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "9a0bb3b1195c8682", h_schema: "0de92a14972e527c", h_rule: "c2dc8f1e98acd733" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "number", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "9b4430fdce31e1af", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 7, ordinal: 0, local_name: "resp", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "d8fee69ee6a0a654", h_schema: "d12f7c978de0f0d7", h_rule: "" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "body", kind: "column", type_id: 5, arity: 0, module_id: 7, h_id: "7fcf766f83fc2efb", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 8, ordinal: 0, local_name: "__delta_json_array_spread_skips_non_matching_elements_numbered", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "706752590f9a9d6a", h_schema: "6462e68e7590cd3e", h_rule: "" },
  { rel_id: 13, parent_id: 8, ordinal: 0, local_name: "__frontier_json_array_spread_skips_non_matching_elements_numbered", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "7a5c6e058a1021af", h_schema: "90e224fdda0dd2df", h_rule: "" },
  { rel_id: 14, parent_id: 8, ordinal: 0, local_name: "__next_frontier_json_array_spread_skips_non_matching_elements_numbered", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "a5c0f3deaaecb5fd", h_schema: "90e224fdda0dd2df", h_rule: "" },
  { rel_id: 15, parent_id: 10, ordinal: 0, local_name: "__delta_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "7c57578a30776b01", h_schema: "669db8c8e0e7991f", h_rule: "" },
  { rel_id: 16, parent_id: 10, ordinal: 0, local_name: "__frontier_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "056b65a199a0a730", h_schema: "c0ff50f985c9f689", h_rule: "" },
  { rel_id: 17, parent_id: 10, ordinal: 0, local_name: "__next_frontier_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "87fde8c80c47d26a", h_schema: "c0ff50f985c9f689", h_rule: "" },
  { rel_id: 18, parent_id: 8, ordinal: 0, local_name: "__support_next_json_array_spread_skips_non_matching_elements_numbered", kind: "refcount", type_id: 0, arity: 2, module_id: 7, h_id: "4d211e759602b02a", h_schema: "", h_rule: "" },
  { rel_id: 19, parent_id: 8, ordinal: 0, local_name: "__new_json_array_spread_skips_non_matching_elements_numbered", kind: "refcount_staging", type_id: 0, arity: 2, module_id: 7, h_id: "06b76bcaead5a829", h_schema: "", h_rule: "" },
  { rel_id: 20, parent_id: 9, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "10e5de2c64b218e0", h_schema: "", h_rule: "" },
  { rel_id: 21, parent_id: 11, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "67e5a7bb977b5686", h_schema: "", h_rule: "" },
);

const rel_declared_column_types: Record<string, readonly string[]> = {
  numbered: ["int"],
  resp: ["json"],
};

const arrival_targets: readonly string[] = ["resp"];

const boot: readonly IBootStatement[] = [
  { rel: "resp", sql: `INSERT OR IGNORE INTO "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" ("body") VALUES (?)`, params: ["[{\"number\":1},{\"title\":\"no_number\"},{\"number\":3}]"] },
  { rel: "numbered", sql: `DELETE FROM "json_array_spread_skips_non_matching_elements_numbered"`, params: [] },
  { rel: "numbered", sql: `INSERT OR IGNORE INTO "json_array_spread_skips_non_matching_elements_numbered" ("number") SELECT json_extract(j0.value, '$."number"') FROM "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" b0, json_each(b0."body") j0 WHERE json_type(b0."body", '$') = 'array' AND j0.type = 'object' AND json_extract(j0.value, '$."number"') IS NOT NULL`, params: [] },
];

const final_select: Record<string, string> = {
  numbered: `SELECT t."number" FROM "json_array_spread_skips_non_matching_elements_numbered" t`,
  resp: `SELECT t."body" FROM "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "numbered", kind: "set", table_name: "json_array_spread_skips_non_matching_elements_numbered", delta_table_name: "__delta_json_array_spread_skips_non_matching_elements_numbered", frontier_table_name: "__frontier_json_array_spread_skips_non_matching_elements_numbered", next_frontier_table_name: "__next_frontier_json_array_spread_skips_non_matching_elements_numbered", columns: ["number"], column_types: ["int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT t."number", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_json_array_spread_skips_non_matching_elements_numbered" t WHERE t."_sign" IN (-1, 1) GROUP BY t."number", t."_sign"`, rule_observers: [] },
  { rel: "resp", kind: "set", table_name: "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b", delta_table_name: "__delta_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b", frontier_table_name: "__frontier_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b", next_frontier_table_name: "__next_frontier_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b", columns: ["body"], column_types: ["json"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" ("body") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "body"`, arrival_del_sql: `DELETE FROM "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" WHERE ("body") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "body"`, boundary_sql: `SELECT t."body", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" t WHERE t."_sign" IN (-1, 1) GROUP BY t."body", t."_sign"`, rule_observers: ["numbered/1"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "numbered", rule_id: "json_array_spread_skips_non_matching_elements:numbered/1#1", head_delta_table_name: "__delta_json_array_spread_skips_non_matching_elements_numbered", head_columns: ["number"], insert_sql: `INSERT OR IGNORE INTO "json_array_spread_skips_non_matching_elements_numbered" ("number") SELECT DISTINCT json_extract(j0.value, '$."number"') FROM "__frontier_json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" d0, json_each(d0."body") j0 WHERE d0."_phase" >= 0 AND json_type(d0."body", '$') = 'array' AND j0.type = 'object' AND json_extract(j0.value, '$."number"') IS NOT NULL RETURNING "number"`, select_sql: `SELECT "number" FROM "json_array_spread_skips_non_matching_elements_numbered"`, recompute_sql: `DELETE FROM "json_array_spread_skips_non_matching_elements_numbered";
INSERT OR IGNORE INTO "json_array_spread_skips_non_matching_elements_numbered" ("number") SELECT json_extract(j0.value, '$."number"') FROM "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" b0, json_each(b0."body") j0 WHERE json_type(b0."body", '$') = 'array' AND j0.type = 'object' AND json_extract(j0.value, '$."number"') IS NOT NULL`, support_sql: [`DELETE FROM "__support_next_json_array_spread_skips_non_matching_elements_numbered"`, `INSERT INTO "__support_next_json_array_spread_skips_non_matching_elements_numbered" ("number", "__refcount") SELECT "number", sum("__refcount") FROM (SELECT json_extract(j0.value, '$."number"') AS "number", count(*) AS "__refcount" FROM "json_array_spread_skips_non_matching_elements_resp_d9c0b388b53b" b0, json_each(b0."body") j0 WHERE json_type(b0."body", '$') = 'array' AND j0.type = 'object' AND json_extract(j0.value, '$."number"') IS NOT NULL GROUP BY json_extract(j0.value, '$."number"')) GROUP BY "number"`, `UPDATE "json_array_spread_skips_non_matching_elements_numbered" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_json_array_spread_skips_non_matching_elements_numbered" n WHERE n."number" = h."number"), 0)`, `INSERT INTO "__delta_json_array_spread_skips_non_matching_elements_numbered" ("_sign", "_sequence", "number") SELECT -1, row_number() OVER () - 1, "number" FROM "json_array_spread_skips_non_matching_elements_numbered" WHERE "__refcount" <= 0`, `DELETE FROM "json_array_spread_skips_non_matching_elements_numbered" WHERE "__refcount" <= 0`, `DELETE FROM "__new_json_array_spread_skips_non_matching_elements_numbered"`, `INSERT INTO "__new_json_array_spread_skips_non_matching_elements_numbered" ("number", "__refcount") SELECT n."number", n."__refcount" FROM "__support_next_json_array_spread_skips_non_matching_elements_numbered" n LEFT JOIN "json_array_spread_skips_non_matching_elements_numbered" h ON n."number" = h."number" WHERE h."number" IS NULL`, `INSERT INTO "__delta_json_array_spread_skips_non_matching_elements_numbered" ("_sign", "_sequence", "number") SELECT 1, "rowid" - 1, "number" FROM "__new_json_array_spread_skips_non_matching_elements_numbered"`, `INSERT INTO "__frontier_json_array_spread_skips_non_matching_elements_numbered" ("_phase", "_sequence", "number") SELECT ?, "rowid" - 1, "number" FROM "__new_json_array_spread_skips_non_matching_elements_numbered"`, `INSERT INTO "__next_frontier_json_array_spread_skips_non_matching_elements_numbered" ("_phase", "_sequence", "number") SELECT ?, "rowid" - 1, "number" FROM "__new_json_array_spread_skips_non_matching_elements_numbered"`, `INSERT OR IGNORE INTO "json_array_spread_skips_non_matching_elements_numbered" ("number", "__refcount") SELECT n."number", n."__refcount" FROM "__support_next_json_array_spread_skips_non_matching_elements_numbered" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      concatMap((carry_pending) => EnumPlane.decode_deltas(seam, ENUM_TYPES, ENUM_REF_COLUMNS, SUBSCRIBED_RELATIONS, rels).pipe(
        map((decoded): ITickDeltas => ({ rels: decoded, carry_pending })),
      )),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return EnumPlane.intern(seam, ENUM_TYPES, ENUM_REF_COLUMNS, arrivals).pipe(
    concatMap((normalized) => run_incremental_tick(seam, validate_arrivals(normalized))),
  );
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "json_array_spread_skips_non_matching_elements",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_physical_names,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  enum_types: ENUM_TYPES,
  enum_ref_columns: ENUM_REF_COLUMNS,
  unsupported_execution,
  tick: run_tick,
};
