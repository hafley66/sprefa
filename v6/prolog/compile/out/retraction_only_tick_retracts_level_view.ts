// GENERATED by v6/prolog/compile (tsv2 emitter P2). Do not
// hand-edit; recompile. Program: retraction_only_tick_retracts_level_view.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lowerSql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. The snapshot recompute path remains
// selectable with SPREFA_TSV2_EMITTER_MODE=naive and is used automatically
// for negative bodies and ticks containing retractions.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { multisetDiff } from "../runtime/diff.ts";
import { selectRows } from "../runtime/rows.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelDelta,
  IRow,
  IRowValue,
  ISqlSeam,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IBootStatement {
  sql: string;
  params: readonly (string | number)[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[] };

function bindArgs(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => (typeof value === "number" && Number.isInteger(value) ? BigInt(value) : value));
}

const ddl: readonly string[] = [
  `CREATE TABLE "mirror" ("item" TEXT NOT NULL, PRIMARY KEY ("item")) WITHOUT ROWID`,
  `CREATE TABLE "source_row" ("item" TEXT NOT NULL, PRIMARY KEY ("item")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__delta_mirror" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_mirror_sign" ON "__delta_mirror" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_mirror" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" TEXT NOT NULL)`,
  `CREATE INDEX "__frontier_mirror_phase" ON "__frontier_mirror" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_mirror" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" TEXT NOT NULL)`,
  `CREATE INDEX "__next_frontier_mirror_phase" ON "__next_frontier_mirror" ("_phase")`,
  `CREATE TEMP TABLE "__delta_source_row" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" TEXT NOT NULL)`,
  `CREATE INDEX "__delta_source_row_sign" ON "__delta_source_row" ("_sign")`,
  `CREATE TEMP TABLE "__frontier_source_row" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" TEXT NOT NULL)`,
  `CREATE INDEX "__frontier_source_row_phase" ON "__frontier_source_row" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_source_row" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "item" TEXT NOT NULL)`,
  `CREATE INDEX "__next_frontier_source_row_phase" ON "__next_frontier_source_row" ("_phase")`,
];

const relColumns: Record<string, readonly string[]> = {
  mirror: ["item"],
  source_row: ["item"],
};

const arrivalTargets: readonly string[] = ["source_row"];

const boot: readonly IBootStatement[] = [
  { sql: `DELETE FROM "mirror"`, params: [] },
  { sql: `INSERT OR IGNORE INTO "mirror" ("item") SELECT b0."item" FROM "source_row" b0`, params: [] },
];

type Snapshot = {
  readonly mirror: readonly IRow[];
  readonly source_row: readonly IRow[];
};

function readSnapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    mirror: selectRows(seam, `SELECT CASE WHEN json_valid("item") AND json_type("item") = 'object' THEN json_extract("item", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("item", '$.args')) || ')' ELSE "item" END AS "item" FROM "mirror"`, relColumns.mirror!),
    source_row: selectRows(seam, `SELECT CASE WHEN json_valid("item") AND json_type("item") = 'object' THEN json_extract("item", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("item", '$.args')) || ')' ELSE "item" END AS "item" FROM "source_row"`, relColumns.source_row!),
  });
}

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; addSql: string; delSql: string | null }> = {
  source_row: { kind: "set", addSql: `INSERT OR IGNORE INTO "source_row" ("item") VALUES (?)`, delSql: `DELETE FROM "source_row" WHERE "item" = ?` },
};

function arrivalStatement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`retraction_only_tick_retracts_level_view: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`retraction_only_tick_retracts_level_view: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.delSql === null) {
      throw new Error(`retraction_only_tick_retracts_level_view: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.delSql, args: bindArgs(arrival.row) };
  }
  return { sql: template.addSql, args: bindArgs(arrival.row) };
}

function applyArrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrivalStatement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "mirror", kind: "set", tableName: "mirror", deltaTableName: "__delta_mirror", frontierTableName: "__frontier_mirror", nextFrontierTableName: "__next_frontier_mirror", columns: ["item"], arrivalAddSql: null, boundarySql: `SELECT CASE WHEN json_valid("item") AND json_type("item") = 'object' THEN json_extract("item", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("item", '$.args')) || ')' ELSE "item" END AS "item", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_mirror" WHERE "_sign" IN (-1, 1) GROUP BY "item", "_sign"` },
  { rel: "source_row", kind: "set", tableName: "source_row", deltaTableName: "__delta_source_row", frontierTableName: "__frontier_source_row", nextFrontierTableName: "__next_frontier_source_row", columns: ["item"], arrivalAddSql: `INSERT OR IGNORE INTO "source_row" ("item") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "item"`, boundarySql: `SELECT CASE WHEN json_valid("item") AND json_type("item") = 'object' THEN json_extract("item", '$.fn') || '(' || (SELECT group_concat(value, ',') FROM json_each("item", '$.args')) || ')' ELSE "item" END AS "item", "_sign" AS "__sign", count(*) AS "__count" FROM "__delta_source_row" WHERE "_sign" IN (-1, 1) GROUP BY "item", "_sign"` },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { headRel: "mirror", headDeltaTableName: "__delta_mirror", headColumns: ["item"], insertSql: `INSERT OR IGNORE INTO "mirror" ("item") SELECT DISTINCT d0."item" FROM "__frontier_source_row" d0 WHERE d0."_phase" >= 0 RETURNING "item"`, selectSql: `SELECT "item" FROM "mirror"`, recomputeSql: `DELETE FROM "mirror";\nINSERT OR IGNORE INTO "mirror" ("item") SELECT b0."item" FROM "source_row" b0` },
];

function recomputeLevels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "mirror";\nINSERT OR IGNORE INTO "mirror" ("item") SELECT b0."item" FROM "source_row" b0`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function buildDeltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const mirror = multisetDiff(before.mirror, after.mirror);
  const source_row = multisetDiff(before.source_row, after.source_row);
  return {
    rels: [
      { rel: "mirror", add: mirror.add, del: mirror.del },
      { rel: "source_row", add: source_row.add, del: source_row.del },
    ],
    carryPending: false,
  };
}

function runNaiveTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return readSnapshot(seam).pipe(
    concatMap((before) => applyArrivals(seam, arrivals).pipe(map(() => before))),
    concatMap((before) => recomputeLevels(seam).pipe(map(() => before))),
    concatMap((before) => readSnapshot(seam).pipe(map((after) => buildDeltas(before, after)))),
  );
  // retraction_only_tick_retracts_level_view: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

function runIncrementalTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepareTick(seam, INCREMENTAL_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.applyArrivals(seam, arrivals, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyLevelsBeforeEdges(seam, INCREMENTAL_LEVEL_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => IncrementalRuntime.applyEdges(seam, INCREMENTAL_EDGE_STATEMENTS, INCREMENTAL_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.readBoundary(seam, INCREMENTAL_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promoteFrontiers(seam, INCREMENTAL_RELATIONS).pipe(
      map((carryPending): ITickDeltas => ({ rels, carryPending })),
    )),
  );
}

function runTick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  const hasRetraction = arrivals.some((arrival) => arrival.sign === "del");
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE || hasRetraction) {
    return runNaiveTick(seam, arrivals);
  }
  return runIncrementalTick(seam, arrivals);
}

export const incrementalPlan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "retraction_only_tick_retracts_level_view",
  ddl,
  relColumns,
  arrivalTargets,
  boot,
  tick: runTick,
};
