// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: range_join_over_arithmetic.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { EnumPlane } from "../runtime/enumPlane.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IEnumRefColumns,
  IEnumTypePlan,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly ir_version: number; readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly rel_physical_names: Record<string, string>; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const ENUM_TYPES: readonly IEnumTypePlan[] = [];
export const ENUM_REF_COLUMNS: IEnumRefColumns = {};

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "eprintln_hit": [true, false],
    "eprintln_waived": [true, false],
    "eprintln_waiver_line": [true, false],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("__id" INTEGER PRIMARY KEY, "path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL, UNIQUE ("path", "line_number"))`,
  `CREATE TEMP VIEW "__txt_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."path") AS "path", t."line_number" AS "line_number" FROM "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" t`,
  `CREATE TABLE "range_join_over_arithmetic_eprintln_waived" ("__id" INTEGER PRIMARY KEY, "path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("path", "line_number"))`,
  `CREATE TEMP VIEW "__txt_range_join_over_arithmetic_eprintln_waived" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."path") AS "path", t."line_number" AS "line_number", t."__refcount" AS "__refcount" FROM "range_join_over_arithmetic_eprintln_waived" t`,
  `CREATE TABLE "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" ("__id" INTEGER PRIMARY KEY, "path" INTEGER NOT NULL, "waiver_line" INTEGER NOT NULL, UNIQUE ("path", "waiver_line"))`,
  `CREATE TEMP VIEW "__txt_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."path") AS "path", t."waiver_line" AS "waiver_line" FROM "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" t`,
  `CREATE TEMP TABLE "__delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9_sign" ON "__delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("_sign")`,
  `CREATE INDEX "__delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9_group" ON "__delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("path", "line_number")`,
  `CREATE TEMP TABLE "__frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9_phase" ON "__frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."path") AS "path", t."line_number" AS "line_number", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" t`,
  `CREATE TEMP TABLE "__delta_range_join_over_arithmetic_eprintln_waived" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_range_join_over_arithmetic_eprintln_waived_sign" ON "__delta_range_join_over_arithmetic_eprintln_waived" ("_sign")`,
  `CREATE INDEX "__delta_range_join_over_arithmetic_eprintln_waived_group" ON "__delta_range_join_over_arithmetic_eprintln_waived" ("path", "line_number")`,
  `CREATE TEMP TABLE "__frontier_range_join_over_arithmetic_eprintln_waived" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_range_join_over_arithmetic_eprintln_waived_phase" ON "__frontier_range_join_over_arithmetic_eprintln_waived" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_range_join_over_arithmetic_eprintln_waived" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_range_join_over_arithmetic_eprintln_waived" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."path") AS "path", t."line_number" AS "line_number", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_range_join_over_arithmetic_eprintln_waived" t`,
  `CREATE TEMP TABLE "__delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" INTEGER NOT NULL, "waiver_line" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff_sign" ON "__delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" ("_sign")`,
  `CREATE INDEX "__delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff_group" ON "__delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" ("path", "waiver_line")`,
  `CREATE TEMP TABLE "__frontier_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" INTEGER NOT NULL, "waiver_line" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff_phase" ON "__frontier_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "path" INTEGER NOT NULL, "waiver_line" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."path") AS "path", t."waiver_line" AS "waiver_line", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" t`,
  `CREATE TEMP TABLE "__support_next_range_join_over_arithmetic_eprintln_waived" ("path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("path", "line_number")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_range_join_over_arithmetic_eprintln_waived" ("path" INTEGER NOT NULL, "line_number" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "range_join_over_arithmetic_eprintln_waived_zero" ON "range_join_over_arithmetic_eprintln_waived" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  eprintln_hit: ["path", "line_number"],
  eprintln_waived: ["path", "line_number"],
  eprintln_waiver_line: ["path", "waiver_line"],
};

const rel_physical_names: Record<string, string> = {
  eprintln_hit: "range_join_over_arithmetic_eprintln_hit_83ebe90615c9",
  eprintln_waived: "range_join_over_arithmetic_eprintln_waived",
  eprintln_waiver_line: "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff",
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  eprintln_hit: ["text", "int"],
  eprintln_waived: ["text", "int"],
  eprintln_waiver_line: ["text", "int"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  eprintln_hit: ["text", "int"],
  eprintln_waived: ["text", "int"],
  eprintln_waiver_line: ["text", "int"],
};

const rel_catalog: readonly IRelCatalogRow[] = new Array<IRelCatalogRow>(
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "range_join_over_arithmetic", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "34100ce44badf8fb", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "eprintln_hit", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "add6fedcb2775011", h_schema: "eb1c51547565abe6", h_rule: "" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "path", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "af70c59a977c3a55", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 8, ordinal: 2, local_name: "line_number", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "5f2c5935ea2d3ff4", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 7, ordinal: 0, local_name: "eprintln_waived", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "7fc22e4432bb5e74", h_schema: "eb1c51547565abe6", h_rule: "dc3c666e63bd8d53" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "path", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "0fb720aac3c5d5d6", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 11, ordinal: 2, local_name: "line_number", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "b51d6c17b1771785", h_schema: "", h_rule: "" },
  { rel_id: 14, parent_id: 7, ordinal: 0, local_name: "eprintln_waiver_line", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "c0671a0e06ffd6a1", h_schema: "8331cc464dc49f2a", h_rule: "" },
  { rel_id: 15, parent_id: 14, ordinal: 1, local_name: "path", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "fec64fbee4e23df9", h_schema: "", h_rule: "" },
  { rel_id: 16, parent_id: 14, ordinal: 2, local_name: "waiver_line", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "b85305e4109c04df", h_schema: "", h_rule: "" },
  { rel_id: 17, parent_id: 8, ordinal: 0, local_name: "__delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "98c79bf806b4aa2b", h_schema: "c3c162726d846de8", h_rule: "" },
  { rel_id: 18, parent_id: 8, ordinal: 0, local_name: "__frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "7f0421ee92c18db1", h_schema: "7c100606d2d03cc3", h_rule: "" },
  { rel_id: 19, parent_id: 8, ordinal: 0, local_name: "__next_frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "264acc320b8ef01f", h_schema: "7c100606d2d03cc3", h_rule: "" },
  { rel_id: 20, parent_id: 8, ordinal: 0, local_name: "__txt_range_join_over_arithmetic_eprintln_hit_83ebe90615c9", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "3c7d867653419353", h_schema: "eb1c51547565abe6", h_rule: "" },
  { rel_id: 21, parent_id: 17, ordinal: 0, local_name: "__txt___delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "ab27a3768b14e2e5", h_schema: "eb1c51547565abe6", h_rule: "" },
  { rel_id: 22, parent_id: 11, ordinal: 0, local_name: "__delta_range_join_over_arithmetic_eprintln_waived", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "7a1e312399f6b79d", h_schema: "c3c162726d846de8", h_rule: "" },
  { rel_id: 23, parent_id: 11, ordinal: 0, local_name: "__frontier_range_join_over_arithmetic_eprintln_waived", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "5288cc056f7d1cad", h_schema: "7c100606d2d03cc3", h_rule: "" },
  { rel_id: 24, parent_id: 11, ordinal: 0, local_name: "__next_frontier_range_join_over_arithmetic_eprintln_waived", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "5e0e21b2ed69d833", h_schema: "7c100606d2d03cc3", h_rule: "" },
  { rel_id: 25, parent_id: 11, ordinal: 0, local_name: "__txt_range_join_over_arithmetic_eprintln_waived", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "db4d523fd739c7df", h_schema: "eb1c51547565abe6", h_rule: "" },
  { rel_id: 26, parent_id: 22, ordinal: 0, local_name: "__txt___delta_range_join_over_arithmetic_eprintln_waived", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "90b3113218baed31", h_schema: "eb1c51547565abe6", h_rule: "" },
  { rel_id: 27, parent_id: 14, ordinal: 0, local_name: "__delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "adb34fa28420e794", h_schema: "f188ca882914d4e0", h_rule: "" },
  { rel_id: 28, parent_id: 14, ordinal: 0, local_name: "__frontier_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "76978befe0a0ca67", h_schema: "e879026b8a3fc90f", h_rule: "" },
  { rel_id: 29, parent_id: 14, ordinal: 0, local_name: "__next_frontier_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "b7146fb29b04a521", h_schema: "e879026b8a3fc90f", h_rule: "" },
  { rel_id: 30, parent_id: 14, ordinal: 0, local_name: "__txt_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "270bd3d710bb0a87", h_schema: "8331cc464dc49f2a", h_rule: "" },
  { rel_id: 31, parent_id: 27, ordinal: 0, local_name: "__txt___delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "1a6a37506a7b1cd3", h_schema: "8331cc464dc49f2a", h_rule: "" },
  { rel_id: 32, parent_id: 7, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 7, h_id: "82dec9d8441941c2", h_schema: "", h_rule: "" },
  { rel_id: 33, parent_id: 11, ordinal: 0, local_name: "__support_next_range_join_over_arithmetic_eprintln_waived", kind: "refcount", type_id: 0, arity: 3, module_id: 7, h_id: "3d1168ae7d8bca5e", h_schema: "", h_rule: "" },
  { rel_id: 34, parent_id: 11, ordinal: 0, local_name: "__new_range_join_over_arithmetic_eprintln_waived", kind: "refcount_staging", type_id: 0, arity: 3, module_id: 7, h_id: "6e1d49e9aa8dedb3", h_schema: "", h_rule: "" },
  { rel_id: 35, parent_id: 9, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "717345dc9dd7122e", h_schema: "", h_rule: "" },
  { rel_id: 36, parent_id: 10, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "c85b9dec417c04d4", h_schema: "", h_rule: "" },
  { rel_id: 37, parent_id: 12, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "283985a1bf2b88be", h_schema: "", h_rule: "" },
  { rel_id: 38, parent_id: 13, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "5f3735492f758101", h_schema: "", h_rule: "" },
  { rel_id: 39, parent_id: 15, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "25a3836b49c9d38e", h_schema: "", h_rule: "" },
  { rel_id: 40, parent_id: 16, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "cb4bd04ff8d27755", h_schema: "", h_rule: "" },
);

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["eprintln_hit", "eprintln_waiver_line"];

const boot: readonly IBootStatement[] = [
  { rel: "eprintln_hit", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["src/db.rs"] },
  { rel: "eprintln_hit", sql: `INSERT OR IGNORE INTO "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("path", "line_number") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["src/db.rs", 40] },
  { rel: "eprintln_hit", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["src/db.rs"] },
  { rel: "eprintln_hit", sql: `INSERT OR IGNORE INTO "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("path", "line_number") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["src/db.rs", 88] },
  { rel: "eprintln_waiver_line", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["src/db.rs"] },
  { rel: "eprintln_waiver_line", sql: `INSERT OR IGNORE INTO "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" ("path", "waiver_line") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["src/db.rs", 39] },
  { rel: "eprintln_waived", sql: `DELETE FROM "range_join_over_arithmetic_eprintln_waived"`, params: [] },
  { rel: "eprintln_waived", sql: `INSERT OR IGNORE INTO "range_join_over_arithmetic_eprintln_waived" ("path", "line_number") SELECT b0."path", b1."line_number" FROM "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" b0, "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" b1 WHERE b1."path" = b0."path" AND (b0."waiver_line" >= (b1."line_number" - 1)) AND (b0."waiver_line" <= b1."line_number")`, params: [] },
];

const final_select: Record<string, string> = {
  eprintln_hit: `SELECT CASE WHEN json_valid(t."path") AND json_type(t."path") = 'object' AND json_type(t."path", '$.fn') = 'text' AND json_type(t."path", '$.args') = 'array' THEN json_extract(t."path", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."path", '$.args')), '') || ')' ELSE t."path" END AS "path", t."line_number" FROM "__txt_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" t`,
  eprintln_waived: `SELECT CASE WHEN json_valid(t."path") AND json_type(t."path") = 'object' AND json_type(t."path", '$.fn') = 'text' AND json_type(t."path", '$.args') = 'array' THEN json_extract(t."path", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."path", '$.args')), '') || ')' ELSE t."path" END AS "path", t."line_number" FROM "__txt_range_join_over_arithmetic_eprintln_waived" t`,
  eprintln_waiver_line: `SELECT CASE WHEN json_valid(t."path") AND json_type(t."path") = 'object' AND json_type(t."path", '$.fn') = 'text' AND json_type(t."path", '$.args') = 'array' THEN json_extract(t."path", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."path", '$.args')), '') || ')' ELSE t."path" END AS "path", t."waiver_line" FROM "__txt_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "eprintln_hit", kind: "set", table_name: "range_join_over_arithmetic_eprintln_hit_83ebe90615c9", delta_table_name: "__delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9", frontier_table_name: "__frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9", next_frontier_table_name: "__next_frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9", columns: ["path", "line_number"], column_types: ["text", "int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" ("path", "line_number") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "path", "line_number"`, arrival_del_sql: `DELETE FROM "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" WHERE ("path", "line_number") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "path", "line_number"`, boundary_sql: `SELECT CASE WHEN json_valid(t."path") AND json_type(t."path") = 'object' AND json_type(t."path", '$.fn') = 'text' AND json_type(t."path", '$.args') = 'array' THEN json_extract(t."path", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."path", '$.args')), '') || ')' ELSE t."path" END AS "path", t."line_number", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" t WHERE t."_sign" IN (-1, 1) GROUP BY t."path", t."line_number", t."_sign"`, rule_observers: ["eprintln_waived/2"] },
  { rel: "eprintln_waived", kind: "set", table_name: "range_join_over_arithmetic_eprintln_waived", delta_table_name: "__delta_range_join_over_arithmetic_eprintln_waived", frontier_table_name: "__frontier_range_join_over_arithmetic_eprintln_waived", next_frontier_table_name: "__next_frontier_range_join_over_arithmetic_eprintln_waived", columns: ["path", "line_number"], column_types: ["text", "int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."path") AND json_type(t."path") = 'object' AND json_type(t."path", '$.fn') = 'text' AND json_type(t."path", '$.args') = 'array' THEN json_extract(t."path", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."path", '$.args')), '') || ')' ELSE t."path" END AS "path", t."line_number", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_range_join_over_arithmetic_eprintln_waived" t WHERE t."_sign" IN (-1, 1) GROUP BY t."path", t."line_number", t."_sign"`, rule_observers: [] },
  { rel: "eprintln_waiver_line", kind: "set", table_name: "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff", delta_table_name: "__delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff", frontier_table_name: "__frontier_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff", next_frontier_table_name: "__next_frontier_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff", columns: ["path", "waiver_line"], column_types: ["text", "int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" ("path", "waiver_line") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "path", "waiver_line"`, arrival_del_sql: `DELETE FROM "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" WHERE ("path", "waiver_line") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "path", "waiver_line"`, boundary_sql: `SELECT CASE WHEN json_valid(t."path") AND json_type(t."path") = 'object' AND json_type(t."path", '$.fn') = 'text' AND json_type(t."path", '$.args') = 'array' THEN json_extract(t."path", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."path", '$.args')), '') || ')' ELSE t."path" END AS "path", t."waiver_line", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" t WHERE t."_sign" IN (-1, 1) GROUP BY t."path", t."waiver_line", t."_sign"`, rule_observers: ["eprintln_waived/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "eprintln_waived", rule_id: "range_join_over_arithmetic:eprintln_waived/2#1", head_delta_table_name: "__delta_range_join_over_arithmetic_eprintln_waived", head_columns: ["path", "line_number"], insert_sql: `INSERT OR IGNORE INTO "range_join_over_arithmetic_eprintln_waived" ("path", "line_number") SELECT DISTINCT d0."path", b0."line_number" FROM "__frontier_range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" d0, (SELECT old_row."path", old_row."line_number" FROM "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" old_row GROUP BY old_row."path", old_row."line_number" HAVING count(*) > (SELECT count(*) FROM "__frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" old_delta WHERE old_delta."_phase" >= 0 AND old_delta."path" = old_row."path" AND old_delta."line_number" = old_row."line_number")) b0 WHERE d0."_phase" >= 0 AND b0."path" = d0."path" AND (d0."waiver_line" >= (b0."line_number" - 1)) AND (d0."waiver_line" <= b0."line_number") UNION ALL SELECT DISTINCT d0."path", d0."line_number" FROM "__frontier_range_join_over_arithmetic_eprintln_hit_83ebe90615c9" d0, "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" b0 WHERE d0."_phase" >= 0 AND b0."path" = d0."path" AND (b0."waiver_line" >= (d0."line_number" - 1)) AND (b0."waiver_line" <= d0."line_number") RETURNING "path", "line_number"`, select_sql: `SELECT "path", "line_number" FROM "range_join_over_arithmetic_eprintln_waived"`, recompute_sql: `DELETE FROM "range_join_over_arithmetic_eprintln_waived";
INSERT OR IGNORE INTO "range_join_over_arithmetic_eprintln_waived" ("path", "line_number") SELECT b0."path", b1."line_number" FROM "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" b0, "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" b1 WHERE b1."path" = b0."path" AND (b0."waiver_line" >= (b1."line_number" - 1)) AND (b0."waiver_line" <= b1."line_number")`, support_sql: [`DELETE FROM "__support_next_range_join_over_arithmetic_eprintln_waived"`, `INSERT INTO "__support_next_range_join_over_arithmetic_eprintln_waived" ("path", "line_number", "__refcount") SELECT "path", "line_number", sum("__refcount") FROM (SELECT b0."path" AS "path", b1."line_number" AS "line_number", count(*) AS "__refcount" FROM "range_join_over_arithmetic_eprintln_waiver_line_e6900d411eff" b0, "range_join_over_arithmetic_eprintln_hit_83ebe90615c9" b1 WHERE b1."path" = b0."path" AND (b0."waiver_line" >= (b1."line_number" - 1)) AND (b0."waiver_line" <= b1."line_number") GROUP BY b0."path", b1."line_number") GROUP BY "path", "line_number"`, `UPDATE "range_join_over_arithmetic_eprintln_waived" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_range_join_over_arithmetic_eprintln_waived" n WHERE n."path" = h."path" AND n."line_number" = h."line_number"), 0)`, `INSERT INTO "__delta_range_join_over_arithmetic_eprintln_waived" ("_sign", "_sequence", "path", "line_number") SELECT -1, row_number() OVER () - 1, "path", "line_number" FROM "range_join_over_arithmetic_eprintln_waived" WHERE "__refcount" <= 0`, `DELETE FROM "range_join_over_arithmetic_eprintln_waived" WHERE "__refcount" <= 0`, `DELETE FROM "__new_range_join_over_arithmetic_eprintln_waived"`, `INSERT INTO "__new_range_join_over_arithmetic_eprintln_waived" ("path", "line_number", "__refcount") SELECT n."path", n."line_number", n."__refcount" FROM "__support_next_range_join_over_arithmetic_eprintln_waived" n LEFT JOIN "range_join_over_arithmetic_eprintln_waived" h ON n."path" = h."path" AND n."line_number" = h."line_number" WHERE h."path" IS NULL`, `INSERT INTO "__delta_range_join_over_arithmetic_eprintln_waived" ("_sign", "_sequence", "path", "line_number") SELECT 1, "rowid" - 1, "path", "line_number" FROM "__new_range_join_over_arithmetic_eprintln_waived"`, `INSERT INTO "__frontier_range_join_over_arithmetic_eprintln_waived" ("_phase", "_sequence", "path", "line_number") SELECT ?, "rowid" - 1, "path", "line_number" FROM "__new_range_join_over_arithmetic_eprintln_waived"`, `INSERT INTO "__next_frontier_range_join_over_arithmetic_eprintln_waived" ("_phase", "_sequence", "path", "line_number") SELECT ?, "rowid" - 1, "path", "line_number" FROM "__new_range_join_over_arithmetic_eprintln_waived"`, `INSERT OR IGNORE INTO "range_join_over_arithmetic_eprintln_waived" ("path", "line_number", "__refcount") SELECT n."path", n."line_number", n."__refcount" FROM "__support_next_range_join_over_arithmetic_eprintln_waived" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      concatMap((carry_pending) => EnumPlane.decode_deltas(seam, ENUM_TYPES, ENUM_REF_COLUMNS, SUBSCRIBED_RELATIONS, rels).pipe(
        map((decoded): ITickDeltas => ({ rels: decoded, carry_pending })),
      )),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return EnumPlane.intern(seam, ENUM_TYPES, ENUM_REF_COLUMNS, arrivals).pipe(
    concatMap((normalized) => run_incremental_tick(seam, validate_arrivals(normalized))),
  );
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "range_join_over_arithmetic",
  ir_version: 1,
  internMode: "dict",
  ddl,
  rel_columns,
  rel_physical_names,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  enum_types: ENUM_TYPES,
  enum_ref_columns: ENUM_REF_COLUMNS,
  unsupported_execution,
  tick: run_tick,
};
