// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: conjunction_level_control_for_combine.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { EnumPlane } from "../runtime/enumPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IEnumRefColumns,
  IEnumTypePlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly ir_version: number; readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly rel_physical_names: Record<string, string>; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const ENUM_TYPES: readonly IEnumTypePlan[] = [];
export const ENUM_REF_COLUMNS: IEnumRefColumns = {};

const ddl: readonly string[] = [
  `CREATE TABLE "conjunction_level_control_for_combine_pair" ("__id" INTEGER PRIMARY KEY, "left" INTEGER NOT NULL, "right" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("left", "right"))`,
  `CREATE TABLE "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" ("__id" INTEGER PRIMARY KEY, "left" INTEGER NOT NULL, UNIQUE ("left"))`,
  `CREATE TABLE "conjunction_level_control_for_combine_source_b_3c9cd1d22be4" ("__id" INTEGER PRIMARY KEY, "right" INTEGER NOT NULL, UNIQUE ("right"))`,
  `CREATE TEMP TABLE "__delta_conjunction_level_control_for_combine_pair" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_conjunction_level_control_for_combine_pair_sign" ON "__delta_conjunction_level_control_for_combine_pair" ("_sign")`,
  `CREATE INDEX "__delta_conjunction_level_control_for_combine_pair_group" ON "__delta_conjunction_level_control_for_combine_pair" ("left", "right")`,
  `CREATE TEMP TABLE "__frontier_conjunction_level_control_for_combine_pair" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_conjunction_level_control_for_combine_pair_phase" ON "__frontier_conjunction_level_control_for_combine_pair" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_conjunction_level_control_for_combine_pair" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec_sign" ON "__delta_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" ("_sign")`,
  `CREATE INDEX "__delta_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec_group" ON "__delta_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" ("left")`,
  `CREATE TEMP TABLE "__frontier_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec_phase" ON "__frontier_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "left" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__delta_conjunction_level_control_for_combine_source_b_3c9cd1d22be4" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_conjunction_level_control_for_combine_source_b_3c9cd1d22be4_sign" ON "__delta_conjunction_level_control_for_combine_source_b_3c9cd1d22be4" ("_sign")`,
  `CREATE INDEX "__delta_conjunction_level_control_for_combine_source_b_3c9cd1d22be4_group" ON "__delta_conjunction_level_control_for_combine_source_b_3c9cd1d22be4" ("right")`,
  `CREATE TEMP TABLE "__frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4_phase" ON "__frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "right" INTEGER NOT NULL)`,
  `CREATE TEMP TABLE "__support_next_conjunction_level_control_for_combine_pair" ("left" INTEGER NOT NULL, "right" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("left", "right")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_conjunction_level_control_for_combine_pair" ("left" INTEGER NOT NULL, "right" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "conjunction_level_control_for_combine_pair_zero" ON "conjunction_level_control_for_combine_pair" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  pair: ["left", "right"],
  source_a: ["left"],
  source_b: ["right"],
};

const rel_physical_names: Record<string, string> = {
  pair: "conjunction_level_control_for_combine_pair",
  source_a: "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec",
  source_b: "conjunction_level_control_for_combine_source_b_3c9cd1d22be4",
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  pair: ["int", "int"],
  source_a: ["int"],
  source_b: ["int"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  pair: ["int", "int"],
  source_a: ["int"],
  source_b: ["int"],
};

const rel_catalog: readonly IRelCatalogRow[] = new Array<IRelCatalogRow>(
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "conjunction_level_control_for_combine", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "ee5c687d21753259", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "pair", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "4d09b014074f2f72", h_schema: "a3c2241561bccbe3", h_rule: "96f309d2a91468cc" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "left", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "a52e1e3ea6a2e73c", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 8, ordinal: 2, local_name: "right", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "4b021d204406e2a6", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 7, ordinal: 0, local_name: "source_a", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "f110e00f3d0900ea", h_schema: "8e75f4ab37008087", h_rule: "" },
  { rel_id: 12, parent_id: 11, ordinal: 1, local_name: "left", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "e9bef10d9ef4b407", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 7, ordinal: 0, local_name: "source_b", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "1500b85590fc7c72", h_schema: "df1f2b9efcfed35f", h_rule: "" },
  { rel_id: 14, parent_id: 13, ordinal: 1, local_name: "right", kind: "column", type_id: 2, arity: 0, module_id: 7, h_id: "c4ea054b19e10b47", h_schema: "", h_rule: "" },
  { rel_id: 15, parent_id: 8, ordinal: 0, local_name: "__delta_conjunction_level_control_for_combine_pair", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "c87bcb15ccfdb146", h_schema: "691d2aac47379567", h_rule: "" },
  { rel_id: 16, parent_id: 8, ordinal: 0, local_name: "__frontier_conjunction_level_control_for_combine_pair", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "f599a62aecff9aac", h_schema: "e317bdafb05c6ac6", h_rule: "" },
  { rel_id: 17, parent_id: 8, ordinal: 0, local_name: "__next_frontier_conjunction_level_control_for_combine_pair", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "153355ec349a7b0d", h_schema: "e317bdafb05c6ac6", h_rule: "" },
  { rel_id: 18, parent_id: 11, ordinal: 0, local_name: "__delta_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "cea220ff46a5eaae", h_schema: "d3d9e0ceac75549f", h_rule: "" },
  { rel_id: 19, parent_id: 11, ordinal: 0, local_name: "__frontier_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "16a8246ccaa4a49a", h_schema: "bbac68972c4eee12", h_rule: "" },
  { rel_id: 20, parent_id: 11, ordinal: 0, local_name: "__next_frontier_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "aaf0fbc8e0d5a896", h_schema: "bbac68972c4eee12", h_rule: "" },
  { rel_id: 21, parent_id: 13, ordinal: 0, local_name: "__delta_conjunction_level_control_for_combine_source_b_3c9cd1d22be4", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "c352f572039e32c0", h_schema: "ab858f006835a437", h_rule: "" },
  { rel_id: 22, parent_id: 13, ordinal: 0, local_name: "__frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "45e0fdb353ed01ae", h_schema: "dfb634b09e13e15b", h_rule: "" },
  { rel_id: 23, parent_id: 13, ordinal: 0, local_name: "__next_frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "f374d4173ffef943", h_schema: "dfb634b09e13e15b", h_rule: "" },
  { rel_id: 24, parent_id: 8, ordinal: 0, local_name: "__support_next_conjunction_level_control_for_combine_pair", kind: "refcount", type_id: 0, arity: 3, module_id: 7, h_id: "6e043c405ce92e53", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 8, ordinal: 0, local_name: "__new_conjunction_level_control_for_combine_pair", kind: "refcount_staging", type_id: 0, arity: 3, module_id: 7, h_id: "0cf4bfc7588e6314", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 9, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "a5ce12d9afb1e798", h_schema: "", h_rule: "" },
  { rel_id: 27, parent_id: 10, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "7b9de0d6e426a035", h_schema: "", h_rule: "" },
  { rel_id: 28, parent_id: 12, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "fc079e6d48138940", h_schema: "", h_rule: "" },
  { rel_id: 29, parent_id: 14, ordinal: 1, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "6e43f5b2ffe256e3", h_schema: "", h_rule: "" },
);

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["source_a", "source_b"];

const boot: readonly IBootStatement[] = [
  { rel: "pair", sql: `DELETE FROM "conjunction_level_control_for_combine_pair"`, params: [] },
  { rel: "pair", sql: `INSERT OR IGNORE INTO "conjunction_level_control_for_combine_pair" ("left", "right") SELECT b0."left", b1."right" FROM "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" b0, "conjunction_level_control_for_combine_source_b_3c9cd1d22be4" b1`, params: [] },
];

const final_select: Record<string, string> = {
  pair: `SELECT t."left", t."right" FROM "conjunction_level_control_for_combine_pair" t`,
  source_a: `SELECT t."left" FROM "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" t`,
  source_b: `SELECT t."right" FROM "conjunction_level_control_for_combine_source_b_3c9cd1d22be4" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "pair", kind: "set", table_name: "conjunction_level_control_for_combine_pair", delta_table_name: "__delta_conjunction_level_control_for_combine_pair", frontier_table_name: "__frontier_conjunction_level_control_for_combine_pair", next_frontier_table_name: "__next_frontier_conjunction_level_control_for_combine_pair", columns: ["left", "right"], column_types: ["int", "int"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT t."left", t."right", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_conjunction_level_control_for_combine_pair" t WHERE t."_sign" IN (-1, 1) GROUP BY t."left", t."right", t."_sign"`, rule_observers: [] },
  { rel: "source_a", kind: "set", table_name: "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec", delta_table_name: "__delta_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec", frontier_table_name: "__frontier_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec", next_frontier_table_name: "__next_frontier_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec", columns: ["left"], column_types: ["int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" ("left") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "left"`, arrival_del_sql: `DELETE FROM "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" WHERE ("left") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "left"`, boundary_sql: `SELECT t."left", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" t WHERE t."_sign" IN (-1, 1) GROUP BY t."left", t."_sign"`, rule_observers: ["pair/2"] },
  { rel: "source_b", kind: "set", table_name: "conjunction_level_control_for_combine_source_b_3c9cd1d22be4", delta_table_name: "__delta_conjunction_level_control_for_combine_source_b_3c9cd1d22be4", frontier_table_name: "__frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4", next_frontier_table_name: "__next_frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4", columns: ["right"], column_types: ["int"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "conjunction_level_control_for_combine_source_b_3c9cd1d22be4" ("right") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "right"`, arrival_del_sql: `DELETE FROM "conjunction_level_control_for_combine_source_b_3c9cd1d22be4" WHERE ("right") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "right"`, boundary_sql: `SELECT t."right", t."_sign" AS "__sign", count(*) AS "__count" FROM "__delta_conjunction_level_control_for_combine_source_b_3c9cd1d22be4" t WHERE t."_sign" IN (-1, 1) GROUP BY t."right", t."_sign"`, rule_observers: ["pair/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "pair", rule_id: "conjunction_level_control_for_combine:pair/2#1", head_delta_table_name: "__delta_conjunction_level_control_for_combine_pair", head_columns: ["left", "right"], insert_sql: `INSERT OR IGNORE INTO "conjunction_level_control_for_combine_pair" ("left", "right") SELECT DISTINCT d0."left", b0."right" FROM "__frontier_conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" d0, (SELECT old_row."right" FROM "conjunction_level_control_for_combine_source_b_3c9cd1d22be4" old_row GROUP BY old_row."right" HAVING count(*) > (SELECT count(*) FROM "__frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4" old_delta WHERE old_delta."_phase" >= 0 AND old_delta."right" = old_row."right")) b0 WHERE d0."_phase" >= 0 UNION ALL SELECT DISTINCT b0."left", d0."right" FROM "__frontier_conjunction_level_control_for_combine_source_b_3c9cd1d22be4" d0, "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" b0 WHERE d0."_phase" >= 0 RETURNING "left", "right"`, select_sql: `SELECT "left", "right" FROM "conjunction_level_control_for_combine_pair"`, recompute_sql: `DELETE FROM "conjunction_level_control_for_combine_pair";
INSERT OR IGNORE INTO "conjunction_level_control_for_combine_pair" ("left", "right") SELECT b0."left", b1."right" FROM "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" b0, "conjunction_level_control_for_combine_source_b_3c9cd1d22be4" b1`, support_sql: [`DELETE FROM "__support_next_conjunction_level_control_for_combine_pair"`, `INSERT INTO "__support_next_conjunction_level_control_for_combine_pair" ("left", "right", "__refcount") SELECT "left", "right", sum("__refcount") FROM (SELECT b0."left" AS "left", b1."right" AS "right", count(*) AS "__refcount" FROM "conjunction_level_control_for_combine_source_a_cd92ed9ae6ec" b0, "conjunction_level_control_for_combine_source_b_3c9cd1d22be4" b1 GROUP BY b0."left", b1."right") GROUP BY "left", "right"`, `UPDATE "conjunction_level_control_for_combine_pair" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_conjunction_level_control_for_combine_pair" n WHERE n."left" = h."left" AND n."right" = h."right"), 0)`, `INSERT INTO "__delta_conjunction_level_control_for_combine_pair" ("_sign", "_sequence", "left", "right") SELECT -1, row_number() OVER () - 1, "left", "right" FROM "conjunction_level_control_for_combine_pair" WHERE "__refcount" <= 0`, `DELETE FROM "conjunction_level_control_for_combine_pair" WHERE "__refcount" <= 0`, `DELETE FROM "__new_conjunction_level_control_for_combine_pair"`, `INSERT INTO "__new_conjunction_level_control_for_combine_pair" ("left", "right", "__refcount") SELECT n."left", n."right", n."__refcount" FROM "__support_next_conjunction_level_control_for_combine_pair" n LEFT JOIN "conjunction_level_control_for_combine_pair" h ON n."left" = h."left" AND n."right" = h."right" WHERE h."left" IS NULL`, `INSERT INTO "__delta_conjunction_level_control_for_combine_pair" ("_sign", "_sequence", "left", "right") SELECT 1, "rowid" - 1, "left", "right" FROM "__new_conjunction_level_control_for_combine_pair"`, `INSERT INTO "__frontier_conjunction_level_control_for_combine_pair" ("_phase", "_sequence", "left", "right") SELECT ?, "rowid" - 1, "left", "right" FROM "__new_conjunction_level_control_for_combine_pair"`, `INSERT INTO "__next_frontier_conjunction_level_control_for_combine_pair" ("_phase", "_sequence", "left", "right") SELECT ?, "rowid" - 1, "left", "right" FROM "__new_conjunction_level_control_for_combine_pair"`, `INSERT OR IGNORE INTO "conjunction_level_control_for_combine_pair" ("left", "right", "__refcount") SELECT n."left", n."right", n."__refcount" FROM "__support_next_conjunction_level_control_for_combine_pair" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      concatMap((carry_pending) => EnumPlane.decode_deltas(seam, ENUM_TYPES, ENUM_REF_COLUMNS, SUBSCRIBED_RELATIONS, rels).pipe(
        map((decoded): ITickDeltas => ({ rels: decoded, carry_pending })),
      )),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return EnumPlane.intern(seam, ENUM_TYPES, ENUM_REF_COLUMNS, arrivals).pipe(
    concatMap((normalized) => run_incremental_tick(seam, validate_arrivals(normalized))),
  );
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "conjunction_level_control_for_combine",
  ir_version: 1,
  internMode: "dict",
  ddl,
  rel_columns,
  rel_physical_names,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  enum_types: ENUM_TYPES,
  enum_ref_columns: ENUM_REF_COLUMNS,
  unsupported_execution,
  tick: run_tick,
};
