// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: ordered_group_concat_explicit_comma_matches_one_argument.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation. The snapshot path remains selectable with
// SPREFA_TSV2_EMITTER_MODE=naive as a byte-identity referee.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    throw new Error("a list value reached a SQL parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "explicit_joined": [true, true],
    "item": [true, true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "explicit_joined" ("__id" INTEGER PRIMARY KEY, "group" INTEGER NOT NULL, "col2" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("group", "col2"))`,
  `CREATE TEMP VIEW "__txt_explicit_joined" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."group") AS "group", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."col2") AS "col2", t."__refcount" AS "__refcount" FROM "explicit_joined" t`,
  `CREATE TABLE "item" ("__id" INTEGER PRIMARY KEY, "group" INTEGER NOT NULL, "value" INTEGER NOT NULL, UNIQUE ("group", "value"))`,
  `CREATE TEMP VIEW "__txt_item" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."group") AS "group", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value" FROM "item" t`,
  `CREATE TEMP TABLE "__delta_explicit_joined" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "col2" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_explicit_joined_sign" ON "__delta_explicit_joined" ("_sign")`,
  `CREATE INDEX "__delta_explicit_joined_group" ON "__delta_explicit_joined" ("group", "col2")`,
  `CREATE TEMP TABLE "__frontier_explicit_joined" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "col2" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_explicit_joined_phase" ON "__frontier_explicit_joined" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_explicit_joined" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "col2" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_explicit_joined" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."group") AS "group", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."col2") AS "col2", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_explicit_joined" t`,
  `CREATE TEMP TABLE "__delta_item" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_item_sign" ON "__delta_item" ("_sign")`,
  `CREATE INDEX "__delta_item_group" ON "__delta_item" ("group", "value")`,
  `CREATE TEMP TABLE "__frontier_item" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_item_phase" ON "__frontier_item" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_item" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "group" INTEGER NOT NULL, "value" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_item" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."group") AS "group", (SELECT s."content" FROM "__str" s WHERE s."__id" = t."value") AS "value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_item" t`,
  `CREATE TEMP TABLE "__agg_scope_explicit_joined" ("group" INTEGER NOT NULL, PRIMARY KEY ("group")) WITHOUT ROWID`,
];

const rel_columns: Record<string, readonly string[]> = {
  explicit_joined: ["group", "col2"],
  item: ["group", "value"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  explicit_joined: ["text", "text"],
  item: ["text", "text"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "ordered_group_concat_explicit_comma_matches_one_argument", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "2b3f1cc7ebcbd8fd", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "explicit_joined", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "d5abd3b730a221a3", h_schema: "c6d4da8e821f81d5", h_rule: "8fc9fceeab2fb51a" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "group", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "65a930e0c0c32ab6", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 7, ordinal: 2, local_name: "col2", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "e555390ad3cd119b", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 6, ordinal: 0, local_name: "item", kind: "rel", type_id: 0, arity: 2, module_id: 6, h_id: "b3e4a2959389d2b8", h_schema: "3e6b75cd8c69c463", h_rule: "" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "group", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "a879c8760e9a85ca", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 10, ordinal: 2, local_name: "value", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "34444485f1c7f0f2", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 7, ordinal: 0, local_name: "__delta_explicit_joined", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "d3136cb4945a8b42", h_schema: "5ac8161a56c5926e", h_rule: "" },
  { rel_id: 14, parent_id: 7, ordinal: 0, local_name: "__frontier_explicit_joined", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "2bc3945bc63166b1", h_schema: "6614be3fef246678", h_rule: "" },
  { rel_id: 15, parent_id: 7, ordinal: 0, local_name: "__next_frontier_explicit_joined", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "02a20833917ffd40", h_schema: "6614be3fef246678", h_rule: "" },
  { rel_id: 16, parent_id: 7, ordinal: 0, local_name: "__txt_explicit_joined", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "b317d4072667fe02", h_schema: "c6d4da8e821f81d5", h_rule: "" },
  { rel_id: 17, parent_id: 13, ordinal: 0, local_name: "__txt___delta_explicit_joined", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "bce182d7ef6f9bb8", h_schema: "c6d4da8e821f81d5", h_rule: "" },
  { rel_id: 18, parent_id: 10, ordinal: 0, local_name: "__delta_item", kind: "delta", type_id: 0, arity: 4, module_id: 6, h_id: "50db0210859fdc4b", h_schema: "2096717cef865b75", h_rule: "" },
  { rel_id: 19, parent_id: 10, ordinal: 0, local_name: "__frontier_item", kind: "frontier", type_id: 0, arity: 4, module_id: 6, h_id: "300403d0914c73a7", h_schema: "11544203a3231836", h_rule: "" },
  { rel_id: 20, parent_id: 10, ordinal: 0, local_name: "__next_frontier_item", kind: "next_frontier", type_id: 0, arity: 4, module_id: 6, h_id: "0d9bc90ffcb8121d", h_schema: "11544203a3231836", h_rule: "" },
  { rel_id: 21, parent_id: 10, ordinal: 0, local_name: "__txt_item", kind: "view", type_id: 0, arity: 2, module_id: 6, h_id: "8e8b8c06a5b5b228", h_schema: "3e6b75cd8c69c463", h_rule: "" },
  { rel_id: 22, parent_id: 18, ordinal: 0, local_name: "__txt___delta_item", kind: "view", type_id: 0, arity: 4, module_id: 6, h_id: "e179104769d94816", h_schema: "3e6b75cd8c69c463", h_rule: "" },
  { rel_id: 23, parent_id: 6, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 6, h_id: "e78aae94b0ec8de8", h_schema: "", h_rule: "" },
  { rel_id: 24, parent_id: 7, ordinal: 0, local_name: "__agg_scope_explicit_joined", kind: "scope", type_id: 0, arity: 1, module_id: 6, h_id: "27b2785158d35952", h_schema: "af8b07b10d8881d5", h_rule: "" },
  { rel_id: 25, parent_id: 8, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "c9aaddac77bd230d", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 9, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "143d94f285d00c03", h_schema: "", h_rule: "" },
  { rel_id: 27, parent_id: 11, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "aec0157c20d84d99", h_schema: "", h_rule: "" },
  { rel_id: 28, parent_id: 12, ordinal: 2, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "159c8059d8acd708", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["item"];

const boot: readonly IBootStatement[] = [
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["north"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["pear"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "item" ("group", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), (SELECT "__id" FROM "__str" WHERE "content" = ?))`, params: ["north", "pear"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["north"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["orange"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "item" ("group", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), (SELECT "__id" FROM "__str" WHERE "content" = ?))`, params: ["north", "orange"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["north"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["apple"] },
  { rel: "item", sql: `INSERT OR IGNORE INTO "item" ("group", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), (SELECT "__id" FROM "__str" WHERE "content" = ?))`, params: ["north", "apple"] },
  { rel: "explicit_joined", sql: `DELETE FROM "explicit_joined"`, params: [] },
  { rel: "explicit_joined", sql: `INSERT OR IGNORE INTO "__str" ("content") SELECT group_concat((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value"), ',' ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value")) FROM "item" b0 GROUP BY b0."group" HAVING count(*) > 0`, params: [] },
  { rel: "explicit_joined", sql: `INSERT OR IGNORE INTO "explicit_joined" ("group", "col2") SELECT "__agg_1", (SELECT s."__id" FROM "__str" s WHERE s."content" = "__agg_2") FROM (SELECT b0."group" AS "__agg_1", group_concat((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value"), ',' ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value")) AS "__agg_2" FROM "item" b0 GROUP BY b0."group" HAVING count(*) > 0)`, params: [] },
];

type Snapshot = {
  readonly explicit_joined: readonly IRow[];
  readonly item: readonly IRow[];
};

function read_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    explicit_joined: select_rows(seam, `SELECT CASE WHEN json_valid(t."group") AND json_type(t."group") = 'object' AND json_type(t."group", '$.fn') = 'text' AND json_type(t."group", '$.args') = 'array' THEN json_extract(t."group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."group", '$.args')), '') || ')' ELSE t."group" END AS "group", CASE WHEN json_valid(t."col2") AND json_type(t."col2") = 'object' AND json_type(t."col2", '$.fn') = 'text' AND json_type(t."col2", '$.args') = 'array' THEN json_extract(t."col2", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."col2", '$.args')), '') || ')' ELSE t."col2" END AS "col2" FROM "__txt_explicit_joined" t`, rel_columns.explicit_joined!, rel_column_types.explicit_joined!),
    item: select_rows(seam, `SELECT CASE WHEN json_valid(t."group") AND json_type(t."group") = 'object' AND json_type(t."group", '$.fn') = 'text' AND json_type(t."group", '$.args') = 'array' THEN json_extract(t."group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."group", '$.args')), '') || ')' ELSE t."group" END AS "group", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value" FROM "__txt_item" t`, rel_columns.item!, rel_column_types.item!),
  });
}

type Snapshots = { readonly decoded: Snapshot; readonly stored: Snapshot };

function read_stored_snapshot(seam: ISqlSeam): Observable<Snapshot> {
  return forkJoin({
    explicit_joined: select_rows(seam, `SELECT "group", "col2" FROM "explicit_joined"`, rel_columns.explicit_joined!, rel_column_types.explicit_joined!),
    item: select_rows(seam, `SELECT "group", "value" FROM "item"`, rel_columns.item!, rel_column_types.item!),
  });
}

function read_snapshots(seam: ISqlSeam): Observable<Snapshots> {
  return forkJoin({ decoded: read_snapshot(seam), stored: read_stored_snapshot(seam) });
}

const final_select: Record<string, string> = {
  explicit_joined: `SELECT CASE WHEN json_valid(t."group") AND json_type(t."group") = 'object' AND json_type(t."group", '$.fn') = 'text' AND json_type(t."group", '$.args') = 'array' THEN json_extract(t."group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."group", '$.args')), '') || ')' ELSE t."group" END AS "group", CASE WHEN json_valid(t."col2") AND json_type(t."col2") = 'object' AND json_type(t."col2", '$.fn') = 'text' AND json_type(t."col2", '$.args') = 'array' THEN json_extract(t."col2", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."col2", '$.args')), '') || ')' ELSE t."col2" END AS "col2" FROM "__txt_explicit_joined" t`,
  item: `SELECT CASE WHEN json_valid(t."group") AND json_type(t."group") = 'object' AND json_type(t."group", '$.fn') = 'text' AND json_type(t."group", '$.args') = 'array' THEN json_extract(t."group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."group", '$.args')), '') || ')' ELSE t."group" END AS "group", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value" FROM "__txt_item" t`,
};

const ARRIVAL_STATEMENTS: Record<string, { kind: "log" | "set"; add_sql: string; del_sql: string | null }> = {
  item: { kind: "set", add_sql: `INSERT OR IGNORE INTO "item" ("group", "value") VALUES (?, ?)`, del_sql: `DELETE FROM "item" WHERE "group" = ? AND "value" = ?` },
};

function arrival_statement(arrival: IArrivalRow): SqlStatement {
  const template = ARRIVAL_STATEMENTS[arrival.rel];
  if (template === undefined) {
    throw new Error(`ordered_group_concat_explicit_comma_matches_one_argument: tick received an arrival for undeclared rel '${arrival.rel}'`);
  }
  if (arrival.sign === "del") {
    if (template.kind === "log") {
      throw new Error(`ordered_group_concat_explicit_comma_matches_one_argument: retract from log rel '${arrival.rel}' (engine.pl retract_from_log)`);
    }
    if (template.del_sql === null) {
      throw new Error(`ordered_group_concat_explicit_comma_matches_one_argument: rel '${arrival.rel}' has no delete statement`);
    }
    return { sql: template.del_sql, args: bind_args(arrival.row) };
  }
  return { sql: template.add_sql, args: bind_args(arrival.row) };
}

function apply_arrivals(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<unknown> {
  const statements: SqlStatement[] = arrivals.map(arrival_statement);
  return seam.runner.batch(seam.db, statements);
}

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "explicit_joined", kind: "set", table_name: "explicit_joined", delta_table_name: "__delta_explicit_joined", frontier_table_name: "__frontier_explicit_joined", next_frontier_table_name: "__next_frontier_explicit_joined", columns: ["group", "col2"], column_types: ["text", "text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."group") AND json_type(t."group") = 'object' AND json_type(t."group", '$.fn') = 'text' AND json_type(t."group", '$.args') = 'array' THEN json_extract(t."group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."group", '$.args')), '') || ')' ELSE t."group" END AS "group", CASE WHEN json_valid(t."col2") AND json_type(t."col2") = 'object' AND json_type(t."col2", '$.fn') = 'text' AND json_type(t."col2", '$.args') = 'array' THEN json_extract(t."col2", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."col2", '$.args')), '') || ')' ELSE t."col2" END AS "col2", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_explicit_joined" t WHERE t."_sign" IN (-1, 1) GROUP BY t."group", t."col2", t."_sign"`, rule_observers: [] },
  { rel: "item", kind: "set", table_name: "item", delta_table_name: "__delta_item", frontier_table_name: "__frontier_item", next_frontier_table_name: "__next_frontier_item", columns: ["group", "value"], column_types: ["text", "text"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "item" ("group", "value") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "group", "value"`, arrival_del_sql: `DELETE FROM "item" WHERE ("group", "value") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "group", "value"`, boundary_sql: `SELECT CASE WHEN json_valid(t."group") AND json_type(t."group") = 'object' AND json_type(t."group", '$.fn') = 'text' AND json_type(t."group", '$.args') = 'array' THEN json_extract(t."group", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."group", '$.args')), '') || ')' ELSE t."group" END AS "group", CASE WHEN json_valid(t."value") AND json_type(t."value") = 'object' AND json_type(t."value", '$.fn') = 'text' AND json_type(t."value", '$.args') = 'array' THEN json_extract(t."value", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."value", '$.args')), '') || ')' ELSE t."value" END AS "value", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_item" t WHERE t."_sign" IN (-1, 1) GROUP BY t."group", t."value", t."_sign"`, rule_observers: ["explicit_joined/2"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "explicit_joined", rule_id: "ordered_group_concat_explicit_comma_matches_one_argument:explicit_joined/2#1", head_delta_table_name: "__delta_explicit_joined", head_columns: ["group", "col2"], insert_sql: null, select_sql: `SELECT "group", "col2" FROM "explicit_joined"`, recompute_sql: `DELETE FROM "explicit_joined";
INSERT OR IGNORE INTO "__str" ("content") SELECT group_concat((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value"), ',' ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value")) FROM "item" b0 GROUP BY b0."group" HAVING count(*) > 0;
INSERT OR IGNORE INTO "explicit_joined" ("group", "col2") SELECT "__agg_1", (SELECT s."__id" FROM "__str" s WHERE s."content" = "__agg_2") FROM (SELECT b0."group" AS "__agg_1", group_concat((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value"), ',' ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value")) AS "__agg_2" FROM "item" b0 GROUP BY b0."group" HAVING count(*) > 0)`, support_sql: null, expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: { scope_clear_sql: `DELETE FROM "__agg_scope_explicit_joined"`, scope_seed_sql: [`INSERT OR IGNORE INTO "__agg_scope_explicit_joined" ("group") SELECT DISTINCT d0."group" FROM "__delta_item" d0 WHERE d0."_sign" IN (-1, 1)`], delete_scoped_sql: `DELETE FROM "explicit_joined" WHERE ("group") IN (SELECT "group" FROM "__agg_scope_explicit_joined") RETURNING "group", "col2"`, insert_scoped_sql: [`INSERT OR IGNORE INTO "explicit_joined" ("group", "col2") SELECT "__agg_1", (SELECT s."__id" FROM "__str" s WHERE s."content" = "__agg_2") FROM (SELECT b0."group" AS "__agg_1", group_concat((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value"), ',' ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value")) AS "__agg_2" FROM "item" b0 WHERE (b0."group") IN (SELECT "group" FROM "__agg_scope_explicit_joined") GROUP BY b0."group" HAVING count(*) > 0) RETURNING "group", "col2"`], intern_sql: [`INSERT OR IGNORE INTO "__str" ("content") SELECT group_concat((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value"), ',' ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value")) FROM "item" b0 WHERE (b0."group") IN (SELECT "group" FROM "__agg_scope_explicit_joined") GROUP BY b0."group" HAVING count(*) > 0`], delta_maintained: false } },
];

function recompute_levels(seam: ISqlSeam): Observable<void> {
  const sql = `DELETE FROM "explicit_joined";
INSERT OR IGNORE INTO "__str" ("content") SELECT group_concat((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value"), ',' ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value")) FROM "item" b0 GROUP BY b0."group" HAVING count(*) > 0;
INSERT OR IGNORE INTO "explicit_joined" ("group", "col2") SELECT "__agg_1", (SELECT s."__id" FROM "__str" s WHERE s."content" = "__agg_2") FROM (SELECT b0."group" AS "__agg_1", group_concat((SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value"), ',' ORDER BY (SELECT s."content" FROM "__str" s WHERE s."__id" = b0."value")) AS "__agg_2" FROM "item" b0 GROUP BY b0."group" HAVING count(*) > 0)`;
  return seam.runner.executeMultiple(seam.db, sql);
}

function build_deltas(before: Snapshot, after: Snapshot): ITickDeltas {
  const explicit_joined = multiset_diff(before.explicit_joined, after.explicit_joined);
  const item = multiset_diff(before.item, after.item);
  return {
    rels: [
      { rel: "explicit_joined", add: explicit_joined.add, del: explicit_joined.del },
      { rel: "item", add: item.add, del: item.del },
    ],
    carry_pending: false,
  };
}

function run_naive_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return read_snapshot(seam).pipe(
    concatMap((before) => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; return before; }))),
    concatMap((before) => apply_arrivals(seam, arrivals).pipe(map(() => before))),
  ).pipe(
    concatMap((before) => recompute_levels(seam).pipe(map(() => before))),
    concatMap((before) => read_snapshot(seam).pipe(map((after) => build_deltas(before, after)))),
  );
  // ordered_group_concat_explicit_comma_matches_one_argument: no edge rules -- absorb arrivals, recompute levels, diff.
}

const INCREMENTAL_PROGRAM_SAFE = true;
const RECONCILE_EVERY_TICK = false;
const EMITTER_MODE = process.env.SPREFA_TSV2_EMITTER_MODE === "naive" ? "naive" : "incremental";

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = EMITTER_MODE;
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  if (EMITTER_MODE === "naive" || !INCREMENTAL_PROGRAM_SAFE) {
    return run_naive_tick(seam, arrivals);
  }
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  safe: INCREMENTAL_PROGRAM_SAFE,
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "ordered_group_concat_explicit_comma_matches_one_argument",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
