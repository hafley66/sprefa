// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: quote_in_literal_doubles_the_escape.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "person": [true],
    "selected": [true],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `INSERT OR IGNORE INTO "__str" ("content") VALUES ('O''''Brien')`,
  `CREATE TABLE "person" ("__id" INTEGER PRIMARY KEY, "name" INTEGER NOT NULL, UNIQUE ("name"))`,
  `CREATE TEMP VIEW "__txt_person" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name" FROM "person" t`,
  `CREATE TABLE "selected" ("__id" INTEGER PRIMARY KEY, "name" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("name"))`,
  `CREATE TEMP VIEW "__txt_selected" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."__refcount" AS "__refcount" FROM "selected" t`,
  `CREATE TEMP TABLE "__delta_person" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_person_sign" ON "__delta_person" ("_sign")`,
  `CREATE INDEX "__delta_person_group" ON "__delta_person" ("name")`,
  `CREATE TEMP TABLE "__frontier_person" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_person_phase" ON "__frontier_person" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_person" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_person" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_person" t`,
  `CREATE TEMP TABLE "__delta_selected" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_selected_sign" ON "__delta_selected" ("_sign")`,
  `CREATE INDEX "__delta_selected_group" ON "__delta_selected" ("name")`,
  `CREATE TEMP TABLE "__frontier_selected" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_selected_phase" ON "__frontier_selected" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_selected" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_selected" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_selected" t`,
  `CREATE TEMP TABLE "__support_next_selected" ("name" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("name")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_selected" ("name" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "selected_zero" ON "selected" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  person: ["name"],
  selected: ["name"],
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  person: ["text"],
  selected: ["text"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  person: ["text"],
  selected: ["text"],
};

const rel_catalog: readonly IRelCatalogRow[] = [
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "quote_in_literal_doubles_the_escape", kind: "module", type_id: 0, arity: 0, module_id: 6, h_id: "54bfba85f1a5f107", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 6, ordinal: 0, local_name: "person", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "423465f775903b4e", h_schema: "a30b139c04a632dd", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 1, local_name: "name", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "045f56c098110488", h_schema: "", h_rule: "" },
  { rel_id: 9, parent_id: 6, ordinal: 0, local_name: "selected", kind: "rel", type_id: 0, arity: 1, module_id: 6, h_id: "3a395bcca2c9faf4", h_schema: "a30b139c04a632dd", h_rule: "c5f9a9c514d20cf5" },
  { rel_id: 10, parent_id: 9, ordinal: 1, local_name: "name", kind: "column", type_id: 1, arity: 0, module_id: 6, h_id: "64a1ba0c5897c26d", h_schema: "", h_rule: "" },
  { rel_id: 11, parent_id: 7, ordinal: 0, local_name: "__delta_person", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "e78962c279542336", h_schema: "178788c545e561e2", h_rule: "" },
  { rel_id: 12, parent_id: 7, ordinal: 0, local_name: "__frontier_person", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "46ecc38efa387ff2", h_schema: "de5b51999f205894", h_rule: "" },
  { rel_id: 13, parent_id: 7, ordinal: 0, local_name: "__next_frontier_person", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "a423f837a689e204", h_schema: "de5b51999f205894", h_rule: "" },
  { rel_id: 14, parent_id: 7, ordinal: 0, local_name: "__txt_person", kind: "view", type_id: 0, arity: 1, module_id: 6, h_id: "cc985853c2b23d7b", h_schema: "a30b139c04a632dd", h_rule: "" },
  { rel_id: 15, parent_id: 11, ordinal: 0, local_name: "__txt___delta_person", kind: "view", type_id: 0, arity: 3, module_id: 6, h_id: "ec33d80cc0ddabec", h_schema: "a30b139c04a632dd", h_rule: "" },
  { rel_id: 16, parent_id: 9, ordinal: 0, local_name: "__delta_selected", kind: "delta", type_id: 0, arity: 3, module_id: 6, h_id: "b9c49e8200133c60", h_schema: "178788c545e561e2", h_rule: "" },
  { rel_id: 17, parent_id: 9, ordinal: 0, local_name: "__frontier_selected", kind: "frontier", type_id: 0, arity: 3, module_id: 6, h_id: "27fce7c1024e759e", h_schema: "de5b51999f205894", h_rule: "" },
  { rel_id: 18, parent_id: 9, ordinal: 0, local_name: "__next_frontier_selected", kind: "next_frontier", type_id: 0, arity: 3, module_id: 6, h_id: "33fc42f07d3ffdae", h_schema: "de5b51999f205894", h_rule: "" },
  { rel_id: 19, parent_id: 9, ordinal: 0, local_name: "__txt_selected", kind: "view", type_id: 0, arity: 1, module_id: 6, h_id: "481350ce2a49b168", h_schema: "a30b139c04a632dd", h_rule: "" },
  { rel_id: 20, parent_id: 16, ordinal: 0, local_name: "__txt___delta_selected", kind: "view", type_id: 0, arity: 3, module_id: 6, h_id: "e567b7fad25a4932", h_schema: "a30b139c04a632dd", h_rule: "" },
  { rel_id: 21, parent_id: 6, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 6, h_id: "95ca9d496cd04541", h_schema: "", h_rule: "" },
  { rel_id: 22, parent_id: 9, ordinal: 0, local_name: "__support_next_selected", kind: "refcount", type_id: 0, arity: 2, module_id: 6, h_id: "5f7d0262e3553bc1", h_schema: "", h_rule: "" },
  { rel_id: 23, parent_id: 9, ordinal: 0, local_name: "__new_selected", kind: "refcount_staging", type_id: 0, arity: 2, module_id: 6, h_id: "ede5bb3e68ba7342", h_schema: "", h_rule: "" },
  { rel_id: 24, parent_id: 8, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "f28c167dc575957c", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 10, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 6, h_id: "aa34598530618490", h_schema: "", h_rule: "" },
];

const rel_declared_column_types: Record<string, readonly string[]> = {
};

const arrival_targets: readonly string[] = ["person"];

const boot: readonly IBootStatement[] = [
  { rel: "person", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["Ada"] },
  { rel: "person", sql: `INSERT OR IGNORE INTO "person" ("name") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?))`, params: ["Ada"] },
  { rel: "person", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["O'Brien"] },
  { rel: "person", sql: `INSERT OR IGNORE INTO "person" ("name") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?))`, params: ["O'Brien"] },
  { rel: "selected", sql: `DELETE FROM "selected"`, params: [] },
  { rel: "selected", sql: `INSERT OR IGNORE INTO "selected" ("name") SELECT b0."name" FROM "person" b0 WHERE (b0."name" IS (SELECT s."__id" FROM "__str" s WHERE s."content" = 'O''Brien'))`, params: [] },
];

const final_select: Record<string, string> = {
  person: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name" FROM "__txt_person" t`,
  selected: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name" FROM "__txt_selected" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "person", kind: "set", table_name: "person", delta_table_name: "__delta_person", frontier_table_name: "__frontier_person", next_frontier_table_name: "__next_frontier_person", columns: ["name"], column_types: ["text"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "person" ("name") SELECT json_extract(value, '$[0]') FROM json_each(?) RETURNING "name"`, arrival_del_sql: `DELETE FROM "person" WHERE ("name") IN (SELECT json_extract(value, '$[0]') FROM json_each(?)) RETURNING "name"`, boundary_sql: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_person" t WHERE t."_sign" IN (-1, 1) GROUP BY t."name", t."_sign"`, rule_observers: ["selected/1"] },
  { rel: "selected", kind: "set", table_name: "selected", delta_table_name: "__delta_selected", frontier_table_name: "__frontier_selected", next_frontier_table_name: "__next_frontier_selected", columns: ["name"], column_types: ["text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_selected" t WHERE t."_sign" IN (-1, 1) GROUP BY t."name", t."_sign"`, rule_observers: [] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "selected", rule_id: "quote_in_literal_doubles_the_escape:selected/1#1", head_delta_table_name: "__delta_selected", head_columns: ["name"], insert_sql: `INSERT OR IGNORE INTO "selected" ("name") SELECT DISTINCT d0."name" FROM "__frontier_person" d0 WHERE d0."_phase" >= 0 AND (d0."name" IS (SELECT s."__id" FROM "__str" s WHERE s."content" = 'O''Brien')) RETURNING "name"`, select_sql: `SELECT "name" FROM "selected"`, recompute_sql: `DELETE FROM "selected";
INSERT OR IGNORE INTO "selected" ("name") SELECT b0."name" FROM "person" b0 WHERE (b0."name" IS (SELECT s."__id" FROM "__str" s WHERE s."content" = 'O''Brien'))`, support_sql: [`DELETE FROM "__support_next_selected"`, `INSERT INTO "__support_next_selected" ("name", "__refcount") SELECT "name", sum("__refcount") FROM (SELECT b0."name" AS "name", count(*) AS "__refcount" FROM "person" b0 WHERE (b0."name" IS (SELECT s."__id" FROM "__str" s WHERE s."content" = 'O''Brien')) GROUP BY b0."name") GROUP BY "name"`, `UPDATE "selected" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_selected" n WHERE n."name" = h."name"), 0)`, `INSERT INTO "__delta_selected" ("_sign", "_sequence", "name") SELECT -1, row_number() OVER () - 1, "name" FROM "selected" WHERE "__refcount" <= 0`, `DELETE FROM "selected" WHERE "__refcount" <= 0`, `DELETE FROM "__new_selected"`, `INSERT INTO "__new_selected" ("name", "__refcount") SELECT n."name", n."__refcount" FROM "__support_next_selected" n LEFT JOIN "selected" h ON n."name" = h."name" WHERE h."name" IS NULL`, `INSERT INTO "__delta_selected" ("_sign", "_sequence", "name") SELECT 1, "rowid" - 1, "name" FROM "__new_selected"`, `INSERT INTO "__frontier_selected" ("_phase", "_sequence", "name") SELECT ?, "rowid" - 1, "name" FROM "__new_selected"`, `INSERT INTO "__next_frontier_selected" ("_phase", "_sequence", "name") SELECT ?, "rowid" - 1, "name" FROM "__new_selected"`, `INSERT OR IGNORE INTO "selected" ("name", "__refcount") SELECT n."name", n."__refcount" FROM "__support_next_selected" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      map((carry_pending): ITickDeltas => ({ rels, carry_pending })),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  arrivals = validate_arrivals(arrivals);
  return run_incremental_tick(seam, arrivals);
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "quote_in_literal_doubles_the_escape",
  internMode: "dict",
  ddl,
  rel_columns,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  unsupported_execution,
  tick: run_tick,
};
