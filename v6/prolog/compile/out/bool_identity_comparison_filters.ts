// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: bool_identity_comparison_filters.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { EnumPlane } from "../runtime/enumPlane.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IEnumRefColumns,
  IEnumTypePlan,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly ir_version: number; readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly rel_physical_names: Record<string, string>; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const ENUM_TYPES: readonly IEnumTypePlan[] = [];
export const ENUM_REF_COLUMNS: IEnumRefColumns = {};

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "enabled_name": [true],
    "flag": [true, false],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "bool_identity_comparison_filters_enabled_name" ("__id" INTEGER PRIMARY KEY, "name" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("name"))`,
  `CREATE TEMP VIEW "__txt_bool_identity_comparison_filters_enabled_name" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."__refcount" AS "__refcount" FROM "bool_identity_comparison_filters_enabled_name" t`,
  `CREATE TABLE "bool_identity_comparison_filters_flag_928da2c2d5b2" ("__id" INTEGER PRIMARY KEY, "name" INTEGER NOT NULL, "enabled" INTEGER NOT NULL CHECK ("enabled" IN (0,1)), UNIQUE ("name", "enabled"))`,
  `CREATE TEMP VIEW "__txt_bool_identity_comparison_filters_flag_928da2c2d5b2" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."enabled" AS "enabled" FROM "bool_identity_comparison_filters_flag_928da2c2d5b2" t`,
  `CREATE INDEX "bool_identity_comparison_filters_flag_928da2c2d5b2__scan_enabled" ON "bool_identity_comparison_filters_flag_928da2c2d5b2" ("enabled")`,
  `CREATE TEMP TABLE "__delta_bool_identity_comparison_filters_enabled_name" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_bool_identity_comparison_filters_enabled_name_sign" ON "__delta_bool_identity_comparison_filters_enabled_name" ("_sign")`,
  `CREATE INDEX "__delta_bool_identity_comparison_filters_enabled_name_group" ON "__delta_bool_identity_comparison_filters_enabled_name" ("name")`,
  `CREATE TEMP TABLE "__frontier_bool_identity_comparison_filters_enabled_name" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_bool_identity_comparison_filters_enabled_name_phase" ON "__frontier_bool_identity_comparison_filters_enabled_name" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_bool_identity_comparison_filters_enabled_name" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_bool_identity_comparison_filters_enabled_name" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_bool_identity_comparison_filters_enabled_name" t`,
  `CREATE TEMP TABLE "__delta_bool_identity_comparison_filters_flag_928da2c2d5b2" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "enabled" INTEGER NOT NULL CHECK ("enabled" IN (0,1)))`,
  `CREATE INDEX "__delta_bool_identity_comparison_filters_flag_928da2c2d5b2_sign" ON "__delta_bool_identity_comparison_filters_flag_928da2c2d5b2" ("_sign")`,
  `CREATE INDEX "__delta_bool_identity_comparison_filters_flag_928da2c2d5b2_group" ON "__delta_bool_identity_comparison_filters_flag_928da2c2d5b2" ("name", "enabled")`,
  `CREATE TEMP TABLE "__frontier_bool_identity_comparison_filters_flag_928da2c2d5b2" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "enabled" INTEGER NOT NULL CHECK ("enabled" IN (0,1)))`,
  `CREATE INDEX "__frontier_bool_identity_comparison_filters_flag_928da2c2d5b2_phase" ON "__frontier_bool_identity_comparison_filters_flag_928da2c2d5b2" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_bool_identity_comparison_filters_flag_928da2c2d5b2" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "enabled" INTEGER NOT NULL CHECK ("enabled" IN (0,1)))`,
  `CREATE TEMP VIEW "__txt___delta_bool_identity_comparison_filters_flag_928da2c2d5b2" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."enabled" AS "enabled", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_bool_identity_comparison_filters_flag_928da2c2d5b2" t`,
  `CREATE TEMP TABLE "__support_next_bool_identity_comparison_filters_enabled_name" ("name" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("name")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_bool_identity_comparison_filters_enabled_name" ("name" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "bool_identity_comparison_filters_enabled_name_zero" ON "bool_identity_comparison_filters_enabled_name" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  enabled_name: ["name"],
  flag: ["name", "enabled"],
};

const rel_physical_names: Record<string, string> = {
  enabled_name: "bool_identity_comparison_filters_enabled_name",
  flag: "bool_identity_comparison_filters_flag_928da2c2d5b2",
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  enabled_name: ["text"],
  flag: ["text", "bool"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  enabled_name: ["text"],
  flag: ["text", "bool"],
};

const rel_catalog: readonly IRelCatalogRow[] = new Array<IRelCatalogRow>(
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "bool_identity_comparison_filters", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "c6da8b31510d61cc", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "enabled_name", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "492c2c17f0168978", h_schema: "a30b139c04a632dd", h_rule: "1028d71ebf8991c0" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "name", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "eb588fd1d61dd9f8", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 7, ordinal: 0, local_name: "flag", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "943796181e567fbb", h_schema: "49645800ff8032d7", h_rule: "" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "name", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "db3b9f73b2952d51", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 10, ordinal: 2, local_name: "enabled", kind: "column", type_id: 4, arity: 0, module_id: 7, h_id: "9a95607300a30704", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 8, ordinal: 0, local_name: "__delta_bool_identity_comparison_filters_enabled_name", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "fe75293fb7567cb6", h_schema: "178788c545e561e2", h_rule: "" },
  { rel_id: 14, parent_id: 8, ordinal: 0, local_name: "__frontier_bool_identity_comparison_filters_enabled_name", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "5c4092f91a25fc20", h_schema: "de5b51999f205894", h_rule: "" },
  { rel_id: 15, parent_id: 8, ordinal: 0, local_name: "__next_frontier_bool_identity_comparison_filters_enabled_name", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "9d1808e1aa7dc828", h_schema: "de5b51999f205894", h_rule: "" },
  { rel_id: 16, parent_id: 8, ordinal: 0, local_name: "__txt_bool_identity_comparison_filters_enabled_name", kind: "view", type_id: 0, arity: 1, module_id: 7, h_id: "4d1928f788baed97", h_schema: "a30b139c04a632dd", h_rule: "" },
  { rel_id: 17, parent_id: 13, ordinal: 0, local_name: "__txt___delta_bool_identity_comparison_filters_enabled_name", kind: "view", type_id: 0, arity: 3, module_id: 7, h_id: "48d3cb8ff9e12446", h_schema: "a30b139c04a632dd", h_rule: "" },
  { rel_id: 18, parent_id: 10, ordinal: 0, local_name: "__delta_bool_identity_comparison_filters_flag_928da2c2d5b2", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "69cf1c64a8cf7d1d", h_schema: "67b17ffc3ff3e088", h_rule: "" },
  { rel_id: 19, parent_id: 10, ordinal: 0, local_name: "__frontier_bool_identity_comparison_filters_flag_928da2c2d5b2", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "c2eea642250f263a", h_schema: "53802fc1d9d453a7", h_rule: "" },
  { rel_id: 20, parent_id: 10, ordinal: 0, local_name: "__next_frontier_bool_identity_comparison_filters_flag_928da2c2d5b2", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "ba62693a1e54b619", h_schema: "53802fc1d9d453a7", h_rule: "" },
  { rel_id: 21, parent_id: 10, ordinal: 0, local_name: "__txt_bool_identity_comparison_filters_flag_928da2c2d5b2", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "67be899eafea3806", h_schema: "49645800ff8032d7", h_rule: "" },
  { rel_id: 22, parent_id: 18, ordinal: 0, local_name: "__txt___delta_bool_identity_comparison_filters_flag_928da2c2d5b2", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "e80cc91bfc10a704", h_schema: "49645800ff8032d7", h_rule: "" },
  { rel_id: 23, parent_id: 7, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 7, h_id: "215e4298de33b643", h_schema: "", h_rule: "" },
  { rel_id: 24, parent_id: 8, ordinal: 0, local_name: "__support_next_bool_identity_comparison_filters_enabled_name", kind: "refcount", type_id: 0, arity: 2, module_id: 7, h_id: "47789793571d475a", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 8, ordinal: 0, local_name: "__new_bool_identity_comparison_filters_enabled_name", kind: "refcount_staging", type_id: 0, arity: 2, module_id: 7, h_id: "7c74932116563c22", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 9, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "b2061e1267b9690e", h_schema: "", h_rule: "" },
  { rel_id: 27, parent_id: 11, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "209072638984458c", h_schema: "", h_rule: "" },
  { rel_id: 28, parent_id: 12, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "2c2d4630b9f461fd", h_schema: "", h_rule: "" },
);

const rel_declared_column_types: Record<string, readonly string[]> = {
  enabled_name: ["text"],
  flag: ["text", "bool"],
};

const arrival_targets: readonly string[] = ["flag"];

const boot: readonly IBootStatement[] = [
  { rel: "flag", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["alpha"] },
  { rel: "flag", sql: `INSERT OR IGNORE INTO "bool_identity_comparison_filters_flag_928da2c2d5b2" ("name", "enabled") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["alpha", true] },
  { rel: "flag", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["beta"] },
  { rel: "flag", sql: `INSERT OR IGNORE INTO "bool_identity_comparison_filters_flag_928da2c2d5b2" ("name", "enabled") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["beta", false] },
  { rel: "enabled_name", sql: `DELETE FROM "bool_identity_comparison_filters_enabled_name"`, params: [] },
  { rel: "enabled_name", sql: `INSERT OR IGNORE INTO "bool_identity_comparison_filters_enabled_name" ("name") SELECT b0."name" FROM "bool_identity_comparison_filters_flag_928da2c2d5b2" b0 WHERE (b0."enabled" IS 1)`, params: [] },
];

const final_select: Record<string, string> = {
  enabled_name: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name" FROM "__txt_bool_identity_comparison_filters_enabled_name" t`,
  flag: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."enabled" FROM "__txt_bool_identity_comparison_filters_flag_928da2c2d5b2" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "enabled_name", kind: "set", table_name: "bool_identity_comparison_filters_enabled_name", delta_table_name: "__delta_bool_identity_comparison_filters_enabled_name", frontier_table_name: "__frontier_bool_identity_comparison_filters_enabled_name", next_frontier_table_name: "__next_frontier_bool_identity_comparison_filters_enabled_name", columns: ["name"], column_types: ["text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_bool_identity_comparison_filters_enabled_name" t WHERE t."_sign" IN (-1, 1) GROUP BY t."name", t."_sign"`, rule_observers: [] },
  { rel: "flag", kind: "set", table_name: "bool_identity_comparison_filters_flag_928da2c2d5b2", delta_table_name: "__delta_bool_identity_comparison_filters_flag_928da2c2d5b2", frontier_table_name: "__frontier_bool_identity_comparison_filters_flag_928da2c2d5b2", next_frontier_table_name: "__next_frontier_bool_identity_comparison_filters_flag_928da2c2d5b2", columns: ["name", "enabled"], column_types: ["text", "bool"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "bool_identity_comparison_filters_flag_928da2c2d5b2" ("name", "enabled") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "name", "enabled"`, arrival_del_sql: `DELETE FROM "bool_identity_comparison_filters_flag_928da2c2d5b2" WHERE ("name", "enabled") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "name", "enabled"`, boundary_sql: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."enabled", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_bool_identity_comparison_filters_flag_928da2c2d5b2" t WHERE t."_sign" IN (-1, 1) GROUP BY t."name", t."enabled", t."_sign"`, rule_observers: ["enabled_name/1"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "enabled_name", rule_id: "bool_identity_comparison_filters:enabled_name/1#1", head_delta_table_name: "__delta_bool_identity_comparison_filters_enabled_name", head_columns: ["name"], insert_sql: `INSERT OR IGNORE INTO "bool_identity_comparison_filters_enabled_name" ("name") SELECT DISTINCT d0."name" FROM "__frontier_bool_identity_comparison_filters_flag_928da2c2d5b2" d0 WHERE d0."_phase" >= 0 AND (d0."enabled" IS 1) RETURNING "name"`, select_sql: `SELECT "name" FROM "bool_identity_comparison_filters_enabled_name"`, recompute_sql: `DELETE FROM "bool_identity_comparison_filters_enabled_name";
INSERT OR IGNORE INTO "bool_identity_comparison_filters_enabled_name" ("name") SELECT b0."name" FROM "bool_identity_comparison_filters_flag_928da2c2d5b2" b0 WHERE (b0."enabled" IS 1)`, support_sql: [`DELETE FROM "__support_next_bool_identity_comparison_filters_enabled_name"`, `INSERT INTO "__support_next_bool_identity_comparison_filters_enabled_name" ("name", "__refcount") SELECT "name", sum("__refcount") FROM (SELECT b0."name" AS "name", count(*) AS "__refcount" FROM "bool_identity_comparison_filters_flag_928da2c2d5b2" b0 WHERE (b0."enabled" IS 1) GROUP BY b0."name") GROUP BY "name"`, `UPDATE "bool_identity_comparison_filters_enabled_name" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_bool_identity_comparison_filters_enabled_name" n WHERE n."name" = h."name"), 0)`, `INSERT INTO "__delta_bool_identity_comparison_filters_enabled_name" ("_sign", "_sequence", "name") SELECT -1, row_number() OVER () - 1, "name" FROM "bool_identity_comparison_filters_enabled_name" WHERE "__refcount" <= 0`, `DELETE FROM "bool_identity_comparison_filters_enabled_name" WHERE "__refcount" <= 0`, `DELETE FROM "__new_bool_identity_comparison_filters_enabled_name"`, `INSERT INTO "__new_bool_identity_comparison_filters_enabled_name" ("name", "__refcount") SELECT n."name", n."__refcount" FROM "__support_next_bool_identity_comparison_filters_enabled_name" n LEFT JOIN "bool_identity_comparison_filters_enabled_name" h ON n."name" = h."name" WHERE h."name" IS NULL`, `INSERT INTO "__delta_bool_identity_comparison_filters_enabled_name" ("_sign", "_sequence", "name") SELECT 1, "rowid" - 1, "name" FROM "__new_bool_identity_comparison_filters_enabled_name"`, `INSERT INTO "__frontier_bool_identity_comparison_filters_enabled_name" ("_phase", "_sequence", "name") SELECT ?, "rowid" - 1, "name" FROM "__new_bool_identity_comparison_filters_enabled_name"`, `INSERT INTO "__next_frontier_bool_identity_comparison_filters_enabled_name" ("_phase", "_sequence", "name") SELECT ?, "rowid" - 1, "name" FROM "__new_bool_identity_comparison_filters_enabled_name"`, `INSERT OR IGNORE INTO "bool_identity_comparison_filters_enabled_name" ("name", "__refcount") SELECT n."name", n."__refcount" FROM "__support_next_bool_identity_comparison_filters_enabled_name" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      concatMap((carry_pending) => EnumPlane.decode_deltas(seam, ENUM_TYPES, ENUM_REF_COLUMNS, SUBSCRIBED_RELATIONS, rels).pipe(
        map((decoded): ITickDeltas => ({ rels: decoded, carry_pending })),
      )),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return EnumPlane.intern(seam, ENUM_TYPES, ENUM_REF_COLUMNS, arrivals).pipe(
    concatMap((normalized) => run_incremental_tick(seam, validate_arrivals(normalized))),
  );
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "bool_identity_comparison_filters",
  ir_version: 1,
  internMode: "dict",
  ddl,
  rel_columns,
  rel_physical_names,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  enum_types: ENUM_TYPES,
  enum_ref_columns: ENUM_REF_COLUMNS,
  unsupported_execution,
  tick: run_tick,
};
