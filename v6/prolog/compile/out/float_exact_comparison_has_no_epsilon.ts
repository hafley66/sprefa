// GENERATED by v6/prolog/compile (tsv2 emitter P3). Do not
// hand-edit; recompile. Program: float_exact_comparison_has_no_epsilon.
// Compiles the reference engine's occurrence / keyed-replace / boundary-diff
// semantics (engine.pl) to SQLite + the real v6/tsv2 runtime seam, not
// lower/lower_sql.ts's.
//
// The default path stages effective tick changes in indexed TEMP tables,
// executes emitted frontier-side joins for positive level rules, promotes
// edge and post-write level growth across drain ticks, and computes boundary
// changes from the staged stream. Retractions and negative bodies use emitted
// support-count reconciliation.
//
// IGenProgram has no slot for boot-time work (seeding Initial rows before
// tick 1). `boot` is an extra field added beyond the five pinned names
// ("extend by adding fields, never renaming"); v6/tsv2/scripts/
// run-emitted.ts (the reconciliation runner) runs it after DDL and before
// the tick fold.

import { concatMap, forkJoin, map, of, type Observable } from "rxjs";

import { IncrementalRuntime } from "../runtime/1_incremental.ts";
import { SubscribeCone } from "../runtime/3_subscribe.ts";
import { multiset_diff } from "../runtime/diff.ts";
import { select_rows } from "../runtime/rows.ts";
import { list_at_scalar_seam } from "../runtime/boundary.ts";
import { EnumPlane } from "../runtime/enumPlane.ts";
import { TextPlane } from "../runtime/textPlane.ts";
import type {
  IArrivalBatch,
  IArrivalRow,
  IGenProgram,
  IIncrementalEdgeStatement,
  IIncrementalLevelStatement,
  IIncrementalProgramPlan,
  IIncrementalRelationPlan,
  IRelCatalogRow,
  IRelDelta,
  IRow,
  IRowColumnType,
  IRowScalar,
  IRowValue,
  ISqlSeam,
  IEnumRefColumns,
  IEnumTypePlan,
  ITextInternPlan,
  ITickDeltas,
  SqlStatement,
} from "../runtime/types.ts";

interface IHostColumnPlan { readonly name: string; readonly type: string }
interface IHostPlanData { readonly name: string; readonly inputs: readonly IHostColumnPlan[]; readonly outputs: readonly IHostColumnPlan[]; readonly template: string; readonly demand_rel: string; readonly response_rel: string; readonly execution: string }
interface IBindPlanData { readonly name: string; readonly columns: readonly IHostColumnPlan[]; readonly literals: readonly IRowScalar[]; readonly execution: string }
interface IQueryPlanData { readonly rel: string; readonly arity: number; readonly columns: readonly (IRowScalar | null)[]; readonly bound: readonly number[]; readonly snapshot: "current" }

interface IBootStatement {
  rel: string;
  sql: string;
  params: readonly IRowScalar[];
}

type IGenProgramWithBoot = IGenProgram & { readonly ir_version: number; readonly boot: readonly IBootStatement[]; readonly final_select: Record<string, string>; readonly host_plans: readonly IHostPlanData[]; readonly bind_plans: readonly IBindPlanData[]; readonly query_plans: readonly IQueryPlanData[]; readonly subscribed_rels: readonly string[]; readonly rel_catalog: readonly IRelCatalogRow[]; readonly rel_physical_names: Record<string, string>; readonly unsupported_execution: readonly string[] };

export const host_plans: readonly IHostPlanData[] = [];
export const bind_plans: readonly IBindPlanData[] = [];
export const query_plans: readonly IQueryPlanData[] = [];
export const subscribed_rels: readonly string[] = [];
export const unsupported_execution: readonly string[] = [];

function bind_args(values: readonly IRowValue[]): (string | number | bigint | Uint8Array)[] {
  return values.map((value) => {
    if (typeof value === "boolean") return BigInt(value ? 1 : 0);
    if (typeof value === "number") return Number.isSafeInteger(value) ? BigInt(value) : value;
    if (typeof value === "string") return value;
    if (value instanceof Uint8Array) return value;
    throw list_at_scalar_seam("sql_parameter");
  });
}

const SAFE_INTEGER_LIMIT = 9007199254740991n;

function wide_integer_witness(value: unknown): boolean {
  if (typeof value === "bigint") return value < -SAFE_INTEGER_LIMIT || value > SAFE_INTEGER_LIMIT;
  if (typeof value === "number") return Number.isInteger(value) && !Number.isSafeInteger(value);
  return false;
}

/** A json/list column arrives as the document TEXT, and the text is the
 *  only place the int-versus-float distinction still exists: a JSON
 *  number token with no `.` and no exponent IS an integer, which is
 *  exactly how the prolog reader parses it. String contents are blanked
 *  first so digits inside a string never read as a number. Unparseable
 *  text is not this scan's business (the json arm below names it). */
const JSON_NUMBER = /-?\d+(?:\.\d+)?(?:[e_e][+-]?\d+)?/g;

function wide_integer_in_json_text(value: IRowValue): boolean {
  if (typeof value !== "string") return wide_integer_witness(value);
  const without_strings = value.replace(/"(?:\\.|[^"\\])*"/g, '""');
  for (const token of without_strings.match(JSON_NUMBER) ?? []) {
    if (/[.e_e]/.test(token)) continue;
    const parsed = BigInt(token);
    if (parsed < -SAFE_INTEGER_LIMIT || parsed > SAFE_INTEGER_LIMIT) return true;
  }
  return false;
}

function validate_arrivals(arrivals: IArrivalBatch): IArrivalBatch {
  return arrivals.map((arrival): IArrivalRow => {
    const types = rel_column_types[arrival.rel];
    if (types === undefined || types.length !== arrival.row.length) throw new Error(`arrival shape mismatch for ${arrival.rel}`);
    const declared = rel_declared_column_types[arrival.rel];
    const row = arrival.row.map((value, index): IRowValue => {
      const type = declared === undefined ? undefined : declared[index];
      const scanned = type === "json" ? wide_integer_in_json_text(value)
        : type === "float" ? false
        : wide_integer_witness(value);
      if (scanned) throw new Error(`int_out_of_range ${arrival.rel}[${index}]`);
      if (type === "bytes") {
        if (!(value instanceof Uint8Array)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bytes`);
        return value;
      }
      if (type === "bool") {
        if (typeof value !== "boolean") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_bool`);
        return value;
      }
      if (type === "float") {
        // SQLite REAL affinity: an integer widens, everything else is refused.
        if (typeof value !== "number" || !Number.isFinite(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_finite_float`);
        return Object.is(value, -0) ? 0 : value;
      }
      if (type === "int") {
        if (typeof value !== "number" || !Number.isInteger(value)) throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_int`);
        return value;
      }
      if (type === "text") {
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_text`);
        return value;
      }
      if (type === "json") {
        // The shared reader (compile/scripts/0_json_arrival.pl
        // json_column_term/4) takes a document TEXT or a bare number,
        // and refuses everything else. Same two shapes here.
        if (typeof value === "number") return value;
        if (typeof value !== "string") throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`);
        try { JSON.parse(value); } catch { throw new Error(`type_arrival_shape_mismatch ${arrival.rel}[${index}] field_not_json`); }
        return value;
      }
      return value;
    });
    return { ...arrival, row };
  });
}

export const ENUM_TYPES: readonly IEnumTypePlan[] = [];
export const ENUM_REF_COLUMNS: IEnumRefColumns = {};

export const TEXT_INTERN_PLAN: ITextInternPlan = {
  internSql: `INSERT OR IGNORE INTO "__str" ("content") SELECT i.value FROM json_each(?) i`,
  lookupSql: `SELECT s."content" AS "__lookup", s."__id" AS "__id" FROM json_each(?) i JOIN "__str" s ON s."content" = i.value`,
  relColumns: {
    "exact": [true],
    "score": [true, false],
  },
};

const ddl: readonly string[] = [
  `CREATE TABLE "__str" ("__id" INTEGER PRIMARY KEY, "content" TEXT NOT NULL UNIQUE)`,
  `CREATE TABLE "float_exact_comparison_has_no_epsilon_exact" ("__id" INTEGER PRIMARY KEY, "name" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL DEFAULT 1, UNIQUE ("name"))`,
  `CREATE TEMP VIEW "__txt_float_exact_comparison_has_no_epsilon_exact" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."__refcount" AS "__refcount" FROM "float_exact_comparison_has_no_epsilon_exact" t`,
  `CREATE TABLE "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("__id" INTEGER PRIMARY KEY, "name" INTEGER NOT NULL, "value" REAL NOT NULL CHECK (typeof("value") = 'real' AND "value" BETWEEN -1.7976931348623157e308 AND 1.7976931348623157e308), UNIQUE ("name", "value"))`,
  `CREATE TEMP VIEW "__txt_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."value" AS "value" FROM "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" t`,
  `CREATE INDEX "float_exact_comparison_has_no_epsilon_score_74f788ec9f37__scan_value" ON "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("value")`,
  `CREATE TEMP TABLE "__delta_float_exact_comparison_has_no_epsilon_exact" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE INDEX "__delta_float_exact_comparison_has_no_epsilon_exact_sign" ON "__delta_float_exact_comparison_has_no_epsilon_exact" ("_sign")`,
  `CREATE INDEX "__delta_float_exact_comparison_has_no_epsilon_exact_group" ON "__delta_float_exact_comparison_has_no_epsilon_exact" ("name")`,
  `CREATE TEMP TABLE "__frontier_float_exact_comparison_has_no_epsilon_exact" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE INDEX "__frontier_float_exact_comparison_has_no_epsilon_exact_phase" ON "__frontier_float_exact_comparison_has_no_epsilon_exact" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_float_exact_comparison_has_no_epsilon_exact" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL)`,
  `CREATE TEMP VIEW "__txt___delta_float_exact_comparison_has_no_epsilon_exact" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_float_exact_comparison_has_no_epsilon_exact" t`,
  `CREATE TEMP TABLE "__delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("_sign" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "value" REAL NOT NULL CHECK (typeof("value") = 'real' AND "value" BETWEEN -1.7976931348623157e308 AND 1.7976931348623157e308))`,
  `CREATE INDEX "__delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37_sign" ON "__delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("_sign")`,
  `CREATE INDEX "__delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37_group" ON "__delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("name", "value")`,
  `CREATE TEMP TABLE "__frontier_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "value" REAL NOT NULL CHECK (typeof("value") = 'real' AND "value" BETWEEN -1.7976931348623157e308 AND 1.7976931348623157e308))`,
  `CREATE INDEX "__frontier_float_exact_comparison_has_no_epsilon_score_74f788ec9f37_phase" ON "__frontier_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("_phase")`,
  `CREATE TEMP TABLE "__next_frontier_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("_phase" INTEGER NOT NULL, "_sequence" INTEGER NOT NULL, "name" INTEGER NOT NULL, "value" REAL NOT NULL CHECK (typeof("value") = 'real' AND "value" BETWEEN -1.7976931348623157e308 AND 1.7976931348623157e308))`,
  `CREATE TEMP VIEW "__txt___delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" AS SELECT (SELECT s."content" FROM "__str" s WHERE s."__id" = t."name") AS "name", t."value" AS "value", t."_sign" AS "_sign", t."_sequence" AS "_sequence" FROM "__delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" t`,
  `CREATE TEMP TABLE "__support_next_float_exact_comparison_has_no_epsilon_exact" ("name" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL, PRIMARY KEY ("name")) WITHOUT ROWID`,
  `CREATE TEMP TABLE "__new_float_exact_comparison_has_no_epsilon_exact" ("name" INTEGER NOT NULL, "__refcount" INTEGER NOT NULL)`,
  `CREATE INDEX "float_exact_comparison_has_no_epsilon_exact_zero" ON "float_exact_comparison_has_no_epsilon_exact" ("__refcount") WHERE "__refcount" <= 0`,
];

const rel_columns: Record<string, readonly string[]> = {
  exact: ["name"],
  score: ["name", "value"],
};

const rel_physical_names: Record<string, string> = {
  exact: "float_exact_comparison_has_no_epsilon_exact",
  score: "float_exact_comparison_has_no_epsilon_score_74f788ec9f37",
};

const rel_column_types: Record<string, readonly IRowColumnType[]> = {
  exact: ["text"],
  score: ["text", "float"],
};

const rel_stored_column_types: Record<string, readonly IRowColumnType[]> = {
  exact: ["text"],
  score: ["text", "float"],
};

const rel_catalog: readonly IRelCatalogRow[] = new Array<IRelCatalogRow>(
  { rel_id: 1, parent_id: 0, ordinal: 0, local_name: "text", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 2, parent_id: 0, ordinal: 0, local_name: "int", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 3, parent_id: 0, ordinal: 0, local_name: "float", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 4, parent_id: 0, ordinal: 0, local_name: "bool", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 5, parent_id: 0, ordinal: 0, local_name: "json", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 6, parent_id: 0, ordinal: 0, local_name: "bytes", kind: "primitive", type_id: 0, arity: 0, module_id: 0, h_id: "", h_schema: "", h_rule: "" },
  { rel_id: 7, parent_id: 0, ordinal: 0, local_name: "float_exact_comparison_has_no_epsilon", kind: "module", type_id: 0, arity: 0, module_id: 7, h_id: "ac2114b90a1beb2b", h_schema: "", h_rule: "" },
  { rel_id: 8, parent_id: 7, ordinal: 0, local_name: "exact", kind: "rel", type_id: 0, arity: 1, module_id: 7, h_id: "048a4f9cc80961bc", h_schema: "a30b139c04a632dd", h_rule: "8a09eca4a9a88f6d" },
  { rel_id: 9, parent_id: 8, ordinal: 1, local_name: "name", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "9958ab49d6a76ed4", h_schema: "", h_rule: "" },
  { rel_id: 10, parent_id: 7, ordinal: 0, local_name: "score", kind: "rel", type_id: 0, arity: 2, module_id: 7, h_id: "f90aee35af678230", h_schema: "0afc9e4df0350232", h_rule: "" },
  { rel_id: 11, parent_id: 10, ordinal: 1, local_name: "name", kind: "column", type_id: 1, arity: 0, module_id: 7, h_id: "1a8344fe5be75cff", h_schema: "", h_rule: "" },
  { rel_id: 12, parent_id: 10, ordinal: 2, local_name: "value", kind: "column", type_id: 3, arity: 0, module_id: 7, h_id: "394db00336fa2717", h_schema: "", h_rule: "" },
  { rel_id: 13, parent_id: 8, ordinal: 0, local_name: "__delta_float_exact_comparison_has_no_epsilon_exact", kind: "delta", type_id: 0, arity: 3, module_id: 7, h_id: "7f2a80a13fc373c1", h_schema: "178788c545e561e2", h_rule: "" },
  { rel_id: 14, parent_id: 8, ordinal: 0, local_name: "__frontier_float_exact_comparison_has_no_epsilon_exact", kind: "frontier", type_id: 0, arity: 3, module_id: 7, h_id: "01fbfe85feef65dc", h_schema: "de5b51999f205894", h_rule: "" },
  { rel_id: 15, parent_id: 8, ordinal: 0, local_name: "__next_frontier_float_exact_comparison_has_no_epsilon_exact", kind: "next_frontier", type_id: 0, arity: 3, module_id: 7, h_id: "85377a170ccc94d7", h_schema: "de5b51999f205894", h_rule: "" },
  { rel_id: 16, parent_id: 8, ordinal: 0, local_name: "__txt_float_exact_comparison_has_no_epsilon_exact", kind: "view", type_id: 0, arity: 1, module_id: 7, h_id: "c0f96bcf8765799e", h_schema: "a30b139c04a632dd", h_rule: "" },
  { rel_id: 17, parent_id: 13, ordinal: 0, local_name: "__txt___delta_float_exact_comparison_has_no_epsilon_exact", kind: "view", type_id: 0, arity: 3, module_id: 7, h_id: "a18d1e8c954d36db", h_schema: "a30b139c04a632dd", h_rule: "" },
  { rel_id: 18, parent_id: 10, ordinal: 0, local_name: "__delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37", kind: "delta", type_id: 0, arity: 4, module_id: 7, h_id: "0ff772a05a0a3062", h_schema: "1cc9f6eaa9e75953", h_rule: "" },
  { rel_id: 19, parent_id: 10, ordinal: 0, local_name: "__frontier_float_exact_comparison_has_no_epsilon_score_74f788ec9f37", kind: "frontier", type_id: 0, arity: 4, module_id: 7, h_id: "981bb2c3e154cc9c", h_schema: "4bd2ec3502cac462", h_rule: "" },
  { rel_id: 20, parent_id: 10, ordinal: 0, local_name: "__next_frontier_float_exact_comparison_has_no_epsilon_score_74f788ec9f37", kind: "next_frontier", type_id: 0, arity: 4, module_id: 7, h_id: "ed09bc7256e379e9", h_schema: "4bd2ec3502cac462", h_rule: "" },
  { rel_id: 21, parent_id: 10, ordinal: 0, local_name: "__txt_float_exact_comparison_has_no_epsilon_score_74f788ec9f37", kind: "view", type_id: 0, arity: 2, module_id: 7, h_id: "1d2b4fa6909f0747", h_schema: "0afc9e4df0350232", h_rule: "" },
  { rel_id: 22, parent_id: 18, ordinal: 0, local_name: "__txt___delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37", kind: "view", type_id: 0, arity: 4, module_id: 7, h_id: "f6b5be408ff6a3e8", h_schema: "0afc9e4df0350232", h_rule: "" },
  { rel_id: 23, parent_id: 7, ordinal: 0, local_name: "__str", kind: "dictionary", type_id: 0, arity: 2, module_id: 7, h_id: "09ebe8e8fe9909d9", h_schema: "", h_rule: "" },
  { rel_id: 24, parent_id: 8, ordinal: 0, local_name: "__support_next_float_exact_comparison_has_no_epsilon_exact", kind: "refcount", type_id: 0, arity: 2, module_id: 7, h_id: "ace14ffd2f64c865", h_schema: "", h_rule: "" },
  { rel_id: 25, parent_id: 8, ordinal: 0, local_name: "__new_float_exact_comparison_has_no_epsilon_exact", kind: "refcount_staging", type_id: 0, arity: 2, module_id: 7, h_id: "1434317374239167", h_schema: "", h_rule: "" },
  { rel_id: 26, parent_id: 9, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "29620d01ddd87e11", h_schema: "", h_rule: "" },
  { rel_id: 27, parent_id: 11, ordinal: 1, local_name: "interned_id", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "87531d903a3b883f", h_schema: "", h_rule: "" },
  { rel_id: 28, parent_id: 12, ordinal: 2, local_name: "raw_characters", kind: "storage", type_id: 0, arity: 0, module_id: 7, h_id: "e9c0a9181736e253", h_schema: "", h_rule: "" },
);

const rel_declared_column_types: Record<string, readonly string[]> = {
  exact: ["text"],
  score: ["text", "float"],
};

const arrival_targets: readonly string[] = ["score"];

const boot: readonly IBootStatement[] = [
  { rel: "score", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["sum"] },
  { rel: "score", sql: `INSERT OR IGNORE INTO "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("name", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["sum", 0.30000000000000004] },
  { rel: "score", sql: `INSERT OR IGNORE INTO "__str" ("content") VALUES (?)`, params: ["decimal"] },
  { rel: "score", sql: `INSERT OR IGNORE INTO "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("name", "value") VALUES ((SELECT "__id" FROM "__str" WHERE "content" = ?), ?)`, params: ["decimal", 0.3] },
  { rel: "exact", sql: `DELETE FROM "float_exact_comparison_has_no_epsilon_exact"`, params: [] },
  { rel: "exact", sql: `INSERT OR IGNORE INTO "float_exact_comparison_has_no_epsilon_exact" ("name") SELECT b0."name" FROM "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" b0 WHERE (b0."value" IS 0.30000000000000004)`, params: [] },
];

const final_select: Record<string, string> = {
  exact: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name" FROM "__txt_float_exact_comparison_has_no_epsilon_exact" t`,
  score: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."value" FROM "__txt_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" t`,
};

const INCREMENTAL_RELATIONS: readonly IIncrementalRelationPlan[] = [
  { rel: "exact", kind: "set", table_name: "float_exact_comparison_has_no_epsilon_exact", delta_table_name: "__delta_float_exact_comparison_has_no_epsilon_exact", frontier_table_name: "__frontier_float_exact_comparison_has_no_epsilon_exact", next_frontier_table_name: "__next_frontier_float_exact_comparison_has_no_epsilon_exact", columns: ["name"], column_types: ["text"], key_indices: [], arrival_add_sql: null, arrival_del_sql: null, boundary_sql: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_float_exact_comparison_has_no_epsilon_exact" t WHERE t."_sign" IN (-1, 1) GROUP BY t."name", t."_sign"`, rule_observers: [] },
  { rel: "score", kind: "set", table_name: "float_exact_comparison_has_no_epsilon_score_74f788ec9f37", delta_table_name: "__delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37", frontier_table_name: "__frontier_float_exact_comparison_has_no_epsilon_score_74f788ec9f37", next_frontier_table_name: "__next_frontier_float_exact_comparison_has_no_epsilon_score_74f788ec9f37", columns: ["name", "value"], column_types: ["text", "float"], key_indices: [], arrival_add_sql: `INSERT OR IGNORE INTO "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" ("name", "value") SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?) RETURNING "name", "value"`, arrival_del_sql: `DELETE FROM "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" WHERE ("name", "value") IN (SELECT json_extract(value, '$[0]'), json_extract(value, '$[1]') FROM json_each(?)) RETURNING "name", "value"`, boundary_sql: `SELECT CASE WHEN json_valid(t."name") AND json_type(t."name") = 'object' AND json_type(t."name", '$.fn') = 'text' AND json_type(t."name", '$.args') = 'array' THEN json_extract(t."name", '$.fn') || '(' || coalesce((SELECT group_concat(value, ',') FROM json_each(t."name", '$.args')), '') || ')' ELSE t."name" END AS "name", t."value", t."_sign" AS "__sign", count(*) AS "__count" FROM "__txt___delta_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" t WHERE t."_sign" IN (-1, 1) GROUP BY t."name", t."value", t."_sign"`, rule_observers: ["exact/1"] },
];

const INCREMENTAL_EDGE_STATEMENTS: readonly IIncrementalEdgeStatement[] = [
];

const INCREMENTAL_LEVEL_STATEMENTS: readonly IIncrementalLevelStatement[] = [
  { head_rel: "exact", rule_id: "float_exact_comparison_has_no_epsilon:exact/1#1", head_delta_table_name: "__delta_float_exact_comparison_has_no_epsilon_exact", head_columns: ["name"], insert_sql: `INSERT OR IGNORE INTO "float_exact_comparison_has_no_epsilon_exact" ("name") SELECT DISTINCT d0."name" FROM "__frontier_float_exact_comparison_has_no_epsilon_score_74f788ec9f37" d0 WHERE d0."_phase" >= 0 AND (d0."value" IS 0.30000000000000004) RETURNING "name"`, select_sql: `SELECT "name" FROM "float_exact_comparison_has_no_epsilon_exact"`, recompute_sql: `DELETE FROM "float_exact_comparison_has_no_epsilon_exact";
INSERT OR IGNORE INTO "float_exact_comparison_has_no_epsilon_exact" ("name") SELECT b0."name" FROM "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" b0 WHERE (b0."value" IS 0.30000000000000004)`, support_sql: [`DELETE FROM "__support_next_float_exact_comparison_has_no_epsilon_exact"`, `INSERT INTO "__support_next_float_exact_comparison_has_no_epsilon_exact" ("name", "__refcount") SELECT "name", sum("__refcount") FROM (SELECT b0."name" AS "name", count(*) AS "__refcount" FROM "float_exact_comparison_has_no_epsilon_score_74f788ec9f37" b0 WHERE (b0."value" IS 0.30000000000000004) GROUP BY b0."name") GROUP BY "name"`, `UPDATE "float_exact_comparison_has_no_epsilon_exact" AS h SET "__refcount" = COALESCE((SELECT n."__refcount" FROM "__support_next_float_exact_comparison_has_no_epsilon_exact" n WHERE n."name" = h."name"), 0)`, `INSERT INTO "__delta_float_exact_comparison_has_no_epsilon_exact" ("_sign", "_sequence", "name") SELECT -1, row_number() OVER () - 1, "name" FROM "float_exact_comparison_has_no_epsilon_exact" WHERE "__refcount" <= 0`, `DELETE FROM "float_exact_comparison_has_no_epsilon_exact" WHERE "__refcount" <= 0`, `DELETE FROM "__new_float_exact_comparison_has_no_epsilon_exact"`, `INSERT INTO "__new_float_exact_comparison_has_no_epsilon_exact" ("name", "__refcount") SELECT n."name", n."__refcount" FROM "__support_next_float_exact_comparison_has_no_epsilon_exact" n LEFT JOIN "float_exact_comparison_has_no_epsilon_exact" h ON n."name" = h."name" WHERE h."name" IS NULL`, `INSERT INTO "__delta_float_exact_comparison_has_no_epsilon_exact" ("_sign", "_sequence", "name") SELECT 1, "rowid" - 1, "name" FROM "__new_float_exact_comparison_has_no_epsilon_exact"`, `INSERT INTO "__frontier_float_exact_comparison_has_no_epsilon_exact" ("_phase", "_sequence", "name") SELECT ?, "rowid" - 1, "name" FROM "__new_float_exact_comparison_has_no_epsilon_exact"`, `INSERT INTO "__next_frontier_float_exact_comparison_has_no_epsilon_exact" ("_phase", "_sequence", "name") SELECT ?, "rowid" - 1, "name" FROM "__new_float_exact_comparison_has_no_epsilon_exact"`, `INSERT OR IGNORE INTO "float_exact_comparison_has_no_epsilon_exact" ("name", "__refcount") SELECT n."name", n."__refcount" FROM "__support_next_float_exact_comparison_has_no_epsilon_exact" n`], expand_sql: null, dred_sql: null, fixpoint_ir: null, aggregate_sql: null },
];

const RECONCILE_EVERY_TICK = false;

const SUBSCRIBE_PRUNE = SubscribeCone.mode();
const SUBSCRIBE_PRUNE_TICK_PATH: string = "incremental";
if (SUBSCRIBE_PRUNE === "on" && SUBSCRIBE_PRUNE_TICK_PATH !== "incremental") {
  throw new Error(`subscribe_prune_unsupported_tick_path ${SUBSCRIBE_PRUNE_TICK_PATH}`);
}
const SUBSCRIBED_RELATIONS = SubscribeCone.relations(SUBSCRIBE_PRUNE, INCREMENTAL_RELATIONS, subscribed_rels, arrival_targets);
const SUBSCRIBED_EDGE_STATEMENTS = SubscribeCone.edges(SUBSCRIBE_PRUNE, INCREMENTAL_EDGE_STATEMENTS, subscribed_rels);
const SUBSCRIBED_LEVEL_STATEMENTS = SubscribeCone.levels(SUBSCRIBE_PRUNE, INCREMENTAL_LEVEL_STATEMENTS, subscribed_rels);
const SUBSCRIBED_BOOT = SubscribeCone.boot(SUBSCRIBE_PRUNE, boot, subscribed_rels, arrival_targets);

function run_incremental_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return IncrementalRuntime.prepare_tick(seam, SUBSCRIBED_RELATIONS).pipe(
    concatMap(() => TextPlane.intern(seam, TEXT_INTERN_PLAN, arrivals)
      .pipe(map((interned) => { arrivals = interned; }))),
    concatMap(() => IncrementalRuntime.apply_arrivals(seam, arrivals, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_levels_before_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => IncrementalRuntime.apply_edges(seam, SUBSCRIBED_EDGE_STATEMENTS, SUBSCRIBED_RELATIONS)),
    concatMap(() => of(undefined)),
    concatMap(() => of(undefined)),
  ).pipe(
    concatMap(() => IncrementalRuntime.recompute_levels_after_edges(seam, SUBSCRIBED_LEVEL_STATEMENTS, SUBSCRIBED_RELATIONS, RECONCILE_EVERY_TICK)),
    concatMap(() => IncrementalRuntime.read_boundary(seam, SUBSCRIBED_RELATIONS)),
    concatMap((rels) => IncrementalRuntime.promote_frontiers(seam, SUBSCRIBED_RELATIONS).pipe(
      concatMap((carry_pending) => EnumPlane.decode_deltas(seam, ENUM_TYPES, ENUM_REF_COLUMNS, SUBSCRIBED_RELATIONS, rels).pipe(
        map((decoded): ITickDeltas => ({ rels: decoded, carry_pending })),
      )),
    )),
  );
}

function run_tick(seam: ISqlSeam, arrivals: IArrivalBatch): Observable<ITickDeltas> {
  return EnumPlane.intern(seam, ENUM_TYPES, ENUM_REF_COLUMNS, arrivals).pipe(
    concatMap((normalized) => run_incremental_tick(seam, validate_arrivals(normalized))),
  );
}

export const incremental_plan: IIncrementalProgramPlan = {
  reconcile_every_tick: RECONCILE_EVERY_TICK,
  retraction_guard: "plain-count-acyclic",
  relations: INCREMENTAL_RELATIONS,
  edges: INCREMENTAL_EDGE_STATEMENTS,
  levels: INCREMENTAL_LEVEL_STATEMENTS,
};

export const program: IGenProgramWithBoot = {
  name: "float_exact_comparison_has_no_epsilon",
  ir_version: 1,
  internMode: "dict",
  ddl,
  rel_columns,
  rel_physical_names,
  rel_column_types,
  arrival_targets,
  boot: SUBSCRIBED_BOOT,
  final_select,
  host_plans,
  bind_plans,
  query_plans,
  subscribed_rels,
  rel_catalog,
  enum_types: ENUM_TYPES,
  enum_ref_columns: ENUM_REF_COLUMNS,
  unsupported_execution,
  tick: run_tick,
};
