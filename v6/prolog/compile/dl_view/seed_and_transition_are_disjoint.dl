rel increment(name, col2) log keep(all).
rel counter(name, next) key(1).

counter(Name, 1) <+ increment(Name, _), not(pre(counter(Name, _))).
counter(Name, Next) <+ increment(Name, _), pre(counter(Name, Total)), Next := Total + 1.
