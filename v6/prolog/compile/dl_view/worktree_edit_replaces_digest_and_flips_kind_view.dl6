rel worktree_edit(path: text, digest: text, kind: text) log keep(all).
rel worktree_file(path, digest, kind) key(1).

worktree_file(Path, Digest, Kind) <+ worktree_edit(Path, Digest, Kind).
rust_file(Path) <- worktree_file(Path, _, 'rust').
