rel span(end: int, start: int).
rel node_fact(path: text, name: text, at: span) log keep(all).
rel def_start(path: text, offset: int).

def_start(Path, Offset) <- node_fact(Path, _Name, At), decode(At, {start: Offset}).
