rel stale(endpoint) log keep(all).
rel fetch_demand(endpoint) set.
rel fetch_call(endpoint) log keep(all).

fetch_demand(Endpoint) <- stale(Endpoint).
fetch_call(Endpoint) <+ only(fetch_demand(Endpoint)).
