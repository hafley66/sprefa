rel division_input(label: text, numerator: int, denominator: int).

probe(Label, Quotient, Remainder) <- division_input(Label, Numerator, Denominator), Quotient := Numerator / Denominator, Remainder := Numerator mod Denominator.
