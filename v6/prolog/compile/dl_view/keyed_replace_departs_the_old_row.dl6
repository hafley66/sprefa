rel from_poll(key: text, value: text) log keep(all).
rel latest(key, value) key(1).
rel replaced_value(key, old_value) log keep(all).

latest(Key, Value) <+ from_poll(Key, Value).
replaced_value(Key, OldValue) <+ finalize(latest(Key, OldValue)).
