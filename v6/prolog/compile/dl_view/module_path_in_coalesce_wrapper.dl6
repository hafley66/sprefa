rel repo(name: text).
rel git.latest_commit(name: text, commit: text).
rel repo_latest(name: text, commit: text).

repo_latest(Name, Commit) <-
  repo(Name),
  coalesce(git.latest_commit(Name, Commit), 'absent').
