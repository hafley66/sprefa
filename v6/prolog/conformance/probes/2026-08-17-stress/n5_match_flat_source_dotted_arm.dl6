# NARROW for p08: flat match source, DOTTED arm head only.
rel resp(tree_id: int, status: int) log keep(all).
rel orchard.ok(tree_id: int).

match resp(TreeId, Status) (
  ; Status == 200 |-> orchard.ok(TreeId)
).
