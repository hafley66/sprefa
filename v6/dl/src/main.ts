/**
 * main.ts — boot: db path + port via env (DL_DB_PATH, DL_PORT, default :7171),
 * startup log lists routes. Wired by package M6; stub until then.
 */
throw new Error("sprefa-dl serve: M6 (http front) has not landed yet");
