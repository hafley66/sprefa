// knip: SILENT, `./util` names this directory's index file. rail dead bucket:
// MUST FIRE, nothing calls these names. rail unreachable bucket: MUST STAY
// SILENT, which takes the directory-index rung of the resolution ladder: the
// specifier text `./util` names no file on disk at all.
export function directoryOne(): number { return 21; }
export function directoryTwo(): number { return 22; }
export function directoryThree(): number { return 23; }
export function directoryFour(): number { return 24; }
export function directoryFive(): number { return 25; }
