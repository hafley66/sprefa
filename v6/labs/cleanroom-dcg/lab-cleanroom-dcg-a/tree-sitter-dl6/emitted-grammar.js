// emitted-grammar.js -- rules the emitter derived from dcg.pl.
// Token rules only; the structural rules are the hand overlay.
// derived rule count: length([identifier,number,atom_literal,string_literal],_58582)
    identifier: ( $ ) => /,
    number: ( $ ) => /,
    atom_literal: ( $ ) => /,
    string_literal: ( $ ) => /,
