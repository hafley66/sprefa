# REVIEW-opus-eyes — @hafley66/vitest-playwright, source read 2026-09-08

Package read at `/Users/chrishafley/projects/hafley-rxjs/packages/vitest-playwright` (absent from the review worktree; untracked in the shared checkout). `pnpm exec tsc -p tsconfig.build.json --noEmit` exits 0. Prior rows in `REVIEW-2026-09-08-astra*.md` grade the PLAN, not the code; no row below repeats one.

| # | severity | file:line | defect | evidence | fix |
|---|---|---|---|---|---|
| 1 | P0 crash | src/2_global-setup.ts:26 | `newest()` calls `require("node:fs")` inside an ES module, so any `serve: { kind: "vite", reuseExisting: true }` throws `ReferenceError: require is not defined` before the build runs. | `dist/2_global-setup.js:28` emits `const { readdirSync } = require("node:fs");` next to `import { existsSync, statSync } from "node:fs"` at `dist/2_global-setup.js:3`, with `package.json:5` `"type": "module"` | Add `readdirSync` to the static `node:fs` import at src/2_global-setup.ts:3 and delete the `require`. |
| 2 | P0 leak | src/8_around.ts:24 | `releaseLazyBrowser()` is called only from the `browser` fixture teardown, so a worker whose files use plain `it` + `$page` (never destructuring a fixture) launches a browser through `ensureBrowser` and never closes it. | fixtures resolve only when destructured or `auto` — `node_modules/@vitest/runner/dist/chunk-artifact.js:305-312` (`usedProps.has(fixture.name)`, `if (!usedFixtures.length) return fn(context)`); worker cleanup runs only registered fixture cleanups — `chunk-artifact.js:3270-3271` | Register the lazy browser release through `runner.onCleanupWorkerContext` (or an `auto: true` worker fixture) instead of piggybacking on `browser`. |
| 3 | P0 leak | src/4_test.ts:40 | Under `contextScope: "file"` the file context starts tracing (`context$`, src/5_streams.ts:64) but `capture()` is gated on `ctxH`, which is undefined on the file branch, so `tracing.stop()` is never called: the trace buffer grows for the whole file and no `trace.zip` is ever written. | src/8_around.ts:89 `if (page && ctxH) await capture(...)` while src/8_around.ts:58-63 leaves `ctxH` undefined; `tracing.start` opens the first chunk — `node_modules/playwright-core/lib/coreBundle.js:58567` | Pass the file context into `capture()` and stop/flush tracing per attempt with `startChunk`/`stopChunk`. |
| 4 | P1 | src/8_around.ts:36 | `attemptDir` reads `task.result.repeatCount` during the aroundEach setup phase, but vitest assigns it inside `runTest`, so every repeat of a `repeats: N` test writes into the same `.p0` directory and overwrites the previous repeat's png/zip/webm. | `node_modules/@vitest/runner/dist/chunk-artifact.js:2947` `test.result.repeatCount = repeatCount;` sits inside the callback passed to `callAroundEachHooks` at `chunk-artifact.js:2941`; the retry counterpart is assigned before the next attempt at `chunk-artifact.js:3029` | Take `repeatCount` from the hook's own counter, or compute the directory after `runTest()` has started. |
| 5 | P1 | src/6_roots.ts:31 | The test's `AbortSignal` is created once per test and never replaced between retries, so a test that fails by timeout has an already-aborted signal on every retry attempt and each matcher returns the abort result instantly. | one controller per context, created at collection and never reset — `node_modules/@vitest/runner/dist/chunk-artifact.js:2352-2357`; the retry loop resets only `test.result.state` — `chunk-artifact.js:3029-3031`; `_expect` maps the abort to a failed matcher — `node_modules/playwright-core/lib/coreBundle.js:60243` | Compose the matcher signal as `AbortSignal.any([ctx.signal, perAttemptController.signal])`, ignoring `ctx.signal` when it is already aborted at attempt start. |
| 6 | P1 | src/8_around.ts:67 | Every test acquires a `BrowserContext` and a `Page` whether or not it uses one; a pure-logic test using the extended `test` still pays a context create/close round trip (for example `tests/1_matchers.test.ts:142`, the `toPass matrix` case). | playwright makes `page` lazy through fixture resolution, which vitest also supports — `node_modules/@vitest/runner/dist/chunk-artifact.js:305-312` skips unrequested fixtures | Acquire the context/page inside the `context`/`page` fixtures and on the first `$page` read, not unconditionally in the hook. |
| 7 | P1 | src/7_page-global.ts:5 | The `declare global { var $page }` block lives in a module that no public entry point re-exports, so a consumer importing the package or `@hafley66/vitest-playwright/setup` gets `Cannot find name '$page'`; only this package's own `tsconfig.json` sees it. | `dist/setup.d.ts` is exactly `export {}` (tsc elided the value-only import of `7_page-global.js`), `dist/index.d.ts` never references it, and `package.json:8-15` exposes no subpath reaching `7_page-global.d.ts` | Re-export the globals module type-side from `index.ts` and `setup.ts`, or add a `./globals` types entry. |
| 8 | P1 | src/1_plugin.ts:17 | `serve` reaches global setup as `JSON.stringify(options.serve)`, so a `kind: "vite"` slot silently loses every function-valued field of `InlineConfig` (`plugins`, function aliases, `define` getters) and the global setup builds the app without its plugins. | the object is re-hydrated with `JSON.parse` at src/2_global-setup.ts:66; nothing else transports it | Keep serve options in plugin module state behind a getter, or require `serve.build` to be a config file path. |
| 9 | P1 | src/6_roots.ts:55 | `workerState.workerLog` is a bare event Signal nothing subscribes to, so `log.api: true` (the default) installs a playwright instrumentation listener and allocates a span per API call for a sink with zero readers; `TestLogState.api` stays `[]` forever. | `Signal<T>()` with no argument is an event signal with no replay and no default consumer — `packages/signals/src/2_Signal.ts:46-49`; the planned `apiSub = workerLog.$.pipe(filter(e => e.owner === task.id))` (PLAN §6.3 step 2) is absent from src/8_around.ts | Subscribe `workerLog` into `testLog.api` as planned, or drop `log.api`/`apiCall$` until a sink exists. |
| 10 | P1 | src/5_streams.ts:133 | `takeUntil(fromEvent(signal, "abort"))` never fires for a signal that is already aborted when `poll$` subscribes, so `toPass` inside a retry or after a hook timeout polls to its own deadline instead of stopping. | `poll$` never reads `signal.aborted`; playwright passes the signal into the transport, which rejects immediately for a pre-aborted signal — `node_modules/playwright-core/lib/coreBundle.js:60243` | Use `takeUntil(signal ? merge(iif(() => signal.aborted, of(0), EMPTY), fromEvent(signal, "abort")) : NEVER)`. |
| 11 | P1 | src/3_matchers.ts:187 | The `toHaveURL` predicate/`URLPattern` branch calls `waitForURL` without the abort signal, so a timed-out test leaves the URL waiter running for the full matcher timeout. | playwright passes it: `await page.mainFrame().waitForURL(..., { timeout, signal: options?.signal })` — `node_modules/playwright/lib/matchers/expect.js:12238` | Pass `signal: s.signal` in the `waitForURL` options. |
| 12 | P1 | src/4_test.ts:31 | If a plain `it` + `$page` test runs before the first extended-`test` test in a worker, `ensureBrowser` launches browser A and the `browser` fixture then unconditionally launches browser B, leaving two chromium processes alive for the rest of the worker. | `ensureBrowser` short-circuits on `workerState.root?.browser.$()` (src/8_around.ts:17) but the fixture has no such guard; both close only at worker cleanup — `node_modules/@vitest/runner/dist/chunk-artifact.js:3270-3271` | Make the `browser` fixture delegate to `ensureBrowser(options)` so there is one launch path. |
| 13 | P1 | src/0_options.ts:33 | `browser.launch` goes verbatim into `test.provide`, which crosses the worker boundary by serialization; `LaunchOptions.logger` (or any function-valued field) fails to transfer. | provided context is serialized for the worker — `node_modules/vitest/dist/chunks/cli-api.CnMVyzaz.js:2542` (`stringify(this.project.getProvidedContext())`), read back at `node_modules/vitest/dist/chunks/test.DNmyFkvJ.js:4165` | Strip or reject non-cloneable `LaunchOptions` keys in `resolveOptions`, mirroring the `har.url` RegExp handling at src/0_options.ts:54. |
| 14 | P2 | src/8_around.ts:98 | `artifacts.video` is silently ignored under `contextScope: "file"`: `context$` is called without `hooks` at src/4_test.ts:40 so `recordVideo` is never set, and `keepOrDropVideos` is gated on `ctxH`. | src/5_streams.ts:53-54 sets `recordVideo` only when `hooks.video && hooks.attemptDir` | Pass the artifact hooks to the file-scope `context$` call, or reject `video !== "off"` with `contextScope: "file"` in `resolveOptions`. |
| 15 | P2 | src/9_runner.ts:8 | `(this as any).config?.provide?.[KEY.options]` is always `undefined` (`provide` is not part of the serialized worker config), so the concurrency guard silently depends on the undocumented `globalThis.__vitest_worker__` fallback. | `serializeConfig` (`node_modules/vitest/dist/chunks/cli-api.CnMVyzaz.js:9217-9345`) has no `provide` key; the worker state global is `"__vitest_worker__"` — `node_modules/vitest/dist/chunks/utils.BX5Fg8C4.js:3` | Delete the dead branch and use `inject(KEY.options)`. |
| 16 | P2 | src/9_runner.ts:11 | The `contextScope: "file"` + concurrent rejection throws out of `onCollected`, after vitest has already reported the collected file over RPC, so it surfaces as a bare worker error with no file attribution. | `rpc().onCollected(files)` runs before the user hook — `node_modules/vitest/dist/chunks/index.DXx9Dtk7.js:167-168`; `startTests` awaits `onCollected` outside any per-file try — `node_modules/@vitest/runner/dist/chunk-artifact.js:3280` | Mark the offending tasks `mode: "skip"` with a task-level error instead of throwing. |
| 17 | P2 | src/0_options.ts:54 | `har.url` keeps only `RegExp.source`, so flags are dropped and the pattern is rebuilt case-sensitively. | src/5_streams.ts:58 `url: o.har.url ? new RegExp(o.har.url) : undefined` | Carry `{ source, flags }` across `provide` and restore both. |
| 18 | P2 | src/5_streams.ts:117 | `startSpan` assigns `span` in a `.then` continuation, so the span's start time is the promise resolution rather than the API call, and any `end()` reached in the same microtask turn creates the span afterwards and never ends it. | `tracerPromise.then(t => { span = t?.startSpan(...) })` with `end: e => { ...; span?.end?.() }` in the same closure | Await the tracer once inside the `apiCall$` subscribe and start the span synchronously in `onApiCallBegin`. |
| 19 | P2 | src/3_matchers.ts:20 | When `als.getStore()` is empty (a `beforeAll`, a `describe` body, a helper outside the store) `scope()` silently substitutes a hard-coded 5000 ms and drops the configured `expect.timeout`. | src/3_matchers.ts:20 `s?.root.options.$().expectTimeout ?? 5000` | Fall back to `inject(KEY.options).expectTimeout`, or throw the named error `$page` uses. |
| 20 | P2 | src/3_matchers.ts:46 | `format()` prints a `Locator:` line for page-level matchers (`Locator:  page`) and always prints `Timeout:  Nms` even when the failure was not a timeout, so the message shape diverges from playwright's. | playwright's `formatMatcherMessage` takes `locator` and `timedOut` separately and omits the timeout line when `timedOut` is falsy — `node_modules/playwright/lib/matchers/expect.js:12116-12130` | Label the receiver by kind and emit the timeout line only when `r.timedOut`. |
| 21 | P2 | src/3_matchers.ts:218 | Every matcher in `PlaywrightMatchers<R>` is declared `Promise<R>`, so `await expect(locator).toBeVisible()` types as `Promise<Locator>` while the runtime value is `undefined`. | the extend wrapper returns `thenable.then(({ pass, message, ... }) => { ... })` with no return value — `node_modules/@vitest/expect/dist/index.js:1875-1883` | Declare them `Promise<void>`. |
| 22 | P2 | src/5_streams.ts:136 | `moveArtifact` and `dropArtifact` are dead: nothing imports them, `index.ts` does not export them, and src/8_around.ts:128 re-imports `node:fs` inline to do the same job. | src/index.ts:7 lists the stream exports and omits both | Delete them, or route `keepOrDropVideos` through them. |
| 23 | P2 | src/3_matchers.ts:127 | `toContainClass` throws the array-form message (`cannot contain RegExp values`) for a scalar RegExp, where playwright throws `cannot be a RegExp value`. | `node_modules/playwright/lib/matchers/expect.js:12857-12866` carries the two distinct messages | Branch the message on `Array.isArray(expected)`. |
| 24 | P2 | src/3_matchers.ts:90 | `withArg` shifts `expected` into `options` whenever it is a non-RegExp object, including `null` and including calls that already passed a third argument; playwright shifts only when `options` is absent. | `if (!options) { if (typeof expected === "object" && !isRegExp2(expected)) { options = expected; expected = void 0 } }` — `node_modules/playwright/lib/matchers/expect.js:12833-12838` | Guard with `options === undefined && expected !== null`. |
| 25 | P2 | src/1_plugin.ts:23 | `config()` hard-sets `isolate: false` and `runner`, and vite merges the plugin result over the user config, so a project with its own runner or `isolate: true` is overridden without warning. | vitest serializes `runner` and `isolate` from the merged config to the worker — `node_modules/vitest/dist/chunks/cli-api.CnMVyzaz.js:9224-9232` | Read the incoming config in `config(userConfig)`, fill only empty slots, and error loudly on a conflicting runner. |
| 26 | P2 | src/8_around.ts:18 | A failed browser launch is memoized: `lazyBrowser ??= acquire(browser$(o))` keeps the rejected promise, so every later test in the worker rethrows the first launch error even when it was transient. | src/8_around.ts:18-22; only `releaseLazyBrowser` clears the field | Clear `lazyBrowser` in a `.catch` before rethrowing. |
| 27 | P2 | src/4_test.ts:36 | `finally { apiSub?.unsubscribe(); await h.release(); await releaseLazyBrowser() }` — a throwing `browser.close()` skips `releaseLazyBrowser()` and leaks the lazily launched browser. | src/4_test.ts:36 | Wrap each release in its own try/finally, or use `Promise.allSettled`. |
| 28 | P2 | src/8_around.ts:111 | `bounded()` adds a listener to the shared budget signal per artifact and never removes it, and it never rejects when the budget aborted before the call, so a blown budget silently reverts to an unbounded wait. | `AbortSignal.timeout` fires `abort` once; a listener added after that point is never invoked | Check `budget.aborted` up front and add listeners with `{ once: true }`. |
| 29 | P2 | src/6_roots.ts:48 | `als` is a bare module singleton with no `globalThis` guard, so a project loading the plugin from `src` (as `vitest.config.ts:2` does) while a test file imports from `dist` gets two stores, and `$page` throws "no running test" with no hint about the duplication. | src/7_page-global.ts:14 raises the generic message; `installPageGlobals` short-circuits on `if (!("$page" in globalThis))` at src/7_page-global.ts:30, pinning the first instance's proxy | Key `als` and `taskRoots` off a `Symbol.for("vitest-playwright:als")` slot on `globalThis`. |
| 30 | P2 | src/5_streams.ts:131 | `poll$` schedules retry delays with rxjs `timer`, which uses the global `setTimeout`; under `vi.useFakeTimers()` `toPass` never retries and hangs until the test timeout, and playwright's client timeouts stop firing too. | vitest's own poll loop compensates explicitly with `if (vi.isFakeTimers()) vi.advanceTimersByTime(interval)` — `node_modules/vitest/dist/chunks/test.DNmyFkvJ.js:3796` | Use `getSafeTimers()`-backed scheduling for `poll$` and the artifact budget, or document required `fakeTimers.toFake` exclusions. |
| 31 | P2 | src/3_matchers.ts:30 | A module-local helper is named `describe`, shadowing the test-framework identifier this same package re-exports at src/index.ts:3. | src/3_matchers.ts:30 `const describe = (x: unknown) => ...`, used only in the guard message at src/3_matchers.ts:25 | Rename to `typeName`. |
| 32 | P2 | src/3_matchers.ts:212 | `toPass` renders `timeout ${options.timeout ?? "test"}ms exceeded`, producing the string `timeout testms exceeded` in the default unbounded case. | src/3_matchers.ts:212 | Emit `the test deadline` for the `undefined` case. |
| 33 | P2 | tests/2_fixtures.test.ts:65 | Overriding one fixture needs `test.extend({ ... } as any) as typeof test`, so the package's own receipt shows the documented extension point does not typecheck in its natural form. | `ScopedFixturesObject` requires the `$test`/`$file`/`$worker` shape — `node_modules/@vitest/runner/dist/tasks.d-DEYaIMIu.d.ts:1194` | Show and test `test.extend<{ $test: { contextOptions: BrowserContextOptions } }>({ ... })` and drop the casts. |
| 34 | P2 | tests/1_matchers.test.ts:123 | `browser.newContext()` is closed on the happy path only; a failing `toHaveURL` at line 126 leaks a context into the rest of the worker and would break the `browser.contexts().length` receipt at tests/2_fixtures.test.ts:57. | tests/1_matchers.test.ts:123-127 | Wrap in `try/finally`, or use `onTestFinished(() => ctx.close())`. |

## Summary

- Rows by severity: P0 3, P1 10, P2 21 (34 total).
- Worst defect: row 1 — `require("node:fs")` inside the ESM global setup makes `serve: { kind: "vite", reuseExisting: true }` a guaranteed `ReferenceError`, and nothing in `tests/` exercises the serve slot.
- Next concern is lifetime ownership: the lazy browser is released only through a fixture a plain-`it` file never resolves (row 2), and `contextScope: "file"` starts a trace nothing ever stops (row 3).
- The matcher layer is the strongest part: expressions, payload fields and `pass` polarity match `playwright/lib/matchers/expect.js` field for field, and `__vitest_poll_takeover__` is a real vitest 4 protocol (`@vitest/expect/dist/index.js:1900`), not an invention.
- Shape overall: a correct wire-level playwright port bolted onto an aroundEach lifetime that assumes every test wants a browser and that reads mutable `task.result` fields one phase too early.
