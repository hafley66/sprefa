Lane monoemit. First: git merge --ff-only 33b52887 (STOP.md if not). Goal: mono shootout lane becomes COMPILER-EMITTED rust (direct lowering), replacing the hand-written v6/labs/exec_shootout/mono/src/main.rs. Read v6/labs/exec_shootout/CONTRACT.md (IO contract: stdin edges, JSON events loaded/fixpoint/done, checksum) and v6/prolog/compile/emit_ts.pl or nearest ts emitter for the emit style. Build v6/prolog/labs/emit_rust_shootout/emit_rust.pl: input = the 2-rule reachability program as prolog facts in the same file; output = complete main.rs text implementing semi-naive delta join per rule, specialized (no dyn, no generic tuples). swipl writes it to v6/labs/exec_shootout/mono/src/main.rs. Keep dedup design free: you may shard the seen set per source node (Vec<FxHashSet<u32>> keyed by x) instead of one global Pair set. Gates: (1) emitted file compiles: cargo build --release in mono/; (2) same checksum as before on a small input: echo edges for 3-node chain, expect checksum from mono/src tests; (3) MONO_TRACE preserved. Update CONTRACT.md mono row text to 'compiler-emitted'. Commit all, push -u origin lab/monoemit, gh pr create --title 'xmonoemit: mono lane = compiler-emitted rust' --body 'emit_rust.pl generates mono main.rs; hand impl replaced'. Banned words: provenance substrate load-bearing regime support honest distill ground ruling. Comments max 2 lines. STOP.md on any deviation.
